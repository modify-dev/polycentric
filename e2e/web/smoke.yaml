url: ${MAESTRO_WEB_URL}
---
- runFlow: ../elements/load.yaml
- runFlow: ../steps/fresh-start.yaml
- assertVisible: ${output.onboarding.title}
- assertVisible: ${output.onboarding.createIdentity}
- assertVisible: ${output.onboarding.haveIdentity}
