apiVersion: v1
kind: Service
metadata:
  name: {{ include "harbor-scraper.fullname" . }}
  labels:
    {{- include "harbor-scraper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harbor-scraper.selectorLabels" . | nindent 4 }}
