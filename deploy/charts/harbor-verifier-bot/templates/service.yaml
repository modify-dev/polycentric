apiVersion: v1
kind: Service
metadata:
  name: {{ include "harbor-verifier-bot.fullname" . }}
  labels:
    {{- include "harbor-verifier-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harbor-verifier-bot.selectorLabels" . | nindent 4 }}
