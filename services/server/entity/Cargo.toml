[package]
edition      = "2024"
name         = "entity"
publish      = false
rust-version = "1.85.0"
version      = "0.1.0"

[lib]
name = "entity"
path = "src/lib.rs"

[dependencies]
sea-orm = { workspace = true, features = ["macros", "with-chrono", "with-json"] }
chrono.workspace = true
