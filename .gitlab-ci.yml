include:
  - local: .gitlab/ci/build_rs_core.yml
  - local: .gitlab/ci/build_js_sdks.yml
  - local: .gitlab/ci/build_server.yml

stages:
  - setup
  - check
  - build
  - publish

default:
  tags:
    - dind
