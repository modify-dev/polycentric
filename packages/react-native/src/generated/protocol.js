/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import * as $protobuf from 'protobufjs/minimal';

// Common aliases
const $Reader = $protobuf.Reader,
  $Writer = $protobuf.Writer,
  $util = $protobuf.util;

// Exported root namespace
const $root = $protobuf.roots['default'] || ($protobuf.roots['default'] = {});

export const polycentric = ($root.polycentric = (() => {
  /**
   * Namespace polycentric.
   * @exports polycentric
   * @namespace
   */
  const polycentric = {};

  /**
   * ContentType enum.
   * @name polycentric.ContentType
   * @enum {number}
   * @property {number} UNKNOWN=0 UNKNOWN value
   * @property {number} DELETE=1 DELETE value
   * @property {number} SYSTEM_PROCESSES=2 SYSTEM_PROCESSES value
   * @property {number} POST=3 POST value
   * @property {number} FOLLOW=4 FOLLOW value
   * @property {number} USERNAME=5 USERNAME value
   * @property {number} DESCRIPTION=6 DESCRIPTION value
   * @property {number} BLOB_META=7 BLOB_META value
   * @property {number} BLOB_SECTION=8 BLOB_SECTION value
   * @property {number} AVATAR=9 AVATAR value
   * @property {number} SERVER=10 SERVER value
   * @property {number} VOUCH=11 VOUCH value
   * @property {number} CLAIM=12 CLAIM value
   * @property {number} BANNER=13 BANNER value
   * @property {number} OPINION=14 OPINION value
   * @property {number} STORE=15 STORE value
   * @property {number} AUTHORITY=16 AUTHORITY value
   * @property {number} JOIN_TOPIC=17 JOIN_TOPIC value
   * @property {number} BLOCK=18 BLOCK value
   */
  polycentric.ContentType = (function () {
    const valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = 'UNKNOWN')] = 0;
    values[(valuesById[1] = 'DELETE')] = 1;
    values[(valuesById[2] = 'SYSTEM_PROCESSES')] = 2;
    values[(valuesById[3] = 'POST')] = 3;
    values[(valuesById[4] = 'FOLLOW')] = 4;
    values[(valuesById[5] = 'USERNAME')] = 5;
    values[(valuesById[6] = 'DESCRIPTION')] = 6;
    values[(valuesById[7] = 'BLOB_META')] = 7;
    values[(valuesById[8] = 'BLOB_SECTION')] = 8;
    values[(valuesById[9] = 'AVATAR')] = 9;
    values[(valuesById[10] = 'SERVER')] = 10;
    values[(valuesById[11] = 'VOUCH')] = 11;
    values[(valuesById[12] = 'CLAIM')] = 12;
    values[(valuesById[13] = 'BANNER')] = 13;
    values[(valuesById[14] = 'OPINION')] = 14;
    values[(valuesById[15] = 'STORE')] = 15;
    values[(valuesById[16] = 'AUTHORITY')] = 16;
    values[(valuesById[17] = 'JOIN_TOPIC')] = 17;
    values[(valuesById[18] = 'BLOCK')] = 18;
    return values;
  })();

  /**
   * Opinion enum.
   * @name polycentric.Opinion
   * @enum {number}
   * @property {number} UNSPECIFIED=0 UNSPECIFIED value
   * @property {number} LIKE=1 LIKE value
   * @property {number} DISLIKE=2 DISLIKE value
   * @property {number} NEUTRAL=3 NEUTRAL value
   */
  polycentric.Opinion = (function () {
    const valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = 'UNSPECIFIED')] = 0;
    values[(valuesById[1] = 'LIKE')] = 1;
    values[(valuesById[2] = 'DISLIKE')] = 2;
    values[(valuesById[3] = 'NEUTRAL')] = 3;
    return values;
  })();

  polycentric.PublicKey = (function () {
    /**
     * Properties of a PublicKey.
     * @memberof polycentric
     * @interface IPublicKey
     * @property {number|Long|null} [keyType] PublicKey keyType
     * @property {Uint8Array|null} [key] PublicKey key
     */

    /**
     * Constructs a new PublicKey.
     * @memberof polycentric
     * @classdesc Represents a PublicKey.
     * @implements IPublicKey
     * @constructor
     * @param {polycentric.IPublicKey=} [properties] Properties to set
     */
    function PublicKey(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * PublicKey keyType.
     * @member {number|Long} keyType
     * @memberof polycentric.PublicKey
     * @instance
     */
    PublicKey.prototype.keyType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * PublicKey key.
     * @member {Uint8Array} key
     * @memberof polycentric.PublicKey
     * @instance
     */
    PublicKey.prototype.key = $util.newBuffer([]);

    /**
     * Creates a new PublicKey instance using the specified properties.
     * @function create
     * @memberof polycentric.PublicKey
     * @static
     * @param {polycentric.IPublicKey=} [properties] Properties to set
     * @returns {polycentric.PublicKey} PublicKey instance
     */
    PublicKey.create = function create(properties) {
      return new PublicKey(properties);
    };

    /**
     * Encodes the specified PublicKey message. Does not implicitly {@link polycentric.PublicKey.verify|verify} messages.
     * @function encode
     * @memberof polycentric.PublicKey
     * @static
     * @param {polycentric.IPublicKey} message PublicKey message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PublicKey.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.keyType != null &&
        Object.hasOwnProperty.call(message, 'keyType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.keyType);
      if (message.key != null && Object.hasOwnProperty.call(message, 'key'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.key);
      return writer;
    };

    /**
     * Encodes the specified PublicKey message, length delimited. Does not implicitly {@link polycentric.PublicKey.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.PublicKey
     * @static
     * @param {polycentric.IPublicKey} message PublicKey message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PublicKey.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PublicKey message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.PublicKey
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.PublicKey} PublicKey
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PublicKey.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.PublicKey();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.keyType = reader.uint64();
            break;
          }
          case 2: {
            message.key = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a PublicKey message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.PublicKey
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.PublicKey} PublicKey
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PublicKey.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PublicKey message.
     * @function verify
     * @memberof polycentric.PublicKey
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PublicKey.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.keyType != null && message.hasOwnProperty('keyType'))
        if (
          !$util.isInteger(message.keyType) &&
          !(
            message.keyType &&
            $util.isInteger(message.keyType.low) &&
            $util.isInteger(message.keyType.high)
          )
        )
          return 'keyType: integer|Long expected';
      if (message.key != null && message.hasOwnProperty('key'))
        if (
          !(
            (message.key && typeof message.key.length === 'number') ||
            $util.isString(message.key)
          )
        )
          return 'key: buffer expected';
      return null;
    };

    /**
     * Creates a PublicKey message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.PublicKey
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.PublicKey} PublicKey
     */
    PublicKey.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.PublicKey) return object;
      let message = new $root.polycentric.PublicKey();
      if (object.keyType != null)
        if ($util.Long)
          (message.keyType = $util.Long.fromValue(object.keyType)).unsigned =
            true;
        else if (typeof object.keyType === 'string')
          message.keyType = parseInt(object.keyType, 10);
        else if (typeof object.keyType === 'number')
          message.keyType = object.keyType;
        else if (typeof object.keyType === 'object')
          message.keyType = new $util.LongBits(
            object.keyType.low >>> 0,
            object.keyType.high >>> 0
          ).toNumber(true);
      if (object.key != null)
        if (typeof object.key === 'string')
          $util.base64.decode(
            object.key,
            (message.key = $util.newBuffer($util.base64.length(object.key))),
            0
          );
        else if (object.key.length >= 0) message.key = object.key;
      return message;
    };

    /**
     * Creates a plain object from a PublicKey message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.PublicKey
     * @static
     * @param {polycentric.PublicKey} message PublicKey
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PublicKey.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.keyType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.keyType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.key = '';
        else {
          object.key = [];
          if (options.bytes !== Array) object.key = $util.newBuffer(object.key);
        }
      }
      if (message.keyType != null && message.hasOwnProperty('keyType'))
        if (typeof message.keyType === 'number')
          object.keyType =
            options.longs === String
              ? String(message.keyType)
              : message.keyType;
        else
          object.keyType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.keyType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.keyType.low >>> 0,
                    message.keyType.high >>> 0
                  ).toNumber(true)
                : message.keyType;
      if (message.key != null && message.hasOwnProperty('key'))
        object.key =
          options.bytes === String
            ? $util.base64.encode(message.key, 0, message.key.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.key)
              : message.key;
      return object;
    };

    /**
     * Converts this PublicKey to JSON.
     * @function toJSON
     * @memberof polycentric.PublicKey
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PublicKey.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PublicKey
     * @function getTypeUrl
     * @memberof polycentric.PublicKey
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PublicKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.PublicKey';
    };

    return PublicKey;
  })();

  polycentric.PrivateKey = (function () {
    /**
     * Properties of a PrivateKey.
     * @memberof polycentric
     * @interface IPrivateKey
     * @property {number|Long|null} [keyType] PrivateKey keyType
     * @property {Uint8Array|null} [key] PrivateKey key
     */

    /**
     * Constructs a new PrivateKey.
     * @memberof polycentric
     * @classdesc Represents a PrivateKey.
     * @implements IPrivateKey
     * @constructor
     * @param {polycentric.IPrivateKey=} [properties] Properties to set
     */
    function PrivateKey(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * PrivateKey keyType.
     * @member {number|Long} keyType
     * @memberof polycentric.PrivateKey
     * @instance
     */
    PrivateKey.prototype.keyType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * PrivateKey key.
     * @member {Uint8Array} key
     * @memberof polycentric.PrivateKey
     * @instance
     */
    PrivateKey.prototype.key = $util.newBuffer([]);

    /**
     * Creates a new PrivateKey instance using the specified properties.
     * @function create
     * @memberof polycentric.PrivateKey
     * @static
     * @param {polycentric.IPrivateKey=} [properties] Properties to set
     * @returns {polycentric.PrivateKey} PrivateKey instance
     */
    PrivateKey.create = function create(properties) {
      return new PrivateKey(properties);
    };

    /**
     * Encodes the specified PrivateKey message. Does not implicitly {@link polycentric.PrivateKey.verify|verify} messages.
     * @function encode
     * @memberof polycentric.PrivateKey
     * @static
     * @param {polycentric.IPrivateKey} message PrivateKey message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PrivateKey.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.keyType != null &&
        Object.hasOwnProperty.call(message, 'keyType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.keyType);
      if (message.key != null && Object.hasOwnProperty.call(message, 'key'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.key);
      return writer;
    };

    /**
     * Encodes the specified PrivateKey message, length delimited. Does not implicitly {@link polycentric.PrivateKey.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.PrivateKey
     * @static
     * @param {polycentric.IPrivateKey} message PrivateKey message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PrivateKey.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PrivateKey message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.PrivateKey
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.PrivateKey} PrivateKey
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PrivateKey.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.PrivateKey();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.keyType = reader.uint64();
            break;
          }
          case 2: {
            message.key = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a PrivateKey message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.PrivateKey
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.PrivateKey} PrivateKey
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PrivateKey.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PrivateKey message.
     * @function verify
     * @memberof polycentric.PrivateKey
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PrivateKey.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.keyType != null && message.hasOwnProperty('keyType'))
        if (
          !$util.isInteger(message.keyType) &&
          !(
            message.keyType &&
            $util.isInteger(message.keyType.low) &&
            $util.isInteger(message.keyType.high)
          )
        )
          return 'keyType: integer|Long expected';
      if (message.key != null && message.hasOwnProperty('key'))
        if (
          !(
            (message.key && typeof message.key.length === 'number') ||
            $util.isString(message.key)
          )
        )
          return 'key: buffer expected';
      return null;
    };

    /**
     * Creates a PrivateKey message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.PrivateKey
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.PrivateKey} PrivateKey
     */
    PrivateKey.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.PrivateKey) return object;
      let message = new $root.polycentric.PrivateKey();
      if (object.keyType != null)
        if ($util.Long)
          (message.keyType = $util.Long.fromValue(object.keyType)).unsigned =
            true;
        else if (typeof object.keyType === 'string')
          message.keyType = parseInt(object.keyType, 10);
        else if (typeof object.keyType === 'number')
          message.keyType = object.keyType;
        else if (typeof object.keyType === 'object')
          message.keyType = new $util.LongBits(
            object.keyType.low >>> 0,
            object.keyType.high >>> 0
          ).toNumber(true);
      if (object.key != null)
        if (typeof object.key === 'string')
          $util.base64.decode(
            object.key,
            (message.key = $util.newBuffer($util.base64.length(object.key))),
            0
          );
        else if (object.key.length >= 0) message.key = object.key;
      return message;
    };

    /**
     * Creates a plain object from a PrivateKey message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.PrivateKey
     * @static
     * @param {polycentric.PrivateKey} message PrivateKey
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PrivateKey.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.keyType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.keyType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.key = '';
        else {
          object.key = [];
          if (options.bytes !== Array) object.key = $util.newBuffer(object.key);
        }
      }
      if (message.keyType != null && message.hasOwnProperty('keyType'))
        if (typeof message.keyType === 'number')
          object.keyType =
            options.longs === String
              ? String(message.keyType)
              : message.keyType;
        else
          object.keyType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.keyType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.keyType.low >>> 0,
                    message.keyType.high >>> 0
                  ).toNumber(true)
                : message.keyType;
      if (message.key != null && message.hasOwnProperty('key'))
        object.key =
          options.bytes === String
            ? $util.base64.encode(message.key, 0, message.key.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.key)
              : message.key;
      return object;
    };

    /**
     * Converts this PrivateKey to JSON.
     * @function toJSON
     * @memberof polycentric.PrivateKey
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PrivateKey.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PrivateKey
     * @function getTypeUrl
     * @memberof polycentric.PrivateKey
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PrivateKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.PrivateKey';
    };

    return PrivateKey;
  })();

  polycentric.VectorClock = (function () {
    /**
     * Properties of a VectorClock.
     * @memberof polycentric
     * @interface IVectorClock
     * @property {Array.<number|Long>|null} [logicalClocks] VectorClock logicalClocks
     */

    /**
     * Constructs a new VectorClock.
     * @memberof polycentric
     * @classdesc Represents a VectorClock.
     * @implements IVectorClock
     * @constructor
     * @param {polycentric.IVectorClock=} [properties] Properties to set
     */
    function VectorClock(properties) {
      this.logicalClocks = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * VectorClock logicalClocks.
     * @member {Array.<number|Long>} logicalClocks
     * @memberof polycentric.VectorClock
     * @instance
     */
    VectorClock.prototype.logicalClocks = $util.emptyArray;

    /**
     * Creates a new VectorClock instance using the specified properties.
     * @function create
     * @memberof polycentric.VectorClock
     * @static
     * @param {polycentric.IVectorClock=} [properties] Properties to set
     * @returns {polycentric.VectorClock} VectorClock instance
     */
    VectorClock.create = function create(properties) {
      return new VectorClock(properties);
    };

    /**
     * Encodes the specified VectorClock message. Does not implicitly {@link polycentric.VectorClock.verify|verify} messages.
     * @function encode
     * @memberof polycentric.VectorClock
     * @static
     * @param {polycentric.IVectorClock} message VectorClock message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    VectorClock.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.logicalClocks != null && message.logicalClocks.length) {
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork();
        for (let i = 0; i < message.logicalClocks.length; ++i)
          writer.uint64(message.logicalClocks[i]);
        writer.ldelim();
      }
      return writer;
    };

    /**
     * Encodes the specified VectorClock message, length delimited. Does not implicitly {@link polycentric.VectorClock.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.VectorClock
     * @static
     * @param {polycentric.IVectorClock} message VectorClock message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    VectorClock.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a VectorClock message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.VectorClock
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.VectorClock} VectorClock
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    VectorClock.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.VectorClock();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.logicalClocks && message.logicalClocks.length))
              message.logicalClocks = [];
            if ((tag & 7) === 2) {
              let end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2)
                message.logicalClocks.push(reader.uint64());
            } else message.logicalClocks.push(reader.uint64());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a VectorClock message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.VectorClock
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.VectorClock} VectorClock
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    VectorClock.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a VectorClock message.
     * @function verify
     * @memberof polycentric.VectorClock
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    VectorClock.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.logicalClocks != null &&
        message.hasOwnProperty('logicalClocks')
      ) {
        if (!Array.isArray(message.logicalClocks))
          return 'logicalClocks: array expected';
        for (let i = 0; i < message.logicalClocks.length; ++i)
          if (
            !$util.isInteger(message.logicalClocks[i]) &&
            !(
              message.logicalClocks[i] &&
              $util.isInteger(message.logicalClocks[i].low) &&
              $util.isInteger(message.logicalClocks[i].high)
            )
          )
            return 'logicalClocks: integer|Long[] expected';
      }
      return null;
    };

    /**
     * Creates a VectorClock message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.VectorClock
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.VectorClock} VectorClock
     */
    VectorClock.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.VectorClock) return object;
      let message = new $root.polycentric.VectorClock();
      if (object.logicalClocks) {
        if (!Array.isArray(object.logicalClocks))
          throw TypeError(
            '.polycentric.VectorClock.logicalClocks: array expected'
          );
        message.logicalClocks = [];
        for (let i = 0; i < object.logicalClocks.length; ++i)
          if ($util.Long)
            (message.logicalClocks[i] = $util.Long.fromValue(
              object.logicalClocks[i]
            )).unsigned = true;
          else if (typeof object.logicalClocks[i] === 'string')
            message.logicalClocks[i] = parseInt(object.logicalClocks[i], 10);
          else if (typeof object.logicalClocks[i] === 'number')
            message.logicalClocks[i] = object.logicalClocks[i];
          else if (typeof object.logicalClocks[i] === 'object')
            message.logicalClocks[i] = new $util.LongBits(
              object.logicalClocks[i].low >>> 0,
              object.logicalClocks[i].high >>> 0
            ).toNumber(true);
      }
      return message;
    };

    /**
     * Creates a plain object from a VectorClock message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.VectorClock
     * @static
     * @param {polycentric.VectorClock} message VectorClock
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    VectorClock.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.logicalClocks = [];
      if (message.logicalClocks && message.logicalClocks.length) {
        object.logicalClocks = [];
        for (let j = 0; j < message.logicalClocks.length; ++j)
          if (typeof message.logicalClocks[j] === 'number')
            object.logicalClocks[j] =
              options.longs === String
                ? String(message.logicalClocks[j])
                : message.logicalClocks[j];
          else
            object.logicalClocks[j] =
              options.longs === String
                ? $util.Long.prototype.toString.call(message.logicalClocks[j])
                : options.longs === Number
                  ? new $util.LongBits(
                      message.logicalClocks[j].low >>> 0,
                      message.logicalClocks[j].high >>> 0
                    ).toNumber(true)
                  : message.logicalClocks[j];
      }
      return object;
    };

    /**
     * Converts this VectorClock to JSON.
     * @function toJSON
     * @memberof polycentric.VectorClock
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    VectorClock.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for VectorClock
     * @function getTypeUrl
     * @memberof polycentric.VectorClock
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    VectorClock.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.VectorClock';
    };

    return VectorClock;
  })();

  polycentric.Index = (function () {
    /**
     * Properties of an Index.
     * @memberof polycentric
     * @interface IIndex
     * @property {number|Long|null} [indexType] Index indexType
     * @property {number|Long|null} [logicalClock] Index logicalClock
     */

    /**
     * Constructs a new Index.
     * @memberof polycentric
     * @classdesc Represents an Index.
     * @implements IIndex
     * @constructor
     * @param {polycentric.IIndex=} [properties] Properties to set
     */
    function Index(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Index indexType.
     * @member {number|Long} indexType
     * @memberof polycentric.Index
     * @instance
     */
    Index.prototype.indexType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Index logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.Index
     * @instance
     */
    Index.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new Index instance using the specified properties.
     * @function create
     * @memberof polycentric.Index
     * @static
     * @param {polycentric.IIndex=} [properties] Properties to set
     * @returns {polycentric.Index} Index instance
     */
    Index.create = function create(properties) {
      return new Index(properties);
    };

    /**
     * Encodes the specified Index message. Does not implicitly {@link polycentric.Index.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Index
     * @static
     * @param {polycentric.IIndex} message Index message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Index.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.indexType != null &&
        Object.hasOwnProperty.call(message, 'indexType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.indexType);
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.logicalClock);
      return writer;
    };

    /**
     * Encodes the specified Index message, length delimited. Does not implicitly {@link polycentric.Index.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Index
     * @static
     * @param {polycentric.IIndex} message Index message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Index.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an Index message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Index
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Index} Index
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Index.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Index();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.indexType = reader.uint64();
            break;
          }
          case 2: {
            message.logicalClock = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an Index message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Index
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Index} Index
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Index.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an Index message.
     * @function verify
     * @memberof polycentric.Index
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Index.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.indexType != null && message.hasOwnProperty('indexType'))
        if (
          !$util.isInteger(message.indexType) &&
          !(
            message.indexType &&
            $util.isInteger(message.indexType.low) &&
            $util.isInteger(message.indexType.high)
          )
        )
          return 'indexType: integer|Long expected';
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      return null;
    };

    /**
     * Creates an Index message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Index
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Index} Index
     */
    Index.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Index) return object;
      let message = new $root.polycentric.Index();
      if (object.indexType != null)
        if ($util.Long)
          (message.indexType = $util.Long.fromValue(
            object.indexType
          )).unsigned = true;
        else if (typeof object.indexType === 'string')
          message.indexType = parseInt(object.indexType, 10);
        else if (typeof object.indexType === 'number')
          message.indexType = object.indexType;
        else if (typeof object.indexType === 'object')
          message.indexType = new $util.LongBits(
            object.indexType.low >>> 0,
            object.indexType.high >>> 0
          ).toNumber(true);
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from an Index message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Index
     * @static
     * @param {polycentric.Index} message Index
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Index.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.indexType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.indexType = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
      }
      if (message.indexType != null && message.hasOwnProperty('indexType'))
        if (typeof message.indexType === 'number')
          object.indexType =
            options.longs === String
              ? String(message.indexType)
              : message.indexType;
        else
          object.indexType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.indexType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.indexType.low >>> 0,
                    message.indexType.high >>> 0
                  ).toNumber(true)
                : message.indexType;
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      return object;
    };

    /**
     * Converts this Index to JSON.
     * @function toJSON
     * @memberof polycentric.Index
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Index.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Index
     * @function getTypeUrl
     * @memberof polycentric.Index
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Index.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Index';
    };

    return Index;
  })();

  polycentric.Indices = (function () {
    /**
     * Properties of an Indices.
     * @memberof polycentric
     * @interface IIndices
     * @property {Array.<polycentric.IIndex>|null} [indices] Indices indices
     */

    /**
     * Constructs a new Indices.
     * @memberof polycentric
     * @classdesc Represents an Indices.
     * @implements IIndices
     * @constructor
     * @param {polycentric.IIndices=} [properties] Properties to set
     */
    function Indices(properties) {
      this.indices = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Indices indices.
     * @member {Array.<polycentric.IIndex>} indices
     * @memberof polycentric.Indices
     * @instance
     */
    Indices.prototype.indices = $util.emptyArray;

    /**
     * Creates a new Indices instance using the specified properties.
     * @function create
     * @memberof polycentric.Indices
     * @static
     * @param {polycentric.IIndices=} [properties] Properties to set
     * @returns {polycentric.Indices} Indices instance
     */
    Indices.create = function create(properties) {
      return new Indices(properties);
    };

    /**
     * Encodes the specified Indices message. Does not implicitly {@link polycentric.Indices.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Indices
     * @static
     * @param {polycentric.IIndices} message Indices message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Indices.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.indices != null && message.indices.length)
        for (let i = 0; i < message.indices.length; ++i)
          $root.polycentric.Index.encode(
            message.indices[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified Indices message, length delimited. Does not implicitly {@link polycentric.Indices.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Indices
     * @static
     * @param {polycentric.IIndices} message Indices message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Indices.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an Indices message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Indices
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Indices} Indices
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Indices.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Indices();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.indices && message.indices.length))
              message.indices = [];
            message.indices.push(
              $root.polycentric.Index.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an Indices message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Indices
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Indices} Indices
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Indices.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an Indices message.
     * @function verify
     * @memberof polycentric.Indices
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Indices.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.indices != null && message.hasOwnProperty('indices')) {
        if (!Array.isArray(message.indices)) return 'indices: array expected';
        for (let i = 0; i < message.indices.length; ++i) {
          let error = $root.polycentric.Index.verify(message.indices[i]);
          if (error) return 'indices.' + error;
        }
      }
      return null;
    };

    /**
     * Creates an Indices message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Indices
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Indices} Indices
     */
    Indices.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Indices) return object;
      let message = new $root.polycentric.Indices();
      if (object.indices) {
        if (!Array.isArray(object.indices))
          throw TypeError('.polycentric.Indices.indices: array expected');
        message.indices = [];
        for (let i = 0; i < object.indices.length; ++i) {
          if (typeof object.indices[i] !== 'object')
            throw TypeError('.polycentric.Indices.indices: object expected');
          message.indices[i] = $root.polycentric.Index.fromObject(
            object.indices[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from an Indices message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Indices
     * @static
     * @param {polycentric.Indices} message Indices
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Indices.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.indices = [];
      if (message.indices && message.indices.length) {
        object.indices = [];
        for (let j = 0; j < message.indices.length; ++j)
          object.indices[j] = $root.polycentric.Index.toObject(
            message.indices[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this Indices to JSON.
     * @function toJSON
     * @memberof polycentric.Indices
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Indices.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Indices
     * @function getTypeUrl
     * @memberof polycentric.Indices
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Indices.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Indices';
    };

    return Indices;
  })();

  polycentric.Process = (function () {
    /**
     * Properties of a Process.
     * @memberof polycentric
     * @interface IProcess
     * @property {Uint8Array|null} [process] Process process
     */

    /**
     * Constructs a new Process.
     * @memberof polycentric
     * @classdesc Represents a Process.
     * @implements IProcess
     * @constructor
     * @param {polycentric.IProcess=} [properties] Properties to set
     */
    function Process(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Process process.
     * @member {Uint8Array} process
     * @memberof polycentric.Process
     * @instance
     */
    Process.prototype.process = $util.newBuffer([]);

    /**
     * Creates a new Process instance using the specified properties.
     * @function create
     * @memberof polycentric.Process
     * @static
     * @param {polycentric.IProcess=} [properties] Properties to set
     * @returns {polycentric.Process} Process instance
     */
    Process.create = function create(properties) {
      return new Process(properties);
    };

    /**
     * Encodes the specified Process message. Does not implicitly {@link polycentric.Process.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Process
     * @static
     * @param {polycentric.IProcess} message Process message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Process.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.process);
      return writer;
    };

    /**
     * Encodes the specified Process message, length delimited. Does not implicitly {@link polycentric.Process.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Process
     * @static
     * @param {polycentric.IProcess} message Process message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Process.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Process message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Process
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Process} Process
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Process.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Process();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.process = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Process message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Process
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Process} Process
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Process.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Process message.
     * @function verify
     * @memberof polycentric.Process
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Process.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.process != null && message.hasOwnProperty('process'))
        if (
          !(
            (message.process && typeof message.process.length === 'number') ||
            $util.isString(message.process)
          )
        )
          return 'process: buffer expected';
      return null;
    };

    /**
     * Creates a Process message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Process
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Process} Process
     */
    Process.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Process) return object;
      let message = new $root.polycentric.Process();
      if (object.process != null)
        if (typeof object.process === 'string')
          $util.base64.decode(
            object.process,
            (message.process = $util.newBuffer(
              $util.base64.length(object.process)
            )),
            0
          );
        else if (object.process.length >= 0) message.process = object.process;
      return message;
    };

    /**
     * Creates a plain object from a Process message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Process
     * @static
     * @param {polycentric.Process} message Process
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Process.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults)
        if (options.bytes === String) object.process = '';
        else {
          object.process = [];
          if (options.bytes !== Array)
            object.process = $util.newBuffer(object.process);
        }
      if (message.process != null && message.hasOwnProperty('process'))
        object.process =
          options.bytes === String
            ? $util.base64.encode(message.process, 0, message.process.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.process)
              : message.process;
      return object;
    };

    /**
     * Converts this Process to JSON.
     * @function toJSON
     * @memberof polycentric.Process
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Process.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Process
     * @function getTypeUrl
     * @memberof polycentric.Process
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Process.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Process';
    };

    return Process;
  })();

  polycentric.Digest = (function () {
    /**
     * Properties of a Digest.
     * @memberof polycentric
     * @interface IDigest
     * @property {number|Long|null} [digestType] Digest digestType
     * @property {Uint8Array|null} [digest] Digest digest
     */

    /**
     * Constructs a new Digest.
     * @memberof polycentric
     * @classdesc Represents a Digest.
     * @implements IDigest
     * @constructor
     * @param {polycentric.IDigest=} [properties] Properties to set
     */
    function Digest(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Digest digestType.
     * @member {number|Long} digestType
     * @memberof polycentric.Digest
     * @instance
     */
    Digest.prototype.digestType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Digest digest.
     * @member {Uint8Array} digest
     * @memberof polycentric.Digest
     * @instance
     */
    Digest.prototype.digest = $util.newBuffer([]);

    /**
     * Creates a new Digest instance using the specified properties.
     * @function create
     * @memberof polycentric.Digest
     * @static
     * @param {polycentric.IDigest=} [properties] Properties to set
     * @returns {polycentric.Digest} Digest instance
     */
    Digest.create = function create(properties) {
      return new Digest(properties);
    };

    /**
     * Encodes the specified Digest message. Does not implicitly {@link polycentric.Digest.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Digest
     * @static
     * @param {polycentric.IDigest} message Digest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Digest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.digestType != null &&
        Object.hasOwnProperty.call(message, 'digestType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.digestType);
      if (
        message.digest != null &&
        Object.hasOwnProperty.call(message, 'digest')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.digest);
      return writer;
    };

    /**
     * Encodes the specified Digest message, length delimited. Does not implicitly {@link polycentric.Digest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Digest
     * @static
     * @param {polycentric.IDigest} message Digest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Digest.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Digest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Digest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Digest} Digest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Digest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Digest();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.digestType = reader.uint64();
            break;
          }
          case 2: {
            message.digest = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Digest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Digest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Digest} Digest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Digest.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Digest message.
     * @function verify
     * @memberof polycentric.Digest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Digest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.digestType != null && message.hasOwnProperty('digestType'))
        if (
          !$util.isInteger(message.digestType) &&
          !(
            message.digestType &&
            $util.isInteger(message.digestType.low) &&
            $util.isInteger(message.digestType.high)
          )
        )
          return 'digestType: integer|Long expected';
      if (message.digest != null && message.hasOwnProperty('digest'))
        if (
          !(
            (message.digest && typeof message.digest.length === 'number') ||
            $util.isString(message.digest)
          )
        )
          return 'digest: buffer expected';
      return null;
    };

    /**
     * Creates a Digest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Digest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Digest} Digest
     */
    Digest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Digest) return object;
      let message = new $root.polycentric.Digest();
      if (object.digestType != null)
        if ($util.Long)
          (message.digestType = $util.Long.fromValue(
            object.digestType
          )).unsigned = true;
        else if (typeof object.digestType === 'string')
          message.digestType = parseInt(object.digestType, 10);
        else if (typeof object.digestType === 'number')
          message.digestType = object.digestType;
        else if (typeof object.digestType === 'object')
          message.digestType = new $util.LongBits(
            object.digestType.low >>> 0,
            object.digestType.high >>> 0
          ).toNumber(true);
      if (object.digest != null)
        if (typeof object.digest === 'string')
          $util.base64.decode(
            object.digest,
            (message.digest = $util.newBuffer(
              $util.base64.length(object.digest)
            )),
            0
          );
        else if (object.digest.length >= 0) message.digest = object.digest;
      return message;
    };

    /**
     * Creates a plain object from a Digest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Digest
     * @static
     * @param {polycentric.Digest} message Digest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Digest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.digestType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.digestType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.digest = '';
        else {
          object.digest = [];
          if (options.bytes !== Array)
            object.digest = $util.newBuffer(object.digest);
        }
      }
      if (message.digestType != null && message.hasOwnProperty('digestType'))
        if (typeof message.digestType === 'number')
          object.digestType =
            options.longs === String
              ? String(message.digestType)
              : message.digestType;
        else
          object.digestType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.digestType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.digestType.low >>> 0,
                    message.digestType.high >>> 0
                  ).toNumber(true)
                : message.digestType;
      if (message.digest != null && message.hasOwnProperty('digest'))
        object.digest =
          options.bytes === String
            ? $util.base64.encode(message.digest, 0, message.digest.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.digest)
              : message.digest;
      return object;
    };

    /**
     * Converts this Digest to JSON.
     * @function toJSON
     * @memberof polycentric.Digest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Digest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Digest
     * @function getTypeUrl
     * @memberof polycentric.Digest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Digest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Digest';
    };

    /**
     * DigestType enum.
     * @name polycentric.Digest.DigestType
     * @enum {number}
     * @property {number} Unknown=0 Unknown value
     * @property {number} SHA256=1 SHA256 value
     */
    Digest.DigestType = (function () {
      const valuesById = {},
        values = Object.create(valuesById);
      values[(valuesById[0] = 'Unknown')] = 0;
      values[(valuesById[1] = 'SHA256')] = 1;
      return values;
    })();

    return Digest;
  })();

  polycentric.Pointer = (function () {
    /**
     * Properties of a Pointer.
     * @memberof polycentric
     * @interface IPointer
     * @property {polycentric.IPublicKey|null} [system] Pointer system
     * @property {polycentric.IProcess|null} [process] Pointer process
     * @property {number|Long|null} [logicalClock] Pointer logicalClock
     * @property {polycentric.IDigest|null} [eventDigest] Pointer eventDigest
     */

    /**
     * Constructs a new Pointer.
     * @memberof polycentric
     * @classdesc Represents a Pointer.
     * @implements IPointer
     * @constructor
     * @param {polycentric.IPointer=} [properties] Properties to set
     */
    function Pointer(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Pointer system.
     * @member {polycentric.IPublicKey|null|undefined} system
     * @memberof polycentric.Pointer
     * @instance
     */
    Pointer.prototype.system = null;

    /**
     * Pointer process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.Pointer
     * @instance
     */
    Pointer.prototype.process = null;

    /**
     * Pointer logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.Pointer
     * @instance
     */
    Pointer.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Pointer eventDigest.
     * @member {polycentric.IDigest|null|undefined} eventDigest
     * @memberof polycentric.Pointer
     * @instance
     */
    Pointer.prototype.eventDigest = null;

    /**
     * Creates a new Pointer instance using the specified properties.
     * @function create
     * @memberof polycentric.Pointer
     * @static
     * @param {polycentric.IPointer=} [properties] Properties to set
     * @returns {polycentric.Pointer} Pointer instance
     */
    Pointer.create = function create(properties) {
      return new Pointer(properties);
    };

    /**
     * Encodes the specified Pointer message. Does not implicitly {@link polycentric.Pointer.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Pointer
     * @static
     * @param {polycentric.IPointer} message Pointer message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Pointer.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PublicKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint64(message.logicalClock);
      if (
        message.eventDigest != null &&
        Object.hasOwnProperty.call(message, 'eventDigest')
      )
        $root.polycentric.Digest.encode(
          message.eventDigest,
          writer.uint32(/* id 4, wireType 2 =*/ 34).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified Pointer message, length delimited. Does not implicitly {@link polycentric.Pointer.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Pointer
     * @static
     * @param {polycentric.IPointer} message Pointer message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Pointer.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Pointer message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Pointer
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Pointer} Pointer
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Pointer.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Pointer();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            message.logicalClock = reader.uint64();
            break;
          }
          case 4: {
            message.eventDigest = $root.polycentric.Digest.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Pointer message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Pointer
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Pointer} Pointer
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Pointer.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Pointer message.
     * @function verify
     * @memberof polycentric.Pointer
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Pointer.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PublicKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      if (
        message.eventDigest != null &&
        message.hasOwnProperty('eventDigest')
      ) {
        let error = $root.polycentric.Digest.verify(message.eventDigest);
        if (error) return 'eventDigest.' + error;
      }
      return null;
    };

    /**
     * Creates a Pointer message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Pointer
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Pointer} Pointer
     */
    Pointer.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Pointer) return object;
      let message = new $root.polycentric.Pointer();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError('.polycentric.Pointer.system: object expected');
        message.system = $root.polycentric.PublicKey.fromObject(object.system);
      }
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError('.polycentric.Pointer.process: object expected');
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      if (object.eventDigest != null) {
        if (typeof object.eventDigest !== 'object')
          throw TypeError('.polycentric.Pointer.eventDigest: object expected');
        message.eventDigest = $root.polycentric.Digest.fromObject(
          object.eventDigest
        );
      }
      return message;
    };

    /**
     * Creates a plain object from a Pointer message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Pointer
     * @static
     * @param {polycentric.Pointer} message Pointer
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Pointer.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.system = null;
        object.process = null;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
        object.eventDigest = null;
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PublicKey.toObject(
          message.system,
          options
        );
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      if (message.eventDigest != null && message.hasOwnProperty('eventDigest'))
        object.eventDigest = $root.polycentric.Digest.toObject(
          message.eventDigest,
          options
        );
      return object;
    };

    /**
     * Converts this Pointer to JSON.
     * @function toJSON
     * @memberof polycentric.Pointer
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Pointer.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Pointer
     * @function getTypeUrl
     * @memberof polycentric.Pointer
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Pointer.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Pointer';
    };

    return Pointer;
  })();

  polycentric.LWWElementSet = (function () {
    /**
     * Properties of a LWWElementSet.
     * @memberof polycentric
     * @interface ILWWElementSet
     * @property {polycentric.LWWElementSet.Operation|null} [operation] LWWElementSet operation
     * @property {Uint8Array|null} [value] LWWElementSet value
     * @property {number|Long|null} [unixMilliseconds] LWWElementSet unixMilliseconds
     */

    /**
     * Constructs a new LWWElementSet.
     * @memberof polycentric
     * @classdesc Represents a LWWElementSet.
     * @implements ILWWElementSet
     * @constructor
     * @param {polycentric.ILWWElementSet=} [properties] Properties to set
     */
    function LWWElementSet(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * LWWElementSet operation.
     * @member {polycentric.LWWElementSet.Operation} operation
     * @memberof polycentric.LWWElementSet
     * @instance
     */
    LWWElementSet.prototype.operation = 0;

    /**
     * LWWElementSet value.
     * @member {Uint8Array} value
     * @memberof polycentric.LWWElementSet
     * @instance
     */
    LWWElementSet.prototype.value = $util.newBuffer([]);

    /**
     * LWWElementSet unixMilliseconds.
     * @member {number|Long} unixMilliseconds
     * @memberof polycentric.LWWElementSet
     * @instance
     */
    LWWElementSet.prototype.unixMilliseconds = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new LWWElementSet instance using the specified properties.
     * @function create
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {polycentric.ILWWElementSet=} [properties] Properties to set
     * @returns {polycentric.LWWElementSet} LWWElementSet instance
     */
    LWWElementSet.create = function create(properties) {
      return new LWWElementSet(properties);
    };

    /**
     * Encodes the specified LWWElementSet message. Does not implicitly {@link polycentric.LWWElementSet.verify|verify} messages.
     * @function encode
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {polycentric.ILWWElementSet} message LWWElementSet message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LWWElementSet.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.operation != null &&
        Object.hasOwnProperty.call(message, 'operation')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).int32(message.operation);
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.value);
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 3, wireType 0 =*/ 24)
          .uint64(message.unixMilliseconds);
      return writer;
    };

    /**
     * Encodes the specified LWWElementSet message, length delimited. Does not implicitly {@link polycentric.LWWElementSet.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {polycentric.ILWWElementSet} message LWWElementSet message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LWWElementSet.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a LWWElementSet message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.LWWElementSet} LWWElementSet
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LWWElementSet.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.LWWElementSet();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.operation = reader.int32();
            break;
          }
          case 2: {
            message.value = reader.bytes();
            break;
          }
          case 3: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a LWWElementSet message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.LWWElementSet} LWWElementSet
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LWWElementSet.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a LWWElementSet message.
     * @function verify
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    LWWElementSet.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.operation != null && message.hasOwnProperty('operation'))
        switch (message.operation) {
          default:
            return 'operation: enum value expected';
          case 0:
          case 1:
            break;
        }
      if (message.value != null && message.hasOwnProperty('value'))
        if (
          !(
            (message.value && typeof message.value.length === 'number') ||
            $util.isString(message.value)
          )
        )
          return 'value: buffer expected';
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      return null;
    };

    /**
     * Creates a LWWElementSet message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.LWWElementSet} LWWElementSet
     */
    LWWElementSet.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.LWWElementSet) return object;
      let message = new $root.polycentric.LWWElementSet();
      switch (object.operation) {
        default:
          if (typeof object.operation === 'number') {
            message.operation = object.operation;
            break;
          }
          break;
        case 'ADD':
        case 0:
          message.operation = 0;
          break;
        case 'REMOVE':
        case 1:
          message.operation = 1;
          break;
      }
      if (object.value != null)
        if (typeof object.value === 'string')
          $util.base64.decode(
            object.value,
            (message.value = $util.newBuffer(
              $util.base64.length(object.value)
            )),
            0
          );
        else if (object.value.length >= 0) message.value = object.value;
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a LWWElementSet message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {polycentric.LWWElementSet} message LWWElementSet
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    LWWElementSet.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.operation = options.enums === String ? 'ADD' : 0;
        if (options.bytes === String) object.value = '';
        else {
          object.value = [];
          if (options.bytes !== Array)
            object.value = $util.newBuffer(object.value);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.unixMilliseconds =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.unixMilliseconds = options.longs === String ? '0' : 0;
      }
      if (message.operation != null && message.hasOwnProperty('operation'))
        object.operation =
          options.enums === String
            ? $root.polycentric.LWWElementSet.Operation[message.operation] ===
              undefined
              ? message.operation
              : $root.polycentric.LWWElementSet.Operation[message.operation]
            : message.operation;
      if (message.value != null && message.hasOwnProperty('value'))
        object.value =
          options.bytes === String
            ? $util.base64.encode(message.value, 0, message.value.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.value)
              : message.value;
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
      return object;
    };

    /**
     * Converts this LWWElementSet to JSON.
     * @function toJSON
     * @memberof polycentric.LWWElementSet
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    LWWElementSet.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for LWWElementSet
     * @function getTypeUrl
     * @memberof polycentric.LWWElementSet
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    LWWElementSet.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.LWWElementSet';
    };

    /**
     * Operation enum.
     * @name polycentric.LWWElementSet.Operation
     * @enum {number}
     * @property {number} ADD=0 ADD value
     * @property {number} REMOVE=1 REMOVE value
     */
    LWWElementSet.Operation = (function () {
      const valuesById = {},
        values = Object.create(valuesById);
      values[(valuesById[0] = 'ADD')] = 0;
      values[(valuesById[1] = 'REMOVE')] = 1;
      return values;
    })();

    return LWWElementSet;
  })();

  polycentric.LWWElement = (function () {
    /**
     * Properties of a LWWElement.
     * @memberof polycentric
     * @interface ILWWElement
     * @property {Uint8Array|null} [value] LWWElement value
     * @property {number|Long|null} [unixMilliseconds] LWWElement unixMilliseconds
     */

    /**
     * Constructs a new LWWElement.
     * @memberof polycentric
     * @classdesc Represents a LWWElement.
     * @implements ILWWElement
     * @constructor
     * @param {polycentric.ILWWElement=} [properties] Properties to set
     */
    function LWWElement(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * LWWElement value.
     * @member {Uint8Array} value
     * @memberof polycentric.LWWElement
     * @instance
     */
    LWWElement.prototype.value = $util.newBuffer([]);

    /**
     * LWWElement unixMilliseconds.
     * @member {number|Long} unixMilliseconds
     * @memberof polycentric.LWWElement
     * @instance
     */
    LWWElement.prototype.unixMilliseconds = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new LWWElement instance using the specified properties.
     * @function create
     * @memberof polycentric.LWWElement
     * @static
     * @param {polycentric.ILWWElement=} [properties] Properties to set
     * @returns {polycentric.LWWElement} LWWElement instance
     */
    LWWElement.create = function create(properties) {
      return new LWWElement(properties);
    };

    /**
     * Encodes the specified LWWElement message. Does not implicitly {@link polycentric.LWWElement.verify|verify} messages.
     * @function encode
     * @memberof polycentric.LWWElement
     * @static
     * @param {polycentric.ILWWElement} message LWWElement message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LWWElement.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.value);
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 2, wireType 0 =*/ 16)
          .uint64(message.unixMilliseconds);
      return writer;
    };

    /**
     * Encodes the specified LWWElement message, length delimited. Does not implicitly {@link polycentric.LWWElement.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.LWWElement
     * @static
     * @param {polycentric.ILWWElement} message LWWElement message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LWWElement.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a LWWElement message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.LWWElement
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.LWWElement} LWWElement
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LWWElement.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.LWWElement();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.value = reader.bytes();
            break;
          }
          case 2: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a LWWElement message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.LWWElement
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.LWWElement} LWWElement
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LWWElement.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a LWWElement message.
     * @function verify
     * @memberof polycentric.LWWElement
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    LWWElement.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.value != null && message.hasOwnProperty('value'))
        if (
          !(
            (message.value && typeof message.value.length === 'number') ||
            $util.isString(message.value)
          )
        )
          return 'value: buffer expected';
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      return null;
    };

    /**
     * Creates a LWWElement message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.LWWElement
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.LWWElement} LWWElement
     */
    LWWElement.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.LWWElement) return object;
      let message = new $root.polycentric.LWWElement();
      if (object.value != null)
        if (typeof object.value === 'string')
          $util.base64.decode(
            object.value,
            (message.value = $util.newBuffer(
              $util.base64.length(object.value)
            )),
            0
          );
        else if (object.value.length >= 0) message.value = object.value;
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a LWWElement message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.LWWElement
     * @static
     * @param {polycentric.LWWElement} message LWWElement
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    LWWElement.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if (options.bytes === String) object.value = '';
        else {
          object.value = [];
          if (options.bytes !== Array)
            object.value = $util.newBuffer(object.value);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.unixMilliseconds =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.unixMilliseconds = options.longs === String ? '0' : 0;
      }
      if (message.value != null && message.hasOwnProperty('value'))
        object.value =
          options.bytes === String
            ? $util.base64.encode(message.value, 0, message.value.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.value)
              : message.value;
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
      return object;
    };

    /**
     * Converts this LWWElement to JSON.
     * @function toJSON
     * @memberof polycentric.LWWElement
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    LWWElement.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for LWWElement
     * @function getTypeUrl
     * @memberof polycentric.LWWElement
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    LWWElement.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.LWWElement';
    };

    return LWWElement;
  })();

  polycentric.Reference = (function () {
    /**
     * Properties of a Reference.
     * @memberof polycentric
     * @interface IReference
     * @property {number|Long|null} [referenceType] Reference referenceType
     * @property {Uint8Array|null} [reference] Reference reference
     */

    /**
     * Constructs a new Reference.
     * @memberof polycentric
     * @classdesc Represents a Reference.
     * @implements IReference
     * @constructor
     * @param {polycentric.IReference=} [properties] Properties to set
     */
    function Reference(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Reference referenceType.
     * @member {number|Long} referenceType
     * @memberof polycentric.Reference
     * @instance
     */
    Reference.prototype.referenceType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Reference reference.
     * @member {Uint8Array} reference
     * @memberof polycentric.Reference
     * @instance
     */
    Reference.prototype.reference = $util.newBuffer([]);

    /**
     * Creates a new Reference instance using the specified properties.
     * @function create
     * @memberof polycentric.Reference
     * @static
     * @param {polycentric.IReference=} [properties] Properties to set
     * @returns {polycentric.Reference} Reference instance
     */
    Reference.create = function create(properties) {
      return new Reference(properties);
    };

    /**
     * Encodes the specified Reference message. Does not implicitly {@link polycentric.Reference.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Reference
     * @static
     * @param {polycentric.IReference} message Reference message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Reference.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.referenceType != null &&
        Object.hasOwnProperty.call(message, 'referenceType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.referenceType);
      if (
        message.reference != null &&
        Object.hasOwnProperty.call(message, 'reference')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.reference);
      return writer;
    };

    /**
     * Encodes the specified Reference message, length delimited. Does not implicitly {@link polycentric.Reference.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Reference
     * @static
     * @param {polycentric.IReference} message Reference message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Reference.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Reference message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Reference
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Reference} Reference
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Reference.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Reference();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.referenceType = reader.uint64();
            break;
          }
          case 2: {
            message.reference = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Reference message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Reference
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Reference} Reference
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Reference.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Reference message.
     * @function verify
     * @memberof polycentric.Reference
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Reference.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.referenceType != null &&
        message.hasOwnProperty('referenceType')
      )
        if (
          !$util.isInteger(message.referenceType) &&
          !(
            message.referenceType &&
            $util.isInteger(message.referenceType.low) &&
            $util.isInteger(message.referenceType.high)
          )
        )
          return 'referenceType: integer|Long expected';
      if (message.reference != null && message.hasOwnProperty('reference'))
        if (
          !(
            (message.reference &&
              typeof message.reference.length === 'number') ||
            $util.isString(message.reference)
          )
        )
          return 'reference: buffer expected';
      return null;
    };

    /**
     * Creates a Reference message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Reference
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Reference} Reference
     */
    Reference.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Reference) return object;
      let message = new $root.polycentric.Reference();
      if (object.referenceType != null)
        if ($util.Long)
          (message.referenceType = $util.Long.fromValue(
            object.referenceType
          )).unsigned = true;
        else if (typeof object.referenceType === 'string')
          message.referenceType = parseInt(object.referenceType, 10);
        else if (typeof object.referenceType === 'number')
          message.referenceType = object.referenceType;
        else if (typeof object.referenceType === 'object')
          message.referenceType = new $util.LongBits(
            object.referenceType.low >>> 0,
            object.referenceType.high >>> 0
          ).toNumber(true);
      if (object.reference != null)
        if (typeof object.reference === 'string')
          $util.base64.decode(
            object.reference,
            (message.reference = $util.newBuffer(
              $util.base64.length(object.reference)
            )),
            0
          );
        else if (object.reference.length >= 0)
          message.reference = object.reference;
      return message;
    };

    /**
     * Creates a plain object from a Reference message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Reference
     * @static
     * @param {polycentric.Reference} message Reference
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Reference.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.referenceType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.referenceType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.reference = '';
        else {
          object.reference = [];
          if (options.bytes !== Array)
            object.reference = $util.newBuffer(object.reference);
        }
      }
      if (
        message.referenceType != null &&
        message.hasOwnProperty('referenceType')
      )
        if (typeof message.referenceType === 'number')
          object.referenceType =
            options.longs === String
              ? String(message.referenceType)
              : message.referenceType;
        else
          object.referenceType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.referenceType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.referenceType.low >>> 0,
                    message.referenceType.high >>> 0
                  ).toNumber(true)
                : message.referenceType;
      if (message.reference != null && message.hasOwnProperty('reference'))
        object.reference =
          options.bytes === String
            ? $util.base64.encode(
                message.reference,
                0,
                message.reference.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.reference)
              : message.reference;
      return object;
    };

    /**
     * Converts this Reference to JSON.
     * @function toJSON
     * @memberof polycentric.Reference
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Reference.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Reference
     * @function getTypeUrl
     * @memberof polycentric.Reference
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Reference.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Reference';
    };

    /**
     * ReferenceType enum.
     * @name polycentric.Reference.ReferenceType
     * @enum {number}
     * @property {number} Unknown=0 Unknown value
     * @property {number} System=1 System value
     * @property {number} Pointer=2 Pointer value
     * @property {number} Bytes=3 Bytes value
     */
    Reference.ReferenceType = (function () {
      const valuesById = {},
        values = Object.create(valuesById);
      values[(valuesById[0] = 'Unknown')] = 0;
      values[(valuesById[1] = 'System')] = 1;
      values[(valuesById[2] = 'Pointer')] = 2;
      values[(valuesById[3] = 'Bytes')] = 3;
      return values;
    })();

    return Reference;
  })();

  polycentric.ProcessSecret = (function () {
    /**
     * Properties of a ProcessSecret.
     * @memberof polycentric
     * @interface IProcessSecret
     * @property {polycentric.IPrivateKey|null} [system] ProcessSecret system
     * @property {polycentric.IProcess|null} [process] ProcessSecret process
     */

    /**
     * Constructs a new ProcessSecret.
     * @memberof polycentric
     * @classdesc Represents a ProcessSecret.
     * @implements IProcessSecret
     * @constructor
     * @param {polycentric.IProcessSecret=} [properties] Properties to set
     */
    function ProcessSecret(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ProcessSecret system.
     * @member {polycentric.IPrivateKey|null|undefined} system
     * @memberof polycentric.ProcessSecret
     * @instance
     */
    ProcessSecret.prototype.system = null;

    /**
     * ProcessSecret process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.ProcessSecret
     * @instance
     */
    ProcessSecret.prototype.process = null;

    /**
     * Creates a new ProcessSecret instance using the specified properties.
     * @function create
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {polycentric.IProcessSecret=} [properties] Properties to set
     * @returns {polycentric.ProcessSecret} ProcessSecret instance
     */
    ProcessSecret.create = function create(properties) {
      return new ProcessSecret(properties);
    };

    /**
     * Encodes the specified ProcessSecret message. Does not implicitly {@link polycentric.ProcessSecret.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {polycentric.IProcessSecret} message ProcessSecret message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProcessSecret.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PrivateKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified ProcessSecret message, length delimited. Does not implicitly {@link polycentric.ProcessSecret.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {polycentric.IProcessSecret} message ProcessSecret message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProcessSecret.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ProcessSecret message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ProcessSecret} ProcessSecret
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProcessSecret.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ProcessSecret();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PrivateKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ProcessSecret message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ProcessSecret} ProcessSecret
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProcessSecret.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ProcessSecret message.
     * @function verify
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ProcessSecret.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PrivateKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      return null;
    };

    /**
     * Creates a ProcessSecret message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ProcessSecret} ProcessSecret
     */
    ProcessSecret.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ProcessSecret) return object;
      let message = new $root.polycentric.ProcessSecret();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError('.polycentric.ProcessSecret.system: object expected');
        message.system = $root.polycentric.PrivateKey.fromObject(object.system);
      }
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError(
            '.polycentric.ProcessSecret.process: object expected'
          );
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      return message;
    };

    /**
     * Creates a plain object from a ProcessSecret message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {polycentric.ProcessSecret} message ProcessSecret
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ProcessSecret.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.system = null;
        object.process = null;
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PrivateKey.toObject(
          message.system,
          options
        );
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      return object;
    };

    /**
     * Converts this ProcessSecret to JSON.
     * @function toJSON
     * @memberof polycentric.ProcessSecret
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ProcessSecret.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ProcessSecret
     * @function getTypeUrl
     * @memberof polycentric.ProcessSecret
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ProcessSecret.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ProcessSecret';
    };

    return ProcessSecret;
  })();

  polycentric.ModerationTag = (function () {
    /**
     * Properties of a ModerationTag.
     * @memberof polycentric
     * @interface IModerationTag
     * @property {string|null} [name] ModerationTag name
     * @property {number|null} [level] ModerationTag level
     */

    /**
     * Constructs a new ModerationTag.
     * @memberof polycentric
     * @classdesc Represents a ModerationTag.
     * @implements IModerationTag
     * @constructor
     * @param {polycentric.IModerationTag=} [properties] Properties to set
     */
    function ModerationTag(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ModerationTag name.
     * @member {string} name
     * @memberof polycentric.ModerationTag
     * @instance
     */
    ModerationTag.prototype.name = '';

    /**
     * ModerationTag level.
     * @member {number} level
     * @memberof polycentric.ModerationTag
     * @instance
     */
    ModerationTag.prototype.level = 0;

    /**
     * Creates a new ModerationTag instance using the specified properties.
     * @function create
     * @memberof polycentric.ModerationTag
     * @static
     * @param {polycentric.IModerationTag=} [properties] Properties to set
     * @returns {polycentric.ModerationTag} ModerationTag instance
     */
    ModerationTag.create = function create(properties) {
      return new ModerationTag(properties);
    };

    /**
     * Encodes the specified ModerationTag message. Does not implicitly {@link polycentric.ModerationTag.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ModerationTag
     * @static
     * @param {polycentric.IModerationTag} message ModerationTag message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ModerationTag.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.name != null && Object.hasOwnProperty.call(message, 'name'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.name);
      if (message.level != null && Object.hasOwnProperty.call(message, 'level'))
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.level);
      return writer;
    };

    /**
     * Encodes the specified ModerationTag message, length delimited. Does not implicitly {@link polycentric.ModerationTag.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ModerationTag
     * @static
     * @param {polycentric.IModerationTag} message ModerationTag message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ModerationTag.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ModerationTag message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ModerationTag
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ModerationTag} ModerationTag
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ModerationTag.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ModerationTag();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.name = reader.string();
            break;
          }
          case 2: {
            message.level = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ModerationTag message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ModerationTag
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ModerationTag} ModerationTag
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ModerationTag.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ModerationTag message.
     * @function verify
     * @memberof polycentric.ModerationTag
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ModerationTag.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.name != null && message.hasOwnProperty('name'))
        if (!$util.isString(message.name)) return 'name: string expected';
      if (message.level != null && message.hasOwnProperty('level'))
        if (!$util.isInteger(message.level)) return 'level: integer expected';
      return null;
    };

    /**
     * Creates a ModerationTag message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ModerationTag
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ModerationTag} ModerationTag
     */
    ModerationTag.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ModerationTag) return object;
      let message = new $root.polycentric.ModerationTag();
      if (object.name != null) message.name = String(object.name);
      if (object.level != null) message.level = object.level >>> 0;
      return message;
    };

    /**
     * Creates a plain object from a ModerationTag message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ModerationTag
     * @static
     * @param {polycentric.ModerationTag} message ModerationTag
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ModerationTag.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.name = '';
        object.level = 0;
      }
      if (message.name != null && message.hasOwnProperty('name'))
        object.name = message.name;
      if (message.level != null && message.hasOwnProperty('level'))
        object.level = message.level;
      return object;
    };

    /**
     * Converts this ModerationTag to JSON.
     * @function toJSON
     * @memberof polycentric.ModerationTag
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ModerationTag.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ModerationTag
     * @function getTypeUrl
     * @memberof polycentric.ModerationTag
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ModerationTag.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ModerationTag';
    };

    return ModerationTag;
  })();

  polycentric.SignedEvent = (function () {
    /**
     * Properties of a SignedEvent.
     * @memberof polycentric
     * @interface ISignedEvent
     * @property {Uint8Array|null} [signature] SignedEvent signature
     * @property {Uint8Array|null} [event] SignedEvent event
     * @property {Array.<polycentric.IModerationTag>|null} [moderationTags] SignedEvent moderationTags
     */

    /**
     * Constructs a new SignedEvent.
     * @memberof polycentric
     * @classdesc Represents a SignedEvent.
     * @implements ISignedEvent
     * @constructor
     * @param {polycentric.ISignedEvent=} [properties] Properties to set
     */
    function SignedEvent(properties) {
      this.moderationTags = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * SignedEvent signature.
     * @member {Uint8Array} signature
     * @memberof polycentric.SignedEvent
     * @instance
     */
    SignedEvent.prototype.signature = $util.newBuffer([]);

    /**
     * SignedEvent event.
     * @member {Uint8Array} event
     * @memberof polycentric.SignedEvent
     * @instance
     */
    SignedEvent.prototype.event = $util.newBuffer([]);

    /**
     * SignedEvent moderationTags.
     * @member {Array.<polycentric.IModerationTag>} moderationTags
     * @memberof polycentric.SignedEvent
     * @instance
     */
    SignedEvent.prototype.moderationTags = $util.emptyArray;

    /**
     * Creates a new SignedEvent instance using the specified properties.
     * @function create
     * @memberof polycentric.SignedEvent
     * @static
     * @param {polycentric.ISignedEvent=} [properties] Properties to set
     * @returns {polycentric.SignedEvent} SignedEvent instance
     */
    SignedEvent.create = function create(properties) {
      return new SignedEvent(properties);
    };

    /**
     * Encodes the specified SignedEvent message. Does not implicitly {@link polycentric.SignedEvent.verify|verify} messages.
     * @function encode
     * @memberof polycentric.SignedEvent
     * @static
     * @param {polycentric.ISignedEvent} message SignedEvent message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SignedEvent.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.signature != null &&
        Object.hasOwnProperty.call(message, 'signature')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.signature);
      if (message.event != null && Object.hasOwnProperty.call(message, 'event'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.event);
      if (message.moderationTags != null && message.moderationTags.length)
        for (let i = 0; i < message.moderationTags.length; ++i)
          $root.polycentric.ModerationTag.encode(
            message.moderationTags[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified SignedEvent message, length delimited. Does not implicitly {@link polycentric.SignedEvent.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.SignedEvent
     * @static
     * @param {polycentric.ISignedEvent} message SignedEvent message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SignedEvent.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SignedEvent message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.SignedEvent
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.SignedEvent} SignedEvent
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SignedEvent.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.SignedEvent();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.signature = reader.bytes();
            break;
          }
          case 2: {
            message.event = reader.bytes();
            break;
          }
          case 3: {
            if (!(message.moderationTags && message.moderationTags.length))
              message.moderationTags = [];
            message.moderationTags.push(
              $root.polycentric.ModerationTag.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a SignedEvent message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.SignedEvent
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.SignedEvent} SignedEvent
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SignedEvent.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SignedEvent message.
     * @function verify
     * @memberof polycentric.SignedEvent
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SignedEvent.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.signature != null && message.hasOwnProperty('signature'))
        if (
          !(
            (message.signature &&
              typeof message.signature.length === 'number') ||
            $util.isString(message.signature)
          )
        )
          return 'signature: buffer expected';
      if (message.event != null && message.hasOwnProperty('event'))
        if (
          !(
            (message.event && typeof message.event.length === 'number') ||
            $util.isString(message.event)
          )
        )
          return 'event: buffer expected';
      if (
        message.moderationTags != null &&
        message.hasOwnProperty('moderationTags')
      ) {
        if (!Array.isArray(message.moderationTags))
          return 'moderationTags: array expected';
        for (let i = 0; i < message.moderationTags.length; ++i) {
          let error = $root.polycentric.ModerationTag.verify(
            message.moderationTags[i]
          );
          if (error) return 'moderationTags.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a SignedEvent message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.SignedEvent
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.SignedEvent} SignedEvent
     */
    SignedEvent.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.SignedEvent) return object;
      let message = new $root.polycentric.SignedEvent();
      if (object.signature != null)
        if (typeof object.signature === 'string')
          $util.base64.decode(
            object.signature,
            (message.signature = $util.newBuffer(
              $util.base64.length(object.signature)
            )),
            0
          );
        else if (object.signature.length >= 0)
          message.signature = object.signature;
      if (object.event != null)
        if (typeof object.event === 'string')
          $util.base64.decode(
            object.event,
            (message.event = $util.newBuffer(
              $util.base64.length(object.event)
            )),
            0
          );
        else if (object.event.length >= 0) message.event = object.event;
      if (object.moderationTags) {
        if (!Array.isArray(object.moderationTags))
          throw TypeError(
            '.polycentric.SignedEvent.moderationTags: array expected'
          );
        message.moderationTags = [];
        for (let i = 0; i < object.moderationTags.length; ++i) {
          if (typeof object.moderationTags[i] !== 'object')
            throw TypeError(
              '.polycentric.SignedEvent.moderationTags: object expected'
            );
          message.moderationTags[i] =
            $root.polycentric.ModerationTag.fromObject(
              object.moderationTags[i]
            );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a SignedEvent message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.SignedEvent
     * @static
     * @param {polycentric.SignedEvent} message SignedEvent
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SignedEvent.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.moderationTags = [];
      if (options.defaults) {
        if (options.bytes === String) object.signature = '';
        else {
          object.signature = [];
          if (options.bytes !== Array)
            object.signature = $util.newBuffer(object.signature);
        }
        if (options.bytes === String) object.event = '';
        else {
          object.event = [];
          if (options.bytes !== Array)
            object.event = $util.newBuffer(object.event);
        }
      }
      if (message.signature != null && message.hasOwnProperty('signature'))
        object.signature =
          options.bytes === String
            ? $util.base64.encode(
                message.signature,
                0,
                message.signature.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.signature)
              : message.signature;
      if (message.event != null && message.hasOwnProperty('event'))
        object.event =
          options.bytes === String
            ? $util.base64.encode(message.event, 0, message.event.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.event)
              : message.event;
      if (message.moderationTags && message.moderationTags.length) {
        object.moderationTags = [];
        for (let j = 0; j < message.moderationTags.length; ++j)
          object.moderationTags[j] = $root.polycentric.ModerationTag.toObject(
            message.moderationTags[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this SignedEvent to JSON.
     * @function toJSON
     * @memberof polycentric.SignedEvent
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SignedEvent.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SignedEvent
     * @function getTypeUrl
     * @memberof polycentric.SignedEvent
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SignedEvent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.SignedEvent';
    };

    return SignedEvent;
  })();

  polycentric.Events = (function () {
    /**
     * Properties of an Events.
     * @memberof polycentric
     * @interface IEvents
     * @property {Array.<polycentric.ISignedEvent>|null} [events] Events events
     */

    /**
     * Constructs a new Events.
     * @memberof polycentric
     * @classdesc Represents an Events.
     * @implements IEvents
     * @constructor
     * @param {polycentric.IEvents=} [properties] Properties to set
     */
    function Events(properties) {
      this.events = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Events events.
     * @member {Array.<polycentric.ISignedEvent>} events
     * @memberof polycentric.Events
     * @instance
     */
    Events.prototype.events = $util.emptyArray;

    /**
     * Creates a new Events instance using the specified properties.
     * @function create
     * @memberof polycentric.Events
     * @static
     * @param {polycentric.IEvents=} [properties] Properties to set
     * @returns {polycentric.Events} Events instance
     */
    Events.create = function create(properties) {
      return new Events(properties);
    };

    /**
     * Encodes the specified Events message. Does not implicitly {@link polycentric.Events.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Events
     * @static
     * @param {polycentric.IEvents} message Events message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Events.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.events != null && message.events.length)
        for (let i = 0; i < message.events.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.events[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified Events message, length delimited. Does not implicitly {@link polycentric.Events.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Events
     * @static
     * @param {polycentric.IEvents} message Events message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Events.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an Events message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Events
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Events} Events
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Events.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Events();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.events && message.events.length)) message.events = [];
            message.events.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an Events message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Events
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Events} Events
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Events.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an Events message.
     * @function verify
     * @memberof polycentric.Events
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Events.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.events != null && message.hasOwnProperty('events')) {
        if (!Array.isArray(message.events)) return 'events: array expected';
        for (let i = 0; i < message.events.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(message.events[i]);
          if (error) return 'events.' + error;
        }
      }
      return null;
    };

    /**
     * Creates an Events message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Events
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Events} Events
     */
    Events.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Events) return object;
      let message = new $root.polycentric.Events();
      if (object.events) {
        if (!Array.isArray(object.events))
          throw TypeError('.polycentric.Events.events: array expected');
        message.events = [];
        for (let i = 0; i < object.events.length; ++i) {
          if (typeof object.events[i] !== 'object')
            throw TypeError('.polycentric.Events.events: object expected');
          message.events[i] = $root.polycentric.SignedEvent.fromObject(
            object.events[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from an Events message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Events
     * @static
     * @param {polycentric.Events} message Events
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Events.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.events = [];
      if (message.events && message.events.length) {
        object.events = [];
        for (let j = 0; j < message.events.length; ++j)
          object.events[j] = $root.polycentric.SignedEvent.toObject(
            message.events[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this Events to JSON.
     * @function toJSON
     * @memberof polycentric.Events
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Events.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Events
     * @function getTypeUrl
     * @memberof polycentric.Events
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Events.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Events';
    };

    return Events;
  })();

  polycentric.Post = (function () {
    /**
     * Properties of a Post.
     * @memberof polycentric
     * @interface IPost
     * @property {string|null} [content] Post content
     * @property {polycentric.IImageManifest|null} [image] Post image
     */

    /**
     * Constructs a new Post.
     * @memberof polycentric
     * @classdesc Represents a Post.
     * @implements IPost
     * @constructor
     * @param {polycentric.IPost=} [properties] Properties to set
     */
    function Post(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Post content.
     * @member {string} content
     * @memberof polycentric.Post
     * @instance
     */
    Post.prototype.content = '';

    /**
     * Post image.
     * @member {polycentric.IImageManifest|null|undefined} image
     * @memberof polycentric.Post
     * @instance
     */
    Post.prototype.image = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(Post.prototype, '_image', {
      get: $util.oneOfGetter(($oneOfFields = ['image'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new Post instance using the specified properties.
     * @function create
     * @memberof polycentric.Post
     * @static
     * @param {polycentric.IPost=} [properties] Properties to set
     * @returns {polycentric.Post} Post instance
     */
    Post.create = function create(properties) {
      return new Post(properties);
    };

    /**
     * Encodes the specified Post message. Does not implicitly {@link polycentric.Post.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Post
     * @static
     * @param {polycentric.IPost} message Post message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Post.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.content != null &&
        Object.hasOwnProperty.call(message, 'content')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.content);
      if (message.image != null && Object.hasOwnProperty.call(message, 'image'))
        $root.polycentric.ImageManifest.encode(
          message.image,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified Post message, length delimited. Does not implicitly {@link polycentric.Post.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Post
     * @static
     * @param {polycentric.IPost} message Post message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Post.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Post message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Post
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Post} Post
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Post.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Post();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.content = reader.string();
            break;
          }
          case 2: {
            message.image = $root.polycentric.ImageManifest.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Post message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Post
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Post} Post
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Post.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Post message.
     * @function verify
     * @memberof polycentric.Post
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Post.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.content != null && message.hasOwnProperty('content'))
        if (!$util.isString(message.content)) return 'content: string expected';
      if (message.image != null && message.hasOwnProperty('image')) {
        properties._image = 1;
        {
          let error = $root.polycentric.ImageManifest.verify(message.image);
          if (error) return 'image.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a Post message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Post
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Post} Post
     */
    Post.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Post) return object;
      let message = new $root.polycentric.Post();
      if (object.content != null) message.content = String(object.content);
      if (object.image != null) {
        if (typeof object.image !== 'object')
          throw TypeError('.polycentric.Post.image: object expected');
        message.image = $root.polycentric.ImageManifest.fromObject(
          object.image
        );
      }
      return message;
    };

    /**
     * Creates a plain object from a Post message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Post
     * @static
     * @param {polycentric.Post} message Post
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Post.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) object.content = '';
      if (message.content != null && message.hasOwnProperty('content'))
        object.content = message.content;
      if (message.image != null && message.hasOwnProperty('image')) {
        object.image = $root.polycentric.ImageManifest.toObject(
          message.image,
          options
        );
        if (options.oneofs) object._image = 'image';
      }
      return object;
    };

    /**
     * Converts this Post to JSON.
     * @function toJSON
     * @memberof polycentric.Post
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Post.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Post
     * @function getTypeUrl
     * @memberof polycentric.Post
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Post.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Post';
    };

    return Post;
  })();

  polycentric.ImageManifest = (function () {
    /**
     * Properties of an ImageManifest.
     * @memberof polycentric
     * @interface IImageManifest
     * @property {string|null} [mime] ImageManifest mime
     * @property {number|null} [width] ImageManifest width
     * @property {number|null} [height] ImageManifest height
     * @property {Uint8Array|null} [digest] ImageManifest digest
     */

    /**
     * Constructs a new ImageManifest.
     * @memberof polycentric
     * @classdesc Represents an ImageManifest.
     * @implements IImageManifest
     * @constructor
     * @param {polycentric.IImageManifest=} [properties] Properties to set
     */
    function ImageManifest(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ImageManifest mime.
     * @member {string} mime
     * @memberof polycentric.ImageManifest
     * @instance
     */
    ImageManifest.prototype.mime = '';

    /**
     * ImageManifest width.
     * @member {number} width
     * @memberof polycentric.ImageManifest
     * @instance
     */
    ImageManifest.prototype.width = 0;

    /**
     * ImageManifest height.
     * @member {number} height
     * @memberof polycentric.ImageManifest
     * @instance
     */
    ImageManifest.prototype.height = 0;

    /**
     * ImageManifest digest.
     * @member {Uint8Array} digest
     * @memberof polycentric.ImageManifest
     * @instance
     */
    ImageManifest.prototype.digest = $util.newBuffer([]);

    /**
     * Creates a new ImageManifest instance using the specified properties.
     * @function create
     * @memberof polycentric.ImageManifest
     * @static
     * @param {polycentric.IImageManifest=} [properties] Properties to set
     * @returns {polycentric.ImageManifest} ImageManifest instance
     */
    ImageManifest.create = function create(properties) {
      return new ImageManifest(properties);
    };

    /**
     * Encodes the specified ImageManifest message. Does not implicitly {@link polycentric.ImageManifest.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ImageManifest
     * @static
     * @param {polycentric.IImageManifest} message ImageManifest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ImageManifest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.mime != null && Object.hasOwnProperty.call(message, 'mime'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.mime);
      if (message.width != null && Object.hasOwnProperty.call(message, 'width'))
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint32(message.width);
      if (
        message.height != null &&
        Object.hasOwnProperty.call(message, 'height')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint32(message.height);
      if (
        message.digest != null &&
        Object.hasOwnProperty.call(message, 'digest')
      )
        writer.uint32(/* id 4, wireType 2 =*/ 34).bytes(message.digest);
      return writer;
    };

    /**
     * Encodes the specified ImageManifest message, length delimited. Does not implicitly {@link polycentric.ImageManifest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ImageManifest
     * @static
     * @param {polycentric.IImageManifest} message ImageManifest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ImageManifest.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an ImageManifest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ImageManifest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ImageManifest} ImageManifest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ImageManifest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ImageManifest();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mime = reader.string();
            break;
          }
          case 2: {
            message.width = reader.uint32();
            break;
          }
          case 3: {
            message.height = reader.uint32();
            break;
          }
          case 4: {
            message.digest = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an ImageManifest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ImageManifest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ImageManifest} ImageManifest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ImageManifest.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an ImageManifest message.
     * @function verify
     * @memberof polycentric.ImageManifest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ImageManifest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.mime != null && message.hasOwnProperty('mime'))
        if (!$util.isString(message.mime)) return 'mime: string expected';
      if (message.width != null && message.hasOwnProperty('width'))
        if (!$util.isInteger(message.width)) return 'width: integer expected';
      if (message.height != null && message.hasOwnProperty('height'))
        if (!$util.isInteger(message.height)) return 'height: integer expected';
      if (message.digest != null && message.hasOwnProperty('digest'))
        if (
          !(
            (message.digest && typeof message.digest.length === 'number') ||
            $util.isString(message.digest)
          )
        )
          return 'digest: buffer expected';
      return null;
    };

    /**
     * Creates an ImageManifest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ImageManifest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ImageManifest} ImageManifest
     */
    ImageManifest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ImageManifest) return object;
      let message = new $root.polycentric.ImageManifest();
      if (object.mime != null) message.mime = String(object.mime);
      if (object.width != null) message.width = object.width >>> 0;
      if (object.height != null) message.height = object.height >>> 0;
      if (object.digest != null)
        if (typeof object.digest === 'string')
          $util.base64.decode(
            object.digest,
            (message.digest = $util.newBuffer(
              $util.base64.length(object.digest)
            )),
            0
          );
        else if (object.digest.length >= 0) message.digest = object.digest;
      return message;
    };

    /**
     * Creates a plain object from an ImageManifest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ImageManifest
     * @static
     * @param {polycentric.ImageManifest} message ImageManifest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ImageManifest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.mime = '';
        object.width = 0;
        object.height = 0;
        if (options.bytes === String) object.digest = '';
        else {
          object.digest = [];
          if (options.bytes !== Array)
            object.digest = $util.newBuffer(object.digest);
        }
      }
      if (message.mime != null && message.hasOwnProperty('mime'))
        object.mime = message.mime;
      if (message.width != null && message.hasOwnProperty('width'))
        object.width = message.width;
      if (message.height != null && message.hasOwnProperty('height'))
        object.height = message.height;
      if (message.digest != null && message.hasOwnProperty('digest'))
        object.digest =
          options.bytes === String
            ? $util.base64.encode(message.digest, 0, message.digest.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.digest)
              : message.digest;
      return object;
    };

    /**
     * Converts this ImageManifest to JSON.
     * @function toJSON
     * @memberof polycentric.ImageManifest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ImageManifest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ImageManifest
     * @function getTypeUrl
     * @memberof polycentric.ImageManifest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ImageManifest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ImageManifest';
    };

    return ImageManifest;
  })();

  polycentric.ImageBundle = (function () {
    /**
     * Properties of an ImageBundle.
     * @memberof polycentric
     * @interface IImageBundle
     * @property {Array.<polycentric.IImageManifest>|null} [images] ImageBundle images
     */

    /**
     * Constructs a new ImageBundle.
     * @memberof polycentric
     * @classdesc Represents an ImageBundle.
     * @implements IImageBundle
     * @constructor
     * @param {polycentric.IImageBundle=} [properties] Properties to set
     */
    function ImageBundle(properties) {
      this.images = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ImageBundle images.
     * @member {Array.<polycentric.IImageManifest>} images
     * @memberof polycentric.ImageBundle
     * @instance
     */
    ImageBundle.prototype.images = $util.emptyArray;

    /**
     * Creates a new ImageBundle instance using the specified properties.
     * @function create
     * @memberof polycentric.ImageBundle
     * @static
     * @param {polycentric.IImageBundle=} [properties] Properties to set
     * @returns {polycentric.ImageBundle} ImageBundle instance
     */
    ImageBundle.create = function create(properties) {
      return new ImageBundle(properties);
    };

    /**
     * Encodes the specified ImageBundle message. Does not implicitly {@link polycentric.ImageBundle.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ImageBundle
     * @static
     * @param {polycentric.IImageBundle} message ImageBundle message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ImageBundle.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.images != null && message.images.length)
        for (let i = 0; i < message.images.length; ++i)
          $root.polycentric.ImageManifest.encode(
            message.images[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified ImageBundle message, length delimited. Does not implicitly {@link polycentric.ImageBundle.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ImageBundle
     * @static
     * @param {polycentric.IImageBundle} message ImageBundle message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ImageBundle.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an ImageBundle message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ImageBundle
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ImageBundle} ImageBundle
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ImageBundle.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ImageBundle();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.images && message.images.length)) message.images = [];
            message.images.push(
              $root.polycentric.ImageManifest.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an ImageBundle message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ImageBundle
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ImageBundle} ImageBundle
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ImageBundle.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an ImageBundle message.
     * @function verify
     * @memberof polycentric.ImageBundle
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ImageBundle.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.images != null && message.hasOwnProperty('images')) {
        if (!Array.isArray(message.images)) return 'images: array expected';
        for (let i = 0; i < message.images.length; ++i) {
          let error = $root.polycentric.ImageManifest.verify(message.images[i]);
          if (error) return 'images.' + error;
        }
      }
      return null;
    };

    /**
     * Creates an ImageBundle message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ImageBundle
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ImageBundle} ImageBundle
     */
    ImageBundle.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ImageBundle) return object;
      let message = new $root.polycentric.ImageBundle();
      if (object.images) {
        if (!Array.isArray(object.images))
          throw TypeError('.polycentric.ImageBundle.images: array expected');
        message.images = [];
        for (let i = 0; i < object.images.length; ++i) {
          if (typeof object.images[i] !== 'object')
            throw TypeError('.polycentric.ImageBundle.images: object expected');
          message.images[i] = $root.polycentric.ImageManifest.fromObject(
            object.images[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from an ImageBundle message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ImageBundle
     * @static
     * @param {polycentric.ImageBundle} message ImageBundle
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ImageBundle.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.images = [];
      if (message.images && message.images.length) {
        object.images = [];
        for (let j = 0; j < message.images.length; ++j)
          object.images[j] = $root.polycentric.ImageManifest.toObject(
            message.images[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this ImageBundle to JSON.
     * @function toJSON
     * @memberof polycentric.ImageBundle
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ImageBundle.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ImageBundle
     * @function getTypeUrl
     * @memberof polycentric.ImageBundle
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ImageBundle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ImageBundle';
    };

    return ImageBundle;
  })();

  polycentric.Claim = (function () {
    /**
     * Properties of a Claim.
     * @memberof polycentric
     * @interface IClaim
     * @property {number|Long|null} [claimType] Claim claimType
     * @property {Array.<polycentric.IClaimFieldEntry>|null} [fields] Claim fields
     */

    /**
     * Constructs a new Claim.
     * @memberof polycentric
     * @classdesc Represents a Claim.
     * @implements IClaim
     * @constructor
     * @param {polycentric.IClaim=} [properties] Properties to set
     */
    function Claim(properties) {
      this.fields = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Claim claimType.
     * @member {number|Long} claimType
     * @memberof polycentric.Claim
     * @instance
     */
    Claim.prototype.claimType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Claim fields.
     * @member {Array.<polycentric.IClaimFieldEntry>} fields
     * @memberof polycentric.Claim
     * @instance
     */
    Claim.prototype.fields = $util.emptyArray;

    /**
     * Creates a new Claim instance using the specified properties.
     * @function create
     * @memberof polycentric.Claim
     * @static
     * @param {polycentric.IClaim=} [properties] Properties to set
     * @returns {polycentric.Claim} Claim instance
     */
    Claim.create = function create(properties) {
      return new Claim(properties);
    };

    /**
     * Encodes the specified Claim message. Does not implicitly {@link polycentric.Claim.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Claim
     * @static
     * @param {polycentric.IClaim} message Claim message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Claim.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.claimType != null &&
        Object.hasOwnProperty.call(message, 'claimType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.claimType);
      if (message.fields != null && message.fields.length)
        for (let i = 0; i < message.fields.length; ++i)
          $root.polycentric.ClaimFieldEntry.encode(
            message.fields[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified Claim message, length delimited. Does not implicitly {@link polycentric.Claim.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Claim
     * @static
     * @param {polycentric.IClaim} message Claim message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Claim.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Claim message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Claim
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Claim} Claim
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Claim.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Claim();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.claimType = reader.uint64();
            break;
          }
          case 2: {
            if (!(message.fields && message.fields.length)) message.fields = [];
            message.fields.push(
              $root.polycentric.ClaimFieldEntry.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Claim message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Claim
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Claim} Claim
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Claim.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Claim message.
     * @function verify
     * @memberof polycentric.Claim
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Claim.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.claimType != null && message.hasOwnProperty('claimType'))
        if (
          !$util.isInteger(message.claimType) &&
          !(
            message.claimType &&
            $util.isInteger(message.claimType.low) &&
            $util.isInteger(message.claimType.high)
          )
        )
          return 'claimType: integer|Long expected';
      if (message.fields != null && message.hasOwnProperty('fields')) {
        if (!Array.isArray(message.fields)) return 'fields: array expected';
        for (let i = 0; i < message.fields.length; ++i) {
          let error = $root.polycentric.ClaimFieldEntry.verify(
            message.fields[i]
          );
          if (error) return 'fields.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a Claim message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Claim
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Claim} Claim
     */
    Claim.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Claim) return object;
      let message = new $root.polycentric.Claim();
      if (object.claimType != null)
        if ($util.Long)
          (message.claimType = $util.Long.fromValue(
            object.claimType
          )).unsigned = true;
        else if (typeof object.claimType === 'string')
          message.claimType = parseInt(object.claimType, 10);
        else if (typeof object.claimType === 'number')
          message.claimType = object.claimType;
        else if (typeof object.claimType === 'object')
          message.claimType = new $util.LongBits(
            object.claimType.low >>> 0,
            object.claimType.high >>> 0
          ).toNumber(true);
      if (object.fields) {
        if (!Array.isArray(object.fields))
          throw TypeError('.polycentric.Claim.fields: array expected');
        message.fields = [];
        for (let i = 0; i < object.fields.length; ++i) {
          if (typeof object.fields[i] !== 'object')
            throw TypeError('.polycentric.Claim.fields: object expected');
          message.fields[i] = $root.polycentric.ClaimFieldEntry.fromObject(
            object.fields[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a Claim message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Claim
     * @static
     * @param {polycentric.Claim} message Claim
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Claim.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.fields = [];
      if (options.defaults)
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.claimType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.claimType = options.longs === String ? '0' : 0;
      if (message.claimType != null && message.hasOwnProperty('claimType'))
        if (typeof message.claimType === 'number')
          object.claimType =
            options.longs === String
              ? String(message.claimType)
              : message.claimType;
        else
          object.claimType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.claimType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.claimType.low >>> 0,
                    message.claimType.high >>> 0
                  ).toNumber(true)
                : message.claimType;
      if (message.fields && message.fields.length) {
        object.fields = [];
        for (let j = 0; j < message.fields.length; ++j)
          object.fields[j] = $root.polycentric.ClaimFieldEntry.toObject(
            message.fields[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this Claim to JSON.
     * @function toJSON
     * @memberof polycentric.Claim
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Claim.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Claim
     * @function getTypeUrl
     * @memberof polycentric.Claim
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Claim.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Claim';
    };

    return Claim;
  })();

  polycentric.ClaimFieldEntry = (function () {
    /**
     * Properties of a ClaimFieldEntry.
     * @memberof polycentric
     * @interface IClaimFieldEntry
     * @property {number|Long|null} [key] ClaimFieldEntry key
     * @property {string|null} [value] ClaimFieldEntry value
     */

    /**
     * Constructs a new ClaimFieldEntry.
     * @memberof polycentric
     * @classdesc Represents a ClaimFieldEntry.
     * @implements IClaimFieldEntry
     * @constructor
     * @param {polycentric.IClaimFieldEntry=} [properties] Properties to set
     */
    function ClaimFieldEntry(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ClaimFieldEntry key.
     * @member {number|Long} key
     * @memberof polycentric.ClaimFieldEntry
     * @instance
     */
    ClaimFieldEntry.prototype.key = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ClaimFieldEntry value.
     * @member {string} value
     * @memberof polycentric.ClaimFieldEntry
     * @instance
     */
    ClaimFieldEntry.prototype.value = '';

    /**
     * Creates a new ClaimFieldEntry instance using the specified properties.
     * @function create
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {polycentric.IClaimFieldEntry=} [properties] Properties to set
     * @returns {polycentric.ClaimFieldEntry} ClaimFieldEntry instance
     */
    ClaimFieldEntry.create = function create(properties) {
      return new ClaimFieldEntry(properties);
    };

    /**
     * Encodes the specified ClaimFieldEntry message. Does not implicitly {@link polycentric.ClaimFieldEntry.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {polycentric.IClaimFieldEntry} message ClaimFieldEntry message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ClaimFieldEntry.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.key != null && Object.hasOwnProperty.call(message, 'key'))
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.key);
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.value);
      return writer;
    };

    /**
     * Encodes the specified ClaimFieldEntry message, length delimited. Does not implicitly {@link polycentric.ClaimFieldEntry.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {polycentric.IClaimFieldEntry} message ClaimFieldEntry message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ClaimFieldEntry.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ClaimFieldEntry message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ClaimFieldEntry} ClaimFieldEntry
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ClaimFieldEntry.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ClaimFieldEntry();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.key = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ClaimFieldEntry message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ClaimFieldEntry} ClaimFieldEntry
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ClaimFieldEntry.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ClaimFieldEntry message.
     * @function verify
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ClaimFieldEntry.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.key != null && message.hasOwnProperty('key'))
        if (
          !$util.isInteger(message.key) &&
          !(
            message.key &&
            $util.isInteger(message.key.low) &&
            $util.isInteger(message.key.high)
          )
        )
          return 'key: integer|Long expected';
      if (message.value != null && message.hasOwnProperty('value'))
        if (!$util.isString(message.value)) return 'value: string expected';
      return null;
    };

    /**
     * Creates a ClaimFieldEntry message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ClaimFieldEntry} ClaimFieldEntry
     */
    ClaimFieldEntry.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ClaimFieldEntry) return object;
      let message = new $root.polycentric.ClaimFieldEntry();
      if (object.key != null)
        if ($util.Long)
          (message.key = $util.Long.fromValue(object.key)).unsigned = true;
        else if (typeof object.key === 'string')
          message.key = parseInt(object.key, 10);
        else if (typeof object.key === 'number') message.key = object.key;
        else if (typeof object.key === 'object')
          message.key = new $util.LongBits(
            object.key.low >>> 0,
            object.key.high >>> 0
          ).toNumber(true);
      if (object.value != null) message.value = String(object.value);
      return message;
    };

    /**
     * Creates a plain object from a ClaimFieldEntry message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {polycentric.ClaimFieldEntry} message ClaimFieldEntry
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ClaimFieldEntry.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.key =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.key = options.longs === String ? '0' : 0;
        object.value = '';
      }
      if (message.key != null && message.hasOwnProperty('key'))
        if (typeof message.key === 'number')
          object.key =
            options.longs === String ? String(message.key) : message.key;
        else
          object.key =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.key)
              : options.longs === Number
                ? new $util.LongBits(
                    message.key.low >>> 0,
                    message.key.high >>> 0
                  ).toNumber(true)
                : message.key;
      if (message.value != null && message.hasOwnProperty('value'))
        object.value = message.value;
      return object;
    };

    /**
     * Converts this ClaimFieldEntry to JSON.
     * @function toJSON
     * @memberof polycentric.ClaimFieldEntry
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ClaimFieldEntry.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ClaimFieldEntry
     * @function getTypeUrl
     * @memberof polycentric.ClaimFieldEntry
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ClaimFieldEntry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ClaimFieldEntry';
    };

    return ClaimFieldEntry;
  })();

  polycentric.Delete = (function () {
    /**
     * Properties of a Delete.
     * @memberof polycentric
     * @interface IDelete
     * @property {polycentric.IProcess|null} [process] Delete process
     * @property {number|Long|null} [logicalClock] Delete logicalClock
     * @property {polycentric.IIndices|null} [indices] Delete indices
     * @property {number|Long|null} [unixMilliseconds] Delete unixMilliseconds
     * @property {polycentric.ContentType|null} [contentType] Delete contentType
     */

    /**
     * Constructs a new Delete.
     * @memberof polycentric
     * @classdesc Represents a Delete.
     * @implements IDelete
     * @constructor
     * @param {polycentric.IDelete=} [properties] Properties to set
     */
    function Delete(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Delete process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.Delete
     * @instance
     */
    Delete.prototype.process = null;

    /**
     * Delete logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.Delete
     * @instance
     */
    Delete.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Delete indices.
     * @member {polycentric.IIndices|null|undefined} indices
     * @memberof polycentric.Delete
     * @instance
     */
    Delete.prototype.indices = null;

    /**
     * Delete unixMilliseconds.
     * @member {number|Long|null|undefined} unixMilliseconds
     * @memberof polycentric.Delete
     * @instance
     */
    Delete.prototype.unixMilliseconds = null;

    /**
     * Delete contentType.
     * @member {polycentric.ContentType} contentType
     * @memberof polycentric.Delete
     * @instance
     */
    Delete.prototype.contentType = 0;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(Delete.prototype, '_unixMilliseconds', {
      get: $util.oneOfGetter(($oneOfFields = ['unixMilliseconds'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new Delete instance using the specified properties.
     * @function create
     * @memberof polycentric.Delete
     * @static
     * @param {polycentric.IDelete=} [properties] Properties to set
     * @returns {polycentric.Delete} Delete instance
     */
    Delete.create = function create(properties) {
      return new Delete(properties);
    };

    /**
     * Encodes the specified Delete message. Does not implicitly {@link polycentric.Delete.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Delete
     * @static
     * @param {polycentric.IDelete} message Delete message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Delete.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.logicalClock);
      if (
        message.indices != null &&
        Object.hasOwnProperty.call(message, 'indices')
      )
        $root.polycentric.Indices.encode(
          message.indices,
          writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
        ).ldelim();
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 4, wireType 0 =*/ 32)
          .uint64(message.unixMilliseconds);
      if (
        message.contentType != null &&
        Object.hasOwnProperty.call(message, 'contentType')
      )
        writer.uint32(/* id 5, wireType 0 =*/ 40).int32(message.contentType);
      return writer;
    };

    /**
     * Encodes the specified Delete message, length delimited. Does not implicitly {@link polycentric.Delete.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Delete
     * @static
     * @param {polycentric.IDelete} message Delete message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Delete.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Delete message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Delete
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Delete} Delete
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Delete.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Delete();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.logicalClock = reader.uint64();
            break;
          }
          case 3: {
            message.indices = $root.polycentric.Indices.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 4: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          case 5: {
            message.contentType = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Delete message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Delete
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Delete} Delete
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Delete.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Delete message.
     * @function verify
     * @memberof polycentric.Delete
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Delete.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      if (message.indices != null && message.hasOwnProperty('indices')) {
        let error = $root.polycentric.Indices.verify(message.indices);
        if (error) return 'indices.' + error;
      }
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      ) {
        properties._unixMilliseconds = 1;
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      }
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        switch (message.contentType) {
          default:
            return 'contentType: enum value expected';
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
          case 6:
          case 7:
          case 8:
          case 9:
          case 10:
          case 11:
          case 12:
          case 13:
          case 14:
          case 15:
          case 16:
          case 17:
          case 18:
            break;
        }
      return null;
    };

    /**
     * Creates a Delete message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Delete
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Delete} Delete
     */
    Delete.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Delete) return object;
      let message = new $root.polycentric.Delete();
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError('.polycentric.Delete.process: object expected');
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      if (object.indices != null) {
        if (typeof object.indices !== 'object')
          throw TypeError('.polycentric.Delete.indices: object expected');
        message.indices = $root.polycentric.Indices.fromObject(object.indices);
      }
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      switch (object.contentType) {
        default:
          if (typeof object.contentType === 'number') {
            message.contentType = object.contentType;
            break;
          }
          break;
        case 'UNKNOWN':
        case 0:
          message.contentType = 0;
          break;
        case 'DELETE':
        case 1:
          message.contentType = 1;
          break;
        case 'SYSTEM_PROCESSES':
        case 2:
          message.contentType = 2;
          break;
        case 'POST':
        case 3:
          message.contentType = 3;
          break;
        case 'FOLLOW':
        case 4:
          message.contentType = 4;
          break;
        case 'USERNAME':
        case 5:
          message.contentType = 5;
          break;
        case 'DESCRIPTION':
        case 6:
          message.contentType = 6;
          break;
        case 'BLOB_META':
        case 7:
          message.contentType = 7;
          break;
        case 'BLOB_SECTION':
        case 8:
          message.contentType = 8;
          break;
        case 'AVATAR':
        case 9:
          message.contentType = 9;
          break;
        case 'SERVER':
        case 10:
          message.contentType = 10;
          break;
        case 'VOUCH':
        case 11:
          message.contentType = 11;
          break;
        case 'CLAIM':
        case 12:
          message.contentType = 12;
          break;
        case 'BANNER':
        case 13:
          message.contentType = 13;
          break;
        case 'OPINION':
        case 14:
          message.contentType = 14;
          break;
        case 'STORE':
        case 15:
          message.contentType = 15;
          break;
        case 'AUTHORITY':
        case 16:
          message.contentType = 16;
          break;
        case 'JOIN_TOPIC':
        case 17:
          message.contentType = 17;
          break;
        case 'BLOCK':
        case 18:
          message.contentType = 18;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a Delete message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Delete
     * @static
     * @param {polycentric.Delete} message Delete
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Delete.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.process = null;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
        object.indices = null;
        object.contentType = options.enums === String ? 'UNKNOWN' : 0;
      }
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      if (message.indices != null && message.hasOwnProperty('indices'))
        object.indices = $root.polycentric.Indices.toObject(
          message.indices,
          options
        );
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      ) {
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
        if (options.oneofs) object._unixMilliseconds = 'unixMilliseconds';
      }
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        object.contentType =
          options.enums === String
            ? $root.polycentric.ContentType[message.contentType] === undefined
              ? message.contentType
              : $root.polycentric.ContentType[message.contentType]
            : message.contentType;
      return object;
    };

    /**
     * Converts this Delete to JSON.
     * @function toJSON
     * @memberof polycentric.Delete
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Delete.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Delete
     * @function getTypeUrl
     * @memberof polycentric.Delete
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Delete.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Delete';
    };

    return Delete;
  })();

  polycentric.SystemProcesses = (function () {
    /**
     * Properties of a SystemProcesses.
     * @memberof polycentric
     * @interface ISystemProcesses
     * @property {Array.<polycentric.IProcess>|null} [processes] SystemProcesses processes
     */

    /**
     * Constructs a new SystemProcesses.
     * @memberof polycentric
     * @classdesc Represents a SystemProcesses.
     * @implements ISystemProcesses
     * @constructor
     * @param {polycentric.ISystemProcesses=} [properties] Properties to set
     */
    function SystemProcesses(properties) {
      this.processes = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * SystemProcesses processes.
     * @member {Array.<polycentric.IProcess>} processes
     * @memberof polycentric.SystemProcesses
     * @instance
     */
    SystemProcesses.prototype.processes = $util.emptyArray;

    /**
     * Creates a new SystemProcesses instance using the specified properties.
     * @function create
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {polycentric.ISystemProcesses=} [properties] Properties to set
     * @returns {polycentric.SystemProcesses} SystemProcesses instance
     */
    SystemProcesses.create = function create(properties) {
      return new SystemProcesses(properties);
    };

    /**
     * Encodes the specified SystemProcesses message. Does not implicitly {@link polycentric.SystemProcesses.verify|verify} messages.
     * @function encode
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {polycentric.ISystemProcesses} message SystemProcesses message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SystemProcesses.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.processes != null && message.processes.length)
        for (let i = 0; i < message.processes.length; ++i)
          $root.polycentric.Process.encode(
            message.processes[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified SystemProcesses message, length delimited. Does not implicitly {@link polycentric.SystemProcesses.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {polycentric.ISystemProcesses} message SystemProcesses message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SystemProcesses.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SystemProcesses message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.SystemProcesses} SystemProcesses
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SystemProcesses.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.SystemProcesses();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.processes && message.processes.length))
              message.processes = [];
            message.processes.push(
              $root.polycentric.Process.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a SystemProcesses message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.SystemProcesses} SystemProcesses
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SystemProcesses.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SystemProcesses message.
     * @function verify
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SystemProcesses.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.processes != null && message.hasOwnProperty('processes')) {
        if (!Array.isArray(message.processes))
          return 'processes: array expected';
        for (let i = 0; i < message.processes.length; ++i) {
          let error = $root.polycentric.Process.verify(message.processes[i]);
          if (error) return 'processes.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a SystemProcesses message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.SystemProcesses} SystemProcesses
     */
    SystemProcesses.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.SystemProcesses) return object;
      let message = new $root.polycentric.SystemProcesses();
      if (object.processes) {
        if (!Array.isArray(object.processes))
          throw TypeError(
            '.polycentric.SystemProcesses.processes: array expected'
          );
        message.processes = [];
        for (let i = 0; i < object.processes.length; ++i) {
          if (typeof object.processes[i] !== 'object')
            throw TypeError(
              '.polycentric.SystemProcesses.processes: object expected'
            );
          message.processes[i] = $root.polycentric.Process.fromObject(
            object.processes[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a SystemProcesses message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {polycentric.SystemProcesses} message SystemProcesses
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SystemProcesses.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.processes = [];
      if (message.processes && message.processes.length) {
        object.processes = [];
        for (let j = 0; j < message.processes.length; ++j)
          object.processes[j] = $root.polycentric.Process.toObject(
            message.processes[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this SystemProcesses to JSON.
     * @function toJSON
     * @memberof polycentric.SystemProcesses
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SystemProcesses.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SystemProcesses
     * @function getTypeUrl
     * @memberof polycentric.SystemProcesses
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SystemProcesses.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.SystemProcesses';
    };

    return SystemProcesses;
  })();

  polycentric.SystemState = (function () {
    /**
     * Properties of a SystemState.
     * @memberof polycentric
     * @interface ISystemState
     * @property {Array.<string>|null} [servers] SystemState servers
     * @property {Array.<string>|null} [authorities] SystemState authorities
     * @property {Array.<polycentric.IProcess>|null} [processes] SystemState processes
     */

    /**
     * Constructs a new SystemState.
     * @memberof polycentric
     * @classdesc Represents a SystemState.
     * @implements ISystemState
     * @constructor
     * @param {polycentric.ISystemState=} [properties] Properties to set
     */
    function SystemState(properties) {
      this.servers = [];
      this.authorities = [];
      this.processes = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * SystemState servers.
     * @member {Array.<string>} servers
     * @memberof polycentric.SystemState
     * @instance
     */
    SystemState.prototype.servers = $util.emptyArray;

    /**
     * SystemState authorities.
     * @member {Array.<string>} authorities
     * @memberof polycentric.SystemState
     * @instance
     */
    SystemState.prototype.authorities = $util.emptyArray;

    /**
     * SystemState processes.
     * @member {Array.<polycentric.IProcess>} processes
     * @memberof polycentric.SystemState
     * @instance
     */
    SystemState.prototype.processes = $util.emptyArray;

    /**
     * Creates a new SystemState instance using the specified properties.
     * @function create
     * @memberof polycentric.SystemState
     * @static
     * @param {polycentric.ISystemState=} [properties] Properties to set
     * @returns {polycentric.SystemState} SystemState instance
     */
    SystemState.create = function create(properties) {
      return new SystemState(properties);
    };

    /**
     * Encodes the specified SystemState message. Does not implicitly {@link polycentric.SystemState.verify|verify} messages.
     * @function encode
     * @memberof polycentric.SystemState
     * @static
     * @param {polycentric.ISystemState} message SystemState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SystemState.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.servers != null && message.servers.length)
        for (let i = 0; i < message.servers.length; ++i)
          writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.servers[i]);
      if (message.authorities != null && message.authorities.length)
        for (let i = 0; i < message.authorities.length; ++i)
          writer
            .uint32(/* id 2, wireType 2 =*/ 18)
            .string(message.authorities[i]);
      if (message.processes != null && message.processes.length)
        for (let i = 0; i < message.processes.length; ++i)
          $root.polycentric.Process.encode(
            message.processes[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified SystemState message, length delimited. Does not implicitly {@link polycentric.SystemState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.SystemState
     * @static
     * @param {polycentric.ISystemState} message SystemState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SystemState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SystemState message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.SystemState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.SystemState} SystemState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SystemState.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.SystemState();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.servers && message.servers.length))
              message.servers = [];
            message.servers.push(reader.string());
            break;
          }
          case 2: {
            if (!(message.authorities && message.authorities.length))
              message.authorities = [];
            message.authorities.push(reader.string());
            break;
          }
          case 3: {
            if (!(message.processes && message.processes.length))
              message.processes = [];
            message.processes.push(
              $root.polycentric.Process.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a SystemState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.SystemState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.SystemState} SystemState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SystemState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SystemState message.
     * @function verify
     * @memberof polycentric.SystemState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SystemState.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.servers != null && message.hasOwnProperty('servers')) {
        if (!Array.isArray(message.servers)) return 'servers: array expected';
        for (let i = 0; i < message.servers.length; ++i)
          if (!$util.isString(message.servers[i]))
            return 'servers: string[] expected';
      }
      if (
        message.authorities != null &&
        message.hasOwnProperty('authorities')
      ) {
        if (!Array.isArray(message.authorities))
          return 'authorities: array expected';
        for (let i = 0; i < message.authorities.length; ++i)
          if (!$util.isString(message.authorities[i]))
            return 'authorities: string[] expected';
      }
      if (message.processes != null && message.hasOwnProperty('processes')) {
        if (!Array.isArray(message.processes))
          return 'processes: array expected';
        for (let i = 0; i < message.processes.length; ++i) {
          let error = $root.polycentric.Process.verify(message.processes[i]);
          if (error) return 'processes.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a SystemState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.SystemState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.SystemState} SystemState
     */
    SystemState.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.SystemState) return object;
      let message = new $root.polycentric.SystemState();
      if (object.servers) {
        if (!Array.isArray(object.servers))
          throw TypeError('.polycentric.SystemState.servers: array expected');
        message.servers = [];
        for (let i = 0; i < object.servers.length; ++i)
          message.servers[i] = String(object.servers[i]);
      }
      if (object.authorities) {
        if (!Array.isArray(object.authorities))
          throw TypeError(
            '.polycentric.SystemState.authorities: array expected'
          );
        message.authorities = [];
        for (let i = 0; i < object.authorities.length; ++i)
          message.authorities[i] = String(object.authorities[i]);
      }
      if (object.processes) {
        if (!Array.isArray(object.processes))
          throw TypeError('.polycentric.SystemState.processes: array expected');
        message.processes = [];
        for (let i = 0; i < object.processes.length; ++i) {
          if (typeof object.processes[i] !== 'object')
            throw TypeError(
              '.polycentric.SystemState.processes: object expected'
            );
          message.processes[i] = $root.polycentric.Process.fromObject(
            object.processes[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a SystemState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.SystemState
     * @static
     * @param {polycentric.SystemState} message SystemState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SystemState.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.servers = [];
        object.authorities = [];
        object.processes = [];
      }
      if (message.servers && message.servers.length) {
        object.servers = [];
        for (let j = 0; j < message.servers.length; ++j)
          object.servers[j] = message.servers[j];
      }
      if (message.authorities && message.authorities.length) {
        object.authorities = [];
        for (let j = 0; j < message.authorities.length; ++j)
          object.authorities[j] = message.authorities[j];
      }
      if (message.processes && message.processes.length) {
        object.processes = [];
        for (let j = 0; j < message.processes.length; ++j)
          object.processes[j] = $root.polycentric.Process.toObject(
            message.processes[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this SystemState to JSON.
     * @function toJSON
     * @memberof polycentric.SystemState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SystemState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SystemState
     * @function getTypeUrl
     * @memberof polycentric.SystemState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SystemState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.SystemState';
    };

    return SystemState;
  })();

  polycentric.ProcessState = (function () {
    /**
     * Properties of a ProcessState.
     * @memberof polycentric
     * @interface IProcessState
     * @property {number|Long|null} [logicalClock] ProcessState logicalClock
     * @property {number|Long|null} [unixMilliseconds] ProcessState unixMilliseconds
     * @property {Array.<polycentric.IRange>|null} [ranges] ProcessState ranges
     */

    /**
     * Constructs a new ProcessState.
     * @memberof polycentric
     * @classdesc Represents a ProcessState.
     * @implements IProcessState
     * @constructor
     * @param {polycentric.IProcessState=} [properties] Properties to set
     */
    function ProcessState(properties) {
      this.ranges = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ProcessState logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.ProcessState
     * @instance
     */
    ProcessState.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ProcessState unixMilliseconds.
     * @member {number|Long} unixMilliseconds
     * @memberof polycentric.ProcessState
     * @instance
     */
    ProcessState.prototype.unixMilliseconds = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ProcessState ranges.
     * @member {Array.<polycentric.IRange>} ranges
     * @memberof polycentric.ProcessState
     * @instance
     */
    ProcessState.prototype.ranges = $util.emptyArray;

    /**
     * Creates a new ProcessState instance using the specified properties.
     * @function create
     * @memberof polycentric.ProcessState
     * @static
     * @param {polycentric.IProcessState=} [properties] Properties to set
     * @returns {polycentric.ProcessState} ProcessState instance
     */
    ProcessState.create = function create(properties) {
      return new ProcessState(properties);
    };

    /**
     * Encodes the specified ProcessState message. Does not implicitly {@link polycentric.ProcessState.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ProcessState
     * @static
     * @param {polycentric.IProcessState} message ProcessState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProcessState.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.logicalClock);
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 2, wireType 0 =*/ 16)
          .uint64(message.unixMilliseconds);
      if (message.ranges != null && message.ranges.length)
        for (let i = 0; i < message.ranges.length; ++i)
          $root.polycentric.Range.encode(
            message.ranges[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified ProcessState message, length delimited. Does not implicitly {@link polycentric.ProcessState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ProcessState
     * @static
     * @param {polycentric.IProcessState} message ProcessState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ProcessState.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ProcessState message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ProcessState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ProcessState} ProcessState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProcessState.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ProcessState();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.logicalClock = reader.uint64();
            break;
          }
          case 2: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          case 3: {
            if (!(message.ranges && message.ranges.length)) message.ranges = [];
            message.ranges.push(
              $root.polycentric.Range.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ProcessState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ProcessState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ProcessState} ProcessState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ProcessState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ProcessState message.
     * @function verify
     * @memberof polycentric.ProcessState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ProcessState.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      if (message.ranges != null && message.hasOwnProperty('ranges')) {
        if (!Array.isArray(message.ranges)) return 'ranges: array expected';
        for (let i = 0; i < message.ranges.length; ++i) {
          let error = $root.polycentric.Range.verify(message.ranges[i]);
          if (error) return 'ranges.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a ProcessState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ProcessState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ProcessState} ProcessState
     */
    ProcessState.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ProcessState) return object;
      let message = new $root.polycentric.ProcessState();
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      if (object.ranges) {
        if (!Array.isArray(object.ranges))
          throw TypeError('.polycentric.ProcessState.ranges: array expected');
        message.ranges = [];
        for (let i = 0; i < object.ranges.length; ++i) {
          if (typeof object.ranges[i] !== 'object')
            throw TypeError(
              '.polycentric.ProcessState.ranges: object expected'
            );
          message.ranges[i] = $root.polycentric.Range.fromObject(
            object.ranges[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a ProcessState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ProcessState
     * @static
     * @param {polycentric.ProcessState} message ProcessState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ProcessState.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.ranges = [];
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.unixMilliseconds =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.unixMilliseconds = options.longs === String ? '0' : 0;
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
      if (message.ranges && message.ranges.length) {
        object.ranges = [];
        for (let j = 0; j < message.ranges.length; ++j)
          object.ranges[j] = $root.polycentric.Range.toObject(
            message.ranges[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this ProcessState to JSON.
     * @function toJSON
     * @memberof polycentric.ProcessState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ProcessState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ProcessState
     * @function getTypeUrl
     * @memberof polycentric.ProcessState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ProcessState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ProcessState';
    };

    return ProcessState;
  })();

  polycentric.Range = (function () {
    /**
     * Properties of a Range.
     * @memberof polycentric
     * @interface IRange
     * @property {number|Long|null} [low] Range low
     * @property {number|Long|null} [high] Range high
     */

    /**
     * Constructs a new Range.
     * @memberof polycentric
     * @classdesc Represents a Range.
     * @implements IRange
     * @constructor
     * @param {polycentric.IRange=} [properties] Properties to set
     */
    function Range(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Range low.
     * @member {number|Long} low
     * @memberof polycentric.Range
     * @instance
     */
    Range.prototype.low = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;

    /**
     * Range high.
     * @member {number|Long} high
     * @memberof polycentric.Range
     * @instance
     */
    Range.prototype.high = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;

    /**
     * Creates a new Range instance using the specified properties.
     * @function create
     * @memberof polycentric.Range
     * @static
     * @param {polycentric.IRange=} [properties] Properties to set
     * @returns {polycentric.Range} Range instance
     */
    Range.create = function create(properties) {
      return new Range(properties);
    };

    /**
     * Encodes the specified Range message. Does not implicitly {@link polycentric.Range.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Range
     * @static
     * @param {polycentric.IRange} message Range message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Range.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.low != null && Object.hasOwnProperty.call(message, 'low'))
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.low);
      if (message.high != null && Object.hasOwnProperty.call(message, 'high'))
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.high);
      return writer;
    };

    /**
     * Encodes the specified Range message, length delimited. Does not implicitly {@link polycentric.Range.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Range
     * @static
     * @param {polycentric.IRange} message Range message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Range.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Range message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Range
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Range} Range
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Range.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Range();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.low = reader.uint64();
            break;
          }
          case 2: {
            message.high = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Range message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Range
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Range} Range
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Range.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Range message.
     * @function verify
     * @memberof polycentric.Range
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Range.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.low != null && message.hasOwnProperty('low'))
        if (
          !$util.isInteger(message.low) &&
          !(
            message.low &&
            $util.isInteger(message.low.low) &&
            $util.isInteger(message.low.high)
          )
        )
          return 'low: integer|Long expected';
      if (message.high != null && message.hasOwnProperty('high'))
        if (
          !$util.isInteger(message.high) &&
          !(
            message.high &&
            $util.isInteger(message.high.low) &&
            $util.isInteger(message.high.high)
          )
        )
          return 'high: integer|Long expected';
      return null;
    };

    /**
     * Creates a Range message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Range
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Range} Range
     */
    Range.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Range) return object;
      let message = new $root.polycentric.Range();
      if (object.low != null)
        if ($util.Long)
          (message.low = $util.Long.fromValue(object.low)).unsigned = true;
        else if (typeof object.low === 'string')
          message.low = parseInt(object.low, 10);
        else if (typeof object.low === 'number') message.low = object.low;
        else if (typeof object.low === 'object')
          message.low = new $util.LongBits(
            object.low.low >>> 0,
            object.low.high >>> 0
          ).toNumber(true);
      if (object.high != null)
        if ($util.Long)
          (message.high = $util.Long.fromValue(object.high)).unsigned = true;
        else if (typeof object.high === 'string')
          message.high = parseInt(object.high, 10);
        else if (typeof object.high === 'number') message.high = object.high;
        else if (typeof object.high === 'object')
          message.high = new $util.LongBits(
            object.high.low >>> 0,
            object.high.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a Range message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Range
     * @static
     * @param {polycentric.Range} message Range
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Range.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.low =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.low = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.high =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.high = options.longs === String ? '0' : 0;
      }
      if (message.low != null && message.hasOwnProperty('low'))
        if (typeof message.low === 'number')
          object.low =
            options.longs === String ? String(message.low) : message.low;
        else
          object.low =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.low)
              : options.longs === Number
                ? new $util.LongBits(
                    message.low.low >>> 0,
                    message.low.high >>> 0
                  ).toNumber(true)
                : message.low;
      if (message.high != null && message.hasOwnProperty('high'))
        if (typeof message.high === 'number')
          object.high =
            options.longs === String ? String(message.high) : message.high;
        else
          object.high =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.high)
              : options.longs === Number
                ? new $util.LongBits(
                    message.high.low >>> 0,
                    message.high.high >>> 0
                  ).toNumber(true)
                : message.high;
      return object;
    };

    /**
     * Converts this Range to JSON.
     * @function toJSON
     * @memberof polycentric.Range
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Range.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Range
     * @function getTypeUrl
     * @memberof polycentric.Range
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Range.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Range';
    };

    return Range;
  })();

  polycentric.RangesForProcess = (function () {
    /**
     * Properties of a RangesForProcess.
     * @memberof polycentric
     * @interface IRangesForProcess
     * @property {polycentric.IProcess|null} [process] RangesForProcess process
     * @property {Array.<polycentric.IRange>|null} [ranges] RangesForProcess ranges
     */

    /**
     * Constructs a new RangesForProcess.
     * @memberof polycentric
     * @classdesc Represents a RangesForProcess.
     * @implements IRangesForProcess
     * @constructor
     * @param {polycentric.IRangesForProcess=} [properties] Properties to set
     */
    function RangesForProcess(properties) {
      this.ranges = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * RangesForProcess process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.RangesForProcess
     * @instance
     */
    RangesForProcess.prototype.process = null;

    /**
     * RangesForProcess ranges.
     * @member {Array.<polycentric.IRange>} ranges
     * @memberof polycentric.RangesForProcess
     * @instance
     */
    RangesForProcess.prototype.ranges = $util.emptyArray;

    /**
     * Creates a new RangesForProcess instance using the specified properties.
     * @function create
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {polycentric.IRangesForProcess=} [properties] Properties to set
     * @returns {polycentric.RangesForProcess} RangesForProcess instance
     */
    RangesForProcess.create = function create(properties) {
      return new RangesForProcess(properties);
    };

    /**
     * Encodes the specified RangesForProcess message. Does not implicitly {@link polycentric.RangesForProcess.verify|verify} messages.
     * @function encode
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {polycentric.IRangesForProcess} message RangesForProcess message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RangesForProcess.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (message.ranges != null && message.ranges.length)
        for (let i = 0; i < message.ranges.length; ++i)
          $root.polycentric.Range.encode(
            message.ranges[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified RangesForProcess message, length delimited. Does not implicitly {@link polycentric.RangesForProcess.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {polycentric.IRangesForProcess} message RangesForProcess message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RangesForProcess.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RangesForProcess message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.RangesForProcess} RangesForProcess
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RangesForProcess.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.RangesForProcess();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            if (!(message.ranges && message.ranges.length)) message.ranges = [];
            message.ranges.push(
              $root.polycentric.Range.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a RangesForProcess message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.RangesForProcess} RangesForProcess
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RangesForProcess.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RangesForProcess message.
     * @function verify
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RangesForProcess.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      if (message.ranges != null && message.hasOwnProperty('ranges')) {
        if (!Array.isArray(message.ranges)) return 'ranges: array expected';
        for (let i = 0; i < message.ranges.length; ++i) {
          let error = $root.polycentric.Range.verify(message.ranges[i]);
          if (error) return 'ranges.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a RangesForProcess message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.RangesForProcess} RangesForProcess
     */
    RangesForProcess.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.RangesForProcess) return object;
      let message = new $root.polycentric.RangesForProcess();
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError(
            '.polycentric.RangesForProcess.process: object expected'
          );
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      if (object.ranges) {
        if (!Array.isArray(object.ranges))
          throw TypeError(
            '.polycentric.RangesForProcess.ranges: array expected'
          );
        message.ranges = [];
        for (let i = 0; i < object.ranges.length; ++i) {
          if (typeof object.ranges[i] !== 'object')
            throw TypeError(
              '.polycentric.RangesForProcess.ranges: object expected'
            );
          message.ranges[i] = $root.polycentric.Range.fromObject(
            object.ranges[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a RangesForProcess message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {polycentric.RangesForProcess} message RangesForProcess
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RangesForProcess.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.ranges = [];
      if (options.defaults) object.process = null;
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      if (message.ranges && message.ranges.length) {
        object.ranges = [];
        for (let j = 0; j < message.ranges.length; ++j)
          object.ranges[j] = $root.polycentric.Range.toObject(
            message.ranges[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this RangesForProcess to JSON.
     * @function toJSON
     * @memberof polycentric.RangesForProcess
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RangesForProcess.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RangesForProcess
     * @function getTypeUrl
     * @memberof polycentric.RangesForProcess
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RangesForProcess.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.RangesForProcess';
    };

    return RangesForProcess;
  })();

  polycentric.RangesForSystem = (function () {
    /**
     * Properties of a RangesForSystem.
     * @memberof polycentric
     * @interface IRangesForSystem
     * @property {Array.<polycentric.IRangesForProcess>|null} [rangesForProcesses] RangesForSystem rangesForProcesses
     */

    /**
     * Constructs a new RangesForSystem.
     * @memberof polycentric
     * @classdesc Represents a RangesForSystem.
     * @implements IRangesForSystem
     * @constructor
     * @param {polycentric.IRangesForSystem=} [properties] Properties to set
     */
    function RangesForSystem(properties) {
      this.rangesForProcesses = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * RangesForSystem rangesForProcesses.
     * @member {Array.<polycentric.IRangesForProcess>} rangesForProcesses
     * @memberof polycentric.RangesForSystem
     * @instance
     */
    RangesForSystem.prototype.rangesForProcesses = $util.emptyArray;

    /**
     * Creates a new RangesForSystem instance using the specified properties.
     * @function create
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {polycentric.IRangesForSystem=} [properties] Properties to set
     * @returns {polycentric.RangesForSystem} RangesForSystem instance
     */
    RangesForSystem.create = function create(properties) {
      return new RangesForSystem(properties);
    };

    /**
     * Encodes the specified RangesForSystem message. Does not implicitly {@link polycentric.RangesForSystem.verify|verify} messages.
     * @function encode
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {polycentric.IRangesForSystem} message RangesForSystem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RangesForSystem.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.rangesForProcesses != null &&
        message.rangesForProcesses.length
      )
        for (let i = 0; i < message.rangesForProcesses.length; ++i)
          $root.polycentric.RangesForProcess.encode(
            message.rangesForProcesses[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified RangesForSystem message, length delimited. Does not implicitly {@link polycentric.RangesForSystem.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {polycentric.IRangesForSystem} message RangesForSystem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RangesForSystem.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RangesForSystem message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.RangesForSystem} RangesForSystem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RangesForSystem.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.RangesForSystem();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (
              !(message.rangesForProcesses && message.rangesForProcesses.length)
            )
              message.rangesForProcesses = [];
            message.rangesForProcesses.push(
              $root.polycentric.RangesForProcess.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a RangesForSystem message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.RangesForSystem} RangesForSystem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RangesForSystem.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RangesForSystem message.
     * @function verify
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RangesForSystem.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.rangesForProcesses != null &&
        message.hasOwnProperty('rangesForProcesses')
      ) {
        if (!Array.isArray(message.rangesForProcesses))
          return 'rangesForProcesses: array expected';
        for (let i = 0; i < message.rangesForProcesses.length; ++i) {
          let error = $root.polycentric.RangesForProcess.verify(
            message.rangesForProcesses[i]
          );
          if (error) return 'rangesForProcesses.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a RangesForSystem message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.RangesForSystem} RangesForSystem
     */
    RangesForSystem.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.RangesForSystem) return object;
      let message = new $root.polycentric.RangesForSystem();
      if (object.rangesForProcesses) {
        if (!Array.isArray(object.rangesForProcesses))
          throw TypeError(
            '.polycentric.RangesForSystem.rangesForProcesses: array expected'
          );
        message.rangesForProcesses = [];
        for (let i = 0; i < object.rangesForProcesses.length; ++i) {
          if (typeof object.rangesForProcesses[i] !== 'object')
            throw TypeError(
              '.polycentric.RangesForSystem.rangesForProcesses: object expected'
            );
          message.rangesForProcesses[i] =
            $root.polycentric.RangesForProcess.fromObject(
              object.rangesForProcesses[i]
            );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a RangesForSystem message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {polycentric.RangesForSystem} message RangesForSystem
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RangesForSystem.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.rangesForProcesses = [];
      if (message.rangesForProcesses && message.rangesForProcesses.length) {
        object.rangesForProcesses = [];
        for (let j = 0; j < message.rangesForProcesses.length; ++j)
          object.rangesForProcesses[j] =
            $root.polycentric.RangesForProcess.toObject(
              message.rangesForProcesses[j],
              options
            );
      }
      return object;
    };

    /**
     * Converts this RangesForSystem to JSON.
     * @function toJSON
     * @memberof polycentric.RangesForSystem
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RangesForSystem.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RangesForSystem
     * @function getTypeUrl
     * @memberof polycentric.RangesForSystem
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RangesForSystem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.RangesForSystem';
    };

    return RangesForSystem;
  })();

  polycentric.OpinionSummary = (function () {
    /**
     * Properties of an OpinionSummary.
     * @memberof polycentric
     * @interface IOpinionSummary
     * @property {number|Long|null} [likeCount] OpinionSummary likeCount
     * @property {number|Long|null} [dislikeCount] OpinionSummary dislikeCount
     * @property {number|Long|null} [neutralCount] OpinionSummary neutralCount
     * @property {number|null} [userOpinion] OpinionSummary userOpinion
     */

    /**
     * Constructs a new OpinionSummary.
     * @memberof polycentric
     * @classdesc Represents an OpinionSummary.
     * @implements IOpinionSummary
     * @constructor
     * @param {polycentric.IOpinionSummary=} [properties] Properties to set
     */
    function OpinionSummary(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * OpinionSummary likeCount.
     * @member {number|Long} likeCount
     * @memberof polycentric.OpinionSummary
     * @instance
     */
    OpinionSummary.prototype.likeCount = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * OpinionSummary dislikeCount.
     * @member {number|Long} dislikeCount
     * @memberof polycentric.OpinionSummary
     * @instance
     */
    OpinionSummary.prototype.dislikeCount = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * OpinionSummary neutralCount.
     * @member {number|Long} neutralCount
     * @memberof polycentric.OpinionSummary
     * @instance
     */
    OpinionSummary.prototype.neutralCount = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * OpinionSummary userOpinion.
     * @member {number|null|undefined} userOpinion
     * @memberof polycentric.OpinionSummary
     * @instance
     */
    OpinionSummary.prototype.userOpinion = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(OpinionSummary.prototype, '_userOpinion', {
      get: $util.oneOfGetter(($oneOfFields = ['userOpinion'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new OpinionSummary instance using the specified properties.
     * @function create
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {polycentric.IOpinionSummary=} [properties] Properties to set
     * @returns {polycentric.OpinionSummary} OpinionSummary instance
     */
    OpinionSummary.create = function create(properties) {
      return new OpinionSummary(properties);
    };

    /**
     * Encodes the specified OpinionSummary message. Does not implicitly {@link polycentric.OpinionSummary.verify|verify} messages.
     * @function encode
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {polycentric.IOpinionSummary} message OpinionSummary message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpinionSummary.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.likeCount != null &&
        Object.hasOwnProperty.call(message, 'likeCount')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.likeCount);
      if (
        message.dislikeCount != null &&
        Object.hasOwnProperty.call(message, 'dislikeCount')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.dislikeCount);
      if (
        message.neutralCount != null &&
        Object.hasOwnProperty.call(message, 'neutralCount')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint64(message.neutralCount);
      if (
        message.userOpinion != null &&
        Object.hasOwnProperty.call(message, 'userOpinion')
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).uint32(message.userOpinion);
      return writer;
    };

    /**
     * Encodes the specified OpinionSummary message, length delimited. Does not implicitly {@link polycentric.OpinionSummary.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {polycentric.IOpinionSummary} message OpinionSummary message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    OpinionSummary.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an OpinionSummary message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.OpinionSummary} OpinionSummary
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpinionSummary.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.OpinionSummary();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.likeCount = reader.uint64();
            break;
          }
          case 2: {
            message.dislikeCount = reader.uint64();
            break;
          }
          case 3: {
            message.neutralCount = reader.uint64();
            break;
          }
          case 4: {
            message.userOpinion = reader.uint32();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an OpinionSummary message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.OpinionSummary} OpinionSummary
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    OpinionSummary.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an OpinionSummary message.
     * @function verify
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    OpinionSummary.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.likeCount != null && message.hasOwnProperty('likeCount'))
        if (
          !$util.isInteger(message.likeCount) &&
          !(
            message.likeCount &&
            $util.isInteger(message.likeCount.low) &&
            $util.isInteger(message.likeCount.high)
          )
        )
          return 'likeCount: integer|Long expected';
      if (
        message.dislikeCount != null &&
        message.hasOwnProperty('dislikeCount')
      )
        if (
          !$util.isInteger(message.dislikeCount) &&
          !(
            message.dislikeCount &&
            $util.isInteger(message.dislikeCount.low) &&
            $util.isInteger(message.dislikeCount.high)
          )
        )
          return 'dislikeCount: integer|Long expected';
      if (
        message.neutralCount != null &&
        message.hasOwnProperty('neutralCount')
      )
        if (
          !$util.isInteger(message.neutralCount) &&
          !(
            message.neutralCount &&
            $util.isInteger(message.neutralCount.low) &&
            $util.isInteger(message.neutralCount.high)
          )
        )
          return 'neutralCount: integer|Long expected';
      if (
        message.userOpinion != null &&
        message.hasOwnProperty('userOpinion')
      ) {
        properties._userOpinion = 1;
        if (!$util.isInteger(message.userOpinion))
          return 'userOpinion: integer expected';
      }
      return null;
    };

    /**
     * Creates an OpinionSummary message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.OpinionSummary} OpinionSummary
     */
    OpinionSummary.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.OpinionSummary) return object;
      let message = new $root.polycentric.OpinionSummary();
      if (object.likeCount != null)
        if ($util.Long)
          (message.likeCount = $util.Long.fromValue(
            object.likeCount
          )).unsigned = true;
        else if (typeof object.likeCount === 'string')
          message.likeCount = parseInt(object.likeCount, 10);
        else if (typeof object.likeCount === 'number')
          message.likeCount = object.likeCount;
        else if (typeof object.likeCount === 'object')
          message.likeCount = new $util.LongBits(
            object.likeCount.low >>> 0,
            object.likeCount.high >>> 0
          ).toNumber(true);
      if (object.dislikeCount != null)
        if ($util.Long)
          (message.dislikeCount = $util.Long.fromValue(
            object.dislikeCount
          )).unsigned = true;
        else if (typeof object.dislikeCount === 'string')
          message.dislikeCount = parseInt(object.dislikeCount, 10);
        else if (typeof object.dislikeCount === 'number')
          message.dislikeCount = object.dislikeCount;
        else if (typeof object.dislikeCount === 'object')
          message.dislikeCount = new $util.LongBits(
            object.dislikeCount.low >>> 0,
            object.dislikeCount.high >>> 0
          ).toNumber(true);
      if (object.neutralCount != null)
        if ($util.Long)
          (message.neutralCount = $util.Long.fromValue(
            object.neutralCount
          )).unsigned = true;
        else if (typeof object.neutralCount === 'string')
          message.neutralCount = parseInt(object.neutralCount, 10);
        else if (typeof object.neutralCount === 'number')
          message.neutralCount = object.neutralCount;
        else if (typeof object.neutralCount === 'object')
          message.neutralCount = new $util.LongBits(
            object.neutralCount.low >>> 0,
            object.neutralCount.high >>> 0
          ).toNumber(true);
      if (object.userOpinion != null)
        message.userOpinion = object.userOpinion >>> 0;
      return message;
    };

    /**
     * Creates a plain object from an OpinionSummary message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {polycentric.OpinionSummary} message OpinionSummary
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    OpinionSummary.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.likeCount =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.likeCount = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.dislikeCount =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.dislikeCount = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.neutralCount =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.neutralCount = options.longs === String ? '0' : 0;
      }
      if (message.likeCount != null && message.hasOwnProperty('likeCount'))
        if (typeof message.likeCount === 'number')
          object.likeCount =
            options.longs === String
              ? String(message.likeCount)
              : message.likeCount;
        else
          object.likeCount =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.likeCount)
              : options.longs === Number
                ? new $util.LongBits(
                    message.likeCount.low >>> 0,
                    message.likeCount.high >>> 0
                  ).toNumber(true)
                : message.likeCount;
      if (
        message.dislikeCount != null &&
        message.hasOwnProperty('dislikeCount')
      )
        if (typeof message.dislikeCount === 'number')
          object.dislikeCount =
            options.longs === String
              ? String(message.dislikeCount)
              : message.dislikeCount;
        else
          object.dislikeCount =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.dislikeCount)
              : options.longs === Number
                ? new $util.LongBits(
                    message.dislikeCount.low >>> 0,
                    message.dislikeCount.high >>> 0
                  ).toNumber(true)
                : message.dislikeCount;
      if (
        message.neutralCount != null &&
        message.hasOwnProperty('neutralCount')
      )
        if (typeof message.neutralCount === 'number')
          object.neutralCount =
            options.longs === String
              ? String(message.neutralCount)
              : message.neutralCount;
        else
          object.neutralCount =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.neutralCount)
              : options.longs === Number
                ? new $util.LongBits(
                    message.neutralCount.low >>> 0,
                    message.neutralCount.high >>> 0
                  ).toNumber(true)
                : message.neutralCount;
      if (
        message.userOpinion != null &&
        message.hasOwnProperty('userOpinion')
      ) {
        object.userOpinion = message.userOpinion;
        if (options.oneofs) object._userOpinion = 'userOpinion';
      }
      return object;
    };

    /**
     * Converts this OpinionSummary to JSON.
     * @function toJSON
     * @memberof polycentric.OpinionSummary
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    OpinionSummary.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for OpinionSummary
     * @function getTypeUrl
     * @memberof polycentric.OpinionSummary
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    OpinionSummary.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.OpinionSummary';
    };

    return OpinionSummary;
  })();

  polycentric.BlobMeta = (function () {
    /**
     * Properties of a BlobMeta.
     * @memberof polycentric
     * @interface IBlobMeta
     * @property {string|null} [mime] BlobMeta mime
     * @property {number|Long|null} [size] BlobMeta size
     * @property {Array.<polycentric.IBlobSection>|null} [sections] BlobMeta sections
     */

    /**
     * Constructs a new BlobMeta.
     * @memberof polycentric
     * @classdesc Represents a BlobMeta.
     * @implements IBlobMeta
     * @constructor
     * @param {polycentric.IBlobMeta=} [properties] Properties to set
     */
    function BlobMeta(properties) {
      this.sections = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * BlobMeta mime.
     * @member {string} mime
     * @memberof polycentric.BlobMeta
     * @instance
     */
    BlobMeta.prototype.mime = '';

    /**
     * BlobMeta size.
     * @member {number|Long} size
     * @memberof polycentric.BlobMeta
     * @instance
     */
    BlobMeta.prototype.size = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;

    /**
     * BlobMeta sections.
     * @member {Array.<polycentric.IBlobSection>} sections
     * @memberof polycentric.BlobMeta
     * @instance
     */
    BlobMeta.prototype.sections = $util.emptyArray;

    /**
     * Creates a new BlobMeta instance using the specified properties.
     * @function create
     * @memberof polycentric.BlobMeta
     * @static
     * @param {polycentric.IBlobMeta=} [properties] Properties to set
     * @returns {polycentric.BlobMeta} BlobMeta instance
     */
    BlobMeta.create = function create(properties) {
      return new BlobMeta(properties);
    };

    /**
     * Encodes the specified BlobMeta message. Does not implicitly {@link polycentric.BlobMeta.verify|verify} messages.
     * @function encode
     * @memberof polycentric.BlobMeta
     * @static
     * @param {polycentric.IBlobMeta} message BlobMeta message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BlobMeta.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.mime != null && Object.hasOwnProperty.call(message, 'mime'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.mime);
      if (message.size != null && Object.hasOwnProperty.call(message, 'size'))
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.size);
      if (message.sections != null && message.sections.length)
        for (let i = 0; i < message.sections.length; ++i)
          $root.polycentric.BlobSection.encode(
            message.sections[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified BlobMeta message, length delimited. Does not implicitly {@link polycentric.BlobMeta.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.BlobMeta
     * @static
     * @param {polycentric.IBlobMeta} message BlobMeta message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BlobMeta.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a BlobMeta message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.BlobMeta
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.BlobMeta} BlobMeta
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BlobMeta.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.BlobMeta();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.mime = reader.string();
            break;
          }
          case 2: {
            message.size = reader.uint64();
            break;
          }
          case 3: {
            if (!(message.sections && message.sections.length))
              message.sections = [];
            message.sections.push(
              $root.polycentric.BlobSection.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a BlobMeta message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.BlobMeta
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.BlobMeta} BlobMeta
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BlobMeta.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BlobMeta message.
     * @function verify
     * @memberof polycentric.BlobMeta
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BlobMeta.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.mime != null && message.hasOwnProperty('mime'))
        if (!$util.isString(message.mime)) return 'mime: string expected';
      if (message.size != null && message.hasOwnProperty('size'))
        if (
          !$util.isInteger(message.size) &&
          !(
            message.size &&
            $util.isInteger(message.size.low) &&
            $util.isInteger(message.size.high)
          )
        )
          return 'size: integer|Long expected';
      if (message.sections != null && message.hasOwnProperty('sections')) {
        if (!Array.isArray(message.sections)) return 'sections: array expected';
        for (let i = 0; i < message.sections.length; ++i) {
          let error = $root.polycentric.BlobSection.verify(message.sections[i]);
          if (error) return 'sections.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a BlobMeta message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.BlobMeta
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.BlobMeta} BlobMeta
     */
    BlobMeta.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.BlobMeta) return object;
      let message = new $root.polycentric.BlobMeta();
      if (object.mime != null) message.mime = String(object.mime);
      if (object.size != null)
        if ($util.Long)
          (message.size = $util.Long.fromValue(object.size)).unsigned = true;
        else if (typeof object.size === 'string')
          message.size = parseInt(object.size, 10);
        else if (typeof object.size === 'number') message.size = object.size;
        else if (typeof object.size === 'object')
          message.size = new $util.LongBits(
            object.size.low >>> 0,
            object.size.high >>> 0
          ).toNumber(true);
      if (object.sections) {
        if (!Array.isArray(object.sections))
          throw TypeError('.polycentric.BlobMeta.sections: array expected');
        message.sections = [];
        for (let i = 0; i < object.sections.length; ++i) {
          if (typeof object.sections[i] !== 'object')
            throw TypeError('.polycentric.BlobMeta.sections: object expected');
          message.sections[i] = $root.polycentric.BlobSection.fromObject(
            object.sections[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a BlobMeta message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.BlobMeta
     * @static
     * @param {polycentric.BlobMeta} message BlobMeta
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BlobMeta.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.sections = [];
      if (options.defaults) {
        object.mime = '';
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.size =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.size = options.longs === String ? '0' : 0;
      }
      if (message.mime != null && message.hasOwnProperty('mime'))
        object.mime = message.mime;
      if (message.size != null && message.hasOwnProperty('size'))
        if (typeof message.size === 'number')
          object.size =
            options.longs === String ? String(message.size) : message.size;
        else
          object.size =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.size)
              : options.longs === Number
                ? new $util.LongBits(
                    message.size.low >>> 0,
                    message.size.high >>> 0
                  ).toNumber(true)
                : message.size;
      if (message.sections && message.sections.length) {
        object.sections = [];
        for (let j = 0; j < message.sections.length; ++j)
          object.sections[j] = $root.polycentric.BlobSection.toObject(
            message.sections[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this BlobMeta to JSON.
     * @function toJSON
     * @memberof polycentric.BlobMeta
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BlobMeta.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BlobMeta
     * @function getTypeUrl
     * @memberof polycentric.BlobMeta
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BlobMeta.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.BlobMeta';
    };

    return BlobMeta;
  })();

  polycentric.BlobSection = (function () {
    /**
     * Properties of a BlobSection.
     * @memberof polycentric
     * @interface IBlobSection
     * @property {Uint8Array|null} [digest] BlobSection digest
     * @property {number|Long|null} [size] BlobSection size
     */

    /**
     * Constructs a new BlobSection.
     * @memberof polycentric
     * @classdesc Represents a BlobSection.
     * @implements IBlobSection
     * @constructor
     * @param {polycentric.IBlobSection=} [properties] Properties to set
     */
    function BlobSection(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * BlobSection digest.
     * @member {Uint8Array} digest
     * @memberof polycentric.BlobSection
     * @instance
     */
    BlobSection.prototype.digest = $util.newBuffer([]);

    /**
     * BlobSection size.
     * @member {number|Long} size
     * @memberof polycentric.BlobSection
     * @instance
     */
    BlobSection.prototype.size = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new BlobSection instance using the specified properties.
     * @function create
     * @memberof polycentric.BlobSection
     * @static
     * @param {polycentric.IBlobSection=} [properties] Properties to set
     * @returns {polycentric.BlobSection} BlobSection instance
     */
    BlobSection.create = function create(properties) {
      return new BlobSection(properties);
    };

    /**
     * Encodes the specified BlobSection message. Does not implicitly {@link polycentric.BlobSection.verify|verify} messages.
     * @function encode
     * @memberof polycentric.BlobSection
     * @static
     * @param {polycentric.IBlobSection} message BlobSection message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BlobSection.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.digest != null &&
        Object.hasOwnProperty.call(message, 'digest')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.digest);
      if (message.size != null && Object.hasOwnProperty.call(message, 'size'))
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.size);
      return writer;
    };

    /**
     * Encodes the specified BlobSection message, length delimited. Does not implicitly {@link polycentric.BlobSection.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.BlobSection
     * @static
     * @param {polycentric.IBlobSection} message BlobSection message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    BlobSection.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a BlobSection message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.BlobSection
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.BlobSection} BlobSection
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BlobSection.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.BlobSection();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.digest = reader.bytes();
            break;
          }
          case 2: {
            message.size = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a BlobSection message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.BlobSection
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.BlobSection} BlobSection
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    BlobSection.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a BlobSection message.
     * @function verify
     * @memberof polycentric.BlobSection
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    BlobSection.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.digest != null && message.hasOwnProperty('digest'))
        if (
          !(
            (message.digest && typeof message.digest.length === 'number') ||
            $util.isString(message.digest)
          )
        )
          return 'digest: buffer expected';
      if (message.size != null && message.hasOwnProperty('size'))
        if (
          !$util.isInteger(message.size) &&
          !(
            message.size &&
            $util.isInteger(message.size.low) &&
            $util.isInteger(message.size.high)
          )
        )
          return 'size: integer|Long expected';
      return null;
    };

    /**
     * Creates a BlobSection message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.BlobSection
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.BlobSection} BlobSection
     */
    BlobSection.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.BlobSection) return object;
      let message = new $root.polycentric.BlobSection();
      if (object.digest != null)
        if (typeof object.digest === 'string')
          $util.base64.decode(
            object.digest,
            (message.digest = $util.newBuffer(
              $util.base64.length(object.digest)
            )),
            0
          );
        else if (object.digest.length >= 0) message.digest = object.digest;
      if (object.size != null)
        if ($util.Long)
          (message.size = $util.Long.fromValue(object.size)).unsigned = true;
        else if (typeof object.size === 'string')
          message.size = parseInt(object.size, 10);
        else if (typeof object.size === 'number') message.size = object.size;
        else if (typeof object.size === 'object')
          message.size = new $util.LongBits(
            object.size.low >>> 0,
            object.size.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a BlobSection message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.BlobSection
     * @static
     * @param {polycentric.BlobSection} message BlobSection
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    BlobSection.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if (options.bytes === String) object.digest = '';
        else {
          object.digest = [];
          if (options.bytes !== Array)
            object.digest = $util.newBuffer(object.digest);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.size =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.size = options.longs === String ? '0' : 0;
      }
      if (message.digest != null && message.hasOwnProperty('digest'))
        object.digest =
          options.bytes === String
            ? $util.base64.encode(message.digest, 0, message.digest.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.digest)
              : message.digest;
      if (message.size != null && message.hasOwnProperty('size'))
        if (typeof message.size === 'number')
          object.size =
            options.longs === String ? String(message.size) : message.size;
        else
          object.size =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.size)
              : options.longs === Number
                ? new $util.LongBits(
                    message.size.low >>> 0,
                    message.size.high >>> 0
                  ).toNumber(true)
                : message.size;
      return object;
    };

    /**
     * Converts this BlobSection to JSON.
     * @function toJSON
     * @memberof polycentric.BlobSection
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    BlobSection.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for BlobSection
     * @function getTypeUrl
     * @memberof polycentric.BlobSection
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    BlobSection.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.BlobSection';
    };

    return BlobSection;
  })();

  polycentric.URLInfo = (function () {
    /**
     * Properties of a URLInfo.
     * @memberof polycentric
     * @interface IURLInfo
     * @property {number|Long|null} [urlType] URLInfo urlType
     * @property {Uint8Array|null} [body] URLInfo body
     */

    /**
     * Constructs a new URLInfo.
     * @memberof polycentric
     * @classdesc Represents a URLInfo.
     * @implements IURLInfo
     * @constructor
     * @param {polycentric.IURLInfo=} [properties] Properties to set
     */
    function URLInfo(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * URLInfo urlType.
     * @member {number|Long} urlType
     * @memberof polycentric.URLInfo
     * @instance
     */
    URLInfo.prototype.urlType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * URLInfo body.
     * @member {Uint8Array} body
     * @memberof polycentric.URLInfo
     * @instance
     */
    URLInfo.prototype.body = $util.newBuffer([]);

    /**
     * Creates a new URLInfo instance using the specified properties.
     * @function create
     * @memberof polycentric.URLInfo
     * @static
     * @param {polycentric.IURLInfo=} [properties] Properties to set
     * @returns {polycentric.URLInfo} URLInfo instance
     */
    URLInfo.create = function create(properties) {
      return new URLInfo(properties);
    };

    /**
     * Encodes the specified URLInfo message. Does not implicitly {@link polycentric.URLInfo.verify|verify} messages.
     * @function encode
     * @memberof polycentric.URLInfo
     * @static
     * @param {polycentric.IURLInfo} message URLInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfo.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.urlType != null &&
        Object.hasOwnProperty.call(message, 'urlType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.urlType);
      if (message.body != null && Object.hasOwnProperty.call(message, 'body'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.body);
      return writer;
    };

    /**
     * Encodes the specified URLInfo message, length delimited. Does not implicitly {@link polycentric.URLInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.URLInfo
     * @static
     * @param {polycentric.IURLInfo} message URLInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfo.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a URLInfo message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.URLInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.URLInfo} URLInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfo.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.URLInfo();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.urlType = reader.uint64();
            break;
          }
          case 2: {
            message.body = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a URLInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.URLInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.URLInfo} URLInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfo.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a URLInfo message.
     * @function verify
     * @memberof polycentric.URLInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    URLInfo.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.urlType != null && message.hasOwnProperty('urlType'))
        if (
          !$util.isInteger(message.urlType) &&
          !(
            message.urlType &&
            $util.isInteger(message.urlType.low) &&
            $util.isInteger(message.urlType.high)
          )
        )
          return 'urlType: integer|Long expected';
      if (message.body != null && message.hasOwnProperty('body'))
        if (
          !(
            (message.body && typeof message.body.length === 'number') ||
            $util.isString(message.body)
          )
        )
          return 'body: buffer expected';
      return null;
    };

    /**
     * Creates a URLInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.URLInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.URLInfo} URLInfo
     */
    URLInfo.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.URLInfo) return object;
      let message = new $root.polycentric.URLInfo();
      if (object.urlType != null)
        if ($util.Long)
          (message.urlType = $util.Long.fromValue(object.urlType)).unsigned =
            true;
        else if (typeof object.urlType === 'string')
          message.urlType = parseInt(object.urlType, 10);
        else if (typeof object.urlType === 'number')
          message.urlType = object.urlType;
        else if (typeof object.urlType === 'object')
          message.urlType = new $util.LongBits(
            object.urlType.low >>> 0,
            object.urlType.high >>> 0
          ).toNumber(true);
      if (object.body != null)
        if (typeof object.body === 'string')
          $util.base64.decode(
            object.body,
            (message.body = $util.newBuffer($util.base64.length(object.body))),
            0
          );
        else if (object.body.length >= 0) message.body = object.body;
      return message;
    };

    /**
     * Creates a plain object from a URLInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.URLInfo
     * @static
     * @param {polycentric.URLInfo} message URLInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    URLInfo.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.urlType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.urlType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.body = '';
        else {
          object.body = [];
          if (options.bytes !== Array)
            object.body = $util.newBuffer(object.body);
        }
      }
      if (message.urlType != null && message.hasOwnProperty('urlType'))
        if (typeof message.urlType === 'number')
          object.urlType =
            options.longs === String
              ? String(message.urlType)
              : message.urlType;
        else
          object.urlType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.urlType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.urlType.low >>> 0,
                    message.urlType.high >>> 0
                  ).toNumber(true)
                : message.urlType;
      if (message.body != null && message.hasOwnProperty('body'))
        object.body =
          options.bytes === String
            ? $util.base64.encode(message.body, 0, message.body.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.body)
              : message.body;
      return object;
    };

    /**
     * Converts this URLInfo to JSON.
     * @function toJSON
     * @memberof polycentric.URLInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    URLInfo.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for URLInfo
     * @function getTypeUrl
     * @memberof polycentric.URLInfo
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    URLInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.URLInfo';
    };

    return URLInfo;
  })();

  polycentric.URLInfoSystemLink = (function () {
    /**
     * Properties of a URLInfoSystemLink.
     * @memberof polycentric
     * @interface IURLInfoSystemLink
     * @property {polycentric.IPublicKey|null} [system] URLInfoSystemLink system
     * @property {Array.<string>|null} [servers] URLInfoSystemLink servers
     */

    /**
     * Constructs a new URLInfoSystemLink.
     * @memberof polycentric
     * @classdesc Represents a URLInfoSystemLink.
     * @implements IURLInfoSystemLink
     * @constructor
     * @param {polycentric.IURLInfoSystemLink=} [properties] Properties to set
     */
    function URLInfoSystemLink(properties) {
      this.servers = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * URLInfoSystemLink system.
     * @member {polycentric.IPublicKey|null|undefined} system
     * @memberof polycentric.URLInfoSystemLink
     * @instance
     */
    URLInfoSystemLink.prototype.system = null;

    /**
     * URLInfoSystemLink servers.
     * @member {Array.<string>} servers
     * @memberof polycentric.URLInfoSystemLink
     * @instance
     */
    URLInfoSystemLink.prototype.servers = $util.emptyArray;

    /**
     * Creates a new URLInfoSystemLink instance using the specified properties.
     * @function create
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {polycentric.IURLInfoSystemLink=} [properties] Properties to set
     * @returns {polycentric.URLInfoSystemLink} URLInfoSystemLink instance
     */
    URLInfoSystemLink.create = function create(properties) {
      return new URLInfoSystemLink(properties);
    };

    /**
     * Encodes the specified URLInfoSystemLink message. Does not implicitly {@link polycentric.URLInfoSystemLink.verify|verify} messages.
     * @function encode
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {polycentric.IURLInfoSystemLink} message URLInfoSystemLink message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfoSystemLink.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PublicKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (message.servers != null && message.servers.length)
        for (let i = 0; i < message.servers.length; ++i)
          writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.servers[i]);
      return writer;
    };

    /**
     * Encodes the specified URLInfoSystemLink message, length delimited. Does not implicitly {@link polycentric.URLInfoSystemLink.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {polycentric.IURLInfoSystemLink} message URLInfoSystemLink message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfoSystemLink.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a URLInfoSystemLink message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.URLInfoSystemLink} URLInfoSystemLink
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfoSystemLink.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.URLInfoSystemLink();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            if (!(message.servers && message.servers.length))
              message.servers = [];
            message.servers.push(reader.string());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a URLInfoSystemLink message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.URLInfoSystemLink} URLInfoSystemLink
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfoSystemLink.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a URLInfoSystemLink message.
     * @function verify
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    URLInfoSystemLink.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PublicKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.servers != null && message.hasOwnProperty('servers')) {
        if (!Array.isArray(message.servers)) return 'servers: array expected';
        for (let i = 0; i < message.servers.length; ++i)
          if (!$util.isString(message.servers[i]))
            return 'servers: string[] expected';
      }
      return null;
    };

    /**
     * Creates a URLInfoSystemLink message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.URLInfoSystemLink} URLInfoSystemLink
     */
    URLInfoSystemLink.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.URLInfoSystemLink) return object;
      let message = new $root.polycentric.URLInfoSystemLink();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError(
            '.polycentric.URLInfoSystemLink.system: object expected'
          );
        message.system = $root.polycentric.PublicKey.fromObject(object.system);
      }
      if (object.servers) {
        if (!Array.isArray(object.servers))
          throw TypeError(
            '.polycentric.URLInfoSystemLink.servers: array expected'
          );
        message.servers = [];
        for (let i = 0; i < object.servers.length; ++i)
          message.servers[i] = String(object.servers[i]);
      }
      return message;
    };

    /**
     * Creates a plain object from a URLInfoSystemLink message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {polycentric.URLInfoSystemLink} message URLInfoSystemLink
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    URLInfoSystemLink.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.servers = [];
      if (options.defaults) object.system = null;
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PublicKey.toObject(
          message.system,
          options
        );
      if (message.servers && message.servers.length) {
        object.servers = [];
        for (let j = 0; j < message.servers.length; ++j)
          object.servers[j] = message.servers[j];
      }
      return object;
    };

    /**
     * Converts this URLInfoSystemLink to JSON.
     * @function toJSON
     * @memberof polycentric.URLInfoSystemLink
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    URLInfoSystemLink.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for URLInfoSystemLink
     * @function getTypeUrl
     * @memberof polycentric.URLInfoSystemLink
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    URLInfoSystemLink.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.URLInfoSystemLink';
    };

    return URLInfoSystemLink;
  })();

  polycentric.URLInfoEventLink = (function () {
    /**
     * Properties of a URLInfoEventLink.
     * @memberof polycentric
     * @interface IURLInfoEventLink
     * @property {polycentric.IPublicKey|null} [system] URLInfoEventLink system
     * @property {polycentric.IProcess|null} [process] URLInfoEventLink process
     * @property {number|Long|null} [logicalClock] URLInfoEventLink logicalClock
     * @property {Array.<string>|null} [servers] URLInfoEventLink servers
     */

    /**
     * Constructs a new URLInfoEventLink.
     * @memberof polycentric
     * @classdesc Represents a URLInfoEventLink.
     * @implements IURLInfoEventLink
     * @constructor
     * @param {polycentric.IURLInfoEventLink=} [properties] Properties to set
     */
    function URLInfoEventLink(properties) {
      this.servers = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * URLInfoEventLink system.
     * @member {polycentric.IPublicKey|null|undefined} system
     * @memberof polycentric.URLInfoEventLink
     * @instance
     */
    URLInfoEventLink.prototype.system = null;

    /**
     * URLInfoEventLink process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.URLInfoEventLink
     * @instance
     */
    URLInfoEventLink.prototype.process = null;

    /**
     * URLInfoEventLink logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.URLInfoEventLink
     * @instance
     */
    URLInfoEventLink.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * URLInfoEventLink servers.
     * @member {Array.<string>} servers
     * @memberof polycentric.URLInfoEventLink
     * @instance
     */
    URLInfoEventLink.prototype.servers = $util.emptyArray;

    /**
     * Creates a new URLInfoEventLink instance using the specified properties.
     * @function create
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {polycentric.IURLInfoEventLink=} [properties] Properties to set
     * @returns {polycentric.URLInfoEventLink} URLInfoEventLink instance
     */
    URLInfoEventLink.create = function create(properties) {
      return new URLInfoEventLink(properties);
    };

    /**
     * Encodes the specified URLInfoEventLink message. Does not implicitly {@link polycentric.URLInfoEventLink.verify|verify} messages.
     * @function encode
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {polycentric.IURLInfoEventLink} message URLInfoEventLink message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfoEventLink.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PublicKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint64(message.logicalClock);
      if (message.servers != null && message.servers.length)
        for (let i = 0; i < message.servers.length; ++i)
          writer.uint32(/* id 4, wireType 2 =*/ 34).string(message.servers[i]);
      return writer;
    };

    /**
     * Encodes the specified URLInfoEventLink message, length delimited. Does not implicitly {@link polycentric.URLInfoEventLink.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {polycentric.IURLInfoEventLink} message URLInfoEventLink message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfoEventLink.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a URLInfoEventLink message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.URLInfoEventLink} URLInfoEventLink
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfoEventLink.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.URLInfoEventLink();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            message.logicalClock = reader.uint64();
            break;
          }
          case 4: {
            if (!(message.servers && message.servers.length))
              message.servers = [];
            message.servers.push(reader.string());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a URLInfoEventLink message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.URLInfoEventLink} URLInfoEventLink
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfoEventLink.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a URLInfoEventLink message.
     * @function verify
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    URLInfoEventLink.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PublicKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      if (message.servers != null && message.hasOwnProperty('servers')) {
        if (!Array.isArray(message.servers)) return 'servers: array expected';
        for (let i = 0; i < message.servers.length; ++i)
          if (!$util.isString(message.servers[i]))
            return 'servers: string[] expected';
      }
      return null;
    };

    /**
     * Creates a URLInfoEventLink message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.URLInfoEventLink} URLInfoEventLink
     */
    URLInfoEventLink.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.URLInfoEventLink) return object;
      let message = new $root.polycentric.URLInfoEventLink();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError(
            '.polycentric.URLInfoEventLink.system: object expected'
          );
        message.system = $root.polycentric.PublicKey.fromObject(object.system);
      }
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError(
            '.polycentric.URLInfoEventLink.process: object expected'
          );
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      if (object.servers) {
        if (!Array.isArray(object.servers))
          throw TypeError(
            '.polycentric.URLInfoEventLink.servers: array expected'
          );
        message.servers = [];
        for (let i = 0; i < object.servers.length; ++i)
          message.servers[i] = String(object.servers[i]);
      }
      return message;
    };

    /**
     * Creates a plain object from a URLInfoEventLink message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {polycentric.URLInfoEventLink} message URLInfoEventLink
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    URLInfoEventLink.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.servers = [];
      if (options.defaults) {
        object.system = null;
        object.process = null;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PublicKey.toObject(
          message.system,
          options
        );
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      if (message.servers && message.servers.length) {
        object.servers = [];
        for (let j = 0; j < message.servers.length; ++j)
          object.servers[j] = message.servers[j];
      }
      return object;
    };

    /**
     * Converts this URLInfoEventLink to JSON.
     * @function toJSON
     * @memberof polycentric.URLInfoEventLink
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    URLInfoEventLink.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for URLInfoEventLink
     * @function getTypeUrl
     * @memberof polycentric.URLInfoEventLink
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    URLInfoEventLink.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.URLInfoEventLink';
    };

    return URLInfoEventLink;
  })();

  polycentric.ExportBundle = (function () {
    /**
     * Properties of an ExportBundle.
     * @memberof polycentric
     * @interface IExportBundle
     * @property {Array.<polycentric.ISignedEvent>|null} [events] ExportBundle events
     * @property {Array.<polycentric.IURLInfoSystemLink>|null} [systemLinks] ExportBundle systemLinks
     * @property {Array.<polycentric.IURLInfoEventLink>|null} [eventLinks] ExportBundle eventLinks
     */

    /**
     * Constructs a new ExportBundle.
     * @memberof polycentric
     * @classdesc Represents an ExportBundle.
     * @implements IExportBundle
     * @constructor
     * @param {polycentric.IExportBundle=} [properties] Properties to set
     */
    function ExportBundle(properties) {
      this.events = [];
      this.systemLinks = [];
      this.eventLinks = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ExportBundle events.
     * @member {Array.<polycentric.ISignedEvent>} events
     * @memberof polycentric.ExportBundle
     * @instance
     */
    ExportBundle.prototype.events = $util.emptyArray;

    /**
     * ExportBundle systemLinks.
     * @member {Array.<polycentric.IURLInfoSystemLink>} systemLinks
     * @memberof polycentric.ExportBundle
     * @instance
     */
    ExportBundle.prototype.systemLinks = $util.emptyArray;

    /**
     * ExportBundle eventLinks.
     * @member {Array.<polycentric.IURLInfoEventLink>} eventLinks
     * @memberof polycentric.ExportBundle
     * @instance
     */
    ExportBundle.prototype.eventLinks = $util.emptyArray;

    /**
     * Creates a new ExportBundle instance using the specified properties.
     * @function create
     * @memberof polycentric.ExportBundle
     * @static
     * @param {polycentric.IExportBundle=} [properties] Properties to set
     * @returns {polycentric.ExportBundle} ExportBundle instance
     */
    ExportBundle.create = function create(properties) {
      return new ExportBundle(properties);
    };

    /**
     * Encodes the specified ExportBundle message. Does not implicitly {@link polycentric.ExportBundle.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ExportBundle
     * @static
     * @param {polycentric.IExportBundle} message ExportBundle message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ExportBundle.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.events != null && message.events.length)
        for (let i = 0; i < message.events.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.events[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      if (message.systemLinks != null && message.systemLinks.length)
        for (let i = 0; i < message.systemLinks.length; ++i)
          $root.polycentric.URLInfoSystemLink.encode(
            message.systemLinks[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      if (message.eventLinks != null && message.eventLinks.length)
        for (let i = 0; i < message.eventLinks.length; ++i)
          $root.polycentric.URLInfoEventLink.encode(
            message.eventLinks[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified ExportBundle message, length delimited. Does not implicitly {@link polycentric.ExportBundle.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ExportBundle
     * @static
     * @param {polycentric.IExportBundle} message ExportBundle message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ExportBundle.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an ExportBundle message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ExportBundle
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ExportBundle} ExportBundle
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ExportBundle.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ExportBundle();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.events && message.events.length)) message.events = [];
            message.events.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          case 2: {
            if (!(message.systemLinks && message.systemLinks.length))
              message.systemLinks = [];
            message.systemLinks.push(
              $root.polycentric.URLInfoSystemLink.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          case 3: {
            if (!(message.eventLinks && message.eventLinks.length))
              message.eventLinks = [];
            message.eventLinks.push(
              $root.polycentric.URLInfoEventLink.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an ExportBundle message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ExportBundle
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ExportBundle} ExportBundle
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ExportBundle.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an ExportBundle message.
     * @function verify
     * @memberof polycentric.ExportBundle
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ExportBundle.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.events != null && message.hasOwnProperty('events')) {
        if (!Array.isArray(message.events)) return 'events: array expected';
        for (let i = 0; i < message.events.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(message.events[i]);
          if (error) return 'events.' + error;
        }
      }
      if (
        message.systemLinks != null &&
        message.hasOwnProperty('systemLinks')
      ) {
        if (!Array.isArray(message.systemLinks))
          return 'systemLinks: array expected';
        for (let i = 0; i < message.systemLinks.length; ++i) {
          let error = $root.polycentric.URLInfoSystemLink.verify(
            message.systemLinks[i]
          );
          if (error) return 'systemLinks.' + error;
        }
      }
      if (message.eventLinks != null && message.hasOwnProperty('eventLinks')) {
        if (!Array.isArray(message.eventLinks))
          return 'eventLinks: array expected';
        for (let i = 0; i < message.eventLinks.length; ++i) {
          let error = $root.polycentric.URLInfoEventLink.verify(
            message.eventLinks[i]
          );
          if (error) return 'eventLinks.' + error;
        }
      }
      return null;
    };

    /**
     * Creates an ExportBundle message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ExportBundle
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ExportBundle} ExportBundle
     */
    ExportBundle.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ExportBundle) return object;
      let message = new $root.polycentric.ExportBundle();
      if (object.events) {
        if (!Array.isArray(object.events))
          throw TypeError('.polycentric.ExportBundle.events: array expected');
        message.events = [];
        for (let i = 0; i < object.events.length; ++i) {
          if (typeof object.events[i] !== 'object')
            throw TypeError(
              '.polycentric.ExportBundle.events: object expected'
            );
          message.events[i] = $root.polycentric.SignedEvent.fromObject(
            object.events[i]
          );
        }
      }
      if (object.systemLinks) {
        if (!Array.isArray(object.systemLinks))
          throw TypeError(
            '.polycentric.ExportBundle.systemLinks: array expected'
          );
        message.systemLinks = [];
        for (let i = 0; i < object.systemLinks.length; ++i) {
          if (typeof object.systemLinks[i] !== 'object')
            throw TypeError(
              '.polycentric.ExportBundle.systemLinks: object expected'
            );
          message.systemLinks[i] =
            $root.polycentric.URLInfoSystemLink.fromObject(
              object.systemLinks[i]
            );
        }
      }
      if (object.eventLinks) {
        if (!Array.isArray(object.eventLinks))
          throw TypeError(
            '.polycentric.ExportBundle.eventLinks: array expected'
          );
        message.eventLinks = [];
        for (let i = 0; i < object.eventLinks.length; ++i) {
          if (typeof object.eventLinks[i] !== 'object')
            throw TypeError(
              '.polycentric.ExportBundle.eventLinks: object expected'
            );
          message.eventLinks[i] = $root.polycentric.URLInfoEventLink.fromObject(
            object.eventLinks[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from an ExportBundle message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ExportBundle
     * @static
     * @param {polycentric.ExportBundle} message ExportBundle
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ExportBundle.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.events = [];
        object.systemLinks = [];
        object.eventLinks = [];
      }
      if (message.events && message.events.length) {
        object.events = [];
        for (let j = 0; j < message.events.length; ++j)
          object.events[j] = $root.polycentric.SignedEvent.toObject(
            message.events[j],
            options
          );
      }
      if (message.systemLinks && message.systemLinks.length) {
        object.systemLinks = [];
        for (let j = 0; j < message.systemLinks.length; ++j)
          object.systemLinks[j] = $root.polycentric.URLInfoSystemLink.toObject(
            message.systemLinks[j],
            options
          );
      }
      if (message.eventLinks && message.eventLinks.length) {
        object.eventLinks = [];
        for (let j = 0; j < message.eventLinks.length; ++j)
          object.eventLinks[j] = $root.polycentric.URLInfoEventLink.toObject(
            message.eventLinks[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this ExportBundle to JSON.
     * @function toJSON
     * @memberof polycentric.ExportBundle
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ExportBundle.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ExportBundle
     * @function getTypeUrl
     * @memberof polycentric.ExportBundle
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ExportBundle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ExportBundle';
    };

    return ExportBundle;
  })();

  polycentric.HarborChallengeResponse = (function () {
    /**
     * Properties of a HarborChallengeResponse.
     * @memberof polycentric
     * @interface IHarborChallengeResponse
     * @property {polycentric.IHarborChallengeResponseBody|null} [body] HarborChallengeResponse body
     * @property {Uint8Array|null} [signature] HarborChallengeResponse signature
     */

    /**
     * Constructs a new HarborChallengeResponse.
     * @memberof polycentric
     * @classdesc Represents a HarborChallengeResponse.
     * @implements IHarborChallengeResponse
     * @constructor
     * @param {polycentric.IHarborChallengeResponse=} [properties] Properties to set
     */
    function HarborChallengeResponse(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * HarborChallengeResponse body.
     * @member {polycentric.IHarborChallengeResponseBody|null|undefined} body
     * @memberof polycentric.HarborChallengeResponse
     * @instance
     */
    HarborChallengeResponse.prototype.body = null;

    /**
     * HarborChallengeResponse signature.
     * @member {Uint8Array} signature
     * @memberof polycentric.HarborChallengeResponse
     * @instance
     */
    HarborChallengeResponse.prototype.signature = $util.newBuffer([]);

    /**
     * Creates a new HarborChallengeResponse instance using the specified properties.
     * @function create
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {polycentric.IHarborChallengeResponse=} [properties] Properties to set
     * @returns {polycentric.HarborChallengeResponse} HarborChallengeResponse instance
     */
    HarborChallengeResponse.create = function create(properties) {
      return new HarborChallengeResponse(properties);
    };

    /**
     * Encodes the specified HarborChallengeResponse message. Does not implicitly {@link polycentric.HarborChallengeResponse.verify|verify} messages.
     * @function encode
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {polycentric.IHarborChallengeResponse} message HarborChallengeResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HarborChallengeResponse.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.body != null && Object.hasOwnProperty.call(message, 'body'))
        $root.polycentric.HarborChallengeResponseBody.encode(
          message.body,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.signature != null &&
        Object.hasOwnProperty.call(message, 'signature')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.signature);
      return writer;
    };

    /**
     * Encodes the specified HarborChallengeResponse message, length delimited. Does not implicitly {@link polycentric.HarborChallengeResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {polycentric.IHarborChallengeResponse} message HarborChallengeResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HarborChallengeResponse.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a HarborChallengeResponse message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.HarborChallengeResponse} HarborChallengeResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HarborChallengeResponse.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.HarborChallengeResponse();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.body = $root.polycentric.HarborChallengeResponseBody.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.signature = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a HarborChallengeResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.HarborChallengeResponse} HarborChallengeResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HarborChallengeResponse.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a HarborChallengeResponse message.
     * @function verify
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    HarborChallengeResponse.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.body != null && message.hasOwnProperty('body')) {
        let error = $root.polycentric.HarborChallengeResponseBody.verify(
          message.body
        );
        if (error) return 'body.' + error;
      }
      if (message.signature != null && message.hasOwnProperty('signature'))
        if (
          !(
            (message.signature &&
              typeof message.signature.length === 'number') ||
            $util.isString(message.signature)
          )
        )
          return 'signature: buffer expected';
      return null;
    };

    /**
     * Creates a HarborChallengeResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.HarborChallengeResponse} HarborChallengeResponse
     */
    HarborChallengeResponse.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.HarborChallengeResponse)
        return object;
      let message = new $root.polycentric.HarborChallengeResponse();
      if (object.body != null) {
        if (typeof object.body !== 'object')
          throw TypeError(
            '.polycentric.HarborChallengeResponse.body: object expected'
          );
        message.body = $root.polycentric.HarborChallengeResponseBody.fromObject(
          object.body
        );
      }
      if (object.signature != null)
        if (typeof object.signature === 'string')
          $util.base64.decode(
            object.signature,
            (message.signature = $util.newBuffer(
              $util.base64.length(object.signature)
            )),
            0
          );
        else if (object.signature.length >= 0)
          message.signature = object.signature;
      return message;
    };

    /**
     * Creates a plain object from a HarborChallengeResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {polycentric.HarborChallengeResponse} message HarborChallengeResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    HarborChallengeResponse.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.body = null;
        if (options.bytes === String) object.signature = '';
        else {
          object.signature = [];
          if (options.bytes !== Array)
            object.signature = $util.newBuffer(object.signature);
        }
      }
      if (message.body != null && message.hasOwnProperty('body'))
        object.body = $root.polycentric.HarborChallengeResponseBody.toObject(
          message.body,
          options
        );
      if (message.signature != null && message.hasOwnProperty('signature'))
        object.signature =
          options.bytes === String
            ? $util.base64.encode(
                message.signature,
                0,
                message.signature.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.signature)
              : message.signature;
      return object;
    };

    /**
     * Converts this HarborChallengeResponse to JSON.
     * @function toJSON
     * @memberof polycentric.HarborChallengeResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    HarborChallengeResponse.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for HarborChallengeResponse
     * @function getTypeUrl
     * @memberof polycentric.HarborChallengeResponse
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    HarborChallengeResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.HarborChallengeResponse';
    };

    return HarborChallengeResponse;
  })();

  polycentric.HarborChallengeResponseBody = (function () {
    /**
     * Properties of a HarborChallengeResponseBody.
     * @memberof polycentric
     * @interface IHarborChallengeResponseBody
     * @property {string|null} [challenge] HarborChallengeResponseBody challenge
     * @property {number|Long|null} [unixMilliseconds] HarborChallengeResponseBody unixMilliseconds
     */

    /**
     * Constructs a new HarborChallengeResponseBody.
     * @memberof polycentric
     * @classdesc Represents a HarborChallengeResponseBody.
     * @implements IHarborChallengeResponseBody
     * @constructor
     * @param {polycentric.IHarborChallengeResponseBody=} [properties] Properties to set
     */
    function HarborChallengeResponseBody(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * HarborChallengeResponseBody challenge.
     * @member {string} challenge
     * @memberof polycentric.HarborChallengeResponseBody
     * @instance
     */
    HarborChallengeResponseBody.prototype.challenge = '';

    /**
     * HarborChallengeResponseBody unixMilliseconds.
     * @member {number|Long} unixMilliseconds
     * @memberof polycentric.HarborChallengeResponseBody
     * @instance
     */
    HarborChallengeResponseBody.prototype.unixMilliseconds = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new HarborChallengeResponseBody instance using the specified properties.
     * @function create
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {polycentric.IHarborChallengeResponseBody=} [properties] Properties to set
     * @returns {polycentric.HarborChallengeResponseBody} HarborChallengeResponseBody instance
     */
    HarborChallengeResponseBody.create = function create(properties) {
      return new HarborChallengeResponseBody(properties);
    };

    /**
     * Encodes the specified HarborChallengeResponseBody message. Does not implicitly {@link polycentric.HarborChallengeResponseBody.verify|verify} messages.
     * @function encode
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {polycentric.IHarborChallengeResponseBody} message HarborChallengeResponseBody message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HarborChallengeResponseBody.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.challenge != null &&
        Object.hasOwnProperty.call(message, 'challenge')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.challenge);
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 2, wireType 0 =*/ 16)
          .uint64(message.unixMilliseconds);
      return writer;
    };

    /**
     * Encodes the specified HarborChallengeResponseBody message, length delimited. Does not implicitly {@link polycentric.HarborChallengeResponseBody.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {polycentric.IHarborChallengeResponseBody} message HarborChallengeResponseBody message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HarborChallengeResponseBody.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a HarborChallengeResponseBody message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.HarborChallengeResponseBody} HarborChallengeResponseBody
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HarborChallengeResponseBody.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.HarborChallengeResponseBody();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.challenge = reader.string();
            break;
          }
          case 2: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a HarborChallengeResponseBody message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.HarborChallengeResponseBody} HarborChallengeResponseBody
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HarborChallengeResponseBody.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a HarborChallengeResponseBody message.
     * @function verify
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    HarborChallengeResponseBody.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.challenge != null && message.hasOwnProperty('challenge'))
        if (!$util.isString(message.challenge))
          return 'challenge: string expected';
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      return null;
    };

    /**
     * Creates a HarborChallengeResponseBody message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.HarborChallengeResponseBody} HarborChallengeResponseBody
     */
    HarborChallengeResponseBody.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.HarborChallengeResponseBody)
        return object;
      let message = new $root.polycentric.HarborChallengeResponseBody();
      if (object.challenge != null)
        message.challenge = String(object.challenge);
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a HarborChallengeResponseBody message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {polycentric.HarborChallengeResponseBody} message HarborChallengeResponseBody
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    HarborChallengeResponseBody.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.challenge = '';
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.unixMilliseconds =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.unixMilliseconds = options.longs === String ? '0' : 0;
      }
      if (message.challenge != null && message.hasOwnProperty('challenge'))
        object.challenge = message.challenge;
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
      return object;
    };

    /**
     * Converts this HarborChallengeResponseBody to JSON.
     * @function toJSON
     * @memberof polycentric.HarborChallengeResponseBody
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    HarborChallengeResponseBody.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for HarborChallengeResponseBody
     * @function getTypeUrl
     * @memberof polycentric.HarborChallengeResponseBody
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    HarborChallengeResponseBody.getTypeUrl = function getTypeUrl(
      typeUrlPrefix
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.HarborChallengeResponseBody';
    };

    return HarborChallengeResponseBody;
  })();

  polycentric.HarborValidateRequest = (function () {
    /**
     * Properties of a HarborValidateRequest.
     * @memberof polycentric
     * @interface IHarborValidateRequest
     * @property {string|null} [solution] HarborValidateRequest solution
     * @property {polycentric.IHarborChallengeResponse|null} [challengeResponse] HarborValidateRequest challengeResponse
     */

    /**
     * Constructs a new HarborValidateRequest.
     * @memberof polycentric
     * @classdesc Represents a HarborValidateRequest.
     * @implements IHarborValidateRequest
     * @constructor
     * @param {polycentric.IHarborValidateRequest=} [properties] Properties to set
     */
    function HarborValidateRequest(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * HarborValidateRequest solution.
     * @member {string} solution
     * @memberof polycentric.HarborValidateRequest
     * @instance
     */
    HarborValidateRequest.prototype.solution = '';

    /**
     * HarborValidateRequest challengeResponse.
     * @member {polycentric.IHarborChallengeResponse|null|undefined} challengeResponse
     * @memberof polycentric.HarborValidateRequest
     * @instance
     */
    HarborValidateRequest.prototype.challengeResponse = null;

    /**
     * Creates a new HarborValidateRequest instance using the specified properties.
     * @function create
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {polycentric.IHarborValidateRequest=} [properties] Properties to set
     * @returns {polycentric.HarborValidateRequest} HarborValidateRequest instance
     */
    HarborValidateRequest.create = function create(properties) {
      return new HarborValidateRequest(properties);
    };

    /**
     * Encodes the specified HarborValidateRequest message. Does not implicitly {@link polycentric.HarborValidateRequest.verify|verify} messages.
     * @function encode
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {polycentric.IHarborValidateRequest} message HarborValidateRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HarborValidateRequest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.solution != null &&
        Object.hasOwnProperty.call(message, 'solution')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.solution);
      if (
        message.challengeResponse != null &&
        Object.hasOwnProperty.call(message, 'challengeResponse')
      )
        $root.polycentric.HarborChallengeResponse.encode(
          message.challengeResponse,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified HarborValidateRequest message, length delimited. Does not implicitly {@link polycentric.HarborValidateRequest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {polycentric.IHarborValidateRequest} message HarborValidateRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    HarborValidateRequest.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a HarborValidateRequest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.HarborValidateRequest} HarborValidateRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HarborValidateRequest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.HarborValidateRequest();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.solution = reader.string();
            break;
          }
          case 2: {
            message.challengeResponse =
              $root.polycentric.HarborChallengeResponse.decode(
                reader,
                reader.uint32()
              );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a HarborValidateRequest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.HarborValidateRequest} HarborValidateRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    HarborValidateRequest.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a HarborValidateRequest message.
     * @function verify
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    HarborValidateRequest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.solution != null && message.hasOwnProperty('solution'))
        if (!$util.isString(message.solution))
          return 'solution: string expected';
      if (
        message.challengeResponse != null &&
        message.hasOwnProperty('challengeResponse')
      ) {
        let error = $root.polycentric.HarborChallengeResponse.verify(
          message.challengeResponse
        );
        if (error) return 'challengeResponse.' + error;
      }
      return null;
    };

    /**
     * Creates a HarborValidateRequest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.HarborValidateRequest} HarborValidateRequest
     */
    HarborValidateRequest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.HarborValidateRequest)
        return object;
      let message = new $root.polycentric.HarborValidateRequest();
      if (object.solution != null) message.solution = String(object.solution);
      if (object.challengeResponse != null) {
        if (typeof object.challengeResponse !== 'object')
          throw TypeError(
            '.polycentric.HarborValidateRequest.challengeResponse: object expected'
          );
        message.challengeResponse =
          $root.polycentric.HarborChallengeResponse.fromObject(
            object.challengeResponse
          );
      }
      return message;
    };

    /**
     * Creates a plain object from a HarborValidateRequest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {polycentric.HarborValidateRequest} message HarborValidateRequest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    HarborValidateRequest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.solution = '';
        object.challengeResponse = null;
      }
      if (message.solution != null && message.hasOwnProperty('solution'))
        object.solution = message.solution;
      if (
        message.challengeResponse != null &&
        message.hasOwnProperty('challengeResponse')
      )
        object.challengeResponse =
          $root.polycentric.HarborChallengeResponse.toObject(
            message.challengeResponse,
            options
          );
      return object;
    };

    /**
     * Converts this HarborValidateRequest to JSON.
     * @function toJSON
     * @memberof polycentric.HarborValidateRequest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    HarborValidateRequest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for HarborValidateRequest
     * @function getTypeUrl
     * @memberof polycentric.HarborValidateRequest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    HarborValidateRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.HarborValidateRequest';
    };

    return HarborValidateRequest;
  })();

  polycentric.StorageTypeSystemState = (function () {
    /**
     * Properties of a StorageTypeSystemState.
     * @memberof polycentric
     * @interface IStorageTypeSystemState
     * @property {Array.<string>|null} [servers] StorageTypeSystemState servers
     * @property {Array.<string>|null} [authorities] StorageTypeSystemState authorities
     * @property {Array.<polycentric.IProcess>|null} [processes] StorageTypeSystemState processes
     */

    /**
     * Constructs a new StorageTypeSystemState.
     * @memberof polycentric
     * @classdesc Represents a StorageTypeSystemState.
     * @implements IStorageTypeSystemState
     * @constructor
     * @param {polycentric.IStorageTypeSystemState=} [properties] Properties to set
     */
    function StorageTypeSystemState(properties) {
      this.servers = [];
      this.authorities = [];
      this.processes = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * StorageTypeSystemState servers.
     * @member {Array.<string>} servers
     * @memberof polycentric.StorageTypeSystemState
     * @instance
     */
    StorageTypeSystemState.prototype.servers = $util.emptyArray;

    /**
     * StorageTypeSystemState authorities.
     * @member {Array.<string>} authorities
     * @memberof polycentric.StorageTypeSystemState
     * @instance
     */
    StorageTypeSystemState.prototype.authorities = $util.emptyArray;

    /**
     * StorageTypeSystemState processes.
     * @member {Array.<polycentric.IProcess>} processes
     * @memberof polycentric.StorageTypeSystemState
     * @instance
     */
    StorageTypeSystemState.prototype.processes = $util.emptyArray;

    /**
     * Creates a new StorageTypeSystemState instance using the specified properties.
     * @function create
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {polycentric.IStorageTypeSystemState=} [properties] Properties to set
     * @returns {polycentric.StorageTypeSystemState} StorageTypeSystemState instance
     */
    StorageTypeSystemState.create = function create(properties) {
      return new StorageTypeSystemState(properties);
    };

    /**
     * Encodes the specified StorageTypeSystemState message. Does not implicitly {@link polycentric.StorageTypeSystemState.verify|verify} messages.
     * @function encode
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {polycentric.IStorageTypeSystemState} message StorageTypeSystemState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeSystemState.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.servers != null && message.servers.length)
        for (let i = 0; i < message.servers.length; ++i)
          writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.servers[i]);
      if (message.authorities != null && message.authorities.length)
        for (let i = 0; i < message.authorities.length; ++i)
          writer
            .uint32(/* id 2, wireType 2 =*/ 18)
            .string(message.authorities[i]);
      if (message.processes != null && message.processes.length)
        for (let i = 0; i < message.processes.length; ++i)
          $root.polycentric.Process.encode(
            message.processes[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified StorageTypeSystemState message, length delimited. Does not implicitly {@link polycentric.StorageTypeSystemState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {polycentric.IStorageTypeSystemState} message StorageTypeSystemState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeSystemState.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a StorageTypeSystemState message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.StorageTypeSystemState} StorageTypeSystemState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeSystemState.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.StorageTypeSystemState();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.servers && message.servers.length))
              message.servers = [];
            message.servers.push(reader.string());
            break;
          }
          case 2: {
            if (!(message.authorities && message.authorities.length))
              message.authorities = [];
            message.authorities.push(reader.string());
            break;
          }
          case 3: {
            if (!(message.processes && message.processes.length))
              message.processes = [];
            message.processes.push(
              $root.polycentric.Process.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a StorageTypeSystemState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.StorageTypeSystemState} StorageTypeSystemState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeSystemState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StorageTypeSystemState message.
     * @function verify
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StorageTypeSystemState.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.servers != null && message.hasOwnProperty('servers')) {
        if (!Array.isArray(message.servers)) return 'servers: array expected';
        for (let i = 0; i < message.servers.length; ++i)
          if (!$util.isString(message.servers[i]))
            return 'servers: string[] expected';
      }
      if (
        message.authorities != null &&
        message.hasOwnProperty('authorities')
      ) {
        if (!Array.isArray(message.authorities))
          return 'authorities: array expected';
        for (let i = 0; i < message.authorities.length; ++i)
          if (!$util.isString(message.authorities[i]))
            return 'authorities: string[] expected';
      }
      if (message.processes != null && message.hasOwnProperty('processes')) {
        if (!Array.isArray(message.processes))
          return 'processes: array expected';
        for (let i = 0; i < message.processes.length; ++i) {
          let error = $root.polycentric.Process.verify(message.processes[i]);
          if (error) return 'processes.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a StorageTypeSystemState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.StorageTypeSystemState} StorageTypeSystemState
     */
    StorageTypeSystemState.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.StorageTypeSystemState)
        return object;
      let message = new $root.polycentric.StorageTypeSystemState();
      if (object.servers) {
        if (!Array.isArray(object.servers))
          throw TypeError(
            '.polycentric.StorageTypeSystemState.servers: array expected'
          );
        message.servers = [];
        for (let i = 0; i < object.servers.length; ++i)
          message.servers[i] = String(object.servers[i]);
      }
      if (object.authorities) {
        if (!Array.isArray(object.authorities))
          throw TypeError(
            '.polycentric.StorageTypeSystemState.authorities: array expected'
          );
        message.authorities = [];
        for (let i = 0; i < object.authorities.length; ++i)
          message.authorities[i] = String(object.authorities[i]);
      }
      if (object.processes) {
        if (!Array.isArray(object.processes))
          throw TypeError(
            '.polycentric.StorageTypeSystemState.processes: array expected'
          );
        message.processes = [];
        for (let i = 0; i < object.processes.length; ++i) {
          if (typeof object.processes[i] !== 'object')
            throw TypeError(
              '.polycentric.StorageTypeSystemState.processes: object expected'
            );
          message.processes[i] = $root.polycentric.Process.fromObject(
            object.processes[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a StorageTypeSystemState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {polycentric.StorageTypeSystemState} message StorageTypeSystemState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StorageTypeSystemState.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.servers = [];
        object.authorities = [];
        object.processes = [];
      }
      if (message.servers && message.servers.length) {
        object.servers = [];
        for (let j = 0; j < message.servers.length; ++j)
          object.servers[j] = message.servers[j];
      }
      if (message.authorities && message.authorities.length) {
        object.authorities = [];
        for (let j = 0; j < message.authorities.length; ++j)
          object.authorities[j] = message.authorities[j];
      }
      if (message.processes && message.processes.length) {
        object.processes = [];
        for (let j = 0; j < message.processes.length; ++j)
          object.processes[j] = $root.polycentric.Process.toObject(
            message.processes[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this StorageTypeSystemState to JSON.
     * @function toJSON
     * @memberof polycentric.StorageTypeSystemState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StorageTypeSystemState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StorageTypeSystemState
     * @function getTypeUrl
     * @memberof polycentric.StorageTypeSystemState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StorageTypeSystemState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.StorageTypeSystemState';
    };

    return StorageTypeSystemState;
  })();

  polycentric.StorageTypeProcessSecret = (function () {
    /**
     * Properties of a StorageTypeProcessSecret.
     * @memberof polycentric
     * @interface IStorageTypeProcessSecret
     * @property {polycentric.IPrivateKey|null} [system] StorageTypeProcessSecret system
     * @property {polycentric.IProcess|null} [process] StorageTypeProcessSecret process
     */

    /**
     * Constructs a new StorageTypeProcessSecret.
     * @memberof polycentric
     * @classdesc Represents a StorageTypeProcessSecret.
     * @implements IStorageTypeProcessSecret
     * @constructor
     * @param {polycentric.IStorageTypeProcessSecret=} [properties] Properties to set
     */
    function StorageTypeProcessSecret(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * StorageTypeProcessSecret system.
     * @member {polycentric.IPrivateKey|null|undefined} system
     * @memberof polycentric.StorageTypeProcessSecret
     * @instance
     */
    StorageTypeProcessSecret.prototype.system = null;

    /**
     * StorageTypeProcessSecret process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.StorageTypeProcessSecret
     * @instance
     */
    StorageTypeProcessSecret.prototype.process = null;

    /**
     * Creates a new StorageTypeProcessSecret instance using the specified properties.
     * @function create
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {polycentric.IStorageTypeProcessSecret=} [properties] Properties to set
     * @returns {polycentric.StorageTypeProcessSecret} StorageTypeProcessSecret instance
     */
    StorageTypeProcessSecret.create = function create(properties) {
      return new StorageTypeProcessSecret(properties);
    };

    /**
     * Encodes the specified StorageTypeProcessSecret message. Does not implicitly {@link polycentric.StorageTypeProcessSecret.verify|verify} messages.
     * @function encode
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {polycentric.IStorageTypeProcessSecret} message StorageTypeProcessSecret message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeProcessSecret.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PrivateKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified StorageTypeProcessSecret message, length delimited. Does not implicitly {@link polycentric.StorageTypeProcessSecret.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {polycentric.IStorageTypeProcessSecret} message StorageTypeProcessSecret message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeProcessSecret.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a StorageTypeProcessSecret message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.StorageTypeProcessSecret} StorageTypeProcessSecret
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeProcessSecret.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.StorageTypeProcessSecret();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PrivateKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a StorageTypeProcessSecret message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.StorageTypeProcessSecret} StorageTypeProcessSecret
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeProcessSecret.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StorageTypeProcessSecret message.
     * @function verify
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StorageTypeProcessSecret.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PrivateKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      return null;
    };

    /**
     * Creates a StorageTypeProcessSecret message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.StorageTypeProcessSecret} StorageTypeProcessSecret
     */
    StorageTypeProcessSecret.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.StorageTypeProcessSecret)
        return object;
      let message = new $root.polycentric.StorageTypeProcessSecret();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError(
            '.polycentric.StorageTypeProcessSecret.system: object expected'
          );
        message.system = $root.polycentric.PrivateKey.fromObject(object.system);
      }
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError(
            '.polycentric.StorageTypeProcessSecret.process: object expected'
          );
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      return message;
    };

    /**
     * Creates a plain object from a StorageTypeProcessSecret message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {polycentric.StorageTypeProcessSecret} message StorageTypeProcessSecret
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StorageTypeProcessSecret.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.system = null;
        object.process = null;
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PrivateKey.toObject(
          message.system,
          options
        );
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      return object;
    };

    /**
     * Converts this StorageTypeProcessSecret to JSON.
     * @function toJSON
     * @memberof polycentric.StorageTypeProcessSecret
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StorageTypeProcessSecret.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StorageTypeProcessSecret
     * @function getTypeUrl
     * @memberof polycentric.StorageTypeProcessSecret
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StorageTypeProcessSecret.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.StorageTypeProcessSecret';
    };

    return StorageTypeProcessSecret;
  })();

  polycentric.QueryEngineStats = (function () {
    /**
     * Properties of a QueryEngineStats.
     * @memberof polycentric
     * @interface IQueryEngineStats
     * @property {number|Long|null} [totalEvents] QueryEngineStats totalEvents
     * @property {number|Long|null} [systemCount] QueryEngineStats systemCount
     * @property {number|Long|null} [processCount] QueryEngineStats processCount
     * @property {polycentric.QueryEngineStats.IMemoryUsage|null} [memoryUsage] QueryEngineStats memoryUsage
     */

    /**
     * Constructs a new QueryEngineStats.
     * @memberof polycentric
     * @classdesc Represents a QueryEngineStats.
     * @implements IQueryEngineStats
     * @constructor
     * @param {polycentric.IQueryEngineStats=} [properties] Properties to set
     */
    function QueryEngineStats(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryEngineStats totalEvents.
     * @member {number|Long} totalEvents
     * @memberof polycentric.QueryEngineStats
     * @instance
     */
    QueryEngineStats.prototype.totalEvents = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * QueryEngineStats systemCount.
     * @member {number|Long} systemCount
     * @memberof polycentric.QueryEngineStats
     * @instance
     */
    QueryEngineStats.prototype.systemCount = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * QueryEngineStats processCount.
     * @member {number|Long} processCount
     * @memberof polycentric.QueryEngineStats
     * @instance
     */
    QueryEngineStats.prototype.processCount = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * QueryEngineStats memoryUsage.
     * @member {polycentric.QueryEngineStats.IMemoryUsage|null|undefined} memoryUsage
     * @memberof polycentric.QueryEngineStats
     * @instance
     */
    QueryEngineStats.prototype.memoryUsage = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(QueryEngineStats.prototype, '_memoryUsage', {
      get: $util.oneOfGetter(($oneOfFields = ['memoryUsage'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new QueryEngineStats instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {polycentric.IQueryEngineStats=} [properties] Properties to set
     * @returns {polycentric.QueryEngineStats} QueryEngineStats instance
     */
    QueryEngineStats.create = function create(properties) {
      return new QueryEngineStats(properties);
    };

    /**
     * Encodes the specified QueryEngineStats message. Does not implicitly {@link polycentric.QueryEngineStats.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {polycentric.IQueryEngineStats} message QueryEngineStats message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryEngineStats.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.totalEvents != null &&
        Object.hasOwnProperty.call(message, 'totalEvents')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.totalEvents);
      if (
        message.systemCount != null &&
        Object.hasOwnProperty.call(message, 'systemCount')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.systemCount);
      if (
        message.processCount != null &&
        Object.hasOwnProperty.call(message, 'processCount')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint64(message.processCount);
      if (
        message.memoryUsage != null &&
        Object.hasOwnProperty.call(message, 'memoryUsage')
      )
        $root.polycentric.QueryEngineStats.MemoryUsage.encode(
          message.memoryUsage,
          writer.uint32(/* id 4, wireType 2 =*/ 34).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified QueryEngineStats message, length delimited. Does not implicitly {@link polycentric.QueryEngineStats.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {polycentric.IQueryEngineStats} message QueryEngineStats message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryEngineStats.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryEngineStats message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryEngineStats} QueryEngineStats
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryEngineStats.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryEngineStats();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.totalEvents = reader.uint64();
            break;
          }
          case 2: {
            message.systemCount = reader.uint64();
            break;
          }
          case 3: {
            message.processCount = reader.uint64();
            break;
          }
          case 4: {
            message.memoryUsage =
              $root.polycentric.QueryEngineStats.MemoryUsage.decode(
                reader,
                reader.uint32()
              );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryEngineStats message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryEngineStats} QueryEngineStats
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryEngineStats.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryEngineStats message.
     * @function verify
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryEngineStats.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.totalEvents != null && message.hasOwnProperty('totalEvents'))
        if (
          !$util.isInteger(message.totalEvents) &&
          !(
            message.totalEvents &&
            $util.isInteger(message.totalEvents.low) &&
            $util.isInteger(message.totalEvents.high)
          )
        )
          return 'totalEvents: integer|Long expected';
      if (message.systemCount != null && message.hasOwnProperty('systemCount'))
        if (
          !$util.isInteger(message.systemCount) &&
          !(
            message.systemCount &&
            $util.isInteger(message.systemCount.low) &&
            $util.isInteger(message.systemCount.high)
          )
        )
          return 'systemCount: integer|Long expected';
      if (
        message.processCount != null &&
        message.hasOwnProperty('processCount')
      )
        if (
          !$util.isInteger(message.processCount) &&
          !(
            message.processCount &&
            $util.isInteger(message.processCount.low) &&
            $util.isInteger(message.processCount.high)
          )
        )
          return 'processCount: integer|Long expected';
      if (
        message.memoryUsage != null &&
        message.hasOwnProperty('memoryUsage')
      ) {
        properties._memoryUsage = 1;
        {
          let error = $root.polycentric.QueryEngineStats.MemoryUsage.verify(
            message.memoryUsage
          );
          if (error) return 'memoryUsage.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a QueryEngineStats message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryEngineStats} QueryEngineStats
     */
    QueryEngineStats.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryEngineStats) return object;
      let message = new $root.polycentric.QueryEngineStats();
      if (object.totalEvents != null)
        if ($util.Long)
          (message.totalEvents = $util.Long.fromValue(
            object.totalEvents
          )).unsigned = true;
        else if (typeof object.totalEvents === 'string')
          message.totalEvents = parseInt(object.totalEvents, 10);
        else if (typeof object.totalEvents === 'number')
          message.totalEvents = object.totalEvents;
        else if (typeof object.totalEvents === 'object')
          message.totalEvents = new $util.LongBits(
            object.totalEvents.low >>> 0,
            object.totalEvents.high >>> 0
          ).toNumber(true);
      if (object.systemCount != null)
        if ($util.Long)
          (message.systemCount = $util.Long.fromValue(
            object.systemCount
          )).unsigned = true;
        else if (typeof object.systemCount === 'string')
          message.systemCount = parseInt(object.systemCount, 10);
        else if (typeof object.systemCount === 'number')
          message.systemCount = object.systemCount;
        else if (typeof object.systemCount === 'object')
          message.systemCount = new $util.LongBits(
            object.systemCount.low >>> 0,
            object.systemCount.high >>> 0
          ).toNumber(true);
      if (object.processCount != null)
        if ($util.Long)
          (message.processCount = $util.Long.fromValue(
            object.processCount
          )).unsigned = true;
        else if (typeof object.processCount === 'string')
          message.processCount = parseInt(object.processCount, 10);
        else if (typeof object.processCount === 'number')
          message.processCount = object.processCount;
        else if (typeof object.processCount === 'object')
          message.processCount = new $util.LongBits(
            object.processCount.low >>> 0,
            object.processCount.high >>> 0
          ).toNumber(true);
      if (object.memoryUsage != null) {
        if (typeof object.memoryUsage !== 'object')
          throw TypeError(
            '.polycentric.QueryEngineStats.memoryUsage: object expected'
          );
        message.memoryUsage =
          $root.polycentric.QueryEngineStats.MemoryUsage.fromObject(
            object.memoryUsage
          );
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryEngineStats message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {polycentric.QueryEngineStats} message QueryEngineStats
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryEngineStats.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.totalEvents =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.totalEvents = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.systemCount =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.systemCount = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.processCount =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.processCount = options.longs === String ? '0' : 0;
      }
      if (message.totalEvents != null && message.hasOwnProperty('totalEvents'))
        if (typeof message.totalEvents === 'number')
          object.totalEvents =
            options.longs === String
              ? String(message.totalEvents)
              : message.totalEvents;
        else
          object.totalEvents =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.totalEvents)
              : options.longs === Number
                ? new $util.LongBits(
                    message.totalEvents.low >>> 0,
                    message.totalEvents.high >>> 0
                  ).toNumber(true)
                : message.totalEvents;
      if (message.systemCount != null && message.hasOwnProperty('systemCount'))
        if (typeof message.systemCount === 'number')
          object.systemCount =
            options.longs === String
              ? String(message.systemCount)
              : message.systemCount;
        else
          object.systemCount =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.systemCount)
              : options.longs === Number
                ? new $util.LongBits(
                    message.systemCount.low >>> 0,
                    message.systemCount.high >>> 0
                  ).toNumber(true)
                : message.systemCount;
      if (
        message.processCount != null &&
        message.hasOwnProperty('processCount')
      )
        if (typeof message.processCount === 'number')
          object.processCount =
            options.longs === String
              ? String(message.processCount)
              : message.processCount;
        else
          object.processCount =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.processCount)
              : options.longs === Number
                ? new $util.LongBits(
                    message.processCount.low >>> 0,
                    message.processCount.high >>> 0
                  ).toNumber(true)
                : message.processCount;
      if (
        message.memoryUsage != null &&
        message.hasOwnProperty('memoryUsage')
      ) {
        object.memoryUsage =
          $root.polycentric.QueryEngineStats.MemoryUsage.toObject(
            message.memoryUsage,
            options
          );
        if (options.oneofs) object._memoryUsage = 'memoryUsage';
      }
      return object;
    };

    /**
     * Converts this QueryEngineStats to JSON.
     * @function toJSON
     * @memberof polycentric.QueryEngineStats
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryEngineStats.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryEngineStats
     * @function getTypeUrl
     * @memberof polycentric.QueryEngineStats
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryEngineStats.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryEngineStats';
    };

    QueryEngineStats.MemoryUsage = (function () {
      /**
       * Properties of a MemoryUsage.
       * @memberof polycentric.QueryEngineStats
       * @interface IMemoryUsage
       * @property {number|Long|null} [bytes] MemoryUsage bytes
       */

      /**
       * Constructs a new MemoryUsage.
       * @memberof polycentric.QueryEngineStats
       * @classdesc Represents a MemoryUsage.
       * @implements IMemoryUsage
       * @constructor
       * @param {polycentric.QueryEngineStats.IMemoryUsage=} [properties] Properties to set
       */
      function MemoryUsage(properties) {
        if (properties)
          for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null)
              this[keys[i]] = properties[keys[i]];
      }

      /**
       * MemoryUsage bytes.
       * @member {number|Long} bytes
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @instance
       */
      MemoryUsage.prototype.bytes = $util.Long
        ? $util.Long.fromBits(0, 0, true)
        : 0;

      /**
       * Creates a new MemoryUsage instance using the specified properties.
       * @function create
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {polycentric.QueryEngineStats.IMemoryUsage=} [properties] Properties to set
       * @returns {polycentric.QueryEngineStats.MemoryUsage} MemoryUsage instance
       */
      MemoryUsage.create = function create(properties) {
        return new MemoryUsage(properties);
      };

      /**
       * Encodes the specified MemoryUsage message. Does not implicitly {@link polycentric.QueryEngineStats.MemoryUsage.verify|verify} messages.
       * @function encode
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {polycentric.QueryEngineStats.IMemoryUsage} message MemoryUsage message or plain object to encode
       * @param {$protobuf.Writer} [writer] Writer to encode to
       * @returns {$protobuf.Writer} Writer
       */
      MemoryUsage.encode = function encode(message, writer) {
        if (!writer) writer = $Writer.create();
        if (
          message.bytes != null &&
          Object.hasOwnProperty.call(message, 'bytes')
        )
          writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.bytes);
        return writer;
      };

      /**
       * Encodes the specified MemoryUsage message, length delimited. Does not implicitly {@link polycentric.QueryEngineStats.MemoryUsage.verify|verify} messages.
       * @function encodeDelimited
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {polycentric.QueryEngineStats.IMemoryUsage} message MemoryUsage message or plain object to encode
       * @param {$protobuf.Writer} [writer] Writer to encode to
       * @returns {$protobuf.Writer} Writer
       */
      MemoryUsage.encodeDelimited = function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
      };

      /**
       * Decodes a MemoryUsage message from the specified reader or buffer.
       * @function decode
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
       * @param {number} [length] Message length if known beforehand
       * @returns {polycentric.QueryEngineStats.MemoryUsage} MemoryUsage
       * @throws {Error} If the payload is not a reader or valid buffer
       * @throws {$protobuf.util.ProtocolError} If required fields are missing
       */
      MemoryUsage.decode = function decode(reader, length, error) {
        if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
        let end = length === undefined ? reader.len : reader.pos + length,
          message = new $root.polycentric.QueryEngineStats.MemoryUsage();
        while (reader.pos < end) {
          let tag = reader.uint32();
          if (tag === error) break;
          switch (tag >>> 3) {
            case 1: {
              message.bytes = reader.uint64();
              break;
            }
            default:
              reader.skipType(tag & 7);
              break;
          }
        }
        return message;
      };

      /**
       * Decodes a MemoryUsage message from the specified reader or buffer, length delimited.
       * @function decodeDelimited
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
       * @returns {polycentric.QueryEngineStats.MemoryUsage} MemoryUsage
       * @throws {Error} If the payload is not a reader or valid buffer
       * @throws {$protobuf.util.ProtocolError} If required fields are missing
       */
      MemoryUsage.decodeDelimited = function decodeDelimited(reader) {
        if (!(reader instanceof $Reader)) reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };

      /**
       * Verifies a MemoryUsage message.
       * @function verify
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {Object.<string,*>} message Plain object to verify
       * @returns {string|null} `null` if valid, otherwise the reason why it is not
       */
      MemoryUsage.verify = function verify(message) {
        if (typeof message !== 'object' || message === null)
          return 'object expected';
        if (message.bytes != null && message.hasOwnProperty('bytes'))
          if (
            !$util.isInteger(message.bytes) &&
            !(
              message.bytes &&
              $util.isInteger(message.bytes.low) &&
              $util.isInteger(message.bytes.high)
            )
          )
            return 'bytes: integer|Long expected';
        return null;
      };

      /**
       * Creates a MemoryUsage message from a plain object. Also converts values to their respective internal types.
       * @function fromObject
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {Object.<string,*>} object Plain object
       * @returns {polycentric.QueryEngineStats.MemoryUsage} MemoryUsage
       */
      MemoryUsage.fromObject = function fromObject(object) {
        if (object instanceof $root.polycentric.QueryEngineStats.MemoryUsage)
          return object;
        let message = new $root.polycentric.QueryEngineStats.MemoryUsage();
        if (object.bytes != null)
          if ($util.Long)
            (message.bytes = $util.Long.fromValue(object.bytes)).unsigned =
              true;
          else if (typeof object.bytes === 'string')
            message.bytes = parseInt(object.bytes, 10);
          else if (typeof object.bytes === 'number')
            message.bytes = object.bytes;
          else if (typeof object.bytes === 'object')
            message.bytes = new $util.LongBits(
              object.bytes.low >>> 0,
              object.bytes.high >>> 0
            ).toNumber(true);
        return message;
      };

      /**
       * Creates a plain object from a MemoryUsage message. Also converts values to other types if specified.
       * @function toObject
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {polycentric.QueryEngineStats.MemoryUsage} message MemoryUsage
       * @param {$protobuf.IConversionOptions} [options] Conversion options
       * @returns {Object.<string,*>} Plain object
       */
      MemoryUsage.toObject = function toObject(message, options) {
        if (!options) options = {};
        let object = {};
        if (options.defaults)
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.bytes =
              options.longs === String
                ? long.toString()
                : options.longs === Number
                  ? long.toNumber()
                  : long;
          } else object.bytes = options.longs === String ? '0' : 0;
        if (message.bytes != null && message.hasOwnProperty('bytes'))
          if (typeof message.bytes === 'number')
            object.bytes =
              options.longs === String ? String(message.bytes) : message.bytes;
          else
            object.bytes =
              options.longs === String
                ? $util.Long.prototype.toString.call(message.bytes)
                : options.longs === Number
                  ? new $util.LongBits(
                      message.bytes.low >>> 0,
                      message.bytes.high >>> 0
                    ).toNumber(true)
                  : message.bytes;
        return object;
      };

      /**
       * Converts this MemoryUsage to JSON.
       * @function toJSON
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @instance
       * @returns {Object.<string,*>} JSON object
       */
      MemoryUsage.prototype.toJSON = function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
      };

      /**
       * Gets the default type url for MemoryUsage
       * @function getTypeUrl
       * @memberof polycentric.QueryEngineStats.MemoryUsage
       * @static
       * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
       * @returns {string} The default type url
       */
      MemoryUsage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
          typeUrlPrefix = 'type.googleapis.com';
        }
        return typeUrlPrefix + '/polycentric.QueryEngineStats.MemoryUsage';
      };

      return MemoryUsage;
    })();

    return QueryEngineStats;
  })();

  polycentric.EventCreationData = (function () {
    /**
     * Properties of an EventCreationData.
     * @memberof polycentric
     * @interface IEventCreationData
     * @property {polycentric.IPublicKey|null} [system] EventCreationData system
     * @property {polycentric.IProcess|null} [process] EventCreationData process
     * @property {polycentric.ContentType|null} [contentType] EventCreationData contentType
     * @property {Uint8Array|null} [content] EventCreationData content
     * @property {polycentric.IIndices|null} [indices] EventCreationData indices
     * @property {polycentric.ILWWElementSet|null} [lwwElementSet] EventCreationData lwwElementSet
     * @property {polycentric.ILWWElement|null} [lwwElement] EventCreationData lwwElement
     * @property {Array.<polycentric.IReference>|null} [references] EventCreationData references
     * @property {number|Long|null} [unixMilliseconds] EventCreationData unixMilliseconds
     * @property {number|Long|null} [logicalClock] EventCreationData logicalClock
     */

    /**
     * Constructs a new EventCreationData.
     * @memberof polycentric
     * @classdesc Represents an EventCreationData.
     * @implements IEventCreationData
     * @constructor
     * @param {polycentric.IEventCreationData=} [properties] Properties to set
     */
    function EventCreationData(properties) {
      this.references = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * EventCreationData system.
     * @member {polycentric.IPublicKey|null|undefined} system
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.system = null;

    /**
     * EventCreationData process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.process = null;

    /**
     * EventCreationData contentType.
     * @member {polycentric.ContentType} contentType
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.contentType = 0;

    /**
     * EventCreationData content.
     * @member {Uint8Array} content
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.content = $util.newBuffer([]);

    /**
     * EventCreationData indices.
     * @member {polycentric.IIndices|null|undefined} indices
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.indices = null;

    /**
     * EventCreationData lwwElementSet.
     * @member {polycentric.ILWWElementSet|null|undefined} lwwElementSet
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.lwwElementSet = null;

    /**
     * EventCreationData lwwElement.
     * @member {polycentric.ILWWElement|null|undefined} lwwElement
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.lwwElement = null;

    /**
     * EventCreationData references.
     * @member {Array.<polycentric.IReference>} references
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.references = $util.emptyArray;

    /**
     * EventCreationData unixMilliseconds.
     * @member {number|Long|null|undefined} unixMilliseconds
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.unixMilliseconds = null;

    /**
     * EventCreationData logicalClock.
     * @member {number|Long|null|undefined} logicalClock
     * @memberof polycentric.EventCreationData
     * @instance
     */
    EventCreationData.prototype.logicalClock = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(EventCreationData.prototype, '_indices', {
      get: $util.oneOfGetter(($oneOfFields = ['indices'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(EventCreationData.prototype, '_lwwElementSet', {
      get: $util.oneOfGetter(($oneOfFields = ['lwwElementSet'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(EventCreationData.prototype, '_lwwElement', {
      get: $util.oneOfGetter(($oneOfFields = ['lwwElement'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(EventCreationData.prototype, '_unixMilliseconds', {
      get: $util.oneOfGetter(($oneOfFields = ['unixMilliseconds'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(EventCreationData.prototype, '_logicalClock', {
      get: $util.oneOfGetter(($oneOfFields = ['logicalClock'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new EventCreationData instance using the specified properties.
     * @function create
     * @memberof polycentric.EventCreationData
     * @static
     * @param {polycentric.IEventCreationData=} [properties] Properties to set
     * @returns {polycentric.EventCreationData} EventCreationData instance
     */
    EventCreationData.create = function create(properties) {
      return new EventCreationData(properties);
    };

    /**
     * Encodes the specified EventCreationData message. Does not implicitly {@link polycentric.EventCreationData.verify|verify} messages.
     * @function encode
     * @memberof polycentric.EventCreationData
     * @static
     * @param {polycentric.IEventCreationData} message EventCreationData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EventCreationData.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PublicKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (
        message.contentType != null &&
        Object.hasOwnProperty.call(message, 'contentType')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).int32(message.contentType);
      if (
        message.content != null &&
        Object.hasOwnProperty.call(message, 'content')
      )
        writer.uint32(/* id 4, wireType 2 =*/ 34).bytes(message.content);
      if (
        message.indices != null &&
        Object.hasOwnProperty.call(message, 'indices')
      )
        $root.polycentric.Indices.encode(
          message.indices,
          writer.uint32(/* id 5, wireType 2 =*/ 42).fork()
        ).ldelim();
      if (
        message.lwwElementSet != null &&
        Object.hasOwnProperty.call(message, 'lwwElementSet')
      )
        $root.polycentric.LWWElementSet.encode(
          message.lwwElementSet,
          writer.uint32(/* id 6, wireType 2 =*/ 50).fork()
        ).ldelim();
      if (
        message.lwwElement != null &&
        Object.hasOwnProperty.call(message, 'lwwElement')
      )
        $root.polycentric.LWWElement.encode(
          message.lwwElement,
          writer.uint32(/* id 7, wireType 2 =*/ 58).fork()
        ).ldelim();
      if (message.references != null && message.references.length)
        for (let i = 0; i < message.references.length; ++i)
          $root.polycentric.Reference.encode(
            message.references[i],
            writer.uint32(/* id 8, wireType 2 =*/ 66).fork()
          ).ldelim();
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 9, wireType 0 =*/ 72)
          .uint64(message.unixMilliseconds);
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 10, wireType 0 =*/ 80).uint64(message.logicalClock);
      return writer;
    };

    /**
     * Encodes the specified EventCreationData message, length delimited. Does not implicitly {@link polycentric.EventCreationData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.EventCreationData
     * @static
     * @param {polycentric.IEventCreationData} message EventCreationData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EventCreationData.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an EventCreationData message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.EventCreationData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.EventCreationData} EventCreationData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EventCreationData.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.EventCreationData();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            message.contentType = reader.int32();
            break;
          }
          case 4: {
            message.content = reader.bytes();
            break;
          }
          case 5: {
            message.indices = $root.polycentric.Indices.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 6: {
            message.lwwElementSet = $root.polycentric.LWWElementSet.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 7: {
            message.lwwElement = $root.polycentric.LWWElement.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 8: {
            if (!(message.references && message.references.length))
              message.references = [];
            message.references.push(
              $root.polycentric.Reference.decode(reader, reader.uint32())
            );
            break;
          }
          case 9: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          case 10: {
            message.logicalClock = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an EventCreationData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.EventCreationData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.EventCreationData} EventCreationData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EventCreationData.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an EventCreationData message.
     * @function verify
     * @memberof polycentric.EventCreationData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    EventCreationData.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PublicKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        switch (message.contentType) {
          default:
            return 'contentType: enum value expected';
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
          case 6:
          case 7:
          case 8:
          case 9:
          case 10:
          case 11:
          case 12:
          case 13:
          case 14:
          case 15:
          case 16:
          case 17:
          case 18:
            break;
        }
      if (message.content != null && message.hasOwnProperty('content'))
        if (
          !(
            (message.content && typeof message.content.length === 'number') ||
            $util.isString(message.content)
          )
        )
          return 'content: buffer expected';
      if (message.indices != null && message.hasOwnProperty('indices')) {
        properties._indices = 1;
        {
          let error = $root.polycentric.Indices.verify(message.indices);
          if (error) return 'indices.' + error;
        }
      }
      if (
        message.lwwElementSet != null &&
        message.hasOwnProperty('lwwElementSet')
      ) {
        properties._lwwElementSet = 1;
        {
          let error = $root.polycentric.LWWElementSet.verify(
            message.lwwElementSet
          );
          if (error) return 'lwwElementSet.' + error;
        }
      }
      if (message.lwwElement != null && message.hasOwnProperty('lwwElement')) {
        properties._lwwElement = 1;
        {
          let error = $root.polycentric.LWWElement.verify(message.lwwElement);
          if (error) return 'lwwElement.' + error;
        }
      }
      if (message.references != null && message.hasOwnProperty('references')) {
        if (!Array.isArray(message.references))
          return 'references: array expected';
        for (let i = 0; i < message.references.length; ++i) {
          let error = $root.polycentric.Reference.verify(message.references[i]);
          if (error) return 'references.' + error;
        }
      }
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      ) {
        properties._unixMilliseconds = 1;
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      ) {
        properties._logicalClock = 1;
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      }
      return null;
    };

    /**
     * Creates an EventCreationData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.EventCreationData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.EventCreationData} EventCreationData
     */
    EventCreationData.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.EventCreationData) return object;
      let message = new $root.polycentric.EventCreationData();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError(
            '.polycentric.EventCreationData.system: object expected'
          );
        message.system = $root.polycentric.PublicKey.fromObject(object.system);
      }
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError(
            '.polycentric.EventCreationData.process: object expected'
          );
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      switch (object.contentType) {
        default:
          if (typeof object.contentType === 'number') {
            message.contentType = object.contentType;
            break;
          }
          break;
        case 'UNKNOWN':
        case 0:
          message.contentType = 0;
          break;
        case 'DELETE':
        case 1:
          message.contentType = 1;
          break;
        case 'SYSTEM_PROCESSES':
        case 2:
          message.contentType = 2;
          break;
        case 'POST':
        case 3:
          message.contentType = 3;
          break;
        case 'FOLLOW':
        case 4:
          message.contentType = 4;
          break;
        case 'USERNAME':
        case 5:
          message.contentType = 5;
          break;
        case 'DESCRIPTION':
        case 6:
          message.contentType = 6;
          break;
        case 'BLOB_META':
        case 7:
          message.contentType = 7;
          break;
        case 'BLOB_SECTION':
        case 8:
          message.contentType = 8;
          break;
        case 'AVATAR':
        case 9:
          message.contentType = 9;
          break;
        case 'SERVER':
        case 10:
          message.contentType = 10;
          break;
        case 'VOUCH':
        case 11:
          message.contentType = 11;
          break;
        case 'CLAIM':
        case 12:
          message.contentType = 12;
          break;
        case 'BANNER':
        case 13:
          message.contentType = 13;
          break;
        case 'OPINION':
        case 14:
          message.contentType = 14;
          break;
        case 'STORE':
        case 15:
          message.contentType = 15;
          break;
        case 'AUTHORITY':
        case 16:
          message.contentType = 16;
          break;
        case 'JOIN_TOPIC':
        case 17:
          message.contentType = 17;
          break;
        case 'BLOCK':
        case 18:
          message.contentType = 18;
          break;
      }
      if (object.content != null)
        if (typeof object.content === 'string')
          $util.base64.decode(
            object.content,
            (message.content = $util.newBuffer(
              $util.base64.length(object.content)
            )),
            0
          );
        else if (object.content.length >= 0) message.content = object.content;
      if (object.indices != null) {
        if (typeof object.indices !== 'object')
          throw TypeError(
            '.polycentric.EventCreationData.indices: object expected'
          );
        message.indices = $root.polycentric.Indices.fromObject(object.indices);
      }
      if (object.lwwElementSet != null) {
        if (typeof object.lwwElementSet !== 'object')
          throw TypeError(
            '.polycentric.EventCreationData.lwwElementSet: object expected'
          );
        message.lwwElementSet = $root.polycentric.LWWElementSet.fromObject(
          object.lwwElementSet
        );
      }
      if (object.lwwElement != null) {
        if (typeof object.lwwElement !== 'object')
          throw TypeError(
            '.polycentric.EventCreationData.lwwElement: object expected'
          );
        message.lwwElement = $root.polycentric.LWWElement.fromObject(
          object.lwwElement
        );
      }
      if (object.references) {
        if (!Array.isArray(object.references))
          throw TypeError(
            '.polycentric.EventCreationData.references: array expected'
          );
        message.references = [];
        for (let i = 0; i < object.references.length; ++i) {
          if (typeof object.references[i] !== 'object')
            throw TypeError(
              '.polycentric.EventCreationData.references: object expected'
            );
          message.references[i] = $root.polycentric.Reference.fromObject(
            object.references[i]
          );
        }
      }
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from an EventCreationData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.EventCreationData
     * @static
     * @param {polycentric.EventCreationData} message EventCreationData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    EventCreationData.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.references = [];
      if (options.defaults) {
        object.system = null;
        object.process = null;
        object.contentType = options.enums === String ? 'UNKNOWN' : 0;
        if (options.bytes === String) object.content = '';
        else {
          object.content = [];
          if (options.bytes !== Array)
            object.content = $util.newBuffer(object.content);
        }
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PublicKey.toObject(
          message.system,
          options
        );
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        object.contentType =
          options.enums === String
            ? $root.polycentric.ContentType[message.contentType] === undefined
              ? message.contentType
              : $root.polycentric.ContentType[message.contentType]
            : message.contentType;
      if (message.content != null && message.hasOwnProperty('content'))
        object.content =
          options.bytes === String
            ? $util.base64.encode(message.content, 0, message.content.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.content)
              : message.content;
      if (message.indices != null && message.hasOwnProperty('indices')) {
        object.indices = $root.polycentric.Indices.toObject(
          message.indices,
          options
        );
        if (options.oneofs) object._indices = 'indices';
      }
      if (
        message.lwwElementSet != null &&
        message.hasOwnProperty('lwwElementSet')
      ) {
        object.lwwElementSet = $root.polycentric.LWWElementSet.toObject(
          message.lwwElementSet,
          options
        );
        if (options.oneofs) object._lwwElementSet = 'lwwElementSet';
      }
      if (message.lwwElement != null && message.hasOwnProperty('lwwElement')) {
        object.lwwElement = $root.polycentric.LWWElement.toObject(
          message.lwwElement,
          options
        );
        if (options.oneofs) object._lwwElement = 'lwwElement';
      }
      if (message.references && message.references.length) {
        object.references = [];
        for (let j = 0; j < message.references.length; ++j)
          object.references[j] = $root.polycentric.Reference.toObject(
            message.references[j],
            options
          );
      }
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      ) {
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
        if (options.oneofs) object._unixMilliseconds = 'unixMilliseconds';
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      ) {
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
        if (options.oneofs) object._logicalClock = 'logicalClock';
      }
      return object;
    };

    /**
     * Converts this EventCreationData to JSON.
     * @function toJSON
     * @memberof polycentric.EventCreationData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    EventCreationData.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for EventCreationData
     * @function getTypeUrl
     * @memberof polycentric.EventCreationData
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    EventCreationData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.EventCreationData';
    };

    return EventCreationData;
  })();

  polycentric.Event = (function () {
    /**
     * Properties of an Event.
     * @memberof polycentric
     * @interface IEvent
     * @property {polycentric.IPublicKey|null} [system] Event system
     * @property {polycentric.IProcess|null} [process] Event process
     * @property {number|Long|null} [logicalClock] Event logicalClock
     * @property {polycentric.ContentType|null} [contentType] Event contentType
     * @property {Uint8Array|null} [content] Event content
     * @property {polycentric.IVectorClock|null} [vectorClock] Event vectorClock
     * @property {polycentric.IIndices|null} [indices] Event indices
     * @property {polycentric.ILWWElementSet|null} [lwwElementSet] Event lwwElementSet
     * @property {polycentric.ILWWElement|null} [lwwElement] Event lwwElement
     * @property {Array.<polycentric.IReference>|null} [references] Event references
     * @property {number|Long|null} [unixMilliseconds] Event unixMilliseconds
     */

    /**
     * Constructs a new Event.
     * @memberof polycentric
     * @classdesc Represents an Event.
     * @implements IEvent
     * @constructor
     * @param {polycentric.IEvent=} [properties] Properties to set
     */
    function Event(properties) {
      this.references = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Event system.
     * @member {polycentric.IPublicKey|null|undefined} system
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.system = null;

    /**
     * Event process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.process = null;

    /**
     * Event logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Event contentType.
     * @member {polycentric.ContentType} contentType
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.contentType = 0;

    /**
     * Event content.
     * @member {Uint8Array} content
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.content = $util.newBuffer([]);

    /**
     * Event vectorClock.
     * @member {polycentric.IVectorClock|null|undefined} vectorClock
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.vectorClock = null;

    /**
     * Event indices.
     * @member {polycentric.IIndices|null|undefined} indices
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.indices = null;

    /**
     * Event lwwElementSet.
     * @member {polycentric.ILWWElementSet|null|undefined} lwwElementSet
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.lwwElementSet = null;

    /**
     * Event lwwElement.
     * @member {polycentric.ILWWElement|null|undefined} lwwElement
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.lwwElement = null;

    /**
     * Event references.
     * @member {Array.<polycentric.IReference>} references
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.references = $util.emptyArray;

    /**
     * Event unixMilliseconds.
     * @member {number|Long|null|undefined} unixMilliseconds
     * @memberof polycentric.Event
     * @instance
     */
    Event.prototype.unixMilliseconds = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(Event.prototype, '_lwwElementSet', {
      get: $util.oneOfGetter(($oneOfFields = ['lwwElementSet'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(Event.prototype, '_lwwElement', {
      get: $util.oneOfGetter(($oneOfFields = ['lwwElement'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(Event.prototype, '_unixMilliseconds', {
      get: $util.oneOfGetter(($oneOfFields = ['unixMilliseconds'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new Event instance using the specified properties.
     * @function create
     * @memberof polycentric.Event
     * @static
     * @param {polycentric.IEvent=} [properties] Properties to set
     * @returns {polycentric.Event} Event instance
     */
    Event.create = function create(properties) {
      return new Event(properties);
    };

    /**
     * Encodes the specified Event message. Does not implicitly {@link polycentric.Event.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Event
     * @static
     * @param {polycentric.IEvent} message Event message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Event.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PublicKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint64(message.logicalClock);
      if (
        message.contentType != null &&
        Object.hasOwnProperty.call(message, 'contentType')
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.contentType);
      if (
        message.content != null &&
        Object.hasOwnProperty.call(message, 'content')
      )
        writer.uint32(/* id 5, wireType 2 =*/ 42).bytes(message.content);
      if (
        message.vectorClock != null &&
        Object.hasOwnProperty.call(message, 'vectorClock')
      )
        $root.polycentric.VectorClock.encode(
          message.vectorClock,
          writer.uint32(/* id 6, wireType 2 =*/ 50).fork()
        ).ldelim();
      if (
        message.indices != null &&
        Object.hasOwnProperty.call(message, 'indices')
      )
        $root.polycentric.Indices.encode(
          message.indices,
          writer.uint32(/* id 7, wireType 2 =*/ 58).fork()
        ).ldelim();
      if (
        message.lwwElementSet != null &&
        Object.hasOwnProperty.call(message, 'lwwElementSet')
      )
        $root.polycentric.LWWElementSet.encode(
          message.lwwElementSet,
          writer.uint32(/* id 8, wireType 2 =*/ 66).fork()
        ).ldelim();
      if (
        message.lwwElement != null &&
        Object.hasOwnProperty.call(message, 'lwwElement')
      )
        $root.polycentric.LWWElement.encode(
          message.lwwElement,
          writer.uint32(/* id 9, wireType 2 =*/ 74).fork()
        ).ldelim();
      if (message.references != null && message.references.length)
        for (let i = 0; i < message.references.length; ++i)
          $root.polycentric.Reference.encode(
            message.references[i],
            writer.uint32(/* id 10, wireType 2 =*/ 82).fork()
          ).ldelim();
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 11, wireType 0 =*/ 88)
          .uint64(message.unixMilliseconds);
      return writer;
    };

    /**
     * Encodes the specified Event message, length delimited. Does not implicitly {@link polycentric.Event.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Event
     * @static
     * @param {polycentric.IEvent} message Event message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Event.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an Event message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Event
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Event} Event
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Event.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Event();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            message.logicalClock = reader.uint64();
            break;
          }
          case 4: {
            message.contentType = reader.int32();
            break;
          }
          case 5: {
            message.content = reader.bytes();
            break;
          }
          case 6: {
            message.vectorClock = $root.polycentric.VectorClock.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 7: {
            message.indices = $root.polycentric.Indices.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 8: {
            message.lwwElementSet = $root.polycentric.LWWElementSet.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 9: {
            message.lwwElement = $root.polycentric.LWWElement.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 10: {
            if (!(message.references && message.references.length))
              message.references = [];
            message.references.push(
              $root.polycentric.Reference.decode(reader, reader.uint32())
            );
            break;
          }
          case 11: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an Event message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Event
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Event} Event
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Event.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an Event message.
     * @function verify
     * @memberof polycentric.Event
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Event.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PublicKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        switch (message.contentType) {
          default:
            return 'contentType: enum value expected';
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
          case 6:
          case 7:
          case 8:
          case 9:
          case 10:
          case 11:
          case 12:
          case 13:
          case 14:
          case 15:
          case 16:
          case 17:
          case 18:
            break;
        }
      if (message.content != null && message.hasOwnProperty('content'))
        if (
          !(
            (message.content && typeof message.content.length === 'number') ||
            $util.isString(message.content)
          )
        )
          return 'content: buffer expected';
      if (
        message.vectorClock != null &&
        message.hasOwnProperty('vectorClock')
      ) {
        let error = $root.polycentric.VectorClock.verify(message.vectorClock);
        if (error) return 'vectorClock.' + error;
      }
      if (message.indices != null && message.hasOwnProperty('indices')) {
        let error = $root.polycentric.Indices.verify(message.indices);
        if (error) return 'indices.' + error;
      }
      if (
        message.lwwElementSet != null &&
        message.hasOwnProperty('lwwElementSet')
      ) {
        properties._lwwElementSet = 1;
        {
          let error = $root.polycentric.LWWElementSet.verify(
            message.lwwElementSet
          );
          if (error) return 'lwwElementSet.' + error;
        }
      }
      if (message.lwwElement != null && message.hasOwnProperty('lwwElement')) {
        properties._lwwElement = 1;
        {
          let error = $root.polycentric.LWWElement.verify(message.lwwElement);
          if (error) return 'lwwElement.' + error;
        }
      }
      if (message.references != null && message.hasOwnProperty('references')) {
        if (!Array.isArray(message.references))
          return 'references: array expected';
        for (let i = 0; i < message.references.length; ++i) {
          let error = $root.polycentric.Reference.verify(message.references[i]);
          if (error) return 'references.' + error;
        }
      }
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      ) {
        properties._unixMilliseconds = 1;
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      }
      return null;
    };

    /**
     * Creates an Event message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Event
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Event} Event
     */
    Event.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Event) return object;
      let message = new $root.polycentric.Event();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError('.polycentric.Event.system: object expected');
        message.system = $root.polycentric.PublicKey.fromObject(object.system);
      }
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError('.polycentric.Event.process: object expected');
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      switch (object.contentType) {
        default:
          if (typeof object.contentType === 'number') {
            message.contentType = object.contentType;
            break;
          }
          break;
        case 'UNKNOWN':
        case 0:
          message.contentType = 0;
          break;
        case 'DELETE':
        case 1:
          message.contentType = 1;
          break;
        case 'SYSTEM_PROCESSES':
        case 2:
          message.contentType = 2;
          break;
        case 'POST':
        case 3:
          message.contentType = 3;
          break;
        case 'FOLLOW':
        case 4:
          message.contentType = 4;
          break;
        case 'USERNAME':
        case 5:
          message.contentType = 5;
          break;
        case 'DESCRIPTION':
        case 6:
          message.contentType = 6;
          break;
        case 'BLOB_META':
        case 7:
          message.contentType = 7;
          break;
        case 'BLOB_SECTION':
        case 8:
          message.contentType = 8;
          break;
        case 'AVATAR':
        case 9:
          message.contentType = 9;
          break;
        case 'SERVER':
        case 10:
          message.contentType = 10;
          break;
        case 'VOUCH':
        case 11:
          message.contentType = 11;
          break;
        case 'CLAIM':
        case 12:
          message.contentType = 12;
          break;
        case 'BANNER':
        case 13:
          message.contentType = 13;
          break;
        case 'OPINION':
        case 14:
          message.contentType = 14;
          break;
        case 'STORE':
        case 15:
          message.contentType = 15;
          break;
        case 'AUTHORITY':
        case 16:
          message.contentType = 16;
          break;
        case 'JOIN_TOPIC':
        case 17:
          message.contentType = 17;
          break;
        case 'BLOCK':
        case 18:
          message.contentType = 18;
          break;
      }
      if (object.content != null)
        if (typeof object.content === 'string')
          $util.base64.decode(
            object.content,
            (message.content = $util.newBuffer(
              $util.base64.length(object.content)
            )),
            0
          );
        else if (object.content.length >= 0) message.content = object.content;
      if (object.vectorClock != null) {
        if (typeof object.vectorClock !== 'object')
          throw TypeError('.polycentric.Event.vectorClock: object expected');
        message.vectorClock = $root.polycentric.VectorClock.fromObject(
          object.vectorClock
        );
      }
      if (object.indices != null) {
        if (typeof object.indices !== 'object')
          throw TypeError('.polycentric.Event.indices: object expected');
        message.indices = $root.polycentric.Indices.fromObject(object.indices);
      }
      if (object.lwwElementSet != null) {
        if (typeof object.lwwElementSet !== 'object')
          throw TypeError('.polycentric.Event.lwwElementSet: object expected');
        message.lwwElementSet = $root.polycentric.LWWElementSet.fromObject(
          object.lwwElementSet
        );
      }
      if (object.lwwElement != null) {
        if (typeof object.lwwElement !== 'object')
          throw TypeError('.polycentric.Event.lwwElement: object expected');
        message.lwwElement = $root.polycentric.LWWElement.fromObject(
          object.lwwElement
        );
      }
      if (object.references) {
        if (!Array.isArray(object.references))
          throw TypeError('.polycentric.Event.references: array expected');
        message.references = [];
        for (let i = 0; i < object.references.length; ++i) {
          if (typeof object.references[i] !== 'object')
            throw TypeError('.polycentric.Event.references: object expected');
          message.references[i] = $root.polycentric.Reference.fromObject(
            object.references[i]
          );
        }
      }
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from an Event message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Event
     * @static
     * @param {polycentric.Event} message Event
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Event.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.references = [];
      if (options.defaults) {
        object.system = null;
        object.process = null;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
        object.contentType = options.enums === String ? 'UNKNOWN' : 0;
        if (options.bytes === String) object.content = '';
        else {
          object.content = [];
          if (options.bytes !== Array)
            object.content = $util.newBuffer(object.content);
        }
        object.vectorClock = null;
        object.indices = null;
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PublicKey.toObject(
          message.system,
          options
        );
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        object.contentType =
          options.enums === String
            ? $root.polycentric.ContentType[message.contentType] === undefined
              ? message.contentType
              : $root.polycentric.ContentType[message.contentType]
            : message.contentType;
      if (message.content != null && message.hasOwnProperty('content'))
        object.content =
          options.bytes === String
            ? $util.base64.encode(message.content, 0, message.content.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.content)
              : message.content;
      if (message.vectorClock != null && message.hasOwnProperty('vectorClock'))
        object.vectorClock = $root.polycentric.VectorClock.toObject(
          message.vectorClock,
          options
        );
      if (message.indices != null && message.hasOwnProperty('indices'))
        object.indices = $root.polycentric.Indices.toObject(
          message.indices,
          options
        );
      if (
        message.lwwElementSet != null &&
        message.hasOwnProperty('lwwElementSet')
      ) {
        object.lwwElementSet = $root.polycentric.LWWElementSet.toObject(
          message.lwwElementSet,
          options
        );
        if (options.oneofs) object._lwwElementSet = 'lwwElementSet';
      }
      if (message.lwwElement != null && message.hasOwnProperty('lwwElement')) {
        object.lwwElement = $root.polycentric.LWWElement.toObject(
          message.lwwElement,
          options
        );
        if (options.oneofs) object._lwwElement = 'lwwElement';
      }
      if (message.references && message.references.length) {
        object.references = [];
        for (let j = 0; j < message.references.length; ++j)
          object.references[j] = $root.polycentric.Reference.toObject(
            message.references[j],
            options
          );
      }
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      ) {
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
        if (options.oneofs) object._unixMilliseconds = 'unixMilliseconds';
      }
      return object;
    };

    /**
     * Converts this Event to JSON.
     * @function toJSON
     * @memberof polycentric.Event
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Event.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Event
     * @function getTypeUrl
     * @memberof polycentric.Event
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Event.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Event';
    };

    return Event;
  })();

  polycentric.ReferencesResult = (function () {
    /**
     * Properties of a ReferencesResult.
     * @memberof polycentric
     * @interface IReferencesResult
     * @property {Array.<polycentric.ISignedEvent>|null} [events] ReferencesResult events
     * @property {Array.<polycentric.ISignedEvent>|null} [relatedEvents] ReferencesResult relatedEvents
     * @property {Uint8Array|null} [cursor] ReferencesResult cursor
     */

    /**
     * Constructs a new ReferencesResult.
     * @memberof polycentric
     * @classdesc Represents a ReferencesResult.
     * @implements IReferencesResult
     * @constructor
     * @param {polycentric.IReferencesResult=} [properties] Properties to set
     */
    function ReferencesResult(properties) {
      this.events = [];
      this.relatedEvents = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReferencesResult events.
     * @member {Array.<polycentric.ISignedEvent>} events
     * @memberof polycentric.ReferencesResult
     * @instance
     */
    ReferencesResult.prototype.events = $util.emptyArray;

    /**
     * ReferencesResult relatedEvents.
     * @member {Array.<polycentric.ISignedEvent>} relatedEvents
     * @memberof polycentric.ReferencesResult
     * @instance
     */
    ReferencesResult.prototype.relatedEvents = $util.emptyArray;

    /**
     * ReferencesResult cursor.
     * @member {Uint8Array} cursor
     * @memberof polycentric.ReferencesResult
     * @instance
     */
    ReferencesResult.prototype.cursor = $util.newBuffer([]);

    /**
     * Creates a new ReferencesResult instance using the specified properties.
     * @function create
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {polycentric.IReferencesResult=} [properties] Properties to set
     * @returns {polycentric.ReferencesResult} ReferencesResult instance
     */
    ReferencesResult.create = function create(properties) {
      return new ReferencesResult(properties);
    };

    /**
     * Encodes the specified ReferencesResult message. Does not implicitly {@link polycentric.ReferencesResult.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {polycentric.IReferencesResult} message ReferencesResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReferencesResult.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.events != null && message.events.length)
        for (let i = 0; i < message.events.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.events[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      if (message.relatedEvents != null && message.relatedEvents.length)
        for (let i = 0; i < message.relatedEvents.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.relatedEvents[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.cursor);
      return writer;
    };

    /**
     * Encodes the specified ReferencesResult message, length delimited. Does not implicitly {@link polycentric.ReferencesResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {polycentric.IReferencesResult} message ReferencesResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReferencesResult.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ReferencesResult message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ReferencesResult} ReferencesResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReferencesResult.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ReferencesResult();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.events && message.events.length)) message.events = [];
            message.events.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          case 2: {
            if (!(message.relatedEvents && message.relatedEvents.length))
              message.relatedEvents = [];
            message.relatedEvents.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          case 3: {
            message.cursor = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ReferencesResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ReferencesResult} ReferencesResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReferencesResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReferencesResult message.
     * @function verify
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReferencesResult.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.events != null && message.hasOwnProperty('events')) {
        if (!Array.isArray(message.events)) return 'events: array expected';
        for (let i = 0; i < message.events.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(message.events[i]);
          if (error) return 'events.' + error;
        }
      }
      if (
        message.relatedEvents != null &&
        message.hasOwnProperty('relatedEvents')
      ) {
        if (!Array.isArray(message.relatedEvents))
          return 'relatedEvents: array expected';
        for (let i = 0; i < message.relatedEvents.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(
            message.relatedEvents[i]
          );
          if (error) return 'relatedEvents.' + error;
        }
      }
      if (message.cursor != null && message.hasOwnProperty('cursor'))
        if (
          !(
            (message.cursor && typeof message.cursor.length === 'number') ||
            $util.isString(message.cursor)
          )
        )
          return 'cursor: buffer expected';
      return null;
    };

    /**
     * Creates a ReferencesResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ReferencesResult} ReferencesResult
     */
    ReferencesResult.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ReferencesResult) return object;
      let message = new $root.polycentric.ReferencesResult();
      if (object.events) {
        if (!Array.isArray(object.events))
          throw TypeError(
            '.polycentric.ReferencesResult.events: array expected'
          );
        message.events = [];
        for (let i = 0; i < object.events.length; ++i) {
          if (typeof object.events[i] !== 'object')
            throw TypeError(
              '.polycentric.ReferencesResult.events: object expected'
            );
          message.events[i] = $root.polycentric.SignedEvent.fromObject(
            object.events[i]
          );
        }
      }
      if (object.relatedEvents) {
        if (!Array.isArray(object.relatedEvents))
          throw TypeError(
            '.polycentric.ReferencesResult.relatedEvents: array expected'
          );
        message.relatedEvents = [];
        for (let i = 0; i < object.relatedEvents.length; ++i) {
          if (typeof object.relatedEvents[i] !== 'object')
            throw TypeError(
              '.polycentric.ReferencesResult.relatedEvents: object expected'
            );
          message.relatedEvents[i] = $root.polycentric.SignedEvent.fromObject(
            object.relatedEvents[i]
          );
        }
      }
      if (object.cursor != null)
        if (typeof object.cursor === 'string')
          $util.base64.decode(
            object.cursor,
            (message.cursor = $util.newBuffer(
              $util.base64.length(object.cursor)
            )),
            0
          );
        else if (object.cursor.length >= 0) message.cursor = object.cursor;
      return message;
    };

    /**
     * Creates a plain object from a ReferencesResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {polycentric.ReferencesResult} message ReferencesResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReferencesResult.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.events = [];
        object.relatedEvents = [];
      }
      if (options.defaults)
        if (options.bytes === String) object.cursor = '';
        else {
          object.cursor = [];
          if (options.bytes !== Array)
            object.cursor = $util.newBuffer(object.cursor);
        }
      if (message.events && message.events.length) {
        object.events = [];
        for (let j = 0; j < message.events.length; ++j)
          object.events[j] = $root.polycentric.SignedEvent.toObject(
            message.events[j],
            options
          );
      }
      if (message.relatedEvents && message.relatedEvents.length) {
        object.relatedEvents = [];
        for (let j = 0; j < message.relatedEvents.length; ++j)
          object.relatedEvents[j] = $root.polycentric.SignedEvent.toObject(
            message.relatedEvents[j],
            options
          );
      }
      if (message.cursor != null && message.hasOwnProperty('cursor'))
        object.cursor =
          options.bytes === String
            ? $util.base64.encode(message.cursor, 0, message.cursor.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.cursor)
              : message.cursor;
      return object;
    };

    /**
     * Converts this ReferencesResult to JSON.
     * @function toJSON
     * @memberof polycentric.ReferencesResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReferencesResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReferencesResult
     * @function getTypeUrl
     * @memberof polycentric.ReferencesResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReferencesResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ReferencesResult';
    };

    return ReferencesResult;
  })();

  polycentric.FeedResult = (function () {
    /**
     * Properties of a FeedResult.
     * @memberof polycentric
     * @interface IFeedResult
     * @property {Array.<polycentric.ISignedEvent>|null} [events] FeedResult events
     * @property {Uint8Array|null} [cursor] FeedResult cursor
     */

    /**
     * Constructs a new FeedResult.
     * @memberof polycentric
     * @classdesc Represents a FeedResult.
     * @implements IFeedResult
     * @constructor
     * @param {polycentric.IFeedResult=} [properties] Properties to set
     */
    function FeedResult(properties) {
      this.events = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * FeedResult events.
     * @member {Array.<polycentric.ISignedEvent>} events
     * @memberof polycentric.FeedResult
     * @instance
     */
    FeedResult.prototype.events = $util.emptyArray;

    /**
     * FeedResult cursor.
     * @member {Uint8Array} cursor
     * @memberof polycentric.FeedResult
     * @instance
     */
    FeedResult.prototype.cursor = $util.newBuffer([]);

    /**
     * Creates a new FeedResult instance using the specified properties.
     * @function create
     * @memberof polycentric.FeedResult
     * @static
     * @param {polycentric.IFeedResult=} [properties] Properties to set
     * @returns {polycentric.FeedResult} FeedResult instance
     */
    FeedResult.create = function create(properties) {
      return new FeedResult(properties);
    };

    /**
     * Encodes the specified FeedResult message. Does not implicitly {@link polycentric.FeedResult.verify|verify} messages.
     * @function encode
     * @memberof polycentric.FeedResult
     * @static
     * @param {polycentric.IFeedResult} message FeedResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedResult.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.events != null && message.events.length)
        for (let i = 0; i < message.events.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.events[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.cursor);
      return writer;
    };

    /**
     * Encodes the specified FeedResult message, length delimited. Does not implicitly {@link polycentric.FeedResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.FeedResult
     * @static
     * @param {polycentric.IFeedResult} message FeedResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedResult.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FeedResult message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.FeedResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.FeedResult} FeedResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedResult.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.FeedResult();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.events && message.events.length)) message.events = [];
            message.events.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          case 2: {
            message.cursor = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a FeedResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.FeedResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.FeedResult} FeedResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FeedResult message.
     * @function verify
     * @memberof polycentric.FeedResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FeedResult.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.events != null && message.hasOwnProperty('events')) {
        if (!Array.isArray(message.events)) return 'events: array expected';
        for (let i = 0; i < message.events.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(message.events[i]);
          if (error) return 'events.' + error;
        }
      }
      if (message.cursor != null && message.hasOwnProperty('cursor'))
        if (
          !(
            (message.cursor && typeof message.cursor.length === 'number') ||
            $util.isString(message.cursor)
          )
        )
          return 'cursor: buffer expected';
      return null;
    };

    /**
     * Creates a FeedResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.FeedResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.FeedResult} FeedResult
     */
    FeedResult.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.FeedResult) return object;
      let message = new $root.polycentric.FeedResult();
      if (object.events) {
        if (!Array.isArray(object.events))
          throw TypeError('.polycentric.FeedResult.events: array expected');
        message.events = [];
        for (let i = 0; i < object.events.length; ++i) {
          if (typeof object.events[i] !== 'object')
            throw TypeError('.polycentric.FeedResult.events: object expected');
          message.events[i] = $root.polycentric.SignedEvent.fromObject(
            object.events[i]
          );
        }
      }
      if (object.cursor != null)
        if (typeof object.cursor === 'string')
          $util.base64.decode(
            object.cursor,
            (message.cursor = $util.newBuffer(
              $util.base64.length(object.cursor)
            )),
            0
          );
        else if (object.cursor.length >= 0) message.cursor = object.cursor;
      return message;
    };

    /**
     * Creates a plain object from a FeedResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.FeedResult
     * @static
     * @param {polycentric.FeedResult} message FeedResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FeedResult.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.events = [];
      if (options.defaults)
        if (options.bytes === String) object.cursor = '';
        else {
          object.cursor = [];
          if (options.bytes !== Array)
            object.cursor = $util.newBuffer(object.cursor);
        }
      if (message.events && message.events.length) {
        object.events = [];
        for (let j = 0; j < message.events.length; ++j)
          object.events[j] = $root.polycentric.SignedEvent.toObject(
            message.events[j],
            options
          );
      }
      if (message.cursor != null && message.hasOwnProperty('cursor'))
        object.cursor =
          options.bytes === String
            ? $util.base64.encode(message.cursor, 0, message.cursor.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.cursor)
              : message.cursor;
      return object;
    };

    /**
     * Converts this FeedResult to JSON.
     * @function toJSON
     * @memberof polycentric.FeedResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FeedResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FeedResult
     * @function getTypeUrl
     * @memberof polycentric.FeedResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FeedResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.FeedResult';
    };

    return FeedResult;
  })();

  polycentric.CountReferencesResult = (function () {
    /**
     * Properties of a CountReferencesResult.
     * @memberof polycentric
     * @interface ICountReferencesResult
     * @property {Array.<number|Long>|null} [counts] CountReferencesResult counts
     */

    /**
     * Constructs a new CountReferencesResult.
     * @memberof polycentric
     * @classdesc Represents a CountReferencesResult.
     * @implements ICountReferencesResult
     * @constructor
     * @param {polycentric.ICountReferencesResult=} [properties] Properties to set
     */
    function CountReferencesResult(properties) {
      this.counts = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * CountReferencesResult counts.
     * @member {Array.<number|Long>} counts
     * @memberof polycentric.CountReferencesResult
     * @instance
     */
    CountReferencesResult.prototype.counts = $util.emptyArray;

    /**
     * Creates a new CountReferencesResult instance using the specified properties.
     * @function create
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {polycentric.ICountReferencesResult=} [properties] Properties to set
     * @returns {polycentric.CountReferencesResult} CountReferencesResult instance
     */
    CountReferencesResult.create = function create(properties) {
      return new CountReferencesResult(properties);
    };

    /**
     * Encodes the specified CountReferencesResult message. Does not implicitly {@link polycentric.CountReferencesResult.verify|verify} messages.
     * @function encode
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {polycentric.ICountReferencesResult} message CountReferencesResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountReferencesResult.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.counts != null && message.counts.length)
        for (let i = 0; i < message.counts.length; ++i)
          writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.counts[i]);
      return writer;
    };

    /**
     * Encodes the specified CountReferencesResult message, length delimited. Does not implicitly {@link polycentric.CountReferencesResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {polycentric.ICountReferencesResult} message CountReferencesResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CountReferencesResult.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CountReferencesResult message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.CountReferencesResult} CountReferencesResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountReferencesResult.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.CountReferencesResult();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.counts && message.counts.length)) message.counts = [];
            if ((tag & 7) === 2) {
              let end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) message.counts.push(reader.uint64());
            } else message.counts.push(reader.uint64());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a CountReferencesResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.CountReferencesResult} CountReferencesResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CountReferencesResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CountReferencesResult message.
     * @function verify
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CountReferencesResult.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.counts != null && message.hasOwnProperty('counts')) {
        if (!Array.isArray(message.counts)) return 'counts: array expected';
        for (let i = 0; i < message.counts.length; ++i)
          if (
            !$util.isInteger(message.counts[i]) &&
            !(
              message.counts[i] &&
              $util.isInteger(message.counts[i].low) &&
              $util.isInteger(message.counts[i].high)
            )
          )
            return 'counts: integer|Long[] expected';
      }
      return null;
    };

    /**
     * Creates a CountReferencesResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.CountReferencesResult} CountReferencesResult
     */
    CountReferencesResult.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.CountReferencesResult)
        return object;
      let message = new $root.polycentric.CountReferencesResult();
      if (object.counts) {
        if (!Array.isArray(object.counts))
          throw TypeError(
            '.polycentric.CountReferencesResult.counts: array expected'
          );
        message.counts = [];
        for (let i = 0; i < object.counts.length; ++i)
          if ($util.Long)
            (message.counts[i] = $util.Long.fromValue(
              object.counts[i]
            )).unsigned = true;
          else if (typeof object.counts[i] === 'string')
            message.counts[i] = parseInt(object.counts[i], 10);
          else if (typeof object.counts[i] === 'number')
            message.counts[i] = object.counts[i];
          else if (typeof object.counts[i] === 'object')
            message.counts[i] = new $util.LongBits(
              object.counts[i].low >>> 0,
              object.counts[i].high >>> 0
            ).toNumber(true);
      }
      return message;
    };

    /**
     * Creates a plain object from a CountReferencesResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {polycentric.CountReferencesResult} message CountReferencesResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CountReferencesResult.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.counts = [];
      if (message.counts && message.counts.length) {
        object.counts = [];
        for (let j = 0; j < message.counts.length; ++j)
          if (typeof message.counts[j] === 'number')
            object.counts[j] =
              options.longs === String
                ? String(message.counts[j])
                : message.counts[j];
          else
            object.counts[j] =
              options.longs === String
                ? $util.Long.prototype.toString.call(message.counts[j])
                : options.longs === Number
                  ? new $util.LongBits(
                      message.counts[j].low >>> 0,
                      message.counts[j].high >>> 0
                    ).toNumber(true)
                  : message.counts[j];
      }
      return object;
    };

    /**
     * Converts this CountReferencesResult to JSON.
     * @function toJSON
     * @memberof polycentric.CountReferencesResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CountReferencesResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CountReferencesResult
     * @function getTypeUrl
     * @memberof polycentric.CountReferencesResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CountReferencesResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.CountReferencesResult';
    };

    return CountReferencesResult;
  })();

  polycentric.EventCreationResult = (function () {
    /**
     * Properties of an EventCreationResult.
     * @memberof polycentric
     * @interface IEventCreationResult
     * @property {Uint8Array|null} [event] EventCreationResult event
     * @property {number|Long|null} [logicalClock] EventCreationResult logicalClock
     */

    /**
     * Constructs a new EventCreationResult.
     * @memberof polycentric
     * @classdesc Represents an EventCreationResult.
     * @implements IEventCreationResult
     * @constructor
     * @param {polycentric.IEventCreationResult=} [properties] Properties to set
     */
    function EventCreationResult(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * EventCreationResult event.
     * @member {Uint8Array} event
     * @memberof polycentric.EventCreationResult
     * @instance
     */
    EventCreationResult.prototype.event = $util.newBuffer([]);

    /**
     * EventCreationResult logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.EventCreationResult
     * @instance
     */
    EventCreationResult.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new EventCreationResult instance using the specified properties.
     * @function create
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {polycentric.IEventCreationResult=} [properties] Properties to set
     * @returns {polycentric.EventCreationResult} EventCreationResult instance
     */
    EventCreationResult.create = function create(properties) {
      return new EventCreationResult(properties);
    };

    /**
     * Encodes the specified EventCreationResult message. Does not implicitly {@link polycentric.EventCreationResult.verify|verify} messages.
     * @function encode
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {polycentric.IEventCreationResult} message EventCreationResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EventCreationResult.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.event != null && Object.hasOwnProperty.call(message, 'event'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.event);
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.logicalClock);
      return writer;
    };

    /**
     * Encodes the specified EventCreationResult message, length delimited. Does not implicitly {@link polycentric.EventCreationResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {polycentric.IEventCreationResult} message EventCreationResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EventCreationResult.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an EventCreationResult message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.EventCreationResult} EventCreationResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EventCreationResult.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.EventCreationResult();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.event = reader.bytes();
            break;
          }
          case 2: {
            message.logicalClock = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an EventCreationResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.EventCreationResult} EventCreationResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EventCreationResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an EventCreationResult message.
     * @function verify
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    EventCreationResult.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.event != null && message.hasOwnProperty('event'))
        if (
          !(
            (message.event && typeof message.event.length === 'number') ||
            $util.isString(message.event)
          )
        )
          return 'event: buffer expected';
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      return null;
    };

    /**
     * Creates an EventCreationResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.EventCreationResult} EventCreationResult
     */
    EventCreationResult.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.EventCreationResult)
        return object;
      let message = new $root.polycentric.EventCreationResult();
      if (object.event != null)
        if (typeof object.event === 'string')
          $util.base64.decode(
            object.event,
            (message.event = $util.newBuffer(
              $util.base64.length(object.event)
            )),
            0
          );
        else if (object.event.length >= 0) message.event = object.event;
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from an EventCreationResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {polycentric.EventCreationResult} message EventCreationResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    EventCreationResult.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if (options.bytes === String) object.event = '';
        else {
          object.event = [];
          if (options.bytes !== Array)
            object.event = $util.newBuffer(object.event);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
      }
      if (message.event != null && message.hasOwnProperty('event'))
        object.event =
          options.bytes === String
            ? $util.base64.encode(message.event, 0, message.event.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.event)
              : message.event;
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      return object;
    };

    /**
     * Converts this EventCreationResult to JSON.
     * @function toJSON
     * @memberof polycentric.EventCreationResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    EventCreationResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for EventCreationResult
     * @function getTypeUrl
     * @memberof polycentric.EventCreationResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    EventCreationResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.EventCreationResult';
    };

    return EventCreationResult;
  })();

  polycentric.EventKey = (function () {
    /**
     * Properties of an EventKey.
     * @memberof polycentric
     * @interface IEventKey
     * @property {number|Long|null} [systemKeyType] EventKey systemKeyType
     * @property {Uint8Array|null} [systemKey] EventKey systemKey
     * @property {Uint8Array|null} [process] EventKey process
     * @property {number|Long|null} [logicalClock] EventKey logicalClock
     */

    /**
     * Constructs a new EventKey.
     * @memberof polycentric
     * @classdesc Represents an EventKey.
     * @implements IEventKey
     * @constructor
     * @param {polycentric.IEventKey=} [properties] Properties to set
     */
    function EventKey(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * EventKey systemKeyType.
     * @member {number|Long} systemKeyType
     * @memberof polycentric.EventKey
     * @instance
     */
    EventKey.prototype.systemKeyType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * EventKey systemKey.
     * @member {Uint8Array} systemKey
     * @memberof polycentric.EventKey
     * @instance
     */
    EventKey.prototype.systemKey = $util.newBuffer([]);

    /**
     * EventKey process.
     * @member {Uint8Array} process
     * @memberof polycentric.EventKey
     * @instance
     */
    EventKey.prototype.process = $util.newBuffer([]);

    /**
     * EventKey logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.EventKey
     * @instance
     */
    EventKey.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new EventKey instance using the specified properties.
     * @function create
     * @memberof polycentric.EventKey
     * @static
     * @param {polycentric.IEventKey=} [properties] Properties to set
     * @returns {polycentric.EventKey} EventKey instance
     */
    EventKey.create = function create(properties) {
      return new EventKey(properties);
    };

    /**
     * Encodes the specified EventKey message. Does not implicitly {@link polycentric.EventKey.verify|verify} messages.
     * @function encode
     * @memberof polycentric.EventKey
     * @static
     * @param {polycentric.IEventKey} message EventKey message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EventKey.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.systemKeyType != null &&
        Object.hasOwnProperty.call(message, 'systemKeyType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.systemKeyType);
      if (
        message.systemKey != null &&
        Object.hasOwnProperty.call(message, 'systemKey')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.systemKey);
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.process);
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).uint64(message.logicalClock);
      return writer;
    };

    /**
     * Encodes the specified EventKey message, length delimited. Does not implicitly {@link polycentric.EventKey.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.EventKey
     * @static
     * @param {polycentric.IEventKey} message EventKey message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    EventKey.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an EventKey message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.EventKey
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.EventKey} EventKey
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EventKey.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.EventKey();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.systemKeyType = reader.uint64();
            break;
          }
          case 2: {
            message.systemKey = reader.bytes();
            break;
          }
          case 3: {
            message.process = reader.bytes();
            break;
          }
          case 4: {
            message.logicalClock = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an EventKey message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.EventKey
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.EventKey} EventKey
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    EventKey.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an EventKey message.
     * @function verify
     * @memberof polycentric.EventKey
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    EventKey.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.systemKeyType != null &&
        message.hasOwnProperty('systemKeyType')
      )
        if (
          !$util.isInteger(message.systemKeyType) &&
          !(
            message.systemKeyType &&
            $util.isInteger(message.systemKeyType.low) &&
            $util.isInteger(message.systemKeyType.high)
          )
        )
          return 'systemKeyType: integer|Long expected';
      if (message.systemKey != null && message.hasOwnProperty('systemKey'))
        if (
          !(
            (message.systemKey &&
              typeof message.systemKey.length === 'number') ||
            $util.isString(message.systemKey)
          )
        )
          return 'systemKey: buffer expected';
      if (message.process != null && message.hasOwnProperty('process'))
        if (
          !(
            (message.process && typeof message.process.length === 'number') ||
            $util.isString(message.process)
          )
        )
          return 'process: buffer expected';
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      return null;
    };

    /**
     * Creates an EventKey message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.EventKey
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.EventKey} EventKey
     */
    EventKey.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.EventKey) return object;
      let message = new $root.polycentric.EventKey();
      if (object.systemKeyType != null)
        if ($util.Long)
          (message.systemKeyType = $util.Long.fromValue(
            object.systemKeyType
          )).unsigned = true;
        else if (typeof object.systemKeyType === 'string')
          message.systemKeyType = parseInt(object.systemKeyType, 10);
        else if (typeof object.systemKeyType === 'number')
          message.systemKeyType = object.systemKeyType;
        else if (typeof object.systemKeyType === 'object')
          message.systemKeyType = new $util.LongBits(
            object.systemKeyType.low >>> 0,
            object.systemKeyType.high >>> 0
          ).toNumber(true);
      if (object.systemKey != null)
        if (typeof object.systemKey === 'string')
          $util.base64.decode(
            object.systemKey,
            (message.systemKey = $util.newBuffer(
              $util.base64.length(object.systemKey)
            )),
            0
          );
        else if (object.systemKey.length >= 0)
          message.systemKey = object.systemKey;
      if (object.process != null)
        if (typeof object.process === 'string')
          $util.base64.decode(
            object.process,
            (message.process = $util.newBuffer(
              $util.base64.length(object.process)
            )),
            0
          );
        else if (object.process.length >= 0) message.process = object.process;
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from an EventKey message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.EventKey
     * @static
     * @param {polycentric.EventKey} message EventKey
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    EventKey.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.systemKeyType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.systemKeyType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.systemKey = '';
        else {
          object.systemKey = [];
          if (options.bytes !== Array)
            object.systemKey = $util.newBuffer(object.systemKey);
        }
        if (options.bytes === String) object.process = '';
        else {
          object.process = [];
          if (options.bytes !== Array)
            object.process = $util.newBuffer(object.process);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
      }
      if (
        message.systemKeyType != null &&
        message.hasOwnProperty('systemKeyType')
      )
        if (typeof message.systemKeyType === 'number')
          object.systemKeyType =
            options.longs === String
              ? String(message.systemKeyType)
              : message.systemKeyType;
        else
          object.systemKeyType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.systemKeyType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.systemKeyType.low >>> 0,
                    message.systemKeyType.high >>> 0
                  ).toNumber(true)
                : message.systemKeyType;
      if (message.systemKey != null && message.hasOwnProperty('systemKey'))
        object.systemKey =
          options.bytes === String
            ? $util.base64.encode(
                message.systemKey,
                0,
                message.systemKey.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.systemKey)
              : message.systemKey;
      if (message.process != null && message.hasOwnProperty('process'))
        object.process =
          options.bytes === String
            ? $util.base64.encode(message.process, 0, message.process.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.process)
              : message.process;
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      return object;
    };

    /**
     * Converts this EventKey to JSON.
     * @function toJSON
     * @memberof polycentric.EventKey
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    EventKey.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for EventKey
     * @function getTypeUrl
     * @memberof polycentric.EventKey
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    EventKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.EventKey';
    };

    return EventKey;
  })();

  polycentric.FeedCursor = (function () {
    /**
     * Properties of a FeedCursor.
     * @memberof polycentric
     * @interface IFeedCursor
     * @property {number|Long|null} [unixMilliseconds] FeedCursor unixMilliseconds
     * @property {polycentric.IEventKey|null} [eventKey] FeedCursor eventKey
     */

    /**
     * Constructs a new FeedCursor.
     * @memberof polycentric
     * @classdesc Represents a FeedCursor.
     * @implements IFeedCursor
     * @constructor
     * @param {polycentric.IFeedCursor=} [properties] Properties to set
     */
    function FeedCursor(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * FeedCursor unixMilliseconds.
     * @member {number|Long} unixMilliseconds
     * @memberof polycentric.FeedCursor
     * @instance
     */
    FeedCursor.prototype.unixMilliseconds = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * FeedCursor eventKey.
     * @member {polycentric.IEventKey|null|undefined} eventKey
     * @memberof polycentric.FeedCursor
     * @instance
     */
    FeedCursor.prototype.eventKey = null;

    /**
     * Creates a new FeedCursor instance using the specified properties.
     * @function create
     * @memberof polycentric.FeedCursor
     * @static
     * @param {polycentric.IFeedCursor=} [properties] Properties to set
     * @returns {polycentric.FeedCursor} FeedCursor instance
     */
    FeedCursor.create = function create(properties) {
      return new FeedCursor(properties);
    };

    /**
     * Encodes the specified FeedCursor message. Does not implicitly {@link polycentric.FeedCursor.verify|verify} messages.
     * @function encode
     * @memberof polycentric.FeedCursor
     * @static
     * @param {polycentric.IFeedCursor} message FeedCursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedCursor.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 1, wireType 0 =*/ 8)
          .uint64(message.unixMilliseconds);
      if (
        message.eventKey != null &&
        Object.hasOwnProperty.call(message, 'eventKey')
      )
        $root.polycentric.EventKey.encode(
          message.eventKey,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified FeedCursor message, length delimited. Does not implicitly {@link polycentric.FeedCursor.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.FeedCursor
     * @static
     * @param {polycentric.IFeedCursor} message FeedCursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedCursor.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FeedCursor message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.FeedCursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.FeedCursor} FeedCursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedCursor.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.FeedCursor();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          case 2: {
            message.eventKey = $root.polycentric.EventKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a FeedCursor message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.FeedCursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.FeedCursor} FeedCursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedCursor.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FeedCursor message.
     * @function verify
     * @memberof polycentric.FeedCursor
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FeedCursor.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      if (message.eventKey != null && message.hasOwnProperty('eventKey')) {
        let error = $root.polycentric.EventKey.verify(message.eventKey);
        if (error) return 'eventKey.' + error;
      }
      return null;
    };

    /**
     * Creates a FeedCursor message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.FeedCursor
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.FeedCursor} FeedCursor
     */
    FeedCursor.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.FeedCursor) return object;
      let message = new $root.polycentric.FeedCursor();
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      if (object.eventKey != null) {
        if (typeof object.eventKey !== 'object')
          throw TypeError('.polycentric.FeedCursor.eventKey: object expected');
        message.eventKey = $root.polycentric.EventKey.fromObject(
          object.eventKey
        );
      }
      return message;
    };

    /**
     * Creates a plain object from a FeedCursor message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.FeedCursor
     * @static
     * @param {polycentric.FeedCursor} message FeedCursor
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FeedCursor.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.unixMilliseconds =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.unixMilliseconds = options.longs === String ? '0' : 0;
        object.eventKey = null;
      }
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
      if (message.eventKey != null && message.hasOwnProperty('eventKey'))
        object.eventKey = $root.polycentric.EventKey.toObject(
          message.eventKey,
          options
        );
      return object;
    };

    /**
     * Converts this FeedCursor to JSON.
     * @function toJSON
     * @memberof polycentric.FeedCursor
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FeedCursor.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FeedCursor
     * @function getTypeUrl
     * @memberof polycentric.FeedCursor
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FeedCursor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.FeedCursor';
    };

    return FeedCursor;
  })();

  polycentric.ReferenceCursor = (function () {
    /**
     * Properties of a ReferenceCursor.
     * @memberof polycentric
     * @interface IReferenceCursor
     * @property {polycentric.IEventKey|null} [lastEventKey] ReferenceCursor lastEventKey
     * @property {number|Long|null} [offset] ReferenceCursor offset
     * @property {number|Long|null} [processedCount] ReferenceCursor processedCount
     */

    /**
     * Constructs a new ReferenceCursor.
     * @memberof polycentric
     * @classdesc Represents a ReferenceCursor.
     * @implements IReferenceCursor
     * @constructor
     * @param {polycentric.IReferenceCursor=} [properties] Properties to set
     */
    function ReferenceCursor(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ReferenceCursor lastEventKey.
     * @member {polycentric.IEventKey|null|undefined} lastEventKey
     * @memberof polycentric.ReferenceCursor
     * @instance
     */
    ReferenceCursor.prototype.lastEventKey = null;

    /**
     * ReferenceCursor offset.
     * @member {number|Long} offset
     * @memberof polycentric.ReferenceCursor
     * @instance
     */
    ReferenceCursor.prototype.offset = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * ReferenceCursor processedCount.
     * @member {number|Long} processedCount
     * @memberof polycentric.ReferenceCursor
     * @instance
     */
    ReferenceCursor.prototype.processedCount = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new ReferenceCursor instance using the specified properties.
     * @function create
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {polycentric.IReferenceCursor=} [properties] Properties to set
     * @returns {polycentric.ReferenceCursor} ReferenceCursor instance
     */
    ReferenceCursor.create = function create(properties) {
      return new ReferenceCursor(properties);
    };

    /**
     * Encodes the specified ReferenceCursor message. Does not implicitly {@link polycentric.ReferenceCursor.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {polycentric.IReferenceCursor} message ReferenceCursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReferenceCursor.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.lastEventKey != null &&
        Object.hasOwnProperty.call(message, 'lastEventKey')
      )
        $root.polycentric.EventKey.encode(
          message.lastEventKey,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.offset != null &&
        Object.hasOwnProperty.call(message, 'offset')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.offset);
      if (
        message.processedCount != null &&
        Object.hasOwnProperty.call(message, 'processedCount')
      )
        writer
          .uint32(/* id 3, wireType 0 =*/ 24)
          .uint64(message.processedCount);
      return writer;
    };

    /**
     * Encodes the specified ReferenceCursor message, length delimited. Does not implicitly {@link polycentric.ReferenceCursor.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {polycentric.IReferenceCursor} message ReferenceCursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ReferenceCursor.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ReferenceCursor message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ReferenceCursor} ReferenceCursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReferenceCursor.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ReferenceCursor();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.lastEventKey = $root.polycentric.EventKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.offset = reader.uint64();
            break;
          }
          case 3: {
            message.processedCount = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ReferenceCursor message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ReferenceCursor} ReferenceCursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ReferenceCursor.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ReferenceCursor message.
     * @function verify
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ReferenceCursor.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.lastEventKey != null &&
        message.hasOwnProperty('lastEventKey')
      ) {
        let error = $root.polycentric.EventKey.verify(message.lastEventKey);
        if (error) return 'lastEventKey.' + error;
      }
      if (message.offset != null && message.hasOwnProperty('offset'))
        if (
          !$util.isInteger(message.offset) &&
          !(
            message.offset &&
            $util.isInteger(message.offset.low) &&
            $util.isInteger(message.offset.high)
          )
        )
          return 'offset: integer|Long expected';
      if (
        message.processedCount != null &&
        message.hasOwnProperty('processedCount')
      )
        if (
          !$util.isInteger(message.processedCount) &&
          !(
            message.processedCount &&
            $util.isInteger(message.processedCount.low) &&
            $util.isInteger(message.processedCount.high)
          )
        )
          return 'processedCount: integer|Long expected';
      return null;
    };

    /**
     * Creates a ReferenceCursor message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ReferenceCursor} ReferenceCursor
     */
    ReferenceCursor.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ReferenceCursor) return object;
      let message = new $root.polycentric.ReferenceCursor();
      if (object.lastEventKey != null) {
        if (typeof object.lastEventKey !== 'object')
          throw TypeError(
            '.polycentric.ReferenceCursor.lastEventKey: object expected'
          );
        message.lastEventKey = $root.polycentric.EventKey.fromObject(
          object.lastEventKey
        );
      }
      if (object.offset != null)
        if ($util.Long)
          (message.offset = $util.Long.fromValue(object.offset)).unsigned =
            true;
        else if (typeof object.offset === 'string')
          message.offset = parseInt(object.offset, 10);
        else if (typeof object.offset === 'number')
          message.offset = object.offset;
        else if (typeof object.offset === 'object')
          message.offset = new $util.LongBits(
            object.offset.low >>> 0,
            object.offset.high >>> 0
          ).toNumber(true);
      if (object.processedCount != null)
        if ($util.Long)
          (message.processedCount = $util.Long.fromValue(
            object.processedCount
          )).unsigned = true;
        else if (typeof object.processedCount === 'string')
          message.processedCount = parseInt(object.processedCount, 10);
        else if (typeof object.processedCount === 'number')
          message.processedCount = object.processedCount;
        else if (typeof object.processedCount === 'object')
          message.processedCount = new $util.LongBits(
            object.processedCount.low >>> 0,
            object.processedCount.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a ReferenceCursor message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {polycentric.ReferenceCursor} message ReferenceCursor
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ReferenceCursor.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.lastEventKey = null;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.offset =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.offset = options.longs === String ? '0' : 0;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.processedCount =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.processedCount = options.longs === String ? '0' : 0;
      }
      if (
        message.lastEventKey != null &&
        message.hasOwnProperty('lastEventKey')
      )
        object.lastEventKey = $root.polycentric.EventKey.toObject(
          message.lastEventKey,
          options
        );
      if (message.offset != null && message.hasOwnProperty('offset'))
        if (typeof message.offset === 'number')
          object.offset =
            options.longs === String ? String(message.offset) : message.offset;
        else
          object.offset =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.offset)
              : options.longs === Number
                ? new $util.LongBits(
                    message.offset.low >>> 0,
                    message.offset.high >>> 0
                  ).toNumber(true)
                : message.offset;
      if (
        message.processedCount != null &&
        message.hasOwnProperty('processedCount')
      )
        if (typeof message.processedCount === 'number')
          object.processedCount =
            options.longs === String
              ? String(message.processedCount)
              : message.processedCount;
        else
          object.processedCount =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.processedCount)
              : options.longs === Number
                ? new $util.LongBits(
                    message.processedCount.low >>> 0,
                    message.processedCount.high >>> 0
                  ).toNumber(true)
                : message.processedCount;
      return object;
    };

    /**
     * Converts this ReferenceCursor to JSON.
     * @function toJSON
     * @memberof polycentric.ReferenceCursor
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ReferenceCursor.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ReferenceCursor
     * @function getTypeUrl
     * @memberof polycentric.ReferenceCursor
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ReferenceCursor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ReferenceCursor';
    };

    return ReferenceCursor;
  })();

  polycentric.DebugReferenceDigestResult = (function () {
    /**
     * Properties of a DebugReferenceDigestResult.
     * @memberof polycentric
     * @interface IDebugReferenceDigestResult
     * @property {boolean|null} [success] DebugReferenceDigestResult success
     * @property {boolean|null} [pointerParsed] DebugReferenceDigestResult pointerParsed
     * @property {boolean|null} [hasDigest] DebugReferenceDigestResult hasDigest
     * @property {polycentric.IDigest|null} [referenceDigest] DebugReferenceDigestResult referenceDigest
     * @property {polycentric.IDigest|null} [calculatedDigest] DebugReferenceDigestResult calculatedDigest
     * @property {boolean|null} [digestsMatch] DebugReferenceDigestResult digestsMatch
     * @property {string|null} [error] DebugReferenceDigestResult error
     * @property {number|null} [referenceBytesLength] DebugReferenceDigestResult referenceBytesLength
     * @property {number|null} [eventBytesLength] DebugReferenceDigestResult eventBytesLength
     * @property {boolean|null} [pointerSystemPresent] DebugReferenceDigestResult pointerSystemPresent
     * @property {boolean|null} [pointerProcessPresent] DebugReferenceDigestResult pointerProcessPresent
     * @property {number|Long|null} [pointerLogicalClock] DebugReferenceDigestResult pointerLogicalClock
     */

    /**
     * Constructs a new DebugReferenceDigestResult.
     * @memberof polycentric
     * @classdesc Represents a DebugReferenceDigestResult.
     * @implements IDebugReferenceDigestResult
     * @constructor
     * @param {polycentric.IDebugReferenceDigestResult=} [properties] Properties to set
     */
    function DebugReferenceDigestResult(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * DebugReferenceDigestResult success.
     * @member {boolean} success
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.success = false;

    /**
     * DebugReferenceDigestResult pointerParsed.
     * @member {boolean} pointerParsed
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.pointerParsed = false;

    /**
     * DebugReferenceDigestResult hasDigest.
     * @member {boolean} hasDigest
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.hasDigest = false;

    /**
     * DebugReferenceDigestResult referenceDigest.
     * @member {polycentric.IDigest|null|undefined} referenceDigest
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.referenceDigest = null;

    /**
     * DebugReferenceDigestResult calculatedDigest.
     * @member {polycentric.IDigest|null|undefined} calculatedDigest
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.calculatedDigest = null;

    /**
     * DebugReferenceDigestResult digestsMatch.
     * @member {boolean} digestsMatch
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.digestsMatch = false;

    /**
     * DebugReferenceDigestResult error.
     * @member {string} error
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.error = '';

    /**
     * DebugReferenceDigestResult referenceBytesLength.
     * @member {number} referenceBytesLength
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.referenceBytesLength = 0;

    /**
     * DebugReferenceDigestResult eventBytesLength.
     * @member {number} eventBytesLength
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.eventBytesLength = 0;

    /**
     * DebugReferenceDigestResult pointerSystemPresent.
     * @member {boolean} pointerSystemPresent
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.pointerSystemPresent = false;

    /**
     * DebugReferenceDigestResult pointerProcessPresent.
     * @member {boolean} pointerProcessPresent
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.pointerProcessPresent = false;

    /**
     * DebugReferenceDigestResult pointerLogicalClock.
     * @member {number|Long} pointerLogicalClock
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     */
    DebugReferenceDigestResult.prototype.pointerLogicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(
      DebugReferenceDigestResult.prototype,
      '_referenceDigest',
      {
        get: $util.oneOfGetter(($oneOfFields = ['referenceDigest'])),
        set: $util.oneOfSetter($oneOfFields),
      }
    );

    /**
     * Creates a new DebugReferenceDigestResult instance using the specified properties.
     * @function create
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {polycentric.IDebugReferenceDigestResult=} [properties] Properties to set
     * @returns {polycentric.DebugReferenceDigestResult} DebugReferenceDigestResult instance
     */
    DebugReferenceDigestResult.create = function create(properties) {
      return new DebugReferenceDigestResult(properties);
    };

    /**
     * Encodes the specified DebugReferenceDigestResult message. Does not implicitly {@link polycentric.DebugReferenceDigestResult.verify|verify} messages.
     * @function encode
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {polycentric.IDebugReferenceDigestResult} message DebugReferenceDigestResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DebugReferenceDigestResult.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.success != null &&
        Object.hasOwnProperty.call(message, 'success')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).bool(message.success);
      if (
        message.pointerParsed != null &&
        Object.hasOwnProperty.call(message, 'pointerParsed')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).bool(message.pointerParsed);
      if (
        message.hasDigest != null &&
        Object.hasOwnProperty.call(message, 'hasDigest')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).bool(message.hasDigest);
      if (
        message.referenceDigest != null &&
        Object.hasOwnProperty.call(message, 'referenceDigest')
      )
        $root.polycentric.Digest.encode(
          message.referenceDigest,
          writer.uint32(/* id 4, wireType 2 =*/ 34).fork()
        ).ldelim();
      if (
        message.calculatedDigest != null &&
        Object.hasOwnProperty.call(message, 'calculatedDigest')
      )
        $root.polycentric.Digest.encode(
          message.calculatedDigest,
          writer.uint32(/* id 5, wireType 2 =*/ 42).fork()
        ).ldelim();
      if (
        message.digestsMatch != null &&
        Object.hasOwnProperty.call(message, 'digestsMatch')
      )
        writer.uint32(/* id 6, wireType 0 =*/ 48).bool(message.digestsMatch);
      if (message.error != null && Object.hasOwnProperty.call(message, 'error'))
        writer.uint32(/* id 7, wireType 2 =*/ 58).string(message.error);
      if (
        message.referenceBytesLength != null &&
        Object.hasOwnProperty.call(message, 'referenceBytesLength')
      )
        writer
          .uint32(/* id 8, wireType 0 =*/ 64)
          .uint32(message.referenceBytesLength);
      if (
        message.eventBytesLength != null &&
        Object.hasOwnProperty.call(message, 'eventBytesLength')
      )
        writer
          .uint32(/* id 9, wireType 0 =*/ 72)
          .uint32(message.eventBytesLength);
      if (
        message.pointerSystemPresent != null &&
        Object.hasOwnProperty.call(message, 'pointerSystemPresent')
      )
        writer
          .uint32(/* id 10, wireType 0 =*/ 80)
          .bool(message.pointerSystemPresent);
      if (
        message.pointerProcessPresent != null &&
        Object.hasOwnProperty.call(message, 'pointerProcessPresent')
      )
        writer
          .uint32(/* id 11, wireType 0 =*/ 88)
          .bool(message.pointerProcessPresent);
      if (
        message.pointerLogicalClock != null &&
        Object.hasOwnProperty.call(message, 'pointerLogicalClock')
      )
        writer
          .uint32(/* id 12, wireType 0 =*/ 96)
          .uint64(message.pointerLogicalClock);
      return writer;
    };

    /**
     * Encodes the specified DebugReferenceDigestResult message, length delimited. Does not implicitly {@link polycentric.DebugReferenceDigestResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {polycentric.IDebugReferenceDigestResult} message DebugReferenceDigestResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    DebugReferenceDigestResult.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a DebugReferenceDigestResult message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.DebugReferenceDigestResult} DebugReferenceDigestResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DebugReferenceDigestResult.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.DebugReferenceDigestResult();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.success = reader.bool();
            break;
          }
          case 2: {
            message.pointerParsed = reader.bool();
            break;
          }
          case 3: {
            message.hasDigest = reader.bool();
            break;
          }
          case 4: {
            message.referenceDigest = $root.polycentric.Digest.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 5: {
            message.calculatedDigest = $root.polycentric.Digest.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 6: {
            message.digestsMatch = reader.bool();
            break;
          }
          case 7: {
            message.error = reader.string();
            break;
          }
          case 8: {
            message.referenceBytesLength = reader.uint32();
            break;
          }
          case 9: {
            message.eventBytesLength = reader.uint32();
            break;
          }
          case 10: {
            message.pointerSystemPresent = reader.bool();
            break;
          }
          case 11: {
            message.pointerProcessPresent = reader.bool();
            break;
          }
          case 12: {
            message.pointerLogicalClock = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a DebugReferenceDigestResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.DebugReferenceDigestResult} DebugReferenceDigestResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    DebugReferenceDigestResult.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a DebugReferenceDigestResult message.
     * @function verify
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    DebugReferenceDigestResult.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.success != null && message.hasOwnProperty('success'))
        if (typeof message.success !== 'boolean')
          return 'success: boolean expected';
      if (
        message.pointerParsed != null &&
        message.hasOwnProperty('pointerParsed')
      )
        if (typeof message.pointerParsed !== 'boolean')
          return 'pointerParsed: boolean expected';
      if (message.hasDigest != null && message.hasOwnProperty('hasDigest'))
        if (typeof message.hasDigest !== 'boolean')
          return 'hasDigest: boolean expected';
      if (
        message.referenceDigest != null &&
        message.hasOwnProperty('referenceDigest')
      ) {
        properties._referenceDigest = 1;
        {
          let error = $root.polycentric.Digest.verify(message.referenceDigest);
          if (error) return 'referenceDigest.' + error;
        }
      }
      if (
        message.calculatedDigest != null &&
        message.hasOwnProperty('calculatedDigest')
      ) {
        let error = $root.polycentric.Digest.verify(message.calculatedDigest);
        if (error) return 'calculatedDigest.' + error;
      }
      if (
        message.digestsMatch != null &&
        message.hasOwnProperty('digestsMatch')
      )
        if (typeof message.digestsMatch !== 'boolean')
          return 'digestsMatch: boolean expected';
      if (message.error != null && message.hasOwnProperty('error'))
        if (!$util.isString(message.error)) return 'error: string expected';
      if (
        message.referenceBytesLength != null &&
        message.hasOwnProperty('referenceBytesLength')
      )
        if (!$util.isInteger(message.referenceBytesLength))
          return 'referenceBytesLength: integer expected';
      if (
        message.eventBytesLength != null &&
        message.hasOwnProperty('eventBytesLength')
      )
        if (!$util.isInteger(message.eventBytesLength))
          return 'eventBytesLength: integer expected';
      if (
        message.pointerSystemPresent != null &&
        message.hasOwnProperty('pointerSystemPresent')
      )
        if (typeof message.pointerSystemPresent !== 'boolean')
          return 'pointerSystemPresent: boolean expected';
      if (
        message.pointerProcessPresent != null &&
        message.hasOwnProperty('pointerProcessPresent')
      )
        if (typeof message.pointerProcessPresent !== 'boolean')
          return 'pointerProcessPresent: boolean expected';
      if (
        message.pointerLogicalClock != null &&
        message.hasOwnProperty('pointerLogicalClock')
      )
        if (
          !$util.isInteger(message.pointerLogicalClock) &&
          !(
            message.pointerLogicalClock &&
            $util.isInteger(message.pointerLogicalClock.low) &&
            $util.isInteger(message.pointerLogicalClock.high)
          )
        )
          return 'pointerLogicalClock: integer|Long expected';
      return null;
    };

    /**
     * Creates a DebugReferenceDigestResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.DebugReferenceDigestResult} DebugReferenceDigestResult
     */
    DebugReferenceDigestResult.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.DebugReferenceDigestResult)
        return object;
      let message = new $root.polycentric.DebugReferenceDigestResult();
      if (object.success != null) message.success = Boolean(object.success);
      if (object.pointerParsed != null)
        message.pointerParsed = Boolean(object.pointerParsed);
      if (object.hasDigest != null)
        message.hasDigest = Boolean(object.hasDigest);
      if (object.referenceDigest != null) {
        if (typeof object.referenceDigest !== 'object')
          throw TypeError(
            '.polycentric.DebugReferenceDigestResult.referenceDigest: object expected'
          );
        message.referenceDigest = $root.polycentric.Digest.fromObject(
          object.referenceDigest
        );
      }
      if (object.calculatedDigest != null) {
        if (typeof object.calculatedDigest !== 'object')
          throw TypeError(
            '.polycentric.DebugReferenceDigestResult.calculatedDigest: object expected'
          );
        message.calculatedDigest = $root.polycentric.Digest.fromObject(
          object.calculatedDigest
        );
      }
      if (object.digestsMatch != null)
        message.digestsMatch = Boolean(object.digestsMatch);
      if (object.error != null) message.error = String(object.error);
      if (object.referenceBytesLength != null)
        message.referenceBytesLength = object.referenceBytesLength >>> 0;
      if (object.eventBytesLength != null)
        message.eventBytesLength = object.eventBytesLength >>> 0;
      if (object.pointerSystemPresent != null)
        message.pointerSystemPresent = Boolean(object.pointerSystemPresent);
      if (object.pointerProcessPresent != null)
        message.pointerProcessPresent = Boolean(object.pointerProcessPresent);
      if (object.pointerLogicalClock != null)
        if ($util.Long)
          (message.pointerLogicalClock = $util.Long.fromValue(
            object.pointerLogicalClock
          )).unsigned = true;
        else if (typeof object.pointerLogicalClock === 'string')
          message.pointerLogicalClock = parseInt(
            object.pointerLogicalClock,
            10
          );
        else if (typeof object.pointerLogicalClock === 'number')
          message.pointerLogicalClock = object.pointerLogicalClock;
        else if (typeof object.pointerLogicalClock === 'object')
          message.pointerLogicalClock = new $util.LongBits(
            object.pointerLogicalClock.low >>> 0,
            object.pointerLogicalClock.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a DebugReferenceDigestResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {polycentric.DebugReferenceDigestResult} message DebugReferenceDigestResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    DebugReferenceDigestResult.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.success = false;
        object.pointerParsed = false;
        object.hasDigest = false;
        object.calculatedDigest = null;
        object.digestsMatch = false;
        object.error = '';
        object.referenceBytesLength = 0;
        object.eventBytesLength = 0;
        object.pointerSystemPresent = false;
        object.pointerProcessPresent = false;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.pointerLogicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.pointerLogicalClock = options.longs === String ? '0' : 0;
      }
      if (message.success != null && message.hasOwnProperty('success'))
        object.success = message.success;
      if (
        message.pointerParsed != null &&
        message.hasOwnProperty('pointerParsed')
      )
        object.pointerParsed = message.pointerParsed;
      if (message.hasDigest != null && message.hasOwnProperty('hasDigest'))
        object.hasDigest = message.hasDigest;
      if (
        message.referenceDigest != null &&
        message.hasOwnProperty('referenceDigest')
      ) {
        object.referenceDigest = $root.polycentric.Digest.toObject(
          message.referenceDigest,
          options
        );
        if (options.oneofs) object._referenceDigest = 'referenceDigest';
      }
      if (
        message.calculatedDigest != null &&
        message.hasOwnProperty('calculatedDigest')
      )
        object.calculatedDigest = $root.polycentric.Digest.toObject(
          message.calculatedDigest,
          options
        );
      if (
        message.digestsMatch != null &&
        message.hasOwnProperty('digestsMatch')
      )
        object.digestsMatch = message.digestsMatch;
      if (message.error != null && message.hasOwnProperty('error'))
        object.error = message.error;
      if (
        message.referenceBytesLength != null &&
        message.hasOwnProperty('referenceBytesLength')
      )
        object.referenceBytesLength = message.referenceBytesLength;
      if (
        message.eventBytesLength != null &&
        message.hasOwnProperty('eventBytesLength')
      )
        object.eventBytesLength = message.eventBytesLength;
      if (
        message.pointerSystemPresent != null &&
        message.hasOwnProperty('pointerSystemPresent')
      )
        object.pointerSystemPresent = message.pointerSystemPresent;
      if (
        message.pointerProcessPresent != null &&
        message.hasOwnProperty('pointerProcessPresent')
      )
        object.pointerProcessPresent = message.pointerProcessPresent;
      if (
        message.pointerLogicalClock != null &&
        message.hasOwnProperty('pointerLogicalClock')
      )
        if (typeof message.pointerLogicalClock === 'number')
          object.pointerLogicalClock =
            options.longs === String
              ? String(message.pointerLogicalClock)
              : message.pointerLogicalClock;
        else
          object.pointerLogicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.pointerLogicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.pointerLogicalClock.low >>> 0,
                    message.pointerLogicalClock.high >>> 0
                  ).toNumber(true)
                : message.pointerLogicalClock;
      return object;
    };

    /**
     * Converts this DebugReferenceDigestResult to JSON.
     * @function toJSON
     * @memberof polycentric.DebugReferenceDigestResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    DebugReferenceDigestResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for DebugReferenceDigestResult
     * @function getTypeUrl
     * @memberof polycentric.DebugReferenceDigestResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    DebugReferenceDigestResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.DebugReferenceDigestResult';
    };

    return DebugReferenceDigestResult;
  })();

  polycentric.Server = (function () {
    /**
     * Properties of a Server.
     * @memberof polycentric
     * @interface IServer
     * @property {string|null} [server] Server server
     */

    /**
     * Constructs a new Server.
     * @memberof polycentric
     * @classdesc Represents a Server.
     * @implements IServer
     * @constructor
     * @param {polycentric.IServer=} [properties] Properties to set
     */
    function Server(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Server server.
     * @member {string} server
     * @memberof polycentric.Server
     * @instance
     */
    Server.prototype.server = '';

    /**
     * Creates a new Server instance using the specified properties.
     * @function create
     * @memberof polycentric.Server
     * @static
     * @param {polycentric.IServer=} [properties] Properties to set
     * @returns {polycentric.Server} Server instance
     */
    Server.create = function create(properties) {
      return new Server(properties);
    };

    /**
     * Encodes the specified Server message. Does not implicitly {@link polycentric.Server.verify|verify} messages.
     * @function encode
     * @memberof polycentric.Server
     * @static
     * @param {polycentric.IServer} message Server message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Server.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.server != null &&
        Object.hasOwnProperty.call(message, 'server')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.server);
      return writer;
    };

    /**
     * Encodes the specified Server message, length delimited. Does not implicitly {@link polycentric.Server.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.Server
     * @static
     * @param {polycentric.IServer} message Server message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Server.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Server message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.Server
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.Server} Server
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Server.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.Server();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.server = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Server message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.Server
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.Server} Server
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Server.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Server message.
     * @function verify
     * @memberof polycentric.Server
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Server.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.server != null && message.hasOwnProperty('server'))
        if (!$util.isString(message.server)) return 'server: string expected';
      return null;
    };

    /**
     * Creates a Server message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.Server
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.Server} Server
     */
    Server.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.Server) return object;
      let message = new $root.polycentric.Server();
      if (object.server != null) message.server = String(object.server);
      return message;
    };

    /**
     * Creates a plain object from a Server message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.Server
     * @static
     * @param {polycentric.Server} message Server
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Server.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) object.server = '';
      if (message.server != null && message.hasOwnProperty('server'))
        object.server = message.server;
      return object;
    };

    /**
     * Converts this Server to JSON.
     * @function toJSON
     * @memberof polycentric.Server
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Server.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Server
     * @function getTypeUrl
     * @memberof polycentric.Server
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Server.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.Server';
    };

    return Server;
  })();

  polycentric.AggregationBucket = (function () {
    /**
     * Properties of an AggregationBucket.
     * @memberof polycentric
     * @interface IAggregationBucket
     * @property {Uint8Array|null} [key] AggregationBucket key
     * @property {number|Long|null} [value] AggregationBucket value
     */

    /**
     * Constructs a new AggregationBucket.
     * @memberof polycentric
     * @classdesc Represents an AggregationBucket.
     * @implements IAggregationBucket
     * @constructor
     * @param {polycentric.IAggregationBucket=} [properties] Properties to set
     */
    function AggregationBucket(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * AggregationBucket key.
     * @member {Uint8Array} key
     * @memberof polycentric.AggregationBucket
     * @instance
     */
    AggregationBucket.prototype.key = $util.newBuffer([]);

    /**
     * AggregationBucket value.
     * @member {number|Long} value
     * @memberof polycentric.AggregationBucket
     * @instance
     */
    AggregationBucket.prototype.value = $util.Long
      ? $util.Long.fromBits(0, 0, false)
      : 0;

    /**
     * Creates a new AggregationBucket instance using the specified properties.
     * @function create
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {polycentric.IAggregationBucket=} [properties] Properties to set
     * @returns {polycentric.AggregationBucket} AggregationBucket instance
     */
    AggregationBucket.create = function create(properties) {
      return new AggregationBucket(properties);
    };

    /**
     * Encodes the specified AggregationBucket message. Does not implicitly {@link polycentric.AggregationBucket.verify|verify} messages.
     * @function encode
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {polycentric.IAggregationBucket} message AggregationBucket message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AggregationBucket.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.key != null && Object.hasOwnProperty.call(message, 'key'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.key);
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int64(message.value);
      return writer;
    };

    /**
     * Encodes the specified AggregationBucket message, length delimited. Does not implicitly {@link polycentric.AggregationBucket.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {polycentric.IAggregationBucket} message AggregationBucket message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    AggregationBucket.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an AggregationBucket message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.AggregationBucket} AggregationBucket
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AggregationBucket.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.AggregationBucket();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.key = reader.bytes();
            break;
          }
          case 2: {
            message.value = reader.int64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an AggregationBucket message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.AggregationBucket} AggregationBucket
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    AggregationBucket.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an AggregationBucket message.
     * @function verify
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    AggregationBucket.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.key != null && message.hasOwnProperty('key'))
        if (
          !(
            (message.key && typeof message.key.length === 'number') ||
            $util.isString(message.key)
          )
        )
          return 'key: buffer expected';
      if (message.value != null && message.hasOwnProperty('value'))
        if (
          !$util.isInteger(message.value) &&
          !(
            message.value &&
            $util.isInteger(message.value.low) &&
            $util.isInteger(message.value.high)
          )
        )
          return 'value: integer|Long expected';
      return null;
    };

    /**
     * Creates an AggregationBucket message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.AggregationBucket} AggregationBucket
     */
    AggregationBucket.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.AggregationBucket) return object;
      let message = new $root.polycentric.AggregationBucket();
      if (object.key != null)
        if (typeof object.key === 'string')
          $util.base64.decode(
            object.key,
            (message.key = $util.newBuffer($util.base64.length(object.key))),
            0
          );
        else if (object.key.length >= 0) message.key = object.key;
      if (object.value != null)
        if ($util.Long)
          (message.value = $util.Long.fromValue(object.value)).unsigned = false;
        else if (typeof object.value === 'string')
          message.value = parseInt(object.value, 10);
        else if (typeof object.value === 'number') message.value = object.value;
        else if (typeof object.value === 'object')
          message.value = new $util.LongBits(
            object.value.low >>> 0,
            object.value.high >>> 0
          ).toNumber();
      return message;
    };

    /**
     * Creates a plain object from an AggregationBucket message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {polycentric.AggregationBucket} message AggregationBucket
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    AggregationBucket.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if (options.bytes === String) object.key = '';
        else {
          object.key = [];
          if (options.bytes !== Array) object.key = $util.newBuffer(object.key);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, false);
          object.value =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.value = options.longs === String ? '0' : 0;
      }
      if (message.key != null && message.hasOwnProperty('key'))
        object.key =
          options.bytes === String
            ? $util.base64.encode(message.key, 0, message.key.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.key)
              : message.key;
      if (message.value != null && message.hasOwnProperty('value'))
        if (typeof message.value === 'number')
          object.value =
            options.longs === String ? String(message.value) : message.value;
        else
          object.value =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.value)
              : options.longs === Number
                ? new $util.LongBits(
                    message.value.low >>> 0,
                    message.value.high >>> 0
                  ).toNumber()
                : message.value;
      return object;
    };

    /**
     * Converts this AggregationBucket to JSON.
     * @function toJSON
     * @memberof polycentric.AggregationBucket
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    AggregationBucket.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for AggregationBucket
     * @function getTypeUrl
     * @memberof polycentric.AggregationBucket
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    AggregationBucket.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.AggregationBucket';
    };

    return AggregationBucket;
  })();

  polycentric.PublicKeys = (function () {
    /**
     * Properties of a PublicKeys.
     * @memberof polycentric
     * @interface IPublicKeys
     * @property {Array.<polycentric.IPublicKey>|null} [systems] PublicKeys systems
     */

    /**
     * Constructs a new PublicKeys.
     * @memberof polycentric
     * @classdesc Represents a PublicKeys.
     * @implements IPublicKeys
     * @constructor
     * @param {polycentric.IPublicKeys=} [properties] Properties to set
     */
    function PublicKeys(properties) {
      this.systems = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * PublicKeys systems.
     * @member {Array.<polycentric.IPublicKey>} systems
     * @memberof polycentric.PublicKeys
     * @instance
     */
    PublicKeys.prototype.systems = $util.emptyArray;

    /**
     * Creates a new PublicKeys instance using the specified properties.
     * @function create
     * @memberof polycentric.PublicKeys
     * @static
     * @param {polycentric.IPublicKeys=} [properties] Properties to set
     * @returns {polycentric.PublicKeys} PublicKeys instance
     */
    PublicKeys.create = function create(properties) {
      return new PublicKeys(properties);
    };

    /**
     * Encodes the specified PublicKeys message. Does not implicitly {@link polycentric.PublicKeys.verify|verify} messages.
     * @function encode
     * @memberof polycentric.PublicKeys
     * @static
     * @param {polycentric.IPublicKeys} message PublicKeys message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PublicKeys.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.systems != null && message.systems.length)
        for (let i = 0; i < message.systems.length; ++i)
          $root.polycentric.PublicKey.encode(
            message.systems[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified PublicKeys message, length delimited. Does not implicitly {@link polycentric.PublicKeys.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.PublicKeys
     * @static
     * @param {polycentric.IPublicKeys} message PublicKeys message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PublicKeys.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a PublicKeys message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.PublicKeys
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.PublicKeys} PublicKeys
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PublicKeys.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.PublicKeys();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.systems && message.systems.length))
              message.systems = [];
            message.systems.push(
              $root.polycentric.PublicKey.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a PublicKeys message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.PublicKeys
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.PublicKeys} PublicKeys
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PublicKeys.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PublicKeys message.
     * @function verify
     * @memberof polycentric.PublicKeys
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PublicKeys.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.systems != null && message.hasOwnProperty('systems')) {
        if (!Array.isArray(message.systems)) return 'systems: array expected';
        for (let i = 0; i < message.systems.length; ++i) {
          let error = $root.polycentric.PublicKey.verify(message.systems[i]);
          if (error) return 'systems.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a PublicKeys message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.PublicKeys
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.PublicKeys} PublicKeys
     */
    PublicKeys.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.PublicKeys) return object;
      let message = new $root.polycentric.PublicKeys();
      if (object.systems) {
        if (!Array.isArray(object.systems))
          throw TypeError('.polycentric.PublicKeys.systems: array expected');
        message.systems = [];
        for (let i = 0; i < object.systems.length; ++i) {
          if (typeof object.systems[i] !== 'object')
            throw TypeError('.polycentric.PublicKeys.systems: object expected');
          message.systems[i] = $root.polycentric.PublicKey.fromObject(
            object.systems[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a PublicKeys message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.PublicKeys
     * @static
     * @param {polycentric.PublicKeys} message PublicKeys
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PublicKeys.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.systems = [];
      if (message.systems && message.systems.length) {
        object.systems = [];
        for (let j = 0; j < message.systems.length; ++j)
          object.systems[j] = $root.polycentric.PublicKey.toObject(
            message.systems[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this PublicKeys to JSON.
     * @function toJSON
     * @memberof polycentric.PublicKeys
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PublicKeys.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for PublicKeys
     * @function getTypeUrl
     * @memberof polycentric.PublicKeys
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    PublicKeys.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.PublicKeys';
    };

    return PublicKeys;
  })();

  polycentric.KeyPair = (function () {
    /**
     * Properties of a KeyPair.
     * @memberof polycentric
     * @interface IKeyPair
     * @property {number|Long|null} [keyType] KeyPair keyType
     * @property {Uint8Array|null} [privateKey] KeyPair privateKey
     * @property {Uint8Array|null} [publicKey] KeyPair publicKey
     */

    /**
     * Constructs a new KeyPair.
     * @memberof polycentric
     * @classdesc Represents a KeyPair.
     * @implements IKeyPair
     * @constructor
     * @param {polycentric.IKeyPair=} [properties] Properties to set
     */
    function KeyPair(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * KeyPair keyType.
     * @member {number|Long} keyType
     * @memberof polycentric.KeyPair
     * @instance
     */
    KeyPair.prototype.keyType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * KeyPair privateKey.
     * @member {Uint8Array} privateKey
     * @memberof polycentric.KeyPair
     * @instance
     */
    KeyPair.prototype.privateKey = $util.newBuffer([]);

    /**
     * KeyPair publicKey.
     * @member {Uint8Array} publicKey
     * @memberof polycentric.KeyPair
     * @instance
     */
    KeyPair.prototype.publicKey = $util.newBuffer([]);

    /**
     * Creates a new KeyPair instance using the specified properties.
     * @function create
     * @memberof polycentric.KeyPair
     * @static
     * @param {polycentric.IKeyPair=} [properties] Properties to set
     * @returns {polycentric.KeyPair} KeyPair instance
     */
    KeyPair.create = function create(properties) {
      return new KeyPair(properties);
    };

    /**
     * Encodes the specified KeyPair message. Does not implicitly {@link polycentric.KeyPair.verify|verify} messages.
     * @function encode
     * @memberof polycentric.KeyPair
     * @static
     * @param {polycentric.IKeyPair} message KeyPair message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    KeyPair.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.keyType != null &&
        Object.hasOwnProperty.call(message, 'keyType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.keyType);
      if (
        message.privateKey != null &&
        Object.hasOwnProperty.call(message, 'privateKey')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.privateKey);
      if (
        message.publicKey != null &&
        Object.hasOwnProperty.call(message, 'publicKey')
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.publicKey);
      return writer;
    };

    /**
     * Encodes the specified KeyPair message, length delimited. Does not implicitly {@link polycentric.KeyPair.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.KeyPair
     * @static
     * @param {polycentric.IKeyPair} message KeyPair message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    KeyPair.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a KeyPair message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.KeyPair
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.KeyPair} KeyPair
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    KeyPair.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.KeyPair();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.keyType = reader.uint64();
            break;
          }
          case 2: {
            message.privateKey = reader.bytes();
            break;
          }
          case 3: {
            message.publicKey = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a KeyPair message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.KeyPair
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.KeyPair} KeyPair
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    KeyPair.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a KeyPair message.
     * @function verify
     * @memberof polycentric.KeyPair
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    KeyPair.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.keyType != null && message.hasOwnProperty('keyType'))
        if (
          !$util.isInteger(message.keyType) &&
          !(
            message.keyType &&
            $util.isInteger(message.keyType.low) &&
            $util.isInteger(message.keyType.high)
          )
        )
          return 'keyType: integer|Long expected';
      if (message.privateKey != null && message.hasOwnProperty('privateKey'))
        if (
          !(
            (message.privateKey &&
              typeof message.privateKey.length === 'number') ||
            $util.isString(message.privateKey)
          )
        )
          return 'privateKey: buffer expected';
      if (message.publicKey != null && message.hasOwnProperty('publicKey'))
        if (
          !(
            (message.publicKey &&
              typeof message.publicKey.length === 'number') ||
            $util.isString(message.publicKey)
          )
        )
          return 'publicKey: buffer expected';
      return null;
    };

    /**
     * Creates a KeyPair message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.KeyPair
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.KeyPair} KeyPair
     */
    KeyPair.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.KeyPair) return object;
      let message = new $root.polycentric.KeyPair();
      if (object.keyType != null)
        if ($util.Long)
          (message.keyType = $util.Long.fromValue(object.keyType)).unsigned =
            true;
        else if (typeof object.keyType === 'string')
          message.keyType = parseInt(object.keyType, 10);
        else if (typeof object.keyType === 'number')
          message.keyType = object.keyType;
        else if (typeof object.keyType === 'object')
          message.keyType = new $util.LongBits(
            object.keyType.low >>> 0,
            object.keyType.high >>> 0
          ).toNumber(true);
      if (object.privateKey != null)
        if (typeof object.privateKey === 'string')
          $util.base64.decode(
            object.privateKey,
            (message.privateKey = $util.newBuffer(
              $util.base64.length(object.privateKey)
            )),
            0
          );
        else if (object.privateKey.length >= 0)
          message.privateKey = object.privateKey;
      if (object.publicKey != null)
        if (typeof object.publicKey === 'string')
          $util.base64.decode(
            object.publicKey,
            (message.publicKey = $util.newBuffer(
              $util.base64.length(object.publicKey)
            )),
            0
          );
        else if (object.publicKey.length >= 0)
          message.publicKey = object.publicKey;
      return message;
    };

    /**
     * Creates a plain object from a KeyPair message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.KeyPair
     * @static
     * @param {polycentric.KeyPair} message KeyPair
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    KeyPair.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.keyType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.keyType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.privateKey = '';
        else {
          object.privateKey = [];
          if (options.bytes !== Array)
            object.privateKey = $util.newBuffer(object.privateKey);
        }
        if (options.bytes === String) object.publicKey = '';
        else {
          object.publicKey = [];
          if (options.bytes !== Array)
            object.publicKey = $util.newBuffer(object.publicKey);
        }
      }
      if (message.keyType != null && message.hasOwnProperty('keyType'))
        if (typeof message.keyType === 'number')
          object.keyType =
            options.longs === String
              ? String(message.keyType)
              : message.keyType;
        else
          object.keyType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.keyType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.keyType.low >>> 0,
                    message.keyType.high >>> 0
                  ).toNumber(true)
                : message.keyType;
      if (message.privateKey != null && message.hasOwnProperty('privateKey'))
        object.privateKey =
          options.bytes === String
            ? $util.base64.encode(
                message.privateKey,
                0,
                message.privateKey.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.privateKey)
              : message.privateKey;
      if (message.publicKey != null && message.hasOwnProperty('publicKey'))
        object.publicKey =
          options.bytes === String
            ? $util.base64.encode(
                message.publicKey,
                0,
                message.publicKey.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.publicKey)
              : message.publicKey;
      return object;
    };

    /**
     * Converts this KeyPair to JSON.
     * @function toJSON
     * @memberof polycentric.KeyPair
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    KeyPair.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for KeyPair
     * @function getTypeUrl
     * @memberof polycentric.KeyPair
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    KeyPair.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.KeyPair';
    };

    return KeyPair;
  })();

  polycentric.ResultEventsAndRelatedEventsAndCursor = (function () {
    /**
     * Properties of a ResultEventsAndRelatedEventsAndCursor.
     * @memberof polycentric
     * @interface IResultEventsAndRelatedEventsAndCursor
     * @property {polycentric.IEvents|null} [resultEvents] ResultEventsAndRelatedEventsAndCursor resultEvents
     * @property {polycentric.IEvents|null} [relatedEvents] ResultEventsAndRelatedEventsAndCursor relatedEvents
     * @property {Uint8Array|null} [cursor] ResultEventsAndRelatedEventsAndCursor cursor
     */

    /**
     * Constructs a new ResultEventsAndRelatedEventsAndCursor.
     * @memberof polycentric
     * @classdesc Represents a ResultEventsAndRelatedEventsAndCursor.
     * @implements IResultEventsAndRelatedEventsAndCursor
     * @constructor
     * @param {polycentric.IResultEventsAndRelatedEventsAndCursor=} [properties] Properties to set
     */
    function ResultEventsAndRelatedEventsAndCursor(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ResultEventsAndRelatedEventsAndCursor resultEvents.
     * @member {polycentric.IEvents|null|undefined} resultEvents
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @instance
     */
    ResultEventsAndRelatedEventsAndCursor.prototype.resultEvents = null;

    /**
     * ResultEventsAndRelatedEventsAndCursor relatedEvents.
     * @member {polycentric.IEvents|null|undefined} relatedEvents
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @instance
     */
    ResultEventsAndRelatedEventsAndCursor.prototype.relatedEvents = null;

    /**
     * ResultEventsAndRelatedEventsAndCursor cursor.
     * @member {Uint8Array|null|undefined} cursor
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @instance
     */
    ResultEventsAndRelatedEventsAndCursor.prototype.cursor = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(
      ResultEventsAndRelatedEventsAndCursor.prototype,
      '_cursor',
      {
        get: $util.oneOfGetter(($oneOfFields = ['cursor'])),
        set: $util.oneOfSetter($oneOfFields),
      }
    );

    /**
     * Creates a new ResultEventsAndRelatedEventsAndCursor instance using the specified properties.
     * @function create
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {polycentric.IResultEventsAndRelatedEventsAndCursor=} [properties] Properties to set
     * @returns {polycentric.ResultEventsAndRelatedEventsAndCursor} ResultEventsAndRelatedEventsAndCursor instance
     */
    ResultEventsAndRelatedEventsAndCursor.create = function create(properties) {
      return new ResultEventsAndRelatedEventsAndCursor(properties);
    };

    /**
     * Encodes the specified ResultEventsAndRelatedEventsAndCursor message. Does not implicitly {@link polycentric.ResultEventsAndRelatedEventsAndCursor.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {polycentric.IResultEventsAndRelatedEventsAndCursor} message ResultEventsAndRelatedEventsAndCursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResultEventsAndRelatedEventsAndCursor.encode = function encode(
      message,
      writer
    ) {
      if (!writer) writer = $Writer.create();
      if (
        message.resultEvents != null &&
        Object.hasOwnProperty.call(message, 'resultEvents')
      )
        $root.polycentric.Events.encode(
          message.resultEvents,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.relatedEvents != null &&
        Object.hasOwnProperty.call(message, 'relatedEvents')
      )
        $root.polycentric.Events.encode(
          message.relatedEvents,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.cursor);
      return writer;
    };

    /**
     * Encodes the specified ResultEventsAndRelatedEventsAndCursor message, length delimited. Does not implicitly {@link polycentric.ResultEventsAndRelatedEventsAndCursor.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {polycentric.IResultEventsAndRelatedEventsAndCursor} message ResultEventsAndRelatedEventsAndCursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResultEventsAndRelatedEventsAndCursor.encodeDelimited =
      function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
      };

    /**
     * Decodes a ResultEventsAndRelatedEventsAndCursor message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ResultEventsAndRelatedEventsAndCursor} ResultEventsAndRelatedEventsAndCursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResultEventsAndRelatedEventsAndCursor.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ResultEventsAndRelatedEventsAndCursor();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.resultEvents = $root.polycentric.Events.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.relatedEvents = $root.polycentric.Events.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            message.cursor = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ResultEventsAndRelatedEventsAndCursor message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ResultEventsAndRelatedEventsAndCursor} ResultEventsAndRelatedEventsAndCursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResultEventsAndRelatedEventsAndCursor.decodeDelimited =
      function decodeDelimited(reader) {
        if (!(reader instanceof $Reader)) reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };

    /**
     * Verifies a ResultEventsAndRelatedEventsAndCursor message.
     * @function verify
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ResultEventsAndRelatedEventsAndCursor.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (
        message.resultEvents != null &&
        message.hasOwnProperty('resultEvents')
      ) {
        let error = $root.polycentric.Events.verify(message.resultEvents);
        if (error) return 'resultEvents.' + error;
      }
      if (
        message.relatedEvents != null &&
        message.hasOwnProperty('relatedEvents')
      ) {
        let error = $root.polycentric.Events.verify(message.relatedEvents);
        if (error) return 'relatedEvents.' + error;
      }
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        properties._cursor = 1;
        if (
          !(
            (message.cursor && typeof message.cursor.length === 'number') ||
            $util.isString(message.cursor)
          )
        )
          return 'cursor: buffer expected';
      }
      return null;
    };

    /**
     * Creates a ResultEventsAndRelatedEventsAndCursor message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ResultEventsAndRelatedEventsAndCursor} ResultEventsAndRelatedEventsAndCursor
     */
    ResultEventsAndRelatedEventsAndCursor.fromObject = function fromObject(
      object
    ) {
      if (
        object instanceof
        $root.polycentric.ResultEventsAndRelatedEventsAndCursor
      )
        return object;
      let message =
        new $root.polycentric.ResultEventsAndRelatedEventsAndCursor();
      if (object.resultEvents != null) {
        if (typeof object.resultEvents !== 'object')
          throw TypeError(
            '.polycentric.ResultEventsAndRelatedEventsAndCursor.resultEvents: object expected'
          );
        message.resultEvents = $root.polycentric.Events.fromObject(
          object.resultEvents
        );
      }
      if (object.relatedEvents != null) {
        if (typeof object.relatedEvents !== 'object')
          throw TypeError(
            '.polycentric.ResultEventsAndRelatedEventsAndCursor.relatedEvents: object expected'
          );
        message.relatedEvents = $root.polycentric.Events.fromObject(
          object.relatedEvents
        );
      }
      if (object.cursor != null)
        if (typeof object.cursor === 'string')
          $util.base64.decode(
            object.cursor,
            (message.cursor = $util.newBuffer(
              $util.base64.length(object.cursor)
            )),
            0
          );
        else if (object.cursor.length >= 0) message.cursor = object.cursor;
      return message;
    };

    /**
     * Creates a plain object from a ResultEventsAndRelatedEventsAndCursor message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {polycentric.ResultEventsAndRelatedEventsAndCursor} message ResultEventsAndRelatedEventsAndCursor
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ResultEventsAndRelatedEventsAndCursor.toObject = function toObject(
      message,
      options
    ) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.resultEvents = null;
        object.relatedEvents = null;
      }
      if (
        message.resultEvents != null &&
        message.hasOwnProperty('resultEvents')
      )
        object.resultEvents = $root.polycentric.Events.toObject(
          message.resultEvents,
          options
        );
      if (
        message.relatedEvents != null &&
        message.hasOwnProperty('relatedEvents')
      )
        object.relatedEvents = $root.polycentric.Events.toObject(
          message.relatedEvents,
          options
        );
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        object.cursor =
          options.bytes === String
            ? $util.base64.encode(message.cursor, 0, message.cursor.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.cursor)
              : message.cursor;
        if (options.oneofs) object._cursor = 'cursor';
      }
      return object;
    };

    /**
     * Converts this ResultEventsAndRelatedEventsAndCursor to JSON.
     * @function toJSON
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ResultEventsAndRelatedEventsAndCursor.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ResultEventsAndRelatedEventsAndCursor
     * @function getTypeUrl
     * @memberof polycentric.ResultEventsAndRelatedEventsAndCursor
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ResultEventsAndRelatedEventsAndCursor.getTypeUrl = function getTypeUrl(
      typeUrlPrefix
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return (
        typeUrlPrefix + '/polycentric.ResultEventsAndRelatedEventsAndCursor'
      );
    };

    return ResultEventsAndRelatedEventsAndCursor;
  })();

  polycentric.ResultTopStringReferences = (function () {
    /**
     * Properties of a ResultTopStringReferences.
     * @memberof polycentric
     * @interface IResultTopStringReferences
     * @property {Array.<polycentric.IAggregationBucket>|null} [buckets] ResultTopStringReferences buckets
     */

    /**
     * Constructs a new ResultTopStringReferences.
     * @memberof polycentric
     * @classdesc Represents a ResultTopStringReferences.
     * @implements IResultTopStringReferences
     * @constructor
     * @param {polycentric.IResultTopStringReferences=} [properties] Properties to set
     */
    function ResultTopStringReferences(properties) {
      this.buckets = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ResultTopStringReferences buckets.
     * @member {Array.<polycentric.IAggregationBucket>} buckets
     * @memberof polycentric.ResultTopStringReferences
     * @instance
     */
    ResultTopStringReferences.prototype.buckets = $util.emptyArray;

    /**
     * Creates a new ResultTopStringReferences instance using the specified properties.
     * @function create
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {polycentric.IResultTopStringReferences=} [properties] Properties to set
     * @returns {polycentric.ResultTopStringReferences} ResultTopStringReferences instance
     */
    ResultTopStringReferences.create = function create(properties) {
      return new ResultTopStringReferences(properties);
    };

    /**
     * Encodes the specified ResultTopStringReferences message. Does not implicitly {@link polycentric.ResultTopStringReferences.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {polycentric.IResultTopStringReferences} message ResultTopStringReferences message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResultTopStringReferences.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.buckets != null && message.buckets.length)
        for (let i = 0; i < message.buckets.length; ++i)
          $root.polycentric.AggregationBucket.encode(
            message.buckets[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified ResultTopStringReferences message, length delimited. Does not implicitly {@link polycentric.ResultTopStringReferences.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {polycentric.IResultTopStringReferences} message ResultTopStringReferences message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResultTopStringReferences.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ResultTopStringReferences message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ResultTopStringReferences} ResultTopStringReferences
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResultTopStringReferences.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ResultTopStringReferences();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.buckets && message.buckets.length))
              message.buckets = [];
            message.buckets.push(
              $root.polycentric.AggregationBucket.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ResultTopStringReferences message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ResultTopStringReferences} ResultTopStringReferences
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResultTopStringReferences.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ResultTopStringReferences message.
     * @function verify
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ResultTopStringReferences.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.buckets != null && message.hasOwnProperty('buckets')) {
        if (!Array.isArray(message.buckets)) return 'buckets: array expected';
        for (let i = 0; i < message.buckets.length; ++i) {
          let error = $root.polycentric.AggregationBucket.verify(
            message.buckets[i]
          );
          if (error) return 'buckets.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a ResultTopStringReferences message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ResultTopStringReferences} ResultTopStringReferences
     */
    ResultTopStringReferences.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ResultTopStringReferences)
        return object;
      let message = new $root.polycentric.ResultTopStringReferences();
      if (object.buckets) {
        if (!Array.isArray(object.buckets))
          throw TypeError(
            '.polycentric.ResultTopStringReferences.buckets: array expected'
          );
        message.buckets = [];
        for (let i = 0; i < object.buckets.length; ++i) {
          if (typeof object.buckets[i] !== 'object')
            throw TypeError(
              '.polycentric.ResultTopStringReferences.buckets: object expected'
            );
          message.buckets[i] = $root.polycentric.AggregationBucket.fromObject(
            object.buckets[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a ResultTopStringReferences message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {polycentric.ResultTopStringReferences} message ResultTopStringReferences
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ResultTopStringReferences.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.buckets = [];
      if (message.buckets && message.buckets.length) {
        object.buckets = [];
        for (let j = 0; j < message.buckets.length; ++j)
          object.buckets[j] = $root.polycentric.AggregationBucket.toObject(
            message.buckets[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this ResultTopStringReferences to JSON.
     * @function toJSON
     * @memberof polycentric.ResultTopStringReferences
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ResultTopStringReferences.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ResultTopStringReferences
     * @function getTypeUrl
     * @memberof polycentric.ResultTopStringReferences
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ResultTopStringReferences.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ResultTopStringReferences';
    };

    return ResultTopStringReferences;
  })();

  polycentric.StorageTypeCRDTSetItem = (function () {
    /**
     * Properties of a StorageTypeCRDTSetItem.
     * @memberof polycentric
     * @interface IStorageTypeCRDTSetItem
     * @property {number|Long|null} [contentType] StorageTypeCRDTSetItem contentType
     * @property {Uint8Array|null} [value] StorageTypeCRDTSetItem value
     * @property {number|Long|null} [unixMilliseconds] StorageTypeCRDTSetItem unixMilliseconds
     * @property {polycentric.LWWElementSet.Operation|null} [operation] StorageTypeCRDTSetItem operation
     */

    /**
     * Constructs a new StorageTypeCRDTSetItem.
     * @memberof polycentric
     * @classdesc Represents a StorageTypeCRDTSetItem.
     * @implements IStorageTypeCRDTSetItem
     * @constructor
     * @param {polycentric.IStorageTypeCRDTSetItem=} [properties] Properties to set
     */
    function StorageTypeCRDTSetItem(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * StorageTypeCRDTSetItem contentType.
     * @member {number|Long} contentType
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @instance
     */
    StorageTypeCRDTSetItem.prototype.contentType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * StorageTypeCRDTSetItem value.
     * @member {Uint8Array} value
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @instance
     */
    StorageTypeCRDTSetItem.prototype.value = $util.newBuffer([]);

    /**
     * StorageTypeCRDTSetItem unixMilliseconds.
     * @member {number|Long} unixMilliseconds
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @instance
     */
    StorageTypeCRDTSetItem.prototype.unixMilliseconds = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * StorageTypeCRDTSetItem operation.
     * @member {polycentric.LWWElementSet.Operation} operation
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @instance
     */
    StorageTypeCRDTSetItem.prototype.operation = 0;

    /**
     * Creates a new StorageTypeCRDTSetItem instance using the specified properties.
     * @function create
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {polycentric.IStorageTypeCRDTSetItem=} [properties] Properties to set
     * @returns {polycentric.StorageTypeCRDTSetItem} StorageTypeCRDTSetItem instance
     */
    StorageTypeCRDTSetItem.create = function create(properties) {
      return new StorageTypeCRDTSetItem(properties);
    };

    /**
     * Encodes the specified StorageTypeCRDTSetItem message. Does not implicitly {@link polycentric.StorageTypeCRDTSetItem.verify|verify} messages.
     * @function encode
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {polycentric.IStorageTypeCRDTSetItem} message StorageTypeCRDTSetItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeCRDTSetItem.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.contentType != null &&
        Object.hasOwnProperty.call(message, 'contentType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.contentType);
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.value);
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 3, wireType 0 =*/ 24)
          .uint64(message.unixMilliseconds);
      if (
        message.operation != null &&
        Object.hasOwnProperty.call(message, 'operation')
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).int32(message.operation);
      return writer;
    };

    /**
     * Encodes the specified StorageTypeCRDTSetItem message, length delimited. Does not implicitly {@link polycentric.StorageTypeCRDTSetItem.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {polycentric.IStorageTypeCRDTSetItem} message StorageTypeCRDTSetItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeCRDTSetItem.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a StorageTypeCRDTSetItem message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.StorageTypeCRDTSetItem} StorageTypeCRDTSetItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeCRDTSetItem.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.StorageTypeCRDTSetItem();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.contentType = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.bytes();
            break;
          }
          case 3: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          case 4: {
            message.operation = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a StorageTypeCRDTSetItem message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.StorageTypeCRDTSetItem} StorageTypeCRDTSetItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeCRDTSetItem.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StorageTypeCRDTSetItem message.
     * @function verify
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StorageTypeCRDTSetItem.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        if (
          !$util.isInteger(message.contentType) &&
          !(
            message.contentType &&
            $util.isInteger(message.contentType.low) &&
            $util.isInteger(message.contentType.high)
          )
        )
          return 'contentType: integer|Long expected';
      if (message.value != null && message.hasOwnProperty('value'))
        if (
          !(
            (message.value && typeof message.value.length === 'number') ||
            $util.isString(message.value)
          )
        )
          return 'value: buffer expected';
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      if (message.operation != null && message.hasOwnProperty('operation'))
        switch (message.operation) {
          default:
            return 'operation: enum value expected';
          case 0:
          case 1:
            break;
        }
      return null;
    };

    /**
     * Creates a StorageTypeCRDTSetItem message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.StorageTypeCRDTSetItem} StorageTypeCRDTSetItem
     */
    StorageTypeCRDTSetItem.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.StorageTypeCRDTSetItem)
        return object;
      let message = new $root.polycentric.StorageTypeCRDTSetItem();
      if (object.contentType != null)
        if ($util.Long)
          (message.contentType = $util.Long.fromValue(
            object.contentType
          )).unsigned = true;
        else if (typeof object.contentType === 'string')
          message.contentType = parseInt(object.contentType, 10);
        else if (typeof object.contentType === 'number')
          message.contentType = object.contentType;
        else if (typeof object.contentType === 'object')
          message.contentType = new $util.LongBits(
            object.contentType.low >>> 0,
            object.contentType.high >>> 0
          ).toNumber(true);
      if (object.value != null)
        if (typeof object.value === 'string')
          $util.base64.decode(
            object.value,
            (message.value = $util.newBuffer(
              $util.base64.length(object.value)
            )),
            0
          );
        else if (object.value.length >= 0) message.value = object.value;
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      switch (object.operation) {
        default:
          if (typeof object.operation === 'number') {
            message.operation = object.operation;
            break;
          }
          break;
        case 'ADD':
        case 0:
          message.operation = 0;
          break;
        case 'REMOVE':
        case 1:
          message.operation = 1;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a StorageTypeCRDTSetItem message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {polycentric.StorageTypeCRDTSetItem} message StorageTypeCRDTSetItem
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StorageTypeCRDTSetItem.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.contentType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.contentType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.value = '';
        else {
          object.value = [];
          if (options.bytes !== Array)
            object.value = $util.newBuffer(object.value);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.unixMilliseconds =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.unixMilliseconds = options.longs === String ? '0' : 0;
        object.operation = options.enums === String ? 'ADD' : 0;
      }
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        if (typeof message.contentType === 'number')
          object.contentType =
            options.longs === String
              ? String(message.contentType)
              : message.contentType;
        else
          object.contentType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.contentType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.contentType.low >>> 0,
                    message.contentType.high >>> 0
                  ).toNumber(true)
                : message.contentType;
      if (message.value != null && message.hasOwnProperty('value'))
        object.value =
          options.bytes === String
            ? $util.base64.encode(message.value, 0, message.value.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.value)
              : message.value;
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
      if (message.operation != null && message.hasOwnProperty('operation'))
        object.operation =
          options.enums === String
            ? $root.polycentric.LWWElementSet.Operation[message.operation] ===
              undefined
              ? message.operation
              : $root.polycentric.LWWElementSet.Operation[message.operation]
            : message.operation;
      return object;
    };

    /**
     * Converts this StorageTypeCRDTSetItem to JSON.
     * @function toJSON
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StorageTypeCRDTSetItem.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StorageTypeCRDTSetItem
     * @function getTypeUrl
     * @memberof polycentric.StorageTypeCRDTSetItem
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StorageTypeCRDTSetItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.StorageTypeCRDTSetItem';
    };

    return StorageTypeCRDTSetItem;
  })();

  polycentric.StorageTypeCRDTItem = (function () {
    /**
     * Properties of a StorageTypeCRDTItem.
     * @memberof polycentric
     * @interface IStorageTypeCRDTItem
     * @property {number|Long|null} [contentType] StorageTypeCRDTItem contentType
     * @property {Uint8Array|null} [value] StorageTypeCRDTItem value
     * @property {number|Long|null} [unixMilliseconds] StorageTypeCRDTItem unixMilliseconds
     */

    /**
     * Constructs a new StorageTypeCRDTItem.
     * @memberof polycentric
     * @classdesc Represents a StorageTypeCRDTItem.
     * @implements IStorageTypeCRDTItem
     * @constructor
     * @param {polycentric.IStorageTypeCRDTItem=} [properties] Properties to set
     */
    function StorageTypeCRDTItem(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * StorageTypeCRDTItem contentType.
     * @member {number|Long} contentType
     * @memberof polycentric.StorageTypeCRDTItem
     * @instance
     */
    StorageTypeCRDTItem.prototype.contentType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * StorageTypeCRDTItem value.
     * @member {Uint8Array} value
     * @memberof polycentric.StorageTypeCRDTItem
     * @instance
     */
    StorageTypeCRDTItem.prototype.value = $util.newBuffer([]);

    /**
     * StorageTypeCRDTItem unixMilliseconds.
     * @member {number|Long} unixMilliseconds
     * @memberof polycentric.StorageTypeCRDTItem
     * @instance
     */
    StorageTypeCRDTItem.prototype.unixMilliseconds = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new StorageTypeCRDTItem instance using the specified properties.
     * @function create
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {polycentric.IStorageTypeCRDTItem=} [properties] Properties to set
     * @returns {polycentric.StorageTypeCRDTItem} StorageTypeCRDTItem instance
     */
    StorageTypeCRDTItem.create = function create(properties) {
      return new StorageTypeCRDTItem(properties);
    };

    /**
     * Encodes the specified StorageTypeCRDTItem message. Does not implicitly {@link polycentric.StorageTypeCRDTItem.verify|verify} messages.
     * @function encode
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {polycentric.IStorageTypeCRDTItem} message StorageTypeCRDTItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeCRDTItem.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.contentType != null &&
        Object.hasOwnProperty.call(message, 'contentType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.contentType);
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.value);
      if (
        message.unixMilliseconds != null &&
        Object.hasOwnProperty.call(message, 'unixMilliseconds')
      )
        writer
          .uint32(/* id 3, wireType 0 =*/ 24)
          .uint64(message.unixMilliseconds);
      return writer;
    };

    /**
     * Encodes the specified StorageTypeCRDTItem message, length delimited. Does not implicitly {@link polycentric.StorageTypeCRDTItem.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {polycentric.IStorageTypeCRDTItem} message StorageTypeCRDTItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeCRDTItem.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a StorageTypeCRDTItem message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.StorageTypeCRDTItem} StorageTypeCRDTItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeCRDTItem.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.StorageTypeCRDTItem();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.contentType = reader.uint64();
            break;
          }
          case 2: {
            message.value = reader.bytes();
            break;
          }
          case 3: {
            message.unixMilliseconds = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a StorageTypeCRDTItem message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.StorageTypeCRDTItem} StorageTypeCRDTItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeCRDTItem.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StorageTypeCRDTItem message.
     * @function verify
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StorageTypeCRDTItem.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        if (
          !$util.isInteger(message.contentType) &&
          !(
            message.contentType &&
            $util.isInteger(message.contentType.low) &&
            $util.isInteger(message.contentType.high)
          )
        )
          return 'contentType: integer|Long expected';
      if (message.value != null && message.hasOwnProperty('value'))
        if (
          !(
            (message.value && typeof message.value.length === 'number') ||
            $util.isString(message.value)
          )
        )
          return 'value: buffer expected';
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (
          !$util.isInteger(message.unixMilliseconds) &&
          !(
            message.unixMilliseconds &&
            $util.isInteger(message.unixMilliseconds.low) &&
            $util.isInteger(message.unixMilliseconds.high)
          )
        )
          return 'unixMilliseconds: integer|Long expected';
      return null;
    };

    /**
     * Creates a StorageTypeCRDTItem message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.StorageTypeCRDTItem} StorageTypeCRDTItem
     */
    StorageTypeCRDTItem.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.StorageTypeCRDTItem)
        return object;
      let message = new $root.polycentric.StorageTypeCRDTItem();
      if (object.contentType != null)
        if ($util.Long)
          (message.contentType = $util.Long.fromValue(
            object.contentType
          )).unsigned = true;
        else if (typeof object.contentType === 'string')
          message.contentType = parseInt(object.contentType, 10);
        else if (typeof object.contentType === 'number')
          message.contentType = object.contentType;
        else if (typeof object.contentType === 'object')
          message.contentType = new $util.LongBits(
            object.contentType.low >>> 0,
            object.contentType.high >>> 0
          ).toNumber(true);
      if (object.value != null)
        if (typeof object.value === 'string')
          $util.base64.decode(
            object.value,
            (message.value = $util.newBuffer(
              $util.base64.length(object.value)
            )),
            0
          );
        else if (object.value.length >= 0) message.value = object.value;
      if (object.unixMilliseconds != null)
        if ($util.Long)
          (message.unixMilliseconds = $util.Long.fromValue(
            object.unixMilliseconds
          )).unsigned = true;
        else if (typeof object.unixMilliseconds === 'string')
          message.unixMilliseconds = parseInt(object.unixMilliseconds, 10);
        else if (typeof object.unixMilliseconds === 'number')
          message.unixMilliseconds = object.unixMilliseconds;
        else if (typeof object.unixMilliseconds === 'object')
          message.unixMilliseconds = new $util.LongBits(
            object.unixMilliseconds.low >>> 0,
            object.unixMilliseconds.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a StorageTypeCRDTItem message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {polycentric.StorageTypeCRDTItem} message StorageTypeCRDTItem
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StorageTypeCRDTItem.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.contentType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.contentType = options.longs === String ? '0' : 0;
        if (options.bytes === String) object.value = '';
        else {
          object.value = [];
          if (options.bytes !== Array)
            object.value = $util.newBuffer(object.value);
        }
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.unixMilliseconds =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.unixMilliseconds = options.longs === String ? '0' : 0;
      }
      if (message.contentType != null && message.hasOwnProperty('contentType'))
        if (typeof message.contentType === 'number')
          object.contentType =
            options.longs === String
              ? String(message.contentType)
              : message.contentType;
        else
          object.contentType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.contentType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.contentType.low >>> 0,
                    message.contentType.high >>> 0
                  ).toNumber(true)
                : message.contentType;
      if (message.value != null && message.hasOwnProperty('value'))
        object.value =
          options.bytes === String
            ? $util.base64.encode(message.value, 0, message.value.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.value)
              : message.value;
      if (
        message.unixMilliseconds != null &&
        message.hasOwnProperty('unixMilliseconds')
      )
        if (typeof message.unixMilliseconds === 'number')
          object.unixMilliseconds =
            options.longs === String
              ? String(message.unixMilliseconds)
              : message.unixMilliseconds;
        else
          object.unixMilliseconds =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.unixMilliseconds)
              : options.longs === Number
                ? new $util.LongBits(
                    message.unixMilliseconds.low >>> 0,
                    message.unixMilliseconds.high >>> 0
                  ).toNumber(true)
                : message.unixMilliseconds;
      return object;
    };

    /**
     * Converts this StorageTypeCRDTItem to JSON.
     * @function toJSON
     * @memberof polycentric.StorageTypeCRDTItem
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StorageTypeCRDTItem.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StorageTypeCRDTItem
     * @function getTypeUrl
     * @memberof polycentric.StorageTypeCRDTItem
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StorageTypeCRDTItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.StorageTypeCRDTItem';
    };

    return StorageTypeCRDTItem;
  })();

  polycentric.StorageTypeProcessState = (function () {
    /**
     * Properties of a StorageTypeProcessState.
     * @memberof polycentric
     * @interface IStorageTypeProcessState
     * @property {number|Long|null} [logicalClock] StorageTypeProcessState logicalClock
     * @property {Array.<polycentric.IRange>|null} [ranges] StorageTypeProcessState ranges
     * @property {polycentric.IIndices|null} [indices] StorageTypeProcessState indices
     */

    /**
     * Constructs a new StorageTypeProcessState.
     * @memberof polycentric
     * @classdesc Represents a StorageTypeProcessState.
     * @implements IStorageTypeProcessState
     * @constructor
     * @param {polycentric.IStorageTypeProcessState=} [properties] Properties to set
     */
    function StorageTypeProcessState(properties) {
      this.ranges = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * StorageTypeProcessState logicalClock.
     * @member {number|Long} logicalClock
     * @memberof polycentric.StorageTypeProcessState
     * @instance
     */
    StorageTypeProcessState.prototype.logicalClock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * StorageTypeProcessState ranges.
     * @member {Array.<polycentric.IRange>} ranges
     * @memberof polycentric.StorageTypeProcessState
     * @instance
     */
    StorageTypeProcessState.prototype.ranges = $util.emptyArray;

    /**
     * StorageTypeProcessState indices.
     * @member {polycentric.IIndices|null|undefined} indices
     * @memberof polycentric.StorageTypeProcessState
     * @instance
     */
    StorageTypeProcessState.prototype.indices = null;

    /**
     * Creates a new StorageTypeProcessState instance using the specified properties.
     * @function create
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {polycentric.IStorageTypeProcessState=} [properties] Properties to set
     * @returns {polycentric.StorageTypeProcessState} StorageTypeProcessState instance
     */
    StorageTypeProcessState.create = function create(properties) {
      return new StorageTypeProcessState(properties);
    };

    /**
     * Encodes the specified StorageTypeProcessState message. Does not implicitly {@link polycentric.StorageTypeProcessState.verify|verify} messages.
     * @function encode
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {polycentric.IStorageTypeProcessState} message StorageTypeProcessState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeProcessState.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.logicalClock != null &&
        Object.hasOwnProperty.call(message, 'logicalClock')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.logicalClock);
      if (message.ranges != null && message.ranges.length)
        for (let i = 0; i < message.ranges.length; ++i)
          $root.polycentric.Range.encode(
            message.ranges[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      if (
        message.indices != null &&
        Object.hasOwnProperty.call(message, 'indices')
      )
        $root.polycentric.Indices.encode(
          message.indices,
          writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified StorageTypeProcessState message, length delimited. Does not implicitly {@link polycentric.StorageTypeProcessState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {polycentric.IStorageTypeProcessState} message StorageTypeProcessState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    StorageTypeProcessState.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a StorageTypeProcessState message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.StorageTypeProcessState} StorageTypeProcessState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeProcessState.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.StorageTypeProcessState();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.logicalClock = reader.uint64();
            break;
          }
          case 2: {
            if (!(message.ranges && message.ranges.length)) message.ranges = [];
            message.ranges.push(
              $root.polycentric.Range.decode(reader, reader.uint32())
            );
            break;
          }
          case 3: {
            message.indices = $root.polycentric.Indices.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a StorageTypeProcessState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.StorageTypeProcessState} StorageTypeProcessState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    StorageTypeProcessState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a StorageTypeProcessState message.
     * @function verify
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    StorageTypeProcessState.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (
          !$util.isInteger(message.logicalClock) &&
          !(
            message.logicalClock &&
            $util.isInteger(message.logicalClock.low) &&
            $util.isInteger(message.logicalClock.high)
          )
        )
          return 'logicalClock: integer|Long expected';
      if (message.ranges != null && message.hasOwnProperty('ranges')) {
        if (!Array.isArray(message.ranges)) return 'ranges: array expected';
        for (let i = 0; i < message.ranges.length; ++i) {
          let error = $root.polycentric.Range.verify(message.ranges[i]);
          if (error) return 'ranges.' + error;
        }
      }
      if (message.indices != null && message.hasOwnProperty('indices')) {
        let error = $root.polycentric.Indices.verify(message.indices);
        if (error) return 'indices.' + error;
      }
      return null;
    };

    /**
     * Creates a StorageTypeProcessState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.StorageTypeProcessState} StorageTypeProcessState
     */
    StorageTypeProcessState.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.StorageTypeProcessState)
        return object;
      let message = new $root.polycentric.StorageTypeProcessState();
      if (object.logicalClock != null)
        if ($util.Long)
          (message.logicalClock = $util.Long.fromValue(
            object.logicalClock
          )).unsigned = true;
        else if (typeof object.logicalClock === 'string')
          message.logicalClock = parseInt(object.logicalClock, 10);
        else if (typeof object.logicalClock === 'number')
          message.logicalClock = object.logicalClock;
        else if (typeof object.logicalClock === 'object')
          message.logicalClock = new $util.LongBits(
            object.logicalClock.low >>> 0,
            object.logicalClock.high >>> 0
          ).toNumber(true);
      if (object.ranges) {
        if (!Array.isArray(object.ranges))
          throw TypeError(
            '.polycentric.StorageTypeProcessState.ranges: array expected'
          );
        message.ranges = [];
        for (let i = 0; i < object.ranges.length; ++i) {
          if (typeof object.ranges[i] !== 'object')
            throw TypeError(
              '.polycentric.StorageTypeProcessState.ranges: object expected'
            );
          message.ranges[i] = $root.polycentric.Range.fromObject(
            object.ranges[i]
          );
        }
      }
      if (object.indices != null) {
        if (typeof object.indices !== 'object')
          throw TypeError(
            '.polycentric.StorageTypeProcessState.indices: object expected'
          );
        message.indices = $root.polycentric.Indices.fromObject(object.indices);
      }
      return message;
    };

    /**
     * Creates a plain object from a StorageTypeProcessState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {polycentric.StorageTypeProcessState} message StorageTypeProcessState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    StorageTypeProcessState.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.ranges = [];
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.logicalClock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.logicalClock = options.longs === String ? '0' : 0;
        object.indices = null;
      }
      if (
        message.logicalClock != null &&
        message.hasOwnProperty('logicalClock')
      )
        if (typeof message.logicalClock === 'number')
          object.logicalClock =
            options.longs === String
              ? String(message.logicalClock)
              : message.logicalClock;
        else
          object.logicalClock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.logicalClock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.logicalClock.low >>> 0,
                    message.logicalClock.high >>> 0
                  ).toNumber(true)
                : message.logicalClock;
      if (message.ranges && message.ranges.length) {
        object.ranges = [];
        for (let j = 0; j < message.ranges.length; ++j)
          object.ranges[j] = $root.polycentric.Range.toObject(
            message.ranges[j],
            options
          );
      }
      if (message.indices != null && message.hasOwnProperty('indices'))
        object.indices = $root.polycentric.Indices.toObject(
          message.indices,
          options
        );
      return object;
    };

    /**
     * Converts this StorageTypeProcessState to JSON.
     * @function toJSON
     * @memberof polycentric.StorageTypeProcessState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    StorageTypeProcessState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for StorageTypeProcessState
     * @function getTypeUrl
     * @memberof polycentric.StorageTypeProcessState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    StorageTypeProcessState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.StorageTypeProcessState';
    };

    return StorageTypeProcessState;
  })();

  polycentric.RepeatedUInt64 = (function () {
    /**
     * Properties of a RepeatedUInt64.
     * @memberof polycentric
     * @interface IRepeatedUInt64
     * @property {Array.<number|Long>|null} [numbers] RepeatedUInt64 numbers
     */

    /**
     * Constructs a new RepeatedUInt64.
     * @memberof polycentric
     * @classdesc Represents a RepeatedUInt64.
     * @implements IRepeatedUInt64
     * @constructor
     * @param {polycentric.IRepeatedUInt64=} [properties] Properties to set
     */
    function RepeatedUInt64(properties) {
      this.numbers = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * RepeatedUInt64 numbers.
     * @member {Array.<number|Long>} numbers
     * @memberof polycentric.RepeatedUInt64
     * @instance
     */
    RepeatedUInt64.prototype.numbers = $util.emptyArray;

    /**
     * Creates a new RepeatedUInt64 instance using the specified properties.
     * @function create
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {polycentric.IRepeatedUInt64=} [properties] Properties to set
     * @returns {polycentric.RepeatedUInt64} RepeatedUInt64 instance
     */
    RepeatedUInt64.create = function create(properties) {
      return new RepeatedUInt64(properties);
    };

    /**
     * Encodes the specified RepeatedUInt64 message. Does not implicitly {@link polycentric.RepeatedUInt64.verify|verify} messages.
     * @function encode
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {polycentric.IRepeatedUInt64} message RepeatedUInt64 message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RepeatedUInt64.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.numbers != null && message.numbers.length) {
        writer.uint32(/* id 1, wireType 2 =*/ 10).fork();
        for (let i = 0; i < message.numbers.length; ++i)
          writer.uint64(message.numbers[i]);
        writer.ldelim();
      }
      return writer;
    };

    /**
     * Encodes the specified RepeatedUInt64 message, length delimited. Does not implicitly {@link polycentric.RepeatedUInt64.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {polycentric.IRepeatedUInt64} message RepeatedUInt64 message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    RepeatedUInt64.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a RepeatedUInt64 message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.RepeatedUInt64} RepeatedUInt64
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RepeatedUInt64.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.RepeatedUInt64();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.numbers && message.numbers.length))
              message.numbers = [];
            if ((tag & 7) === 2) {
              let end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) message.numbers.push(reader.uint64());
            } else message.numbers.push(reader.uint64());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a RepeatedUInt64 message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.RepeatedUInt64} RepeatedUInt64
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    RepeatedUInt64.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a RepeatedUInt64 message.
     * @function verify
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    RepeatedUInt64.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.numbers != null && message.hasOwnProperty('numbers')) {
        if (!Array.isArray(message.numbers)) return 'numbers: array expected';
        for (let i = 0; i < message.numbers.length; ++i)
          if (
            !$util.isInteger(message.numbers[i]) &&
            !(
              message.numbers[i] &&
              $util.isInteger(message.numbers[i].low) &&
              $util.isInteger(message.numbers[i].high)
            )
          )
            return 'numbers: integer|Long[] expected';
      }
      return null;
    };

    /**
     * Creates a RepeatedUInt64 message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.RepeatedUInt64} RepeatedUInt64
     */
    RepeatedUInt64.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.RepeatedUInt64) return object;
      let message = new $root.polycentric.RepeatedUInt64();
      if (object.numbers) {
        if (!Array.isArray(object.numbers))
          throw TypeError(
            '.polycentric.RepeatedUInt64.numbers: array expected'
          );
        message.numbers = [];
        for (let i = 0; i < object.numbers.length; ++i)
          if ($util.Long)
            (message.numbers[i] = $util.Long.fromValue(
              object.numbers[i]
            )).unsigned = true;
          else if (typeof object.numbers[i] === 'string')
            message.numbers[i] = parseInt(object.numbers[i], 10);
          else if (typeof object.numbers[i] === 'number')
            message.numbers[i] = object.numbers[i];
          else if (typeof object.numbers[i] === 'object')
            message.numbers[i] = new $util.LongBits(
              object.numbers[i].low >>> 0,
              object.numbers[i].high >>> 0
            ).toNumber(true);
      }
      return message;
    };

    /**
     * Creates a plain object from a RepeatedUInt64 message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {polycentric.RepeatedUInt64} message RepeatedUInt64
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    RepeatedUInt64.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.numbers = [];
      if (message.numbers && message.numbers.length) {
        object.numbers = [];
        for (let j = 0; j < message.numbers.length; ++j)
          if (typeof message.numbers[j] === 'number')
            object.numbers[j] =
              options.longs === String
                ? String(message.numbers[j])
                : message.numbers[j];
          else
            object.numbers[j] =
              options.longs === String
                ? $util.Long.prototype.toString.call(message.numbers[j])
                : options.longs === Number
                  ? new $util.LongBits(
                      message.numbers[j].low >>> 0,
                      message.numbers[j].high >>> 0
                    ).toNumber(true)
                  : message.numbers[j];
      }
      return object;
    };

    /**
     * Converts this RepeatedUInt64 to JSON.
     * @function toJSON
     * @memberof polycentric.RepeatedUInt64
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    RepeatedUInt64.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for RepeatedUInt64
     * @function getTypeUrl
     * @memberof polycentric.RepeatedUInt64
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    RepeatedUInt64.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.RepeatedUInt64';
    };

    return RepeatedUInt64;
  })();

  polycentric.QueryReferencesRequest = (function () {
    /**
     * Properties of a QueryReferencesRequest.
     * @memberof polycentric
     * @interface IQueryReferencesRequest
     * @property {polycentric.IReference|null} [reference] QueryReferencesRequest reference
     * @property {Uint8Array|null} [cursor] QueryReferencesRequest cursor
     * @property {polycentric.IQueryReferencesRequestEvents|null} [requestEvents] QueryReferencesRequest requestEvents
     * @property {Array.<polycentric.IQueryReferencesRequestCountLWWElementReferences>|null} [countLwwElementReferences] QueryReferencesRequest countLwwElementReferences
     * @property {Array.<polycentric.IQueryReferencesRequestCountReferences>|null} [countReferences] QueryReferencesRequest countReferences
     * @property {Array.<Uint8Array>|null} [extraByteReferences] QueryReferencesRequest extraByteReferences
     */

    /**
     * Constructs a new QueryReferencesRequest.
     * @memberof polycentric
     * @classdesc Represents a QueryReferencesRequest.
     * @implements IQueryReferencesRequest
     * @constructor
     * @param {polycentric.IQueryReferencesRequest=} [properties] Properties to set
     */
    function QueryReferencesRequest(properties) {
      this.countLwwElementReferences = [];
      this.countReferences = [];
      this.extraByteReferences = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryReferencesRequest reference.
     * @member {polycentric.IReference|null|undefined} reference
     * @memberof polycentric.QueryReferencesRequest
     * @instance
     */
    QueryReferencesRequest.prototype.reference = null;

    /**
     * QueryReferencesRequest cursor.
     * @member {Uint8Array|null|undefined} cursor
     * @memberof polycentric.QueryReferencesRequest
     * @instance
     */
    QueryReferencesRequest.prototype.cursor = null;

    /**
     * QueryReferencesRequest requestEvents.
     * @member {polycentric.IQueryReferencesRequestEvents|null|undefined} requestEvents
     * @memberof polycentric.QueryReferencesRequest
     * @instance
     */
    QueryReferencesRequest.prototype.requestEvents = null;

    /**
     * QueryReferencesRequest countLwwElementReferences.
     * @member {Array.<polycentric.IQueryReferencesRequestCountLWWElementReferences>} countLwwElementReferences
     * @memberof polycentric.QueryReferencesRequest
     * @instance
     */
    QueryReferencesRequest.prototype.countLwwElementReferences =
      $util.emptyArray;

    /**
     * QueryReferencesRequest countReferences.
     * @member {Array.<polycentric.IQueryReferencesRequestCountReferences>} countReferences
     * @memberof polycentric.QueryReferencesRequest
     * @instance
     */
    QueryReferencesRequest.prototype.countReferences = $util.emptyArray;

    /**
     * QueryReferencesRequest extraByteReferences.
     * @member {Array.<Uint8Array>} extraByteReferences
     * @memberof polycentric.QueryReferencesRequest
     * @instance
     */
    QueryReferencesRequest.prototype.extraByteReferences = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(QueryReferencesRequest.prototype, '_cursor', {
      get: $util.oneOfGetter(($oneOfFields = ['cursor'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(QueryReferencesRequest.prototype, '_requestEvents', {
      get: $util.oneOfGetter(($oneOfFields = ['requestEvents'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new QueryReferencesRequest instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {polycentric.IQueryReferencesRequest=} [properties] Properties to set
     * @returns {polycentric.QueryReferencesRequest} QueryReferencesRequest instance
     */
    QueryReferencesRequest.create = function create(properties) {
      return new QueryReferencesRequest(properties);
    };

    /**
     * Encodes the specified QueryReferencesRequest message. Does not implicitly {@link polycentric.QueryReferencesRequest.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {polycentric.IQueryReferencesRequest} message QueryReferencesRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.reference != null &&
        Object.hasOwnProperty.call(message, 'reference')
      )
        $root.polycentric.Reference.encode(
          message.reference,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).bytes(message.cursor);
      if (
        message.requestEvents != null &&
        Object.hasOwnProperty.call(message, 'requestEvents')
      )
        $root.polycentric.QueryReferencesRequestEvents.encode(
          message.requestEvents,
          writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
        ).ldelim();
      if (
        message.countLwwElementReferences != null &&
        message.countLwwElementReferences.length
      )
        for (let i = 0; i < message.countLwwElementReferences.length; ++i)
          $root.polycentric.QueryReferencesRequestCountLWWElementReferences.encode(
            message.countLwwElementReferences[i],
            writer.uint32(/* id 4, wireType 2 =*/ 34).fork()
          ).ldelim();
      if (message.countReferences != null && message.countReferences.length)
        for (let i = 0; i < message.countReferences.length; ++i)
          $root.polycentric.QueryReferencesRequestCountReferences.encode(
            message.countReferences[i],
            writer.uint32(/* id 5, wireType 2 =*/ 42).fork()
          ).ldelim();
      if (
        message.extraByteReferences != null &&
        message.extraByteReferences.length
      )
        for (let i = 0; i < message.extraByteReferences.length; ++i)
          writer
            .uint32(/* id 6, wireType 2 =*/ 50)
            .bytes(message.extraByteReferences[i]);
      return writer;
    };

    /**
     * Encodes the specified QueryReferencesRequest message, length delimited. Does not implicitly {@link polycentric.QueryReferencesRequest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {polycentric.IQueryReferencesRequest} message QueryReferencesRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequest.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryReferencesRequest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryReferencesRequest} QueryReferencesRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryReferencesRequest();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.reference = $root.polycentric.Reference.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.cursor = reader.bytes();
            break;
          }
          case 3: {
            message.requestEvents =
              $root.polycentric.QueryReferencesRequestEvents.decode(
                reader,
                reader.uint32()
              );
            break;
          }
          case 4: {
            if (
              !(
                message.countLwwElementReferences &&
                message.countLwwElementReferences.length
              )
            )
              message.countLwwElementReferences = [];
            message.countLwwElementReferences.push(
              $root.polycentric.QueryReferencesRequestCountLWWElementReferences.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          case 5: {
            if (!(message.countReferences && message.countReferences.length))
              message.countReferences = [];
            message.countReferences.push(
              $root.polycentric.QueryReferencesRequestCountReferences.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          case 6: {
            if (
              !(
                message.extraByteReferences &&
                message.extraByteReferences.length
              )
            )
              message.extraByteReferences = [];
            message.extraByteReferences.push(reader.bytes());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryReferencesRequest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryReferencesRequest} QueryReferencesRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequest.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryReferencesRequest message.
     * @function verify
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryReferencesRequest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.reference != null && message.hasOwnProperty('reference')) {
        let error = $root.polycentric.Reference.verify(message.reference);
        if (error) return 'reference.' + error;
      }
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        properties._cursor = 1;
        if (
          !(
            (message.cursor && typeof message.cursor.length === 'number') ||
            $util.isString(message.cursor)
          )
        )
          return 'cursor: buffer expected';
      }
      if (
        message.requestEvents != null &&
        message.hasOwnProperty('requestEvents')
      ) {
        properties._requestEvents = 1;
        {
          let error = $root.polycentric.QueryReferencesRequestEvents.verify(
            message.requestEvents
          );
          if (error) return 'requestEvents.' + error;
        }
      }
      if (
        message.countLwwElementReferences != null &&
        message.hasOwnProperty('countLwwElementReferences')
      ) {
        if (!Array.isArray(message.countLwwElementReferences))
          return 'countLwwElementReferences: array expected';
        for (let i = 0; i < message.countLwwElementReferences.length; ++i) {
          let error =
            $root.polycentric.QueryReferencesRequestCountLWWElementReferences.verify(
              message.countLwwElementReferences[i]
            );
          if (error) return 'countLwwElementReferences.' + error;
        }
      }
      if (
        message.countReferences != null &&
        message.hasOwnProperty('countReferences')
      ) {
        if (!Array.isArray(message.countReferences))
          return 'countReferences: array expected';
        for (let i = 0; i < message.countReferences.length; ++i) {
          let error =
            $root.polycentric.QueryReferencesRequestCountReferences.verify(
              message.countReferences[i]
            );
          if (error) return 'countReferences.' + error;
        }
      }
      if (
        message.extraByteReferences != null &&
        message.hasOwnProperty('extraByteReferences')
      ) {
        if (!Array.isArray(message.extraByteReferences))
          return 'extraByteReferences: array expected';
        for (let i = 0; i < message.extraByteReferences.length; ++i)
          if (
            !(
              (message.extraByteReferences[i] &&
                typeof message.extraByteReferences[i].length === 'number') ||
              $util.isString(message.extraByteReferences[i])
            )
          )
            return 'extraByteReferences: buffer[] expected';
      }
      return null;
    };

    /**
     * Creates a QueryReferencesRequest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryReferencesRequest} QueryReferencesRequest
     */
    QueryReferencesRequest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryReferencesRequest)
        return object;
      let message = new $root.polycentric.QueryReferencesRequest();
      if (object.reference != null) {
        if (typeof object.reference !== 'object')
          throw TypeError(
            '.polycentric.QueryReferencesRequest.reference: object expected'
          );
        message.reference = $root.polycentric.Reference.fromObject(
          object.reference
        );
      }
      if (object.cursor != null)
        if (typeof object.cursor === 'string')
          $util.base64.decode(
            object.cursor,
            (message.cursor = $util.newBuffer(
              $util.base64.length(object.cursor)
            )),
            0
          );
        else if (object.cursor.length >= 0) message.cursor = object.cursor;
      if (object.requestEvents != null) {
        if (typeof object.requestEvents !== 'object')
          throw TypeError(
            '.polycentric.QueryReferencesRequest.requestEvents: object expected'
          );
        message.requestEvents =
          $root.polycentric.QueryReferencesRequestEvents.fromObject(
            object.requestEvents
          );
      }
      if (object.countLwwElementReferences) {
        if (!Array.isArray(object.countLwwElementReferences))
          throw TypeError(
            '.polycentric.QueryReferencesRequest.countLwwElementReferences: array expected'
          );
        message.countLwwElementReferences = [];
        for (let i = 0; i < object.countLwwElementReferences.length; ++i) {
          if (typeof object.countLwwElementReferences[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryReferencesRequest.countLwwElementReferences: object expected'
            );
          message.countLwwElementReferences[i] =
            $root.polycentric.QueryReferencesRequestCountLWWElementReferences.fromObject(
              object.countLwwElementReferences[i]
            );
        }
      }
      if (object.countReferences) {
        if (!Array.isArray(object.countReferences))
          throw TypeError(
            '.polycentric.QueryReferencesRequest.countReferences: array expected'
          );
        message.countReferences = [];
        for (let i = 0; i < object.countReferences.length; ++i) {
          if (typeof object.countReferences[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryReferencesRequest.countReferences: object expected'
            );
          message.countReferences[i] =
            $root.polycentric.QueryReferencesRequestCountReferences.fromObject(
              object.countReferences[i]
            );
        }
      }
      if (object.extraByteReferences) {
        if (!Array.isArray(object.extraByteReferences))
          throw TypeError(
            '.polycentric.QueryReferencesRequest.extraByteReferences: array expected'
          );
        message.extraByteReferences = [];
        for (let i = 0; i < object.extraByteReferences.length; ++i)
          if (typeof object.extraByteReferences[i] === 'string')
            $util.base64.decode(
              object.extraByteReferences[i],
              (message.extraByteReferences[i] = $util.newBuffer(
                $util.base64.length(object.extraByteReferences[i])
              )),
              0
            );
          else if (object.extraByteReferences[i].length >= 0)
            message.extraByteReferences[i] = object.extraByteReferences[i];
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryReferencesRequest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {polycentric.QueryReferencesRequest} message QueryReferencesRequest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryReferencesRequest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.countLwwElementReferences = [];
        object.countReferences = [];
        object.extraByteReferences = [];
      }
      if (options.defaults) object.reference = null;
      if (message.reference != null && message.hasOwnProperty('reference'))
        object.reference = $root.polycentric.Reference.toObject(
          message.reference,
          options
        );
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        object.cursor =
          options.bytes === String
            ? $util.base64.encode(message.cursor, 0, message.cursor.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.cursor)
              : message.cursor;
        if (options.oneofs) object._cursor = 'cursor';
      }
      if (
        message.requestEvents != null &&
        message.hasOwnProperty('requestEvents')
      ) {
        object.requestEvents =
          $root.polycentric.QueryReferencesRequestEvents.toObject(
            message.requestEvents,
            options
          );
        if (options.oneofs) object._requestEvents = 'requestEvents';
      }
      if (
        message.countLwwElementReferences &&
        message.countLwwElementReferences.length
      ) {
        object.countLwwElementReferences = [];
        for (let j = 0; j < message.countLwwElementReferences.length; ++j)
          object.countLwwElementReferences[j] =
            $root.polycentric.QueryReferencesRequestCountLWWElementReferences.toObject(
              message.countLwwElementReferences[j],
              options
            );
      }
      if (message.countReferences && message.countReferences.length) {
        object.countReferences = [];
        for (let j = 0; j < message.countReferences.length; ++j)
          object.countReferences[j] =
            $root.polycentric.QueryReferencesRequestCountReferences.toObject(
              message.countReferences[j],
              options
            );
      }
      if (message.extraByteReferences && message.extraByteReferences.length) {
        object.extraByteReferences = [];
        for (let j = 0; j < message.extraByteReferences.length; ++j)
          object.extraByteReferences[j] =
            options.bytes === String
              ? $util.base64.encode(
                  message.extraByteReferences[j],
                  0,
                  message.extraByteReferences[j].length
                )
              : options.bytes === Array
                ? Array.prototype.slice.call(message.extraByteReferences[j])
                : message.extraByteReferences[j];
      }
      return object;
    };

    /**
     * Converts this QueryReferencesRequest to JSON.
     * @function toJSON
     * @memberof polycentric.QueryReferencesRequest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryReferencesRequest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryReferencesRequest
     * @function getTypeUrl
     * @memberof polycentric.QueryReferencesRequest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryReferencesRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryReferencesRequest';
    };

    return QueryReferencesRequest;
  })();

  polycentric.QueryReferencesRequestEvents = (function () {
    /**
     * Properties of a QueryReferencesRequestEvents.
     * @memberof polycentric
     * @interface IQueryReferencesRequestEvents
     * @property {number|Long|null} [fromType] QueryReferencesRequestEvents fromType
     * @property {Array.<polycentric.IQueryReferencesRequestCountLWWElementReferences>|null} [countLwwElementReferences] QueryReferencesRequestEvents countLwwElementReferences
     * @property {Array.<polycentric.IQueryReferencesRequestCountReferences>|null} [countReferences] QueryReferencesRequestEvents countReferences
     */

    /**
     * Constructs a new QueryReferencesRequestEvents.
     * @memberof polycentric
     * @classdesc Represents a QueryReferencesRequestEvents.
     * @implements IQueryReferencesRequestEvents
     * @constructor
     * @param {polycentric.IQueryReferencesRequestEvents=} [properties] Properties to set
     */
    function QueryReferencesRequestEvents(properties) {
      this.countLwwElementReferences = [];
      this.countReferences = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryReferencesRequestEvents fromType.
     * @member {number|Long|null|undefined} fromType
     * @memberof polycentric.QueryReferencesRequestEvents
     * @instance
     */
    QueryReferencesRequestEvents.prototype.fromType = null;

    /**
     * QueryReferencesRequestEvents countLwwElementReferences.
     * @member {Array.<polycentric.IQueryReferencesRequestCountLWWElementReferences>} countLwwElementReferences
     * @memberof polycentric.QueryReferencesRequestEvents
     * @instance
     */
    QueryReferencesRequestEvents.prototype.countLwwElementReferences =
      $util.emptyArray;

    /**
     * QueryReferencesRequestEvents countReferences.
     * @member {Array.<polycentric.IQueryReferencesRequestCountReferences>} countReferences
     * @memberof polycentric.QueryReferencesRequestEvents
     * @instance
     */
    QueryReferencesRequestEvents.prototype.countReferences = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(QueryReferencesRequestEvents.prototype, '_fromType', {
      get: $util.oneOfGetter(($oneOfFields = ['fromType'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new QueryReferencesRequestEvents instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {polycentric.IQueryReferencesRequestEvents=} [properties] Properties to set
     * @returns {polycentric.QueryReferencesRequestEvents} QueryReferencesRequestEvents instance
     */
    QueryReferencesRequestEvents.create = function create(properties) {
      return new QueryReferencesRequestEvents(properties);
    };

    /**
     * Encodes the specified QueryReferencesRequestEvents message. Does not implicitly {@link polycentric.QueryReferencesRequestEvents.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {polycentric.IQueryReferencesRequestEvents} message QueryReferencesRequestEvents message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequestEvents.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.fromType != null &&
        Object.hasOwnProperty.call(message, 'fromType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.fromType);
      if (
        message.countLwwElementReferences != null &&
        message.countLwwElementReferences.length
      )
        for (let i = 0; i < message.countLwwElementReferences.length; ++i)
          $root.polycentric.QueryReferencesRequestCountLWWElementReferences.encode(
            message.countLwwElementReferences[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      if (message.countReferences != null && message.countReferences.length)
        for (let i = 0; i < message.countReferences.length; ++i)
          $root.polycentric.QueryReferencesRequestCountReferences.encode(
            message.countReferences[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified QueryReferencesRequestEvents message, length delimited. Does not implicitly {@link polycentric.QueryReferencesRequestEvents.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {polycentric.IQueryReferencesRequestEvents} message QueryReferencesRequestEvents message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequestEvents.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryReferencesRequestEvents message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryReferencesRequestEvents} QueryReferencesRequestEvents
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequestEvents.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryReferencesRequestEvents();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.fromType = reader.uint64();
            break;
          }
          case 2: {
            if (
              !(
                message.countLwwElementReferences &&
                message.countLwwElementReferences.length
              )
            )
              message.countLwwElementReferences = [];
            message.countLwwElementReferences.push(
              $root.polycentric.QueryReferencesRequestCountLWWElementReferences.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          case 3: {
            if (!(message.countReferences && message.countReferences.length))
              message.countReferences = [];
            message.countReferences.push(
              $root.polycentric.QueryReferencesRequestCountReferences.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryReferencesRequestEvents message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryReferencesRequestEvents} QueryReferencesRequestEvents
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequestEvents.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryReferencesRequestEvents message.
     * @function verify
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryReferencesRequestEvents.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.fromType != null && message.hasOwnProperty('fromType')) {
        properties._fromType = 1;
        if (
          !$util.isInteger(message.fromType) &&
          !(
            message.fromType &&
            $util.isInteger(message.fromType.low) &&
            $util.isInteger(message.fromType.high)
          )
        )
          return 'fromType: integer|Long expected';
      }
      if (
        message.countLwwElementReferences != null &&
        message.hasOwnProperty('countLwwElementReferences')
      ) {
        if (!Array.isArray(message.countLwwElementReferences))
          return 'countLwwElementReferences: array expected';
        for (let i = 0; i < message.countLwwElementReferences.length; ++i) {
          let error =
            $root.polycentric.QueryReferencesRequestCountLWWElementReferences.verify(
              message.countLwwElementReferences[i]
            );
          if (error) return 'countLwwElementReferences.' + error;
        }
      }
      if (
        message.countReferences != null &&
        message.hasOwnProperty('countReferences')
      ) {
        if (!Array.isArray(message.countReferences))
          return 'countReferences: array expected';
        for (let i = 0; i < message.countReferences.length; ++i) {
          let error =
            $root.polycentric.QueryReferencesRequestCountReferences.verify(
              message.countReferences[i]
            );
          if (error) return 'countReferences.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a QueryReferencesRequestEvents message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryReferencesRequestEvents} QueryReferencesRequestEvents
     */
    QueryReferencesRequestEvents.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryReferencesRequestEvents)
        return object;
      let message = new $root.polycentric.QueryReferencesRequestEvents();
      if (object.fromType != null)
        if ($util.Long)
          (message.fromType = $util.Long.fromValue(object.fromType)).unsigned =
            true;
        else if (typeof object.fromType === 'string')
          message.fromType = parseInt(object.fromType, 10);
        else if (typeof object.fromType === 'number')
          message.fromType = object.fromType;
        else if (typeof object.fromType === 'object')
          message.fromType = new $util.LongBits(
            object.fromType.low >>> 0,
            object.fromType.high >>> 0
          ).toNumber(true);
      if (object.countLwwElementReferences) {
        if (!Array.isArray(object.countLwwElementReferences))
          throw TypeError(
            '.polycentric.QueryReferencesRequestEvents.countLwwElementReferences: array expected'
          );
        message.countLwwElementReferences = [];
        for (let i = 0; i < object.countLwwElementReferences.length; ++i) {
          if (typeof object.countLwwElementReferences[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryReferencesRequestEvents.countLwwElementReferences: object expected'
            );
          message.countLwwElementReferences[i] =
            $root.polycentric.QueryReferencesRequestCountLWWElementReferences.fromObject(
              object.countLwwElementReferences[i]
            );
        }
      }
      if (object.countReferences) {
        if (!Array.isArray(object.countReferences))
          throw TypeError(
            '.polycentric.QueryReferencesRequestEvents.countReferences: array expected'
          );
        message.countReferences = [];
        for (let i = 0; i < object.countReferences.length; ++i) {
          if (typeof object.countReferences[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryReferencesRequestEvents.countReferences: object expected'
            );
          message.countReferences[i] =
            $root.polycentric.QueryReferencesRequestCountReferences.fromObject(
              object.countReferences[i]
            );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryReferencesRequestEvents message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {polycentric.QueryReferencesRequestEvents} message QueryReferencesRequestEvents
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryReferencesRequestEvents.toObject = function toObject(
      message,
      options
    ) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.countLwwElementReferences = [];
        object.countReferences = [];
      }
      if (message.fromType != null && message.hasOwnProperty('fromType')) {
        if (typeof message.fromType === 'number')
          object.fromType =
            options.longs === String
              ? String(message.fromType)
              : message.fromType;
        else
          object.fromType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.fromType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.fromType.low >>> 0,
                    message.fromType.high >>> 0
                  ).toNumber(true)
                : message.fromType;
        if (options.oneofs) object._fromType = 'fromType';
      }
      if (
        message.countLwwElementReferences &&
        message.countLwwElementReferences.length
      ) {
        object.countLwwElementReferences = [];
        for (let j = 0; j < message.countLwwElementReferences.length; ++j)
          object.countLwwElementReferences[j] =
            $root.polycentric.QueryReferencesRequestCountLWWElementReferences.toObject(
              message.countLwwElementReferences[j],
              options
            );
      }
      if (message.countReferences && message.countReferences.length) {
        object.countReferences = [];
        for (let j = 0; j < message.countReferences.length; ++j)
          object.countReferences[j] =
            $root.polycentric.QueryReferencesRequestCountReferences.toObject(
              message.countReferences[j],
              options
            );
      }
      return object;
    };

    /**
     * Converts this QueryReferencesRequestEvents to JSON.
     * @function toJSON
     * @memberof polycentric.QueryReferencesRequestEvents
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryReferencesRequestEvents.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryReferencesRequestEvents
     * @function getTypeUrl
     * @memberof polycentric.QueryReferencesRequestEvents
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryReferencesRequestEvents.getTypeUrl = function getTypeUrl(
      typeUrlPrefix
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryReferencesRequestEvents';
    };

    return QueryReferencesRequestEvents;
  })();

  polycentric.QueryReferencesRequestCountLWWElementReferences = (function () {
    /**
     * Properties of a QueryReferencesRequestCountLWWElementReferences.
     * @memberof polycentric
     * @interface IQueryReferencesRequestCountLWWElementReferences
     * @property {Uint8Array|null} [value] QueryReferencesRequestCountLWWElementReferences value
     * @property {number|Long|null} [fromType] QueryReferencesRequestCountLWWElementReferences fromType
     */

    /**
     * Constructs a new QueryReferencesRequestCountLWWElementReferences.
     * @memberof polycentric
     * @classdesc Represents a QueryReferencesRequestCountLWWElementReferences.
     * @implements IQueryReferencesRequestCountLWWElementReferences
     * @constructor
     * @param {polycentric.IQueryReferencesRequestCountLWWElementReferences=} [properties] Properties to set
     */
    function QueryReferencesRequestCountLWWElementReferences(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryReferencesRequestCountLWWElementReferences value.
     * @member {Uint8Array} value
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @instance
     */
    QueryReferencesRequestCountLWWElementReferences.prototype.value =
      $util.newBuffer([]);

    /**
     * QueryReferencesRequestCountLWWElementReferences fromType.
     * @member {number|Long|null|undefined} fromType
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @instance
     */
    QueryReferencesRequestCountLWWElementReferences.prototype.fromType = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(
      QueryReferencesRequestCountLWWElementReferences.prototype,
      '_fromType',
      {
        get: $util.oneOfGetter(($oneOfFields = ['fromType'])),
        set: $util.oneOfSetter($oneOfFields),
      }
    );

    /**
     * Creates a new QueryReferencesRequestCountLWWElementReferences instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {polycentric.IQueryReferencesRequestCountLWWElementReferences=} [properties] Properties to set
     * @returns {polycentric.QueryReferencesRequestCountLWWElementReferences} QueryReferencesRequestCountLWWElementReferences instance
     */
    QueryReferencesRequestCountLWWElementReferences.create = function create(
      properties
    ) {
      return new QueryReferencesRequestCountLWWElementReferences(properties);
    };

    /**
     * Encodes the specified QueryReferencesRequestCountLWWElementReferences message. Does not implicitly {@link polycentric.QueryReferencesRequestCountLWWElementReferences.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {polycentric.IQueryReferencesRequestCountLWWElementReferences} message QueryReferencesRequestCountLWWElementReferences message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequestCountLWWElementReferences.encode = function encode(
      message,
      writer
    ) {
      if (!writer) writer = $Writer.create();
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.value);
      if (
        message.fromType != null &&
        Object.hasOwnProperty.call(message, 'fromType')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.fromType);
      return writer;
    };

    /**
     * Encodes the specified QueryReferencesRequestCountLWWElementReferences message, length delimited. Does not implicitly {@link polycentric.QueryReferencesRequestCountLWWElementReferences.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {polycentric.IQueryReferencesRequestCountLWWElementReferences} message QueryReferencesRequestCountLWWElementReferences message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequestCountLWWElementReferences.encodeDelimited =
      function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
      };

    /**
     * Decodes a QueryReferencesRequestCountLWWElementReferences message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryReferencesRequestCountLWWElementReferences} QueryReferencesRequestCountLWWElementReferences
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequestCountLWWElementReferences.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message =
          new $root.polycentric.QueryReferencesRequestCountLWWElementReferences();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.value = reader.bytes();
            break;
          }
          case 2: {
            message.fromType = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryReferencesRequestCountLWWElementReferences message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryReferencesRequestCountLWWElementReferences} QueryReferencesRequestCountLWWElementReferences
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequestCountLWWElementReferences.decodeDelimited =
      function decodeDelimited(reader) {
        if (!(reader instanceof $Reader)) reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };

    /**
     * Verifies a QueryReferencesRequestCountLWWElementReferences message.
     * @function verify
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryReferencesRequestCountLWWElementReferences.verify = function verify(
      message
    ) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.value != null && message.hasOwnProperty('value'))
        if (
          !(
            (message.value && typeof message.value.length === 'number') ||
            $util.isString(message.value)
          )
        )
          return 'value: buffer expected';
      if (message.fromType != null && message.hasOwnProperty('fromType')) {
        properties._fromType = 1;
        if (
          !$util.isInteger(message.fromType) &&
          !(
            message.fromType &&
            $util.isInteger(message.fromType.low) &&
            $util.isInteger(message.fromType.high)
          )
        )
          return 'fromType: integer|Long expected';
      }
      return null;
    };

    /**
     * Creates a QueryReferencesRequestCountLWWElementReferences message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryReferencesRequestCountLWWElementReferences} QueryReferencesRequestCountLWWElementReferences
     */
    QueryReferencesRequestCountLWWElementReferences.fromObject =
      function fromObject(object) {
        if (
          object instanceof
          $root.polycentric.QueryReferencesRequestCountLWWElementReferences
        )
          return object;
        let message =
          new $root.polycentric.QueryReferencesRequestCountLWWElementReferences();
        if (object.value != null)
          if (typeof object.value === 'string')
            $util.base64.decode(
              object.value,
              (message.value = $util.newBuffer(
                $util.base64.length(object.value)
              )),
              0
            );
          else if (object.value.length >= 0) message.value = object.value;
        if (object.fromType != null)
          if ($util.Long)
            (message.fromType = $util.Long.fromValue(
              object.fromType
            )).unsigned = true;
          else if (typeof object.fromType === 'string')
            message.fromType = parseInt(object.fromType, 10);
          else if (typeof object.fromType === 'number')
            message.fromType = object.fromType;
          else if (typeof object.fromType === 'object')
            message.fromType = new $util.LongBits(
              object.fromType.low >>> 0,
              object.fromType.high >>> 0
            ).toNumber(true);
        return message;
      };

    /**
     * Creates a plain object from a QueryReferencesRequestCountLWWElementReferences message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {polycentric.QueryReferencesRequestCountLWWElementReferences} message QueryReferencesRequestCountLWWElementReferences
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryReferencesRequestCountLWWElementReferences.toObject =
      function toObject(message, options) {
        if (!options) options = {};
        let object = {};
        if (options.defaults)
          if (options.bytes === String) object.value = '';
          else {
            object.value = [];
            if (options.bytes !== Array)
              object.value = $util.newBuffer(object.value);
          }
        if (message.value != null && message.hasOwnProperty('value'))
          object.value =
            options.bytes === String
              ? $util.base64.encode(message.value, 0, message.value.length)
              : options.bytes === Array
                ? Array.prototype.slice.call(message.value)
                : message.value;
        if (message.fromType != null && message.hasOwnProperty('fromType')) {
          if (typeof message.fromType === 'number')
            object.fromType =
              options.longs === String
                ? String(message.fromType)
                : message.fromType;
          else
            object.fromType =
              options.longs === String
                ? $util.Long.prototype.toString.call(message.fromType)
                : options.longs === Number
                  ? new $util.LongBits(
                      message.fromType.low >>> 0,
                      message.fromType.high >>> 0
                    ).toNumber(true)
                  : message.fromType;
          if (options.oneofs) object._fromType = 'fromType';
        }
        return object;
      };

    /**
     * Converts this QueryReferencesRequestCountLWWElementReferences to JSON.
     * @function toJSON
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryReferencesRequestCountLWWElementReferences.prototype.toJSON =
      function toJSON() {
        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
      };

    /**
     * Gets the default type url for QueryReferencesRequestCountLWWElementReferences
     * @function getTypeUrl
     * @memberof polycentric.QueryReferencesRequestCountLWWElementReferences
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryReferencesRequestCountLWWElementReferences.getTypeUrl =
      function getTypeUrl(typeUrlPrefix) {
        if (typeUrlPrefix === undefined) {
          typeUrlPrefix = 'type.googleapis.com';
        }
        return (
          typeUrlPrefix +
          '/polycentric.QueryReferencesRequestCountLWWElementReferences'
        );
      };

    return QueryReferencesRequestCountLWWElementReferences;
  })();

  polycentric.QueryReferencesRequestCountReferences = (function () {
    /**
     * Properties of a QueryReferencesRequestCountReferences.
     * @memberof polycentric
     * @interface IQueryReferencesRequestCountReferences
     * @property {number|Long|null} [fromType] QueryReferencesRequestCountReferences fromType
     */

    /**
     * Constructs a new QueryReferencesRequestCountReferences.
     * @memberof polycentric
     * @classdesc Represents a QueryReferencesRequestCountReferences.
     * @implements IQueryReferencesRequestCountReferences
     * @constructor
     * @param {polycentric.IQueryReferencesRequestCountReferences=} [properties] Properties to set
     */
    function QueryReferencesRequestCountReferences(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryReferencesRequestCountReferences fromType.
     * @member {number|Long|null|undefined} fromType
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @instance
     */
    QueryReferencesRequestCountReferences.prototype.fromType = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(
      QueryReferencesRequestCountReferences.prototype,
      '_fromType',
      {
        get: $util.oneOfGetter(($oneOfFields = ['fromType'])),
        set: $util.oneOfSetter($oneOfFields),
      }
    );

    /**
     * Creates a new QueryReferencesRequestCountReferences instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {polycentric.IQueryReferencesRequestCountReferences=} [properties] Properties to set
     * @returns {polycentric.QueryReferencesRequestCountReferences} QueryReferencesRequestCountReferences instance
     */
    QueryReferencesRequestCountReferences.create = function create(properties) {
      return new QueryReferencesRequestCountReferences(properties);
    };

    /**
     * Encodes the specified QueryReferencesRequestCountReferences message. Does not implicitly {@link polycentric.QueryReferencesRequestCountReferences.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {polycentric.IQueryReferencesRequestCountReferences} message QueryReferencesRequestCountReferences message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequestCountReferences.encode = function encode(
      message,
      writer
    ) {
      if (!writer) writer = $Writer.create();
      if (
        message.fromType != null &&
        Object.hasOwnProperty.call(message, 'fromType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.fromType);
      return writer;
    };

    /**
     * Encodes the specified QueryReferencesRequestCountReferences message, length delimited. Does not implicitly {@link polycentric.QueryReferencesRequestCountReferences.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {polycentric.IQueryReferencesRequestCountReferences} message QueryReferencesRequestCountReferences message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesRequestCountReferences.encodeDelimited =
      function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
      };

    /**
     * Decodes a QueryReferencesRequestCountReferences message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryReferencesRequestCountReferences} QueryReferencesRequestCountReferences
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequestCountReferences.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryReferencesRequestCountReferences();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.fromType = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryReferencesRequestCountReferences message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryReferencesRequestCountReferences} QueryReferencesRequestCountReferences
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesRequestCountReferences.decodeDelimited =
      function decodeDelimited(reader) {
        if (!(reader instanceof $Reader)) reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };

    /**
     * Verifies a QueryReferencesRequestCountReferences message.
     * @function verify
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryReferencesRequestCountReferences.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.fromType != null && message.hasOwnProperty('fromType')) {
        properties._fromType = 1;
        if (
          !$util.isInteger(message.fromType) &&
          !(
            message.fromType &&
            $util.isInteger(message.fromType.low) &&
            $util.isInteger(message.fromType.high)
          )
        )
          return 'fromType: integer|Long expected';
      }
      return null;
    };

    /**
     * Creates a QueryReferencesRequestCountReferences message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryReferencesRequestCountReferences} QueryReferencesRequestCountReferences
     */
    QueryReferencesRequestCountReferences.fromObject = function fromObject(
      object
    ) {
      if (
        object instanceof
        $root.polycentric.QueryReferencesRequestCountReferences
      )
        return object;
      let message =
        new $root.polycentric.QueryReferencesRequestCountReferences();
      if (object.fromType != null)
        if ($util.Long)
          (message.fromType = $util.Long.fromValue(object.fromType)).unsigned =
            true;
        else if (typeof object.fromType === 'string')
          message.fromType = parseInt(object.fromType, 10);
        else if (typeof object.fromType === 'number')
          message.fromType = object.fromType;
        else if (typeof object.fromType === 'object')
          message.fromType = new $util.LongBits(
            object.fromType.low >>> 0,
            object.fromType.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a QueryReferencesRequestCountReferences message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {polycentric.QueryReferencesRequestCountReferences} message QueryReferencesRequestCountReferences
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryReferencesRequestCountReferences.toObject = function toObject(
      message,
      options
    ) {
      if (!options) options = {};
      let object = {};
      if (message.fromType != null && message.hasOwnProperty('fromType')) {
        if (typeof message.fromType === 'number')
          object.fromType =
            options.longs === String
              ? String(message.fromType)
              : message.fromType;
        else
          object.fromType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.fromType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.fromType.low >>> 0,
                    message.fromType.high >>> 0
                  ).toNumber(true)
                : message.fromType;
        if (options.oneofs) object._fromType = 'fromType';
      }
      return object;
    };

    /**
     * Converts this QueryReferencesRequestCountReferences to JSON.
     * @function toJSON
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryReferencesRequestCountReferences.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryReferencesRequestCountReferences
     * @function getTypeUrl
     * @memberof polycentric.QueryReferencesRequestCountReferences
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryReferencesRequestCountReferences.getTypeUrl = function getTypeUrl(
      typeUrlPrefix
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return (
        typeUrlPrefix + '/polycentric.QueryReferencesRequestCountReferences'
      );
    };

    return QueryReferencesRequestCountReferences;
  })();

  polycentric.QueryReferencesResponseEventItem = (function () {
    /**
     * Properties of a QueryReferencesResponseEventItem.
     * @memberof polycentric
     * @interface IQueryReferencesResponseEventItem
     * @property {polycentric.ISignedEvent|null} [event] QueryReferencesResponseEventItem event
     * @property {Array.<number|Long>|null} [counts] QueryReferencesResponseEventItem counts
     */

    /**
     * Constructs a new QueryReferencesResponseEventItem.
     * @memberof polycentric
     * @classdesc Represents a QueryReferencesResponseEventItem.
     * @implements IQueryReferencesResponseEventItem
     * @constructor
     * @param {polycentric.IQueryReferencesResponseEventItem=} [properties] Properties to set
     */
    function QueryReferencesResponseEventItem(properties) {
      this.counts = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryReferencesResponseEventItem event.
     * @member {polycentric.ISignedEvent|null|undefined} event
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @instance
     */
    QueryReferencesResponseEventItem.prototype.event = null;

    /**
     * QueryReferencesResponseEventItem counts.
     * @member {Array.<number|Long>} counts
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @instance
     */
    QueryReferencesResponseEventItem.prototype.counts = $util.emptyArray;

    /**
     * Creates a new QueryReferencesResponseEventItem instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {polycentric.IQueryReferencesResponseEventItem=} [properties] Properties to set
     * @returns {polycentric.QueryReferencesResponseEventItem} QueryReferencesResponseEventItem instance
     */
    QueryReferencesResponseEventItem.create = function create(properties) {
      return new QueryReferencesResponseEventItem(properties);
    };

    /**
     * Encodes the specified QueryReferencesResponseEventItem message. Does not implicitly {@link polycentric.QueryReferencesResponseEventItem.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {polycentric.IQueryReferencesResponseEventItem} message QueryReferencesResponseEventItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesResponseEventItem.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.event != null && Object.hasOwnProperty.call(message, 'event'))
        $root.polycentric.SignedEvent.encode(
          message.event,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (message.counts != null && message.counts.length) {
        writer.uint32(/* id 2, wireType 2 =*/ 18).fork();
        for (let i = 0; i < message.counts.length; ++i)
          writer.uint64(message.counts[i]);
        writer.ldelim();
      }
      return writer;
    };

    /**
     * Encodes the specified QueryReferencesResponseEventItem message, length delimited. Does not implicitly {@link polycentric.QueryReferencesResponseEventItem.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {polycentric.IQueryReferencesResponseEventItem} message QueryReferencesResponseEventItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesResponseEventItem.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryReferencesResponseEventItem message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryReferencesResponseEventItem} QueryReferencesResponseEventItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesResponseEventItem.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryReferencesResponseEventItem();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.event = $root.polycentric.SignedEvent.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            if (!(message.counts && message.counts.length)) message.counts = [];
            if ((tag & 7) === 2) {
              let end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) message.counts.push(reader.uint64());
            } else message.counts.push(reader.uint64());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryReferencesResponseEventItem message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryReferencesResponseEventItem} QueryReferencesResponseEventItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesResponseEventItem.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryReferencesResponseEventItem message.
     * @function verify
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryReferencesResponseEventItem.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.event != null && message.hasOwnProperty('event')) {
        let error = $root.polycentric.SignedEvent.verify(message.event);
        if (error) return 'event.' + error;
      }
      if (message.counts != null && message.hasOwnProperty('counts')) {
        if (!Array.isArray(message.counts)) return 'counts: array expected';
        for (let i = 0; i < message.counts.length; ++i)
          if (
            !$util.isInteger(message.counts[i]) &&
            !(
              message.counts[i] &&
              $util.isInteger(message.counts[i].low) &&
              $util.isInteger(message.counts[i].high)
            )
          )
            return 'counts: integer|Long[] expected';
      }
      return null;
    };

    /**
     * Creates a QueryReferencesResponseEventItem message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryReferencesResponseEventItem} QueryReferencesResponseEventItem
     */
    QueryReferencesResponseEventItem.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryReferencesResponseEventItem)
        return object;
      let message = new $root.polycentric.QueryReferencesResponseEventItem();
      if (object.event != null) {
        if (typeof object.event !== 'object')
          throw TypeError(
            '.polycentric.QueryReferencesResponseEventItem.event: object expected'
          );
        message.event = $root.polycentric.SignedEvent.fromObject(object.event);
      }
      if (object.counts) {
        if (!Array.isArray(object.counts))
          throw TypeError(
            '.polycentric.QueryReferencesResponseEventItem.counts: array expected'
          );
        message.counts = [];
        for (let i = 0; i < object.counts.length; ++i)
          if ($util.Long)
            (message.counts[i] = $util.Long.fromValue(
              object.counts[i]
            )).unsigned = true;
          else if (typeof object.counts[i] === 'string')
            message.counts[i] = parseInt(object.counts[i], 10);
          else if (typeof object.counts[i] === 'number')
            message.counts[i] = object.counts[i];
          else if (typeof object.counts[i] === 'object')
            message.counts[i] = new $util.LongBits(
              object.counts[i].low >>> 0,
              object.counts[i].high >>> 0
            ).toNumber(true);
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryReferencesResponseEventItem message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {polycentric.QueryReferencesResponseEventItem} message QueryReferencesResponseEventItem
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryReferencesResponseEventItem.toObject = function toObject(
      message,
      options
    ) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.counts = [];
      if (options.defaults) object.event = null;
      if (message.event != null && message.hasOwnProperty('event'))
        object.event = $root.polycentric.SignedEvent.toObject(
          message.event,
          options
        );
      if (message.counts && message.counts.length) {
        object.counts = [];
        for (let j = 0; j < message.counts.length; ++j)
          if (typeof message.counts[j] === 'number')
            object.counts[j] =
              options.longs === String
                ? String(message.counts[j])
                : message.counts[j];
          else
            object.counts[j] =
              options.longs === String
                ? $util.Long.prototype.toString.call(message.counts[j])
                : options.longs === Number
                  ? new $util.LongBits(
                      message.counts[j].low >>> 0,
                      message.counts[j].high >>> 0
                    ).toNumber(true)
                  : message.counts[j];
      }
      return object;
    };

    /**
     * Converts this QueryReferencesResponseEventItem to JSON.
     * @function toJSON
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryReferencesResponseEventItem.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryReferencesResponseEventItem
     * @function getTypeUrl
     * @memberof polycentric.QueryReferencesResponseEventItem
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryReferencesResponseEventItem.getTypeUrl = function getTypeUrl(
      typeUrlPrefix
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryReferencesResponseEventItem';
    };

    return QueryReferencesResponseEventItem;
  })();

  polycentric.QueryReferencesResponse = (function () {
    /**
     * Properties of a QueryReferencesResponse.
     * @memberof polycentric
     * @interface IQueryReferencesResponse
     * @property {Array.<polycentric.IQueryReferencesResponseEventItem>|null} [items] QueryReferencesResponse items
     * @property {Array.<polycentric.ISignedEvent>|null} [relatedEvents] QueryReferencesResponse relatedEvents
     * @property {Uint8Array|null} [cursor] QueryReferencesResponse cursor
     * @property {Array.<number|Long>|null} [counts] QueryReferencesResponse counts
     */

    /**
     * Constructs a new QueryReferencesResponse.
     * @memberof polycentric
     * @classdesc Represents a QueryReferencesResponse.
     * @implements IQueryReferencesResponse
     * @constructor
     * @param {polycentric.IQueryReferencesResponse=} [properties] Properties to set
     */
    function QueryReferencesResponse(properties) {
      this.items = [];
      this.relatedEvents = [];
      this.counts = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryReferencesResponse items.
     * @member {Array.<polycentric.IQueryReferencesResponseEventItem>} items
     * @memberof polycentric.QueryReferencesResponse
     * @instance
     */
    QueryReferencesResponse.prototype.items = $util.emptyArray;

    /**
     * QueryReferencesResponse relatedEvents.
     * @member {Array.<polycentric.ISignedEvent>} relatedEvents
     * @memberof polycentric.QueryReferencesResponse
     * @instance
     */
    QueryReferencesResponse.prototype.relatedEvents = $util.emptyArray;

    /**
     * QueryReferencesResponse cursor.
     * @member {Uint8Array|null|undefined} cursor
     * @memberof polycentric.QueryReferencesResponse
     * @instance
     */
    QueryReferencesResponse.prototype.cursor = null;

    /**
     * QueryReferencesResponse counts.
     * @member {Array.<number|Long>} counts
     * @memberof polycentric.QueryReferencesResponse
     * @instance
     */
    QueryReferencesResponse.prototype.counts = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(QueryReferencesResponse.prototype, '_cursor', {
      get: $util.oneOfGetter(($oneOfFields = ['cursor'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new QueryReferencesResponse instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {polycentric.IQueryReferencesResponse=} [properties] Properties to set
     * @returns {polycentric.QueryReferencesResponse} QueryReferencesResponse instance
     */
    QueryReferencesResponse.create = function create(properties) {
      return new QueryReferencesResponse(properties);
    };

    /**
     * Encodes the specified QueryReferencesResponse message. Does not implicitly {@link polycentric.QueryReferencesResponse.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {polycentric.IQueryReferencesResponse} message QueryReferencesResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesResponse.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.items != null && message.items.length)
        for (let i = 0; i < message.items.length; ++i)
          $root.polycentric.QueryReferencesResponseEventItem.encode(
            message.items[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      if (message.relatedEvents != null && message.relatedEvents.length)
        for (let i = 0; i < message.relatedEvents.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.relatedEvents[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.cursor);
      if (message.counts != null && message.counts.length) {
        writer.uint32(/* id 4, wireType 2 =*/ 34).fork();
        for (let i = 0; i < message.counts.length; ++i)
          writer.uint64(message.counts[i]);
        writer.ldelim();
      }
      return writer;
    };

    /**
     * Encodes the specified QueryReferencesResponse message, length delimited. Does not implicitly {@link polycentric.QueryReferencesResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {polycentric.IQueryReferencesResponse} message QueryReferencesResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryReferencesResponse.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryReferencesResponse message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryReferencesResponse} QueryReferencesResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesResponse.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryReferencesResponse();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.items && message.items.length)) message.items = [];
            message.items.push(
              $root.polycentric.QueryReferencesResponseEventItem.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          case 2: {
            if (!(message.relatedEvents && message.relatedEvents.length))
              message.relatedEvents = [];
            message.relatedEvents.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          case 3: {
            message.cursor = reader.bytes();
            break;
          }
          case 4: {
            if (!(message.counts && message.counts.length)) message.counts = [];
            if ((tag & 7) === 2) {
              let end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) message.counts.push(reader.uint64());
            } else message.counts.push(reader.uint64());
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryReferencesResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryReferencesResponse} QueryReferencesResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryReferencesResponse.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryReferencesResponse message.
     * @function verify
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryReferencesResponse.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.items != null && message.hasOwnProperty('items')) {
        if (!Array.isArray(message.items)) return 'items: array expected';
        for (let i = 0; i < message.items.length; ++i) {
          let error = $root.polycentric.QueryReferencesResponseEventItem.verify(
            message.items[i]
          );
          if (error) return 'items.' + error;
        }
      }
      if (
        message.relatedEvents != null &&
        message.hasOwnProperty('relatedEvents')
      ) {
        if (!Array.isArray(message.relatedEvents))
          return 'relatedEvents: array expected';
        for (let i = 0; i < message.relatedEvents.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(
            message.relatedEvents[i]
          );
          if (error) return 'relatedEvents.' + error;
        }
      }
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        properties._cursor = 1;
        if (
          !(
            (message.cursor && typeof message.cursor.length === 'number') ||
            $util.isString(message.cursor)
          )
        )
          return 'cursor: buffer expected';
      }
      if (message.counts != null && message.hasOwnProperty('counts')) {
        if (!Array.isArray(message.counts)) return 'counts: array expected';
        for (let i = 0; i < message.counts.length; ++i)
          if (
            !$util.isInteger(message.counts[i]) &&
            !(
              message.counts[i] &&
              $util.isInteger(message.counts[i].low) &&
              $util.isInteger(message.counts[i].high)
            )
          )
            return 'counts: integer|Long[] expected';
      }
      return null;
    };

    /**
     * Creates a QueryReferencesResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryReferencesResponse} QueryReferencesResponse
     */
    QueryReferencesResponse.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryReferencesResponse)
        return object;
      let message = new $root.polycentric.QueryReferencesResponse();
      if (object.items) {
        if (!Array.isArray(object.items))
          throw TypeError(
            '.polycentric.QueryReferencesResponse.items: array expected'
          );
        message.items = [];
        for (let i = 0; i < object.items.length; ++i) {
          if (typeof object.items[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryReferencesResponse.items: object expected'
            );
          message.items[i] =
            $root.polycentric.QueryReferencesResponseEventItem.fromObject(
              object.items[i]
            );
        }
      }
      if (object.relatedEvents) {
        if (!Array.isArray(object.relatedEvents))
          throw TypeError(
            '.polycentric.QueryReferencesResponse.relatedEvents: array expected'
          );
        message.relatedEvents = [];
        for (let i = 0; i < object.relatedEvents.length; ++i) {
          if (typeof object.relatedEvents[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryReferencesResponse.relatedEvents: object expected'
            );
          message.relatedEvents[i] = $root.polycentric.SignedEvent.fromObject(
            object.relatedEvents[i]
          );
        }
      }
      if (object.cursor != null)
        if (typeof object.cursor === 'string')
          $util.base64.decode(
            object.cursor,
            (message.cursor = $util.newBuffer(
              $util.base64.length(object.cursor)
            )),
            0
          );
        else if (object.cursor.length >= 0) message.cursor = object.cursor;
      if (object.counts) {
        if (!Array.isArray(object.counts))
          throw TypeError(
            '.polycentric.QueryReferencesResponse.counts: array expected'
          );
        message.counts = [];
        for (let i = 0; i < object.counts.length; ++i)
          if ($util.Long)
            (message.counts[i] = $util.Long.fromValue(
              object.counts[i]
            )).unsigned = true;
          else if (typeof object.counts[i] === 'string')
            message.counts[i] = parseInt(object.counts[i], 10);
          else if (typeof object.counts[i] === 'number')
            message.counts[i] = object.counts[i];
          else if (typeof object.counts[i] === 'object')
            message.counts[i] = new $util.LongBits(
              object.counts[i].low >>> 0,
              object.counts[i].high >>> 0
            ).toNumber(true);
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryReferencesResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {polycentric.QueryReferencesResponse} message QueryReferencesResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryReferencesResponse.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.items = [];
        object.relatedEvents = [];
        object.counts = [];
      }
      if (message.items && message.items.length) {
        object.items = [];
        for (let j = 0; j < message.items.length; ++j)
          object.items[j] =
            $root.polycentric.QueryReferencesResponseEventItem.toObject(
              message.items[j],
              options
            );
      }
      if (message.relatedEvents && message.relatedEvents.length) {
        object.relatedEvents = [];
        for (let j = 0; j < message.relatedEvents.length; ++j)
          object.relatedEvents[j] = $root.polycentric.SignedEvent.toObject(
            message.relatedEvents[j],
            options
          );
      }
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        object.cursor =
          options.bytes === String
            ? $util.base64.encode(message.cursor, 0, message.cursor.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.cursor)
              : message.cursor;
        if (options.oneofs) object._cursor = 'cursor';
      }
      if (message.counts && message.counts.length) {
        object.counts = [];
        for (let j = 0; j < message.counts.length; ++j)
          if (typeof message.counts[j] === 'number')
            object.counts[j] =
              options.longs === String
                ? String(message.counts[j])
                : message.counts[j];
          else
            object.counts[j] =
              options.longs === String
                ? $util.Long.prototype.toString.call(message.counts[j])
                : options.longs === Number
                  ? new $util.LongBits(
                      message.counts[j].low >>> 0,
                      message.counts[j].high >>> 0
                    ).toNumber(true)
                  : message.counts[j];
      }
      return object;
    };

    /**
     * Converts this QueryReferencesResponse to JSON.
     * @function toJSON
     * @memberof polycentric.QueryReferencesResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryReferencesResponse.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryReferencesResponse
     * @function getTypeUrl
     * @memberof polycentric.QueryReferencesResponse
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryReferencesResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryReferencesResponse';
    };

    return QueryReferencesResponse;
  })();

  polycentric.QueryClaimToSystemRequest = (function () {
    /**
     * Properties of a QueryClaimToSystemRequest.
     * @memberof polycentric
     * @interface IQueryClaimToSystemRequest
     * @property {number|Long|null} [claimType] QueryClaimToSystemRequest claimType
     * @property {polycentric.IPublicKey|null} [trustRoot] QueryClaimToSystemRequest trustRoot
     * @property {string|null} [matchAnyField] QueryClaimToSystemRequest matchAnyField
     * @property {polycentric.IQueryClaimToSystemRequestMatchAll|null} [matchAllFields] QueryClaimToSystemRequest matchAllFields
     */

    /**
     * Constructs a new QueryClaimToSystemRequest.
     * @memberof polycentric
     * @classdesc Represents a QueryClaimToSystemRequest.
     * @implements IQueryClaimToSystemRequest
     * @constructor
     * @param {polycentric.IQueryClaimToSystemRequest=} [properties] Properties to set
     */
    function QueryClaimToSystemRequest(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryClaimToSystemRequest claimType.
     * @member {number|Long} claimType
     * @memberof polycentric.QueryClaimToSystemRequest
     * @instance
     */
    QueryClaimToSystemRequest.prototype.claimType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * QueryClaimToSystemRequest trustRoot.
     * @member {polycentric.IPublicKey|null|undefined} trustRoot
     * @memberof polycentric.QueryClaimToSystemRequest
     * @instance
     */
    QueryClaimToSystemRequest.prototype.trustRoot = null;

    /**
     * QueryClaimToSystemRequest matchAnyField.
     * @member {string|null|undefined} matchAnyField
     * @memberof polycentric.QueryClaimToSystemRequest
     * @instance
     */
    QueryClaimToSystemRequest.prototype.matchAnyField = null;

    /**
     * QueryClaimToSystemRequest matchAllFields.
     * @member {polycentric.IQueryClaimToSystemRequestMatchAll|null|undefined} matchAllFields
     * @memberof polycentric.QueryClaimToSystemRequest
     * @instance
     */
    QueryClaimToSystemRequest.prototype.matchAllFields = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    /**
     * QueryClaimToSystemRequest query.
     * @member {"matchAnyField"|"matchAllFields"|undefined} query
     * @memberof polycentric.QueryClaimToSystemRequest
     * @instance
     */
    Object.defineProperty(QueryClaimToSystemRequest.prototype, 'query', {
      get: $util.oneOfGetter(
        ($oneOfFields = ['matchAnyField', 'matchAllFields'])
      ),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new QueryClaimToSystemRequest instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {polycentric.IQueryClaimToSystemRequest=} [properties] Properties to set
     * @returns {polycentric.QueryClaimToSystemRequest} QueryClaimToSystemRequest instance
     */
    QueryClaimToSystemRequest.create = function create(properties) {
      return new QueryClaimToSystemRequest(properties);
    };

    /**
     * Encodes the specified QueryClaimToSystemRequest message. Does not implicitly {@link polycentric.QueryClaimToSystemRequest.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {polycentric.IQueryClaimToSystemRequest} message QueryClaimToSystemRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemRequest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.claimType != null &&
        Object.hasOwnProperty.call(message, 'claimType')
      )
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.claimType);
      if (
        message.trustRoot != null &&
        Object.hasOwnProperty.call(message, 'trustRoot')
      )
        $root.polycentric.PublicKey.encode(
          message.trustRoot,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (
        message.matchAnyField != null &&
        Object.hasOwnProperty.call(message, 'matchAnyField')
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.matchAnyField);
      if (
        message.matchAllFields != null &&
        Object.hasOwnProperty.call(message, 'matchAllFields')
      )
        $root.polycentric.QueryClaimToSystemRequestMatchAll.encode(
          message.matchAllFields,
          writer.uint32(/* id 4, wireType 2 =*/ 34).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified QueryClaimToSystemRequest message, length delimited. Does not implicitly {@link polycentric.QueryClaimToSystemRequest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {polycentric.IQueryClaimToSystemRequest} message QueryClaimToSystemRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemRequest.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryClaimToSystemRequest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryClaimToSystemRequest} QueryClaimToSystemRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemRequest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryClaimToSystemRequest();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.claimType = reader.uint64();
            break;
          }
          case 2: {
            message.trustRoot = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            message.matchAnyField = reader.string();
            break;
          }
          case 4: {
            message.matchAllFields =
              $root.polycentric.QueryClaimToSystemRequestMatchAll.decode(
                reader,
                reader.uint32()
              );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryClaimToSystemRequest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryClaimToSystemRequest} QueryClaimToSystemRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemRequest.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryClaimToSystemRequest message.
     * @function verify
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryClaimToSystemRequest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.claimType != null && message.hasOwnProperty('claimType'))
        if (
          !$util.isInteger(message.claimType) &&
          !(
            message.claimType &&
            $util.isInteger(message.claimType.low) &&
            $util.isInteger(message.claimType.high)
          )
        )
          return 'claimType: integer|Long expected';
      if (message.trustRoot != null && message.hasOwnProperty('trustRoot')) {
        let error = $root.polycentric.PublicKey.verify(message.trustRoot);
        if (error) return 'trustRoot.' + error;
      }
      if (
        message.matchAnyField != null &&
        message.hasOwnProperty('matchAnyField')
      ) {
        properties.query = 1;
        if (!$util.isString(message.matchAnyField))
          return 'matchAnyField: string expected';
      }
      if (
        message.matchAllFields != null &&
        message.hasOwnProperty('matchAllFields')
      ) {
        if (properties.query === 1) return 'query: multiple values';
        properties.query = 1;
        {
          let error =
            $root.polycentric.QueryClaimToSystemRequestMatchAll.verify(
              message.matchAllFields
            );
          if (error) return 'matchAllFields.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a QueryClaimToSystemRequest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryClaimToSystemRequest} QueryClaimToSystemRequest
     */
    QueryClaimToSystemRequest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryClaimToSystemRequest)
        return object;
      let message = new $root.polycentric.QueryClaimToSystemRequest();
      if (object.claimType != null)
        if ($util.Long)
          (message.claimType = $util.Long.fromValue(
            object.claimType
          )).unsigned = true;
        else if (typeof object.claimType === 'string')
          message.claimType = parseInt(object.claimType, 10);
        else if (typeof object.claimType === 'number')
          message.claimType = object.claimType;
        else if (typeof object.claimType === 'object')
          message.claimType = new $util.LongBits(
            object.claimType.low >>> 0,
            object.claimType.high >>> 0
          ).toNumber(true);
      if (object.trustRoot != null) {
        if (typeof object.trustRoot !== 'object')
          throw TypeError(
            '.polycentric.QueryClaimToSystemRequest.trustRoot: object expected'
          );
        message.trustRoot = $root.polycentric.PublicKey.fromObject(
          object.trustRoot
        );
      }
      if (object.matchAnyField != null)
        message.matchAnyField = String(object.matchAnyField);
      if (object.matchAllFields != null) {
        if (typeof object.matchAllFields !== 'object')
          throw TypeError(
            '.polycentric.QueryClaimToSystemRequest.matchAllFields: object expected'
          );
        message.matchAllFields =
          $root.polycentric.QueryClaimToSystemRequestMatchAll.fromObject(
            object.matchAllFields
          );
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryClaimToSystemRequest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {polycentric.QueryClaimToSystemRequest} message QueryClaimToSystemRequest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryClaimToSystemRequest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.claimType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.claimType = options.longs === String ? '0' : 0;
        object.trustRoot = null;
      }
      if (message.claimType != null && message.hasOwnProperty('claimType'))
        if (typeof message.claimType === 'number')
          object.claimType =
            options.longs === String
              ? String(message.claimType)
              : message.claimType;
        else
          object.claimType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.claimType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.claimType.low >>> 0,
                    message.claimType.high >>> 0
                  ).toNumber(true)
                : message.claimType;
      if (message.trustRoot != null && message.hasOwnProperty('trustRoot'))
        object.trustRoot = $root.polycentric.PublicKey.toObject(
          message.trustRoot,
          options
        );
      if (
        message.matchAnyField != null &&
        message.hasOwnProperty('matchAnyField')
      ) {
        object.matchAnyField = message.matchAnyField;
        if (options.oneofs) object.query = 'matchAnyField';
      }
      if (
        message.matchAllFields != null &&
        message.hasOwnProperty('matchAllFields')
      ) {
        object.matchAllFields =
          $root.polycentric.QueryClaimToSystemRequestMatchAll.toObject(
            message.matchAllFields,
            options
          );
        if (options.oneofs) object.query = 'matchAllFields';
      }
      return object;
    };

    /**
     * Converts this QueryClaimToSystemRequest to JSON.
     * @function toJSON
     * @memberof polycentric.QueryClaimToSystemRequest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryClaimToSystemRequest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryClaimToSystemRequest
     * @function getTypeUrl
     * @memberof polycentric.QueryClaimToSystemRequest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryClaimToSystemRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryClaimToSystemRequest';
    };

    return QueryClaimToSystemRequest;
  })();

  polycentric.QueryClaimToSystemRequestMatchAll = (function () {
    /**
     * Properties of a QueryClaimToSystemRequestMatchAll.
     * @memberof polycentric
     * @interface IQueryClaimToSystemRequestMatchAll
     * @property {Array.<polycentric.IClaimFieldEntry>|null} [fields] QueryClaimToSystemRequestMatchAll fields
     */

    /**
     * Constructs a new QueryClaimToSystemRequestMatchAll.
     * @memberof polycentric
     * @classdesc Represents a QueryClaimToSystemRequestMatchAll.
     * @implements IQueryClaimToSystemRequestMatchAll
     * @constructor
     * @param {polycentric.IQueryClaimToSystemRequestMatchAll=} [properties] Properties to set
     */
    function QueryClaimToSystemRequestMatchAll(properties) {
      this.fields = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryClaimToSystemRequestMatchAll fields.
     * @member {Array.<polycentric.IClaimFieldEntry>} fields
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @instance
     */
    QueryClaimToSystemRequestMatchAll.prototype.fields = $util.emptyArray;

    /**
     * Creates a new QueryClaimToSystemRequestMatchAll instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {polycentric.IQueryClaimToSystemRequestMatchAll=} [properties] Properties to set
     * @returns {polycentric.QueryClaimToSystemRequestMatchAll} QueryClaimToSystemRequestMatchAll instance
     */
    QueryClaimToSystemRequestMatchAll.create = function create(properties) {
      return new QueryClaimToSystemRequestMatchAll(properties);
    };

    /**
     * Encodes the specified QueryClaimToSystemRequestMatchAll message. Does not implicitly {@link polycentric.QueryClaimToSystemRequestMatchAll.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {polycentric.IQueryClaimToSystemRequestMatchAll} message QueryClaimToSystemRequestMatchAll message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemRequestMatchAll.encode = function encode(
      message,
      writer
    ) {
      if (!writer) writer = $Writer.create();
      if (message.fields != null && message.fields.length)
        for (let i = 0; i < message.fields.length; ++i)
          $root.polycentric.ClaimFieldEntry.encode(
            message.fields[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified QueryClaimToSystemRequestMatchAll message, length delimited. Does not implicitly {@link polycentric.QueryClaimToSystemRequestMatchAll.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {polycentric.IQueryClaimToSystemRequestMatchAll} message QueryClaimToSystemRequestMatchAll message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemRequestMatchAll.encodeDelimited =
      function encodeDelimited(message, writer) {
        return this.encode(message, writer).ldelim();
      };

    /**
     * Decodes a QueryClaimToSystemRequestMatchAll message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryClaimToSystemRequestMatchAll} QueryClaimToSystemRequestMatchAll
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemRequestMatchAll.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryClaimToSystemRequestMatchAll();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.fields && message.fields.length)) message.fields = [];
            message.fields.push(
              $root.polycentric.ClaimFieldEntry.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryClaimToSystemRequestMatchAll message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryClaimToSystemRequestMatchAll} QueryClaimToSystemRequestMatchAll
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemRequestMatchAll.decodeDelimited =
      function decodeDelimited(reader) {
        if (!(reader instanceof $Reader)) reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };

    /**
     * Verifies a QueryClaimToSystemRequestMatchAll message.
     * @function verify
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryClaimToSystemRequestMatchAll.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.fields != null && message.hasOwnProperty('fields')) {
        if (!Array.isArray(message.fields)) return 'fields: array expected';
        for (let i = 0; i < message.fields.length; ++i) {
          let error = $root.polycentric.ClaimFieldEntry.verify(
            message.fields[i]
          );
          if (error) return 'fields.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a QueryClaimToSystemRequestMatchAll message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryClaimToSystemRequestMatchAll} QueryClaimToSystemRequestMatchAll
     */
    QueryClaimToSystemRequestMatchAll.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryClaimToSystemRequestMatchAll)
        return object;
      let message = new $root.polycentric.QueryClaimToSystemRequestMatchAll();
      if (object.fields) {
        if (!Array.isArray(object.fields))
          throw TypeError(
            '.polycentric.QueryClaimToSystemRequestMatchAll.fields: array expected'
          );
        message.fields = [];
        for (let i = 0; i < object.fields.length; ++i) {
          if (typeof object.fields[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryClaimToSystemRequestMatchAll.fields: object expected'
            );
          message.fields[i] = $root.polycentric.ClaimFieldEntry.fromObject(
            object.fields[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryClaimToSystemRequestMatchAll message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {polycentric.QueryClaimToSystemRequestMatchAll} message QueryClaimToSystemRequestMatchAll
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryClaimToSystemRequestMatchAll.toObject = function toObject(
      message,
      options
    ) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.fields = [];
      if (message.fields && message.fields.length) {
        object.fields = [];
        for (let j = 0; j < message.fields.length; ++j)
          object.fields[j] = $root.polycentric.ClaimFieldEntry.toObject(
            message.fields[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this QueryClaimToSystemRequestMatchAll to JSON.
     * @function toJSON
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryClaimToSystemRequestMatchAll.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryClaimToSystemRequestMatchAll
     * @function getTypeUrl
     * @memberof polycentric.QueryClaimToSystemRequestMatchAll
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryClaimToSystemRequestMatchAll.getTypeUrl = function getTypeUrl(
      typeUrlPrefix
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryClaimToSystemRequestMatchAll';
    };

    return QueryClaimToSystemRequestMatchAll;
  })();

  polycentric.QueryClaimToSystemResponse = (function () {
    /**
     * Properties of a QueryClaimToSystemResponse.
     * @memberof polycentric
     * @interface IQueryClaimToSystemResponse
     * @property {Array.<polycentric.IQueryClaimToSystemResponseMatch>|null} [matches] QueryClaimToSystemResponse matches
     */

    /**
     * Constructs a new QueryClaimToSystemResponse.
     * @memberof polycentric
     * @classdesc Represents a QueryClaimToSystemResponse.
     * @implements IQueryClaimToSystemResponse
     * @constructor
     * @param {polycentric.IQueryClaimToSystemResponse=} [properties] Properties to set
     */
    function QueryClaimToSystemResponse(properties) {
      this.matches = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryClaimToSystemResponse matches.
     * @member {Array.<polycentric.IQueryClaimToSystemResponseMatch>} matches
     * @memberof polycentric.QueryClaimToSystemResponse
     * @instance
     */
    QueryClaimToSystemResponse.prototype.matches = $util.emptyArray;

    /**
     * Creates a new QueryClaimToSystemResponse instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {polycentric.IQueryClaimToSystemResponse=} [properties] Properties to set
     * @returns {polycentric.QueryClaimToSystemResponse} QueryClaimToSystemResponse instance
     */
    QueryClaimToSystemResponse.create = function create(properties) {
      return new QueryClaimToSystemResponse(properties);
    };

    /**
     * Encodes the specified QueryClaimToSystemResponse message. Does not implicitly {@link polycentric.QueryClaimToSystemResponse.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {polycentric.IQueryClaimToSystemResponse} message QueryClaimToSystemResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemResponse.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.matches != null && message.matches.length)
        for (let i = 0; i < message.matches.length; ++i)
          $root.polycentric.QueryClaimToSystemResponseMatch.encode(
            message.matches[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified QueryClaimToSystemResponse message, length delimited. Does not implicitly {@link polycentric.QueryClaimToSystemResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {polycentric.IQueryClaimToSystemResponse} message QueryClaimToSystemResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemResponse.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryClaimToSystemResponse message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryClaimToSystemResponse} QueryClaimToSystemResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemResponse.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryClaimToSystemResponse();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.matches && message.matches.length))
              message.matches = [];
            message.matches.push(
              $root.polycentric.QueryClaimToSystemResponseMatch.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryClaimToSystemResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryClaimToSystemResponse} QueryClaimToSystemResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemResponse.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryClaimToSystemResponse message.
     * @function verify
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryClaimToSystemResponse.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.matches != null && message.hasOwnProperty('matches')) {
        if (!Array.isArray(message.matches)) return 'matches: array expected';
        for (let i = 0; i < message.matches.length; ++i) {
          let error = $root.polycentric.QueryClaimToSystemResponseMatch.verify(
            message.matches[i]
          );
          if (error) return 'matches.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a QueryClaimToSystemResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryClaimToSystemResponse} QueryClaimToSystemResponse
     */
    QueryClaimToSystemResponse.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryClaimToSystemResponse)
        return object;
      let message = new $root.polycentric.QueryClaimToSystemResponse();
      if (object.matches) {
        if (!Array.isArray(object.matches))
          throw TypeError(
            '.polycentric.QueryClaimToSystemResponse.matches: array expected'
          );
        message.matches = [];
        for (let i = 0; i < object.matches.length; ++i) {
          if (typeof object.matches[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryClaimToSystemResponse.matches: object expected'
            );
          message.matches[i] =
            $root.polycentric.QueryClaimToSystemResponseMatch.fromObject(
              object.matches[i]
            );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryClaimToSystemResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {polycentric.QueryClaimToSystemResponse} message QueryClaimToSystemResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryClaimToSystemResponse.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.matches = [];
      if (message.matches && message.matches.length) {
        object.matches = [];
        for (let j = 0; j < message.matches.length; ++j)
          object.matches[j] =
            $root.polycentric.QueryClaimToSystemResponseMatch.toObject(
              message.matches[j],
              options
            );
      }
      return object;
    };

    /**
     * Converts this QueryClaimToSystemResponse to JSON.
     * @function toJSON
     * @memberof polycentric.QueryClaimToSystemResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryClaimToSystemResponse.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryClaimToSystemResponse
     * @function getTypeUrl
     * @memberof polycentric.QueryClaimToSystemResponse
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryClaimToSystemResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryClaimToSystemResponse';
    };

    return QueryClaimToSystemResponse;
  })();

  polycentric.QueryClaimToSystemResponseMatch = (function () {
    /**
     * Properties of a QueryClaimToSystemResponseMatch.
     * @memberof polycentric
     * @interface IQueryClaimToSystemResponseMatch
     * @property {polycentric.ISignedEvent|null} [claim] QueryClaimToSystemResponseMatch claim
     * @property {Array.<polycentric.ISignedEvent>|null} [proofChain] QueryClaimToSystemResponseMatch proofChain
     */

    /**
     * Constructs a new QueryClaimToSystemResponseMatch.
     * @memberof polycentric
     * @classdesc Represents a QueryClaimToSystemResponseMatch.
     * @implements IQueryClaimToSystemResponseMatch
     * @constructor
     * @param {polycentric.IQueryClaimToSystemResponseMatch=} [properties] Properties to set
     */
    function QueryClaimToSystemResponseMatch(properties) {
      this.proofChain = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryClaimToSystemResponseMatch claim.
     * @member {polycentric.ISignedEvent|null|undefined} claim
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @instance
     */
    QueryClaimToSystemResponseMatch.prototype.claim = null;

    /**
     * QueryClaimToSystemResponseMatch proofChain.
     * @member {Array.<polycentric.ISignedEvent>} proofChain
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @instance
     */
    QueryClaimToSystemResponseMatch.prototype.proofChain = $util.emptyArray;

    /**
     * Creates a new QueryClaimToSystemResponseMatch instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {polycentric.IQueryClaimToSystemResponseMatch=} [properties] Properties to set
     * @returns {polycentric.QueryClaimToSystemResponseMatch} QueryClaimToSystemResponseMatch instance
     */
    QueryClaimToSystemResponseMatch.create = function create(properties) {
      return new QueryClaimToSystemResponseMatch(properties);
    };

    /**
     * Encodes the specified QueryClaimToSystemResponseMatch message. Does not implicitly {@link polycentric.QueryClaimToSystemResponseMatch.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {polycentric.IQueryClaimToSystemResponseMatch} message QueryClaimToSystemResponseMatch message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemResponseMatch.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.claim != null && Object.hasOwnProperty.call(message, 'claim'))
        $root.polycentric.SignedEvent.encode(
          message.claim,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (message.proofChain != null && message.proofChain.length)
        for (let i = 0; i < message.proofChain.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.proofChain[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified QueryClaimToSystemResponseMatch message, length delimited. Does not implicitly {@link polycentric.QueryClaimToSystemResponseMatch.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {polycentric.IQueryClaimToSystemResponseMatch} message QueryClaimToSystemResponseMatch message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryClaimToSystemResponseMatch.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryClaimToSystemResponseMatch message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryClaimToSystemResponseMatch} QueryClaimToSystemResponseMatch
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemResponseMatch.decode = function decode(
      reader,
      length,
      error
    ) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryClaimToSystemResponseMatch();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.claim = $root.polycentric.SignedEvent.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            if (!(message.proofChain && message.proofChain.length))
              message.proofChain = [];
            message.proofChain.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryClaimToSystemResponseMatch message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryClaimToSystemResponseMatch} QueryClaimToSystemResponseMatch
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryClaimToSystemResponseMatch.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryClaimToSystemResponseMatch message.
     * @function verify
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryClaimToSystemResponseMatch.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.claim != null && message.hasOwnProperty('claim')) {
        let error = $root.polycentric.SignedEvent.verify(message.claim);
        if (error) return 'claim.' + error;
      }
      if (message.proofChain != null && message.hasOwnProperty('proofChain')) {
        if (!Array.isArray(message.proofChain))
          return 'proofChain: array expected';
        for (let i = 0; i < message.proofChain.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(
            message.proofChain[i]
          );
          if (error) return 'proofChain.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a QueryClaimToSystemResponseMatch message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryClaimToSystemResponseMatch} QueryClaimToSystemResponseMatch
     */
    QueryClaimToSystemResponseMatch.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryClaimToSystemResponseMatch)
        return object;
      let message = new $root.polycentric.QueryClaimToSystemResponseMatch();
      if (object.claim != null) {
        if (typeof object.claim !== 'object')
          throw TypeError(
            '.polycentric.QueryClaimToSystemResponseMatch.claim: object expected'
          );
        message.claim = $root.polycentric.SignedEvent.fromObject(object.claim);
      }
      if (object.proofChain) {
        if (!Array.isArray(object.proofChain))
          throw TypeError(
            '.polycentric.QueryClaimToSystemResponseMatch.proofChain: array expected'
          );
        message.proofChain = [];
        for (let i = 0; i < object.proofChain.length; ++i) {
          if (typeof object.proofChain[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryClaimToSystemResponseMatch.proofChain: object expected'
            );
          message.proofChain[i] = $root.polycentric.SignedEvent.fromObject(
            object.proofChain[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryClaimToSystemResponseMatch message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {polycentric.QueryClaimToSystemResponseMatch} message QueryClaimToSystemResponseMatch
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryClaimToSystemResponseMatch.toObject = function toObject(
      message,
      options
    ) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.proofChain = [];
      if (options.defaults) object.claim = null;
      if (message.claim != null && message.hasOwnProperty('claim'))
        object.claim = $root.polycentric.SignedEvent.toObject(
          message.claim,
          options
        );
      if (message.proofChain && message.proofChain.length) {
        object.proofChain = [];
        for (let j = 0; j < message.proofChain.length; ++j)
          object.proofChain[j] = $root.polycentric.SignedEvent.toObject(
            message.proofChain[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this QueryClaimToSystemResponseMatch to JSON.
     * @function toJSON
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryClaimToSystemResponseMatch.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryClaimToSystemResponseMatch
     * @function getTypeUrl
     * @memberof polycentric.QueryClaimToSystemResponseMatch
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryClaimToSystemResponseMatch.getTypeUrl = function getTypeUrl(
      typeUrlPrefix
    ) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryClaimToSystemResponseMatch';
    };

    return QueryClaimToSystemResponseMatch;
  })();

  polycentric.QueryIndexResponse = (function () {
    /**
     * Properties of a QueryIndexResponse.
     * @memberof polycentric
     * @interface IQueryIndexResponse
     * @property {Array.<polycentric.ISignedEvent>|null} [events] QueryIndexResponse events
     * @property {Array.<polycentric.ISignedEvent>|null} [proof] QueryIndexResponse proof
     */

    /**
     * Constructs a new QueryIndexResponse.
     * @memberof polycentric
     * @classdesc Represents a QueryIndexResponse.
     * @implements IQueryIndexResponse
     * @constructor
     * @param {polycentric.IQueryIndexResponse=} [properties] Properties to set
     */
    function QueryIndexResponse(properties) {
      this.events = [];
      this.proof = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * QueryIndexResponse events.
     * @member {Array.<polycentric.ISignedEvent>} events
     * @memberof polycentric.QueryIndexResponse
     * @instance
     */
    QueryIndexResponse.prototype.events = $util.emptyArray;

    /**
     * QueryIndexResponse proof.
     * @member {Array.<polycentric.ISignedEvent>} proof
     * @memberof polycentric.QueryIndexResponse
     * @instance
     */
    QueryIndexResponse.prototype.proof = $util.emptyArray;

    /**
     * Creates a new QueryIndexResponse instance using the specified properties.
     * @function create
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {polycentric.IQueryIndexResponse=} [properties] Properties to set
     * @returns {polycentric.QueryIndexResponse} QueryIndexResponse instance
     */
    QueryIndexResponse.create = function create(properties) {
      return new QueryIndexResponse(properties);
    };

    /**
     * Encodes the specified QueryIndexResponse message. Does not implicitly {@link polycentric.QueryIndexResponse.verify|verify} messages.
     * @function encode
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {polycentric.IQueryIndexResponse} message QueryIndexResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryIndexResponse.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.events != null && message.events.length)
        for (let i = 0; i < message.events.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.events[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      if (message.proof != null && message.proof.length)
        for (let i = 0; i < message.proof.length; ++i)
          $root.polycentric.SignedEvent.encode(
            message.proof[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified QueryIndexResponse message, length delimited. Does not implicitly {@link polycentric.QueryIndexResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {polycentric.IQueryIndexResponse} message QueryIndexResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    QueryIndexResponse.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a QueryIndexResponse message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.QueryIndexResponse} QueryIndexResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryIndexResponse.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.QueryIndexResponse();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.events && message.events.length)) message.events = [];
            message.events.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          case 2: {
            if (!(message.proof && message.proof.length)) message.proof = [];
            message.proof.push(
              $root.polycentric.SignedEvent.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a QueryIndexResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.QueryIndexResponse} QueryIndexResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    QueryIndexResponse.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a QueryIndexResponse message.
     * @function verify
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    QueryIndexResponse.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.events != null && message.hasOwnProperty('events')) {
        if (!Array.isArray(message.events)) return 'events: array expected';
        for (let i = 0; i < message.events.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(message.events[i]);
          if (error) return 'events.' + error;
        }
      }
      if (message.proof != null && message.hasOwnProperty('proof')) {
        if (!Array.isArray(message.proof)) return 'proof: array expected';
        for (let i = 0; i < message.proof.length; ++i) {
          let error = $root.polycentric.SignedEvent.verify(message.proof[i]);
          if (error) return 'proof.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a QueryIndexResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.QueryIndexResponse} QueryIndexResponse
     */
    QueryIndexResponse.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.QueryIndexResponse) return object;
      let message = new $root.polycentric.QueryIndexResponse();
      if (object.events) {
        if (!Array.isArray(object.events))
          throw TypeError(
            '.polycentric.QueryIndexResponse.events: array expected'
          );
        message.events = [];
        for (let i = 0; i < object.events.length; ++i) {
          if (typeof object.events[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryIndexResponse.events: object expected'
            );
          message.events[i] = $root.polycentric.SignedEvent.fromObject(
            object.events[i]
          );
        }
      }
      if (object.proof) {
        if (!Array.isArray(object.proof))
          throw TypeError(
            '.polycentric.QueryIndexResponse.proof: array expected'
          );
        message.proof = [];
        for (let i = 0; i < object.proof.length; ++i) {
          if (typeof object.proof[i] !== 'object')
            throw TypeError(
              '.polycentric.QueryIndexResponse.proof: object expected'
            );
          message.proof[i] = $root.polycentric.SignedEvent.fromObject(
            object.proof[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a QueryIndexResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {polycentric.QueryIndexResponse} message QueryIndexResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    QueryIndexResponse.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.events = [];
        object.proof = [];
      }
      if (message.events && message.events.length) {
        object.events = [];
        for (let j = 0; j < message.events.length; ++j)
          object.events[j] = $root.polycentric.SignedEvent.toObject(
            message.events[j],
            options
          );
      }
      if (message.proof && message.proof.length) {
        object.proof = [];
        for (let j = 0; j < message.proof.length; ++j)
          object.proof[j] = $root.polycentric.SignedEvent.toObject(
            message.proof[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this QueryIndexResponse to JSON.
     * @function toJSON
     * @memberof polycentric.QueryIndexResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    QueryIndexResponse.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for QueryIndexResponse
     * @function getTypeUrl
     * @memberof polycentric.QueryIndexResponse
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    QueryIndexResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.QueryIndexResponse';
    };

    return QueryIndexResponse;
  })();

  polycentric.URLInfoDataLink = (function () {
    /**
     * Properties of a URLInfoDataLink.
     * @memberof polycentric
     * @interface IURLInfoDataLink
     * @property {polycentric.IPublicKey|null} [system] URLInfoDataLink system
     * @property {polycentric.IProcess|null} [process] URLInfoDataLink process
     * @property {Array.<string>|null} [servers] URLInfoDataLink servers
     * @property {number|Long|null} [byteCount] URLInfoDataLink byteCount
     * @property {Array.<polycentric.IRange>|null} [sections] URLInfoDataLink sections
     * @property {string|null} [mime] URLInfoDataLink mime
     */

    /**
     * Constructs a new URLInfoDataLink.
     * @memberof polycentric
     * @classdesc Represents a URLInfoDataLink.
     * @implements IURLInfoDataLink
     * @constructor
     * @param {polycentric.IURLInfoDataLink=} [properties] Properties to set
     */
    function URLInfoDataLink(properties) {
      this.servers = [];
      this.sections = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * URLInfoDataLink system.
     * @member {polycentric.IPublicKey|null|undefined} system
     * @memberof polycentric.URLInfoDataLink
     * @instance
     */
    URLInfoDataLink.prototype.system = null;

    /**
     * URLInfoDataLink process.
     * @member {polycentric.IProcess|null|undefined} process
     * @memberof polycentric.URLInfoDataLink
     * @instance
     */
    URLInfoDataLink.prototype.process = null;

    /**
     * URLInfoDataLink servers.
     * @member {Array.<string>} servers
     * @memberof polycentric.URLInfoDataLink
     * @instance
     */
    URLInfoDataLink.prototype.servers = $util.emptyArray;

    /**
     * URLInfoDataLink byteCount.
     * @member {number|Long} byteCount
     * @memberof polycentric.URLInfoDataLink
     * @instance
     */
    URLInfoDataLink.prototype.byteCount = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * URLInfoDataLink sections.
     * @member {Array.<polycentric.IRange>} sections
     * @memberof polycentric.URLInfoDataLink
     * @instance
     */
    URLInfoDataLink.prototype.sections = $util.emptyArray;

    /**
     * URLInfoDataLink mime.
     * @member {string|null|undefined} mime
     * @memberof polycentric.URLInfoDataLink
     * @instance
     */
    URLInfoDataLink.prototype.mime = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(URLInfoDataLink.prototype, '_mime', {
      get: $util.oneOfGetter(($oneOfFields = ['mime'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new URLInfoDataLink instance using the specified properties.
     * @function create
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {polycentric.IURLInfoDataLink=} [properties] Properties to set
     * @returns {polycentric.URLInfoDataLink} URLInfoDataLink instance
     */
    URLInfoDataLink.create = function create(properties) {
      return new URLInfoDataLink(properties);
    };

    /**
     * Encodes the specified URLInfoDataLink message. Does not implicitly {@link polycentric.URLInfoDataLink.verify|verify} messages.
     * @function encode
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {polycentric.IURLInfoDataLink} message URLInfoDataLink message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfoDataLink.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PublicKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.process != null &&
        Object.hasOwnProperty.call(message, 'process')
      )
        $root.polycentric.Process.encode(
          message.process,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (message.servers != null && message.servers.length)
        for (let i = 0; i < message.servers.length; ++i)
          writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.servers[i]);
      if (
        message.byteCount != null &&
        Object.hasOwnProperty.call(message, 'byteCount')
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).uint64(message.byteCount);
      if (message.sections != null && message.sections.length)
        for (let i = 0; i < message.sections.length; ++i)
          $root.polycentric.Range.encode(
            message.sections[i],
            writer.uint32(/* id 5, wireType 2 =*/ 42).fork()
          ).ldelim();
      if (message.mime != null && Object.hasOwnProperty.call(message, 'mime'))
        writer.uint32(/* id 6, wireType 2 =*/ 50).string(message.mime);
      return writer;
    };

    /**
     * Encodes the specified URLInfoDataLink message, length delimited. Does not implicitly {@link polycentric.URLInfoDataLink.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {polycentric.IURLInfoDataLink} message URLInfoDataLink message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    URLInfoDataLink.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a URLInfoDataLink message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.URLInfoDataLink} URLInfoDataLink
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfoDataLink.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.URLInfoDataLink();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.process = $root.polycentric.Process.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            if (!(message.servers && message.servers.length))
              message.servers = [];
            message.servers.push(reader.string());
            break;
          }
          case 4: {
            message.byteCount = reader.uint64();
            break;
          }
          case 5: {
            if (!(message.sections && message.sections.length))
              message.sections = [];
            message.sections.push(
              $root.polycentric.Range.decode(reader, reader.uint32())
            );
            break;
          }
          case 6: {
            message.mime = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a URLInfoDataLink message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.URLInfoDataLink} URLInfoDataLink
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    URLInfoDataLink.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a URLInfoDataLink message.
     * @function verify
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    URLInfoDataLink.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PublicKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.process != null && message.hasOwnProperty('process')) {
        let error = $root.polycentric.Process.verify(message.process);
        if (error) return 'process.' + error;
      }
      if (message.servers != null && message.hasOwnProperty('servers')) {
        if (!Array.isArray(message.servers)) return 'servers: array expected';
        for (let i = 0; i < message.servers.length; ++i)
          if (!$util.isString(message.servers[i]))
            return 'servers: string[] expected';
      }
      if (message.byteCount != null && message.hasOwnProperty('byteCount'))
        if (
          !$util.isInteger(message.byteCount) &&
          !(
            message.byteCount &&
            $util.isInteger(message.byteCount.low) &&
            $util.isInteger(message.byteCount.high)
          )
        )
          return 'byteCount: integer|Long expected';
      if (message.sections != null && message.hasOwnProperty('sections')) {
        if (!Array.isArray(message.sections)) return 'sections: array expected';
        for (let i = 0; i < message.sections.length; ++i) {
          let error = $root.polycentric.Range.verify(message.sections[i]);
          if (error) return 'sections.' + error;
        }
      }
      if (message.mime != null && message.hasOwnProperty('mime')) {
        properties._mime = 1;
        if (!$util.isString(message.mime)) return 'mime: string expected';
      }
      return null;
    };

    /**
     * Creates a URLInfoDataLink message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.URLInfoDataLink} URLInfoDataLink
     */
    URLInfoDataLink.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.URLInfoDataLink) return object;
      let message = new $root.polycentric.URLInfoDataLink();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError(
            '.polycentric.URLInfoDataLink.system: object expected'
          );
        message.system = $root.polycentric.PublicKey.fromObject(object.system);
      }
      if (object.process != null) {
        if (typeof object.process !== 'object')
          throw TypeError(
            '.polycentric.URLInfoDataLink.process: object expected'
          );
        message.process = $root.polycentric.Process.fromObject(object.process);
      }
      if (object.servers) {
        if (!Array.isArray(object.servers))
          throw TypeError(
            '.polycentric.URLInfoDataLink.servers: array expected'
          );
        message.servers = [];
        for (let i = 0; i < object.servers.length; ++i)
          message.servers[i] = String(object.servers[i]);
      }
      if (object.byteCount != null)
        if ($util.Long)
          (message.byteCount = $util.Long.fromValue(
            object.byteCount
          )).unsigned = true;
        else if (typeof object.byteCount === 'string')
          message.byteCount = parseInt(object.byteCount, 10);
        else if (typeof object.byteCount === 'number')
          message.byteCount = object.byteCount;
        else if (typeof object.byteCount === 'object')
          message.byteCount = new $util.LongBits(
            object.byteCount.low >>> 0,
            object.byteCount.high >>> 0
          ).toNumber(true);
      if (object.sections) {
        if (!Array.isArray(object.sections))
          throw TypeError(
            '.polycentric.URLInfoDataLink.sections: array expected'
          );
        message.sections = [];
        for (let i = 0; i < object.sections.length; ++i) {
          if (typeof object.sections[i] !== 'object')
            throw TypeError(
              '.polycentric.URLInfoDataLink.sections: object expected'
            );
          message.sections[i] = $root.polycentric.Range.fromObject(
            object.sections[i]
          );
        }
      }
      if (object.mime != null) message.mime = String(object.mime);
      return message;
    };

    /**
     * Creates a plain object from a URLInfoDataLink message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {polycentric.URLInfoDataLink} message URLInfoDataLink
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    URLInfoDataLink.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) {
        object.servers = [];
        object.sections = [];
      }
      if (options.defaults) {
        object.system = null;
        object.process = null;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.byteCount =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.byteCount = options.longs === String ? '0' : 0;
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PublicKey.toObject(
          message.system,
          options
        );
      if (message.process != null && message.hasOwnProperty('process'))
        object.process = $root.polycentric.Process.toObject(
          message.process,
          options
        );
      if (message.servers && message.servers.length) {
        object.servers = [];
        for (let j = 0; j < message.servers.length; ++j)
          object.servers[j] = message.servers[j];
      }
      if (message.byteCount != null && message.hasOwnProperty('byteCount'))
        if (typeof message.byteCount === 'number')
          object.byteCount =
            options.longs === String
              ? String(message.byteCount)
              : message.byteCount;
        else
          object.byteCount =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.byteCount)
              : options.longs === Number
                ? new $util.LongBits(
                    message.byteCount.low >>> 0,
                    message.byteCount.high >>> 0
                  ).toNumber(true)
                : message.byteCount;
      if (message.sections && message.sections.length) {
        object.sections = [];
        for (let j = 0; j < message.sections.length; ++j)
          object.sections[j] = $root.polycentric.Range.toObject(
            message.sections[j],
            options
          );
      }
      if (message.mime != null && message.hasOwnProperty('mime')) {
        object.mime = message.mime;
        if (options.oneofs) object._mime = 'mime';
      }
      return object;
    };

    /**
     * Converts this URLInfoDataLink to JSON.
     * @function toJSON
     * @memberof polycentric.URLInfoDataLink
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    URLInfoDataLink.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for URLInfoDataLink
     * @function getTypeUrl
     * @memberof polycentric.URLInfoDataLink
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    URLInfoDataLink.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.URLInfoDataLink';
    };

    return URLInfoDataLink;
  })();

  polycentric.FindClaimAndVouchRequest = (function () {
    /**
     * Properties of a FindClaimAndVouchRequest.
     * @memberof polycentric
     * @interface IFindClaimAndVouchRequest
     * @property {polycentric.IPublicKey|null} [vouchingSystem] FindClaimAndVouchRequest vouchingSystem
     * @property {polycentric.IPublicKey|null} [claimingSystem] FindClaimAndVouchRequest claimingSystem
     * @property {Array.<polycentric.IClaimFieldEntry>|null} [fields] FindClaimAndVouchRequest fields
     * @property {number|Long|null} [claimType] FindClaimAndVouchRequest claimType
     */

    /**
     * Constructs a new FindClaimAndVouchRequest.
     * @memberof polycentric
     * @classdesc Represents a FindClaimAndVouchRequest.
     * @implements IFindClaimAndVouchRequest
     * @constructor
     * @param {polycentric.IFindClaimAndVouchRequest=} [properties] Properties to set
     */
    function FindClaimAndVouchRequest(properties) {
      this.fields = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * FindClaimAndVouchRequest vouchingSystem.
     * @member {polycentric.IPublicKey|null|undefined} vouchingSystem
     * @memberof polycentric.FindClaimAndVouchRequest
     * @instance
     */
    FindClaimAndVouchRequest.prototype.vouchingSystem = null;

    /**
     * FindClaimAndVouchRequest claimingSystem.
     * @member {polycentric.IPublicKey|null|undefined} claimingSystem
     * @memberof polycentric.FindClaimAndVouchRequest
     * @instance
     */
    FindClaimAndVouchRequest.prototype.claimingSystem = null;

    /**
     * FindClaimAndVouchRequest fields.
     * @member {Array.<polycentric.IClaimFieldEntry>} fields
     * @memberof polycentric.FindClaimAndVouchRequest
     * @instance
     */
    FindClaimAndVouchRequest.prototype.fields = $util.emptyArray;

    /**
     * FindClaimAndVouchRequest claimType.
     * @member {number|Long} claimType
     * @memberof polycentric.FindClaimAndVouchRequest
     * @instance
     */
    FindClaimAndVouchRequest.prototype.claimType = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new FindClaimAndVouchRequest instance using the specified properties.
     * @function create
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {polycentric.IFindClaimAndVouchRequest=} [properties] Properties to set
     * @returns {polycentric.FindClaimAndVouchRequest} FindClaimAndVouchRequest instance
     */
    FindClaimAndVouchRequest.create = function create(properties) {
      return new FindClaimAndVouchRequest(properties);
    };

    /**
     * Encodes the specified FindClaimAndVouchRequest message. Does not implicitly {@link polycentric.FindClaimAndVouchRequest.verify|verify} messages.
     * @function encode
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {polycentric.IFindClaimAndVouchRequest} message FindClaimAndVouchRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FindClaimAndVouchRequest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.vouchingSystem != null &&
        Object.hasOwnProperty.call(message, 'vouchingSystem')
      )
        $root.polycentric.PublicKey.encode(
          message.vouchingSystem,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.claimingSystem != null &&
        Object.hasOwnProperty.call(message, 'claimingSystem')
      )
        $root.polycentric.PublicKey.encode(
          message.claimingSystem,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      if (message.fields != null && message.fields.length)
        for (let i = 0; i < message.fields.length; ++i)
          $root.polycentric.ClaimFieldEntry.encode(
            message.fields[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      if (
        message.claimType != null &&
        Object.hasOwnProperty.call(message, 'claimType')
      )
        writer.uint32(/* id 4, wireType 0 =*/ 32).uint64(message.claimType);
      return writer;
    };

    /**
     * Encodes the specified FindClaimAndVouchRequest message, length delimited. Does not implicitly {@link polycentric.FindClaimAndVouchRequest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {polycentric.IFindClaimAndVouchRequest} message FindClaimAndVouchRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FindClaimAndVouchRequest.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FindClaimAndVouchRequest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.FindClaimAndVouchRequest} FindClaimAndVouchRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FindClaimAndVouchRequest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.FindClaimAndVouchRequest();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.vouchingSystem = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.claimingSystem = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 3: {
            if (!(message.fields && message.fields.length)) message.fields = [];
            message.fields.push(
              $root.polycentric.ClaimFieldEntry.decode(reader, reader.uint32())
            );
            break;
          }
          case 4: {
            message.claimType = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a FindClaimAndVouchRequest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.FindClaimAndVouchRequest} FindClaimAndVouchRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FindClaimAndVouchRequest.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FindClaimAndVouchRequest message.
     * @function verify
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FindClaimAndVouchRequest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (
        message.vouchingSystem != null &&
        message.hasOwnProperty('vouchingSystem')
      ) {
        let error = $root.polycentric.PublicKey.verify(message.vouchingSystem);
        if (error) return 'vouchingSystem.' + error;
      }
      if (
        message.claimingSystem != null &&
        message.hasOwnProperty('claimingSystem')
      ) {
        let error = $root.polycentric.PublicKey.verify(message.claimingSystem);
        if (error) return 'claimingSystem.' + error;
      }
      if (message.fields != null && message.hasOwnProperty('fields')) {
        if (!Array.isArray(message.fields)) return 'fields: array expected';
        for (let i = 0; i < message.fields.length; ++i) {
          let error = $root.polycentric.ClaimFieldEntry.verify(
            message.fields[i]
          );
          if (error) return 'fields.' + error;
        }
      }
      if (message.claimType != null && message.hasOwnProperty('claimType'))
        if (
          !$util.isInteger(message.claimType) &&
          !(
            message.claimType &&
            $util.isInteger(message.claimType.low) &&
            $util.isInteger(message.claimType.high)
          )
        )
          return 'claimType: integer|Long expected';
      return null;
    };

    /**
     * Creates a FindClaimAndVouchRequest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.FindClaimAndVouchRequest} FindClaimAndVouchRequest
     */
    FindClaimAndVouchRequest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.FindClaimAndVouchRequest)
        return object;
      let message = new $root.polycentric.FindClaimAndVouchRequest();
      if (object.vouchingSystem != null) {
        if (typeof object.vouchingSystem !== 'object')
          throw TypeError(
            '.polycentric.FindClaimAndVouchRequest.vouchingSystem: object expected'
          );
        message.vouchingSystem = $root.polycentric.PublicKey.fromObject(
          object.vouchingSystem
        );
      }
      if (object.claimingSystem != null) {
        if (typeof object.claimingSystem !== 'object')
          throw TypeError(
            '.polycentric.FindClaimAndVouchRequest.claimingSystem: object expected'
          );
        message.claimingSystem = $root.polycentric.PublicKey.fromObject(
          object.claimingSystem
        );
      }
      if (object.fields) {
        if (!Array.isArray(object.fields))
          throw TypeError(
            '.polycentric.FindClaimAndVouchRequest.fields: array expected'
          );
        message.fields = [];
        for (let i = 0; i < object.fields.length; ++i) {
          if (typeof object.fields[i] !== 'object')
            throw TypeError(
              '.polycentric.FindClaimAndVouchRequest.fields: object expected'
            );
          message.fields[i] = $root.polycentric.ClaimFieldEntry.fromObject(
            object.fields[i]
          );
        }
      }
      if (object.claimType != null)
        if ($util.Long)
          (message.claimType = $util.Long.fromValue(
            object.claimType
          )).unsigned = true;
        else if (typeof object.claimType === 'string')
          message.claimType = parseInt(object.claimType, 10);
        else if (typeof object.claimType === 'number')
          message.claimType = object.claimType;
        else if (typeof object.claimType === 'object')
          message.claimType = new $util.LongBits(
            object.claimType.low >>> 0,
            object.claimType.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a FindClaimAndVouchRequest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {polycentric.FindClaimAndVouchRequest} message FindClaimAndVouchRequest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FindClaimAndVouchRequest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.fields = [];
      if (options.defaults) {
        object.vouchingSystem = null;
        object.claimingSystem = null;
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.claimType =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.claimType = options.longs === String ? '0' : 0;
      }
      if (
        message.vouchingSystem != null &&
        message.hasOwnProperty('vouchingSystem')
      )
        object.vouchingSystem = $root.polycentric.PublicKey.toObject(
          message.vouchingSystem,
          options
        );
      if (
        message.claimingSystem != null &&
        message.hasOwnProperty('claimingSystem')
      )
        object.claimingSystem = $root.polycentric.PublicKey.toObject(
          message.claimingSystem,
          options
        );
      if (message.fields && message.fields.length) {
        object.fields = [];
        for (let j = 0; j < message.fields.length; ++j)
          object.fields[j] = $root.polycentric.ClaimFieldEntry.toObject(
            message.fields[j],
            options
          );
      }
      if (message.claimType != null && message.hasOwnProperty('claimType'))
        if (typeof message.claimType === 'number')
          object.claimType =
            options.longs === String
              ? String(message.claimType)
              : message.claimType;
        else
          object.claimType =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.claimType)
              : options.longs === Number
                ? new $util.LongBits(
                    message.claimType.low >>> 0,
                    message.claimType.high >>> 0
                  ).toNumber(true)
                : message.claimType;
      return object;
    };

    /**
     * Converts this FindClaimAndVouchRequest to JSON.
     * @function toJSON
     * @memberof polycentric.FindClaimAndVouchRequest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FindClaimAndVouchRequest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FindClaimAndVouchRequest
     * @function getTypeUrl
     * @memberof polycentric.FindClaimAndVouchRequest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FindClaimAndVouchRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.FindClaimAndVouchRequest';
    };

    return FindClaimAndVouchRequest;
  })();

  polycentric.FindClaimAndVouchResponse = (function () {
    /**
     * Properties of a FindClaimAndVouchResponse.
     * @memberof polycentric
     * @interface IFindClaimAndVouchResponse
     * @property {polycentric.ISignedEvent|null} [vouch] FindClaimAndVouchResponse vouch
     * @property {polycentric.ISignedEvent|null} [claim] FindClaimAndVouchResponse claim
     */

    /**
     * Constructs a new FindClaimAndVouchResponse.
     * @memberof polycentric
     * @classdesc Represents a FindClaimAndVouchResponse.
     * @implements IFindClaimAndVouchResponse
     * @constructor
     * @param {polycentric.IFindClaimAndVouchResponse=} [properties] Properties to set
     */
    function FindClaimAndVouchResponse(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * FindClaimAndVouchResponse vouch.
     * @member {polycentric.ISignedEvent|null|undefined} vouch
     * @memberof polycentric.FindClaimAndVouchResponse
     * @instance
     */
    FindClaimAndVouchResponse.prototype.vouch = null;

    /**
     * FindClaimAndVouchResponse claim.
     * @member {polycentric.ISignedEvent|null|undefined} claim
     * @memberof polycentric.FindClaimAndVouchResponse
     * @instance
     */
    FindClaimAndVouchResponse.prototype.claim = null;

    /**
     * Creates a new FindClaimAndVouchResponse instance using the specified properties.
     * @function create
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {polycentric.IFindClaimAndVouchResponse=} [properties] Properties to set
     * @returns {polycentric.FindClaimAndVouchResponse} FindClaimAndVouchResponse instance
     */
    FindClaimAndVouchResponse.create = function create(properties) {
      return new FindClaimAndVouchResponse(properties);
    };

    /**
     * Encodes the specified FindClaimAndVouchResponse message. Does not implicitly {@link polycentric.FindClaimAndVouchResponse.verify|verify} messages.
     * @function encode
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {polycentric.IFindClaimAndVouchResponse} message FindClaimAndVouchResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FindClaimAndVouchResponse.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.vouch != null && Object.hasOwnProperty.call(message, 'vouch'))
        $root.polycentric.SignedEvent.encode(
          message.vouch,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (message.claim != null && Object.hasOwnProperty.call(message, 'claim'))
        $root.polycentric.SignedEvent.encode(
          message.claim,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified FindClaimAndVouchResponse message, length delimited. Does not implicitly {@link polycentric.FindClaimAndVouchResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {polycentric.IFindClaimAndVouchResponse} message FindClaimAndVouchResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FindClaimAndVouchResponse.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FindClaimAndVouchResponse message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.FindClaimAndVouchResponse} FindClaimAndVouchResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FindClaimAndVouchResponse.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.FindClaimAndVouchResponse();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.vouch = $root.polycentric.SignedEvent.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.claim = $root.polycentric.SignedEvent.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a FindClaimAndVouchResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.FindClaimAndVouchResponse} FindClaimAndVouchResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FindClaimAndVouchResponse.decodeDelimited = function decodeDelimited(
      reader
    ) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FindClaimAndVouchResponse message.
     * @function verify
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FindClaimAndVouchResponse.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.vouch != null && message.hasOwnProperty('vouch')) {
        let error = $root.polycentric.SignedEvent.verify(message.vouch);
        if (error) return 'vouch.' + error;
      }
      if (message.claim != null && message.hasOwnProperty('claim')) {
        let error = $root.polycentric.SignedEvent.verify(message.claim);
        if (error) return 'claim.' + error;
      }
      return null;
    };

    /**
     * Creates a FindClaimAndVouchResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.FindClaimAndVouchResponse} FindClaimAndVouchResponse
     */
    FindClaimAndVouchResponse.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.FindClaimAndVouchResponse)
        return object;
      let message = new $root.polycentric.FindClaimAndVouchResponse();
      if (object.vouch != null) {
        if (typeof object.vouch !== 'object')
          throw TypeError(
            '.polycentric.FindClaimAndVouchResponse.vouch: object expected'
          );
        message.vouch = $root.polycentric.SignedEvent.fromObject(object.vouch);
      }
      if (object.claim != null) {
        if (typeof object.claim !== 'object')
          throw TypeError(
            '.polycentric.FindClaimAndVouchResponse.claim: object expected'
          );
        message.claim = $root.polycentric.SignedEvent.fromObject(object.claim);
      }
      return message;
    };

    /**
     * Creates a plain object from a FindClaimAndVouchResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {polycentric.FindClaimAndVouchResponse} message FindClaimAndVouchResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FindClaimAndVouchResponse.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.vouch = null;
        object.claim = null;
      }
      if (message.vouch != null && message.hasOwnProperty('vouch'))
        object.vouch = $root.polycentric.SignedEvent.toObject(
          message.vouch,
          options
        );
      if (message.claim != null && message.hasOwnProperty('claim'))
        object.claim = $root.polycentric.SignedEvent.toObject(
          message.claim,
          options
        );
      return object;
    };

    /**
     * Converts this FindClaimAndVouchResponse to JSON.
     * @function toJSON
     * @memberof polycentric.FindClaimAndVouchResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FindClaimAndVouchResponse.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FindClaimAndVouchResponse
     * @function getTypeUrl
     * @memberof polycentric.FindClaimAndVouchResponse
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FindClaimAndVouchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.FindClaimAndVouchResponse';
    };

    return FindClaimAndVouchResponse;
  })();

  polycentric.ClaimHandleRequest = (function () {
    /**
     * Properties of a ClaimHandleRequest.
     * @memberof polycentric
     * @interface IClaimHandleRequest
     * @property {polycentric.IPublicKey|null} [system] ClaimHandleRequest system
     * @property {string|null} [handle] ClaimHandleRequest handle
     */

    /**
     * Constructs a new ClaimHandleRequest.
     * @memberof polycentric
     * @classdesc Represents a ClaimHandleRequest.
     * @implements IClaimHandleRequest
     * @constructor
     * @param {polycentric.IClaimHandleRequest=} [properties] Properties to set
     */
    function ClaimHandleRequest(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ClaimHandleRequest system.
     * @member {polycentric.IPublicKey|null|undefined} system
     * @memberof polycentric.ClaimHandleRequest
     * @instance
     */
    ClaimHandleRequest.prototype.system = null;

    /**
     * ClaimHandleRequest handle.
     * @member {string} handle
     * @memberof polycentric.ClaimHandleRequest
     * @instance
     */
    ClaimHandleRequest.prototype.handle = '';

    /**
     * Creates a new ClaimHandleRequest instance using the specified properties.
     * @function create
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {polycentric.IClaimHandleRequest=} [properties] Properties to set
     * @returns {polycentric.ClaimHandleRequest} ClaimHandleRequest instance
     */
    ClaimHandleRequest.create = function create(properties) {
      return new ClaimHandleRequest(properties);
    };

    /**
     * Encodes the specified ClaimHandleRequest message. Does not implicitly {@link polycentric.ClaimHandleRequest.verify|verify} messages.
     * @function encode
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {polycentric.IClaimHandleRequest} message ClaimHandleRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ClaimHandleRequest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.system != null &&
        Object.hasOwnProperty.call(message, 'system')
      )
        $root.polycentric.PublicKey.encode(
          message.system,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.handle != null &&
        Object.hasOwnProperty.call(message, 'handle')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.handle);
      return writer;
    };

    /**
     * Encodes the specified ClaimHandleRequest message, length delimited. Does not implicitly {@link polycentric.ClaimHandleRequest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {polycentric.IClaimHandleRequest} message ClaimHandleRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ClaimHandleRequest.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ClaimHandleRequest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric.ClaimHandleRequest} ClaimHandleRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ClaimHandleRequest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric.ClaimHandleRequest();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.system = $root.polycentric.PublicKey.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.handle = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ClaimHandleRequest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric.ClaimHandleRequest} ClaimHandleRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ClaimHandleRequest.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ClaimHandleRequest message.
     * @function verify
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ClaimHandleRequest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.system != null && message.hasOwnProperty('system')) {
        let error = $root.polycentric.PublicKey.verify(message.system);
        if (error) return 'system.' + error;
      }
      if (message.handle != null && message.hasOwnProperty('handle'))
        if (!$util.isString(message.handle)) return 'handle: string expected';
      return null;
    };

    /**
     * Creates a ClaimHandleRequest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric.ClaimHandleRequest} ClaimHandleRequest
     */
    ClaimHandleRequest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric.ClaimHandleRequest) return object;
      let message = new $root.polycentric.ClaimHandleRequest();
      if (object.system != null) {
        if (typeof object.system !== 'object')
          throw TypeError(
            '.polycentric.ClaimHandleRequest.system: object expected'
          );
        message.system = $root.polycentric.PublicKey.fromObject(object.system);
      }
      if (object.handle != null) message.handle = String(object.handle);
      return message;
    };

    /**
     * Creates a plain object from a ClaimHandleRequest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {polycentric.ClaimHandleRequest} message ClaimHandleRequest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ClaimHandleRequest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.system = null;
        object.handle = '';
      }
      if (message.system != null && message.hasOwnProperty('system'))
        object.system = $root.polycentric.PublicKey.toObject(
          message.system,
          options
        );
      if (message.handle != null && message.hasOwnProperty('handle'))
        object.handle = message.handle;
      return object;
    };

    /**
     * Converts this ClaimHandleRequest to JSON.
     * @function toJSON
     * @memberof polycentric.ClaimHandleRequest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ClaimHandleRequest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ClaimHandleRequest
     * @function getTypeUrl
     * @memberof polycentric.ClaimHandleRequest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ClaimHandleRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric.ClaimHandleRequest';
    };

    return ClaimHandleRequest;
  })();

  return polycentric;
})());

export const polycentric_ffi = ($root.polycentric_ffi = (() => {
  /**
   * Namespace polycentric_ffi.
   * @exports polycentric_ffi
   * @namespace
   */
  const polycentric_ffi = {};

  polycentric_ffi.NetworkRequest = (function () {
    /**
     * Properties of a NetworkRequest.
     * @memberof polycentric_ffi
     * @interface INetworkRequest
     * @property {string|null} [server] NetworkRequest server
     * @property {string|null} [method] NetworkRequest method
     * @property {string|null} [endpoint] NetworkRequest endpoint
     * @property {Object.<string,string>|null} [parameters] NetworkRequest parameters
     * @property {Uint8Array|null} [body] NetworkRequest body
     */

    /**
     * Constructs a new NetworkRequest.
     * @memberof polycentric_ffi
     * @classdesc Represents a NetworkRequest.
     * @implements INetworkRequest
     * @constructor
     * @param {polycentric_ffi.INetworkRequest=} [properties] Properties to set
     */
    function NetworkRequest(properties) {
      this.parameters = {};
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * NetworkRequest server.
     * @member {string} server
     * @memberof polycentric_ffi.NetworkRequest
     * @instance
     */
    NetworkRequest.prototype.server = '';

    /**
     * NetworkRequest method.
     * @member {string} method
     * @memberof polycentric_ffi.NetworkRequest
     * @instance
     */
    NetworkRequest.prototype.method = '';

    /**
     * NetworkRequest endpoint.
     * @member {string} endpoint
     * @memberof polycentric_ffi.NetworkRequest
     * @instance
     */
    NetworkRequest.prototype.endpoint = '';

    /**
     * NetworkRequest parameters.
     * @member {Object.<string,string>} parameters
     * @memberof polycentric_ffi.NetworkRequest
     * @instance
     */
    NetworkRequest.prototype.parameters = $util.emptyObject;

    /**
     * NetworkRequest body.
     * @member {Uint8Array|null|undefined} body
     * @memberof polycentric_ffi.NetworkRequest
     * @instance
     */
    NetworkRequest.prototype.body = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(NetworkRequest.prototype, '_body', {
      get: $util.oneOfGetter(($oneOfFields = ['body'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new NetworkRequest instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {polycentric_ffi.INetworkRequest=} [properties] Properties to set
     * @returns {polycentric_ffi.NetworkRequest} NetworkRequest instance
     */
    NetworkRequest.create = function create(properties) {
      return new NetworkRequest(properties);
    };

    /**
     * Encodes the specified NetworkRequest message. Does not implicitly {@link polycentric_ffi.NetworkRequest.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {polycentric_ffi.INetworkRequest} message NetworkRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkRequest.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.server != null &&
        Object.hasOwnProperty.call(message, 'server')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.server);
      if (
        message.method != null &&
        Object.hasOwnProperty.call(message, 'method')
      )
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.method);
      if (
        message.endpoint != null &&
        Object.hasOwnProperty.call(message, 'endpoint')
      )
        writer.uint32(/* id 3, wireType 2 =*/ 26).string(message.endpoint);
      if (
        message.parameters != null &&
        Object.hasOwnProperty.call(message, 'parameters')
      )
        for (
          let keys = Object.keys(message.parameters), i = 0;
          i < keys.length;
          ++i
        )
          writer
            .uint32(/* id 4, wireType 2 =*/ 34)
            .fork()
            .uint32(/* id 1, wireType 2 =*/ 10)
            .string(keys[i])
            .uint32(/* id 2, wireType 2 =*/ 18)
            .string(message.parameters[keys[i]])
            .ldelim();
      if (message.body != null && Object.hasOwnProperty.call(message, 'body'))
        writer.uint32(/* id 5, wireType 2 =*/ 42).bytes(message.body);
      return writer;
    };

    /**
     * Encodes the specified NetworkRequest message, length delimited. Does not implicitly {@link polycentric_ffi.NetworkRequest.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {polycentric_ffi.INetworkRequest} message NetworkRequest message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkRequest.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a NetworkRequest message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.NetworkRequest} NetworkRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkRequest.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.NetworkRequest(),
        key,
        value;
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.server = reader.string();
            break;
          }
          case 2: {
            message.method = reader.string();
            break;
          }
          case 3: {
            message.endpoint = reader.string();
            break;
          }
          case 4: {
            if (message.parameters === $util.emptyObject)
              message.parameters = {};
            let end2 = reader.uint32() + reader.pos;
            key = '';
            value = '';
            while (reader.pos < end2) {
              let tag2 = reader.uint32();
              switch (tag2 >>> 3) {
                case 1:
                  key = reader.string();
                  break;
                case 2:
                  value = reader.string();
                  break;
                default:
                  reader.skipType(tag2 & 7);
                  break;
              }
            }
            message.parameters[key] = value;
            break;
          }
          case 5: {
            message.body = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a NetworkRequest message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.NetworkRequest} NetworkRequest
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkRequest.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NetworkRequest message.
     * @function verify
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NetworkRequest.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.server != null && message.hasOwnProperty('server'))
        if (!$util.isString(message.server)) return 'server: string expected';
      if (message.method != null && message.hasOwnProperty('method'))
        if (!$util.isString(message.method)) return 'method: string expected';
      if (message.endpoint != null && message.hasOwnProperty('endpoint'))
        if (!$util.isString(message.endpoint))
          return 'endpoint: string expected';
      if (message.parameters != null && message.hasOwnProperty('parameters')) {
        if (!$util.isObject(message.parameters))
          return 'parameters: object expected';
        let key = Object.keys(message.parameters);
        for (let i = 0; i < key.length; ++i)
          if (!$util.isString(message.parameters[key[i]]))
            return 'parameters: string{k:string} expected';
      }
      if (message.body != null && message.hasOwnProperty('body')) {
        properties._body = 1;
        if (
          !(
            (message.body && typeof message.body.length === 'number') ||
            $util.isString(message.body)
          )
        )
          return 'body: buffer expected';
      }
      return null;
    };

    /**
     * Creates a NetworkRequest message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.NetworkRequest} NetworkRequest
     */
    NetworkRequest.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.NetworkRequest) return object;
      let message = new $root.polycentric_ffi.NetworkRequest();
      if (object.server != null) message.server = String(object.server);
      if (object.method != null) message.method = String(object.method);
      if (object.endpoint != null) message.endpoint = String(object.endpoint);
      if (object.parameters) {
        if (typeof object.parameters !== 'object')
          throw TypeError(
            '.polycentric_ffi.NetworkRequest.parameters: object expected'
          );
        message.parameters = {};
        for (
          let keys = Object.keys(object.parameters), i = 0;
          i < keys.length;
          ++i
        )
          message.parameters[keys[i]] = String(object.parameters[keys[i]]);
      }
      if (object.body != null)
        if (typeof object.body === 'string')
          $util.base64.decode(
            object.body,
            (message.body = $util.newBuffer($util.base64.length(object.body))),
            0
          );
        else if (object.body.length >= 0) message.body = object.body;
      return message;
    };

    /**
     * Creates a plain object from a NetworkRequest message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {polycentric_ffi.NetworkRequest} message NetworkRequest
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NetworkRequest.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.objects || options.defaults) object.parameters = {};
      if (options.defaults) {
        object.server = '';
        object.method = '';
        object.endpoint = '';
      }
      if (message.server != null && message.hasOwnProperty('server'))
        object.server = message.server;
      if (message.method != null && message.hasOwnProperty('method'))
        object.method = message.method;
      if (message.endpoint != null && message.hasOwnProperty('endpoint'))
        object.endpoint = message.endpoint;
      let keys2;
      if (
        message.parameters &&
        (keys2 = Object.keys(message.parameters)).length
      ) {
        object.parameters = {};
        for (let j = 0; j < keys2.length; ++j)
          object.parameters[keys2[j]] = message.parameters[keys2[j]];
      }
      if (message.body != null && message.hasOwnProperty('body')) {
        object.body =
          options.bytes === String
            ? $util.base64.encode(message.body, 0, message.body.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.body)
              : message.body;
        if (options.oneofs) object._body = 'body';
      }
      return object;
    };

    /**
     * Converts this NetworkRequest to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.NetworkRequest
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NetworkRequest.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NetworkRequest
     * @function getTypeUrl
     * @memberof polycentric_ffi.NetworkRequest
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NetworkRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.NetworkRequest';
    };

    return NetworkRequest;
  })();

  polycentric_ffi.NetworkResponse = (function () {
    /**
     * Properties of a NetworkResponse.
     * @memberof polycentric_ffi
     * @interface INetworkResponse
     * @property {Uint8Array|null} [body] NetworkResponse body
     */

    /**
     * Constructs a new NetworkResponse.
     * @memberof polycentric_ffi
     * @classdesc Represents a NetworkResponse.
     * @implements INetworkResponse
     * @constructor
     * @param {polycentric_ffi.INetworkResponse=} [properties] Properties to set
     */
    function NetworkResponse(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * NetworkResponse body.
     * @member {Uint8Array|null|undefined} body
     * @memberof polycentric_ffi.NetworkResponse
     * @instance
     */
    NetworkResponse.prototype.body = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(NetworkResponse.prototype, '_body', {
      get: $util.oneOfGetter(($oneOfFields = ['body'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new NetworkResponse instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {polycentric_ffi.INetworkResponse=} [properties] Properties to set
     * @returns {polycentric_ffi.NetworkResponse} NetworkResponse instance
     */
    NetworkResponse.create = function create(properties) {
      return new NetworkResponse(properties);
    };

    /**
     * Encodes the specified NetworkResponse message. Does not implicitly {@link polycentric_ffi.NetworkResponse.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {polycentric_ffi.INetworkResponse} message NetworkResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkResponse.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.body != null && Object.hasOwnProperty.call(message, 'body'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.body);
      return writer;
    };

    /**
     * Encodes the specified NetworkResponse message, length delimited. Does not implicitly {@link polycentric_ffi.NetworkResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {polycentric_ffi.INetworkResponse} message NetworkResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkResponse.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a NetworkResponse message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.NetworkResponse} NetworkResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkResponse.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.NetworkResponse();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.body = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a NetworkResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.NetworkResponse} NetworkResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkResponse.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NetworkResponse message.
     * @function verify
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NetworkResponse.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.body != null && message.hasOwnProperty('body')) {
        properties._body = 1;
        if (
          !(
            (message.body && typeof message.body.length === 'number') ||
            $util.isString(message.body)
          )
        )
          return 'body: buffer expected';
      }
      return null;
    };

    /**
     * Creates a NetworkResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.NetworkResponse} NetworkResponse
     */
    NetworkResponse.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.NetworkResponse)
        return object;
      let message = new $root.polycentric_ffi.NetworkResponse();
      if (object.body != null)
        if (typeof object.body === 'string')
          $util.base64.decode(
            object.body,
            (message.body = $util.newBuffer($util.base64.length(object.body))),
            0
          );
        else if (object.body.length >= 0) message.body = object.body;
      return message;
    };

    /**
     * Creates a plain object from a NetworkResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {polycentric_ffi.NetworkResponse} message NetworkResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NetworkResponse.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (message.body != null && message.hasOwnProperty('body')) {
        object.body =
          options.bytes === String
            ? $util.base64.encode(message.body, 0, message.body.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.body)
              : message.body;
        if (options.oneofs) object._body = 'body';
      }
      return object;
    };

    /**
     * Converts this NetworkResponse to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.NetworkResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NetworkResponse.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NetworkResponse
     * @function getTypeUrl
     * @memberof polycentric_ffi.NetworkResponse
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NetworkResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.NetworkResponse';
    };

    return NetworkResponse;
  })();

  polycentric_ffi.NetworkRequestResponses = (function () {
    /**
     * Properties of a NetworkRequestResponses.
     * @memberof polycentric_ffi
     * @interface INetworkRequestResponses
     * @property {Array.<polycentric_ffi.INetworkRequestResponse>|null} [pairs] NetworkRequestResponses pairs
     */

    /**
     * Constructs a new NetworkRequestResponses.
     * @memberof polycentric_ffi
     * @classdesc Represents a NetworkRequestResponses.
     * @implements INetworkRequestResponses
     * @constructor
     * @param {polycentric_ffi.INetworkRequestResponses=} [properties] Properties to set
     */
    function NetworkRequestResponses(properties) {
      this.pairs = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * NetworkRequestResponses pairs.
     * @member {Array.<polycentric_ffi.INetworkRequestResponse>} pairs
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @instance
     */
    NetworkRequestResponses.prototype.pairs = $util.emptyArray;

    /**
     * Creates a new NetworkRequestResponses instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {polycentric_ffi.INetworkRequestResponses=} [properties] Properties to set
     * @returns {polycentric_ffi.NetworkRequestResponses} NetworkRequestResponses instance
     */
    NetworkRequestResponses.create = function create(properties) {
      return new NetworkRequestResponses(properties);
    };

    /**
     * Encodes the specified NetworkRequestResponses message. Does not implicitly {@link polycentric_ffi.NetworkRequestResponses.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {polycentric_ffi.INetworkRequestResponses} message NetworkRequestResponses message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkRequestResponses.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.pairs != null && message.pairs.length)
        for (let i = 0; i < message.pairs.length; ++i)
          $root.polycentric_ffi.NetworkRequestResponse.encode(
            message.pairs[i],
            writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified NetworkRequestResponses message, length delimited. Does not implicitly {@link polycentric_ffi.NetworkRequestResponses.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {polycentric_ffi.INetworkRequestResponses} message NetworkRequestResponses message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkRequestResponses.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a NetworkRequestResponses message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.NetworkRequestResponses} NetworkRequestResponses
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkRequestResponses.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.NetworkRequestResponses();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (!(message.pairs && message.pairs.length)) message.pairs = [];
            message.pairs.push(
              $root.polycentric_ffi.NetworkRequestResponse.decode(
                reader,
                reader.uint32()
              )
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a NetworkRequestResponses message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.NetworkRequestResponses} NetworkRequestResponses
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkRequestResponses.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NetworkRequestResponses message.
     * @function verify
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NetworkRequestResponses.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.pairs != null && message.hasOwnProperty('pairs')) {
        if (!Array.isArray(message.pairs)) return 'pairs: array expected';
        for (let i = 0; i < message.pairs.length; ++i) {
          let error = $root.polycentric_ffi.NetworkRequestResponse.verify(
            message.pairs[i]
          );
          if (error) return 'pairs.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a NetworkRequestResponses message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.NetworkRequestResponses} NetworkRequestResponses
     */
    NetworkRequestResponses.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.NetworkRequestResponses)
        return object;
      let message = new $root.polycentric_ffi.NetworkRequestResponses();
      if (object.pairs) {
        if (!Array.isArray(object.pairs))
          throw TypeError(
            '.polycentric_ffi.NetworkRequestResponses.pairs: array expected'
          );
        message.pairs = [];
        for (let i = 0; i < object.pairs.length; ++i) {
          if (typeof object.pairs[i] !== 'object')
            throw TypeError(
              '.polycentric_ffi.NetworkRequestResponses.pairs: object expected'
            );
          message.pairs[i] =
            $root.polycentric_ffi.NetworkRequestResponse.fromObject(
              object.pairs[i]
            );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a NetworkRequestResponses message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {polycentric_ffi.NetworkRequestResponses} message NetworkRequestResponses
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NetworkRequestResponses.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.pairs = [];
      if (message.pairs && message.pairs.length) {
        object.pairs = [];
        for (let j = 0; j < message.pairs.length; ++j)
          object.pairs[j] =
            $root.polycentric_ffi.NetworkRequestResponse.toObject(
              message.pairs[j],
              options
            );
      }
      return object;
    };

    /**
     * Converts this NetworkRequestResponses to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NetworkRequestResponses.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NetworkRequestResponses
     * @function getTypeUrl
     * @memberof polycentric_ffi.NetworkRequestResponses
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NetworkRequestResponses.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.NetworkRequestResponses';
    };

    return NetworkRequestResponses;
  })();

  polycentric_ffi.NetworkRequestResponse = (function () {
    /**
     * Properties of a NetworkRequestResponse.
     * @memberof polycentric_ffi
     * @interface INetworkRequestResponse
     * @property {polycentric_ffi.INetworkRequest|null} [request] NetworkRequestResponse request
     * @property {polycentric_ffi.INetworkResponse|null} [response] NetworkRequestResponse response
     */

    /**
     * Constructs a new NetworkRequestResponse.
     * @memberof polycentric_ffi
     * @classdesc Represents a NetworkRequestResponse.
     * @implements INetworkRequestResponse
     * @constructor
     * @param {polycentric_ffi.INetworkRequestResponse=} [properties] Properties to set
     */
    function NetworkRequestResponse(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * NetworkRequestResponse request.
     * @member {polycentric_ffi.INetworkRequest|null|undefined} request
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @instance
     */
    NetworkRequestResponse.prototype.request = null;

    /**
     * NetworkRequestResponse response.
     * @member {polycentric_ffi.INetworkResponse|null|undefined} response
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @instance
     */
    NetworkRequestResponse.prototype.response = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(NetworkRequestResponse.prototype, '_response', {
      get: $util.oneOfGetter(($oneOfFields = ['response'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new NetworkRequestResponse instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {polycentric_ffi.INetworkRequestResponse=} [properties] Properties to set
     * @returns {polycentric_ffi.NetworkRequestResponse} NetworkRequestResponse instance
     */
    NetworkRequestResponse.create = function create(properties) {
      return new NetworkRequestResponse(properties);
    };

    /**
     * Encodes the specified NetworkRequestResponse message. Does not implicitly {@link polycentric_ffi.NetworkRequestResponse.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {polycentric_ffi.INetworkRequestResponse} message NetworkRequestResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkRequestResponse.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.request != null &&
        Object.hasOwnProperty.call(message, 'request')
      )
        $root.polycentric_ffi.NetworkRequest.encode(
          message.request,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.response != null &&
        Object.hasOwnProperty.call(message, 'response')
      )
        $root.polycentric_ffi.NetworkResponse.encode(
          message.response,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified NetworkRequestResponse message, length delimited. Does not implicitly {@link polycentric_ffi.NetworkRequestResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {polycentric_ffi.INetworkRequestResponse} message NetworkRequestResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    NetworkRequestResponse.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a NetworkRequestResponse message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.NetworkRequestResponse} NetworkRequestResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkRequestResponse.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.NetworkRequestResponse();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.request = $root.polycentric_ffi.NetworkRequest.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.response = $root.polycentric_ffi.NetworkResponse.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a NetworkRequestResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.NetworkRequestResponse} NetworkRequestResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    NetworkRequestResponse.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a NetworkRequestResponse message.
     * @function verify
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    NetworkRequestResponse.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.request != null && message.hasOwnProperty('request')) {
        let error = $root.polycentric_ffi.NetworkRequest.verify(
          message.request
        );
        if (error) return 'request.' + error;
      }
      if (message.response != null && message.hasOwnProperty('response')) {
        properties._response = 1;
        {
          let error = $root.polycentric_ffi.NetworkResponse.verify(
            message.response
          );
          if (error) return 'response.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a NetworkRequestResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.NetworkRequestResponse} NetworkRequestResponse
     */
    NetworkRequestResponse.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.NetworkRequestResponse)
        return object;
      let message = new $root.polycentric_ffi.NetworkRequestResponse();
      if (object.request != null) {
        if (typeof object.request !== 'object')
          throw TypeError(
            '.polycentric_ffi.NetworkRequestResponse.request: object expected'
          );
        message.request = $root.polycentric_ffi.NetworkRequest.fromObject(
          object.request
        );
      }
      if (object.response != null) {
        if (typeof object.response !== 'object')
          throw TypeError(
            '.polycentric_ffi.NetworkRequestResponse.response: object expected'
          );
        message.response = $root.polycentric_ffi.NetworkResponse.fromObject(
          object.response
        );
      }
      return message;
    };

    /**
     * Creates a plain object from a NetworkRequestResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {polycentric_ffi.NetworkRequestResponse} message NetworkRequestResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    NetworkRequestResponse.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) object.request = null;
      if (message.request != null && message.hasOwnProperty('request'))
        object.request = $root.polycentric_ffi.NetworkRequest.toObject(
          message.request,
          options
        );
      if (message.response != null && message.hasOwnProperty('response')) {
        object.response = $root.polycentric_ffi.NetworkResponse.toObject(
          message.response,
          options
        );
        if (options.oneofs) object._response = 'response';
      }
      return object;
    };

    /**
     * Converts this NetworkRequestResponse to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    NetworkRequestResponse.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for NetworkRequestResponse
     * @function getTypeUrl
     * @memberof polycentric_ffi.NetworkRequestResponse
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    NetworkRequestResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.NetworkRequestResponse';
    };

    return NetworkRequestResponse;
  })();

  polycentric_ffi.Result = (function () {
    /**
     * Properties of a Result.
     * @memberof polycentric_ffi
     * @interface IResult
     * @property {polycentric_ffi.INetworkRequestResponses|null} [requests] Result requests
     * @property {string|null} [error] Result error
     * @property {Uint8Array|null} [value] Result value
     */

    /**
     * Constructs a new Result.
     * @memberof polycentric_ffi
     * @classdesc Represents a Result.
     * @implements IResult
     * @constructor
     * @param {polycentric_ffi.IResult=} [properties] Properties to set
     */
    function Result(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Result requests.
     * @member {polycentric_ffi.INetworkRequestResponses|null|undefined} requests
     * @memberof polycentric_ffi.Result
     * @instance
     */
    Result.prototype.requests = null;

    /**
     * Result error.
     * @member {string|null|undefined} error
     * @memberof polycentric_ffi.Result
     * @instance
     */
    Result.prototype.error = null;

    /**
     * Result value.
     * @member {Uint8Array|null|undefined} value
     * @memberof polycentric_ffi.Result
     * @instance
     */
    Result.prototype.value = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    /**
     * Result result.
     * @member {"requests"|"error"|"value"|undefined} result
     * @memberof polycentric_ffi.Result
     * @instance
     */
    Object.defineProperty(Result.prototype, 'result', {
      get: $util.oneOfGetter(($oneOfFields = ['requests', 'error', 'value'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new Result instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.Result
     * @static
     * @param {polycentric_ffi.IResult=} [properties] Properties to set
     * @returns {polycentric_ffi.Result} Result instance
     */
    Result.create = function create(properties) {
      return new Result(properties);
    };

    /**
     * Encodes the specified Result message. Does not implicitly {@link polycentric_ffi.Result.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.Result
     * @static
     * @param {polycentric_ffi.IResult} message Result message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Result.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.requests != null &&
        Object.hasOwnProperty.call(message, 'requests')
      )
        $root.polycentric_ffi.NetworkRequestResponses.encode(
          message.requests,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (message.error != null && Object.hasOwnProperty.call(message, 'error'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.error);
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 3, wireType 2 =*/ 26).bytes(message.value);
      return writer;
    };

    /**
     * Encodes the specified Result message, length delimited. Does not implicitly {@link polycentric_ffi.Result.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.Result
     * @static
     * @param {polycentric_ffi.IResult} message Result message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Result.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Result message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.Result
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.Result} Result
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Result.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.Result();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.requests =
              $root.polycentric_ffi.NetworkRequestResponses.decode(
                reader,
                reader.uint32()
              );
            break;
          }
          case 2: {
            message.error = reader.string();
            break;
          }
          case 3: {
            message.value = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Result message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.Result
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.Result} Result
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Result.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Result message.
     * @function verify
     * @memberof polycentric_ffi.Result
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Result.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.requests != null && message.hasOwnProperty('requests')) {
        properties.result = 1;
        {
          let error = $root.polycentric_ffi.NetworkRequestResponses.verify(
            message.requests
          );
          if (error) return 'requests.' + error;
        }
      }
      if (message.error != null && message.hasOwnProperty('error')) {
        if (properties.result === 1) return 'result: multiple values';
        properties.result = 1;
        if (!$util.isString(message.error)) return 'error: string expected';
      }
      if (message.value != null && message.hasOwnProperty('value')) {
        if (properties.result === 1) return 'result: multiple values';
        properties.result = 1;
        if (
          !(
            (message.value && typeof message.value.length === 'number') ||
            $util.isString(message.value)
          )
        )
          return 'value: buffer expected';
      }
      return null;
    };

    /**
     * Creates a Result message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.Result
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.Result} Result
     */
    Result.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.Result) return object;
      let message = new $root.polycentric_ffi.Result();
      if (object.requests != null) {
        if (typeof object.requests !== 'object')
          throw TypeError('.polycentric_ffi.Result.requests: object expected');
        message.requests =
          $root.polycentric_ffi.NetworkRequestResponses.fromObject(
            object.requests
          );
      }
      if (object.error != null) message.error = String(object.error);
      if (object.value != null)
        if (typeof object.value === 'string')
          $util.base64.decode(
            object.value,
            (message.value = $util.newBuffer(
              $util.base64.length(object.value)
            )),
            0
          );
        else if (object.value.length >= 0) message.value = object.value;
      return message;
    };

    /**
     * Creates a plain object from a Result message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.Result
     * @static
     * @param {polycentric_ffi.Result} message Result
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Result.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (message.requests != null && message.hasOwnProperty('requests')) {
        object.requests =
          $root.polycentric_ffi.NetworkRequestResponses.toObject(
            message.requests,
            options
          );
        if (options.oneofs) object.result = 'requests';
      }
      if (message.error != null && message.hasOwnProperty('error')) {
        object.error = message.error;
        if (options.oneofs) object.result = 'error';
      }
      if (message.value != null && message.hasOwnProperty('value')) {
        object.value =
          options.bytes === String
            ? $util.base64.encode(message.value, 0, message.value.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.value)
              : message.value;
        if (options.oneofs) object.result = 'value';
      }
      return object;
    };

    /**
     * Converts this Result to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.Result
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Result.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Result
     * @function getTypeUrl
     * @memberof polycentric_ffi.Result
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Result.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.Result';
    };

    return Result;
  })();

  polycentric_ffi.Option = (function () {
    /**
     * Properties of an Option.
     * @memberof polycentric_ffi
     * @interface IOption
     * @property {Uint8Array|null} [value] Option value
     */

    /**
     * Constructs a new Option.
     * @memberof polycentric_ffi
     * @classdesc Represents an Option.
     * @implements IOption
     * @constructor
     * @param {polycentric_ffi.IOption=} [properties] Properties to set
     */
    function Option(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Option value.
     * @member {Uint8Array|null|undefined} value
     * @memberof polycentric_ffi.Option
     * @instance
     */
    Option.prototype.value = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(Option.prototype, '_value', {
      get: $util.oneOfGetter(($oneOfFields = ['value'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new Option instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.Option
     * @static
     * @param {polycentric_ffi.IOption=} [properties] Properties to set
     * @returns {polycentric_ffi.Option} Option instance
     */
    Option.create = function create(properties) {
      return new Option(properties);
    };

    /**
     * Encodes the specified Option message. Does not implicitly {@link polycentric_ffi.Option.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.Option
     * @static
     * @param {polycentric_ffi.IOption} message Option message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Option.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.value != null && Object.hasOwnProperty.call(message, 'value'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.value);
      return writer;
    };

    /**
     * Encodes the specified Option message, length delimited. Does not implicitly {@link polycentric_ffi.Option.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.Option
     * @static
     * @param {polycentric_ffi.IOption} message Option message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Option.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an Option message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.Option
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.Option} Option
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Option.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.Option();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.value = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an Option message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.Option
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.Option} Option
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Option.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an Option message.
     * @function verify
     * @memberof polycentric_ffi.Option
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Option.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.value != null && message.hasOwnProperty('value')) {
        properties._value = 1;
        if (
          !(
            (message.value && typeof message.value.length === 'number') ||
            $util.isString(message.value)
          )
        )
          return 'value: buffer expected';
      }
      return null;
    };

    /**
     * Creates an Option message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.Option
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.Option} Option
     */
    Option.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.Option) return object;
      let message = new $root.polycentric_ffi.Option();
      if (object.value != null)
        if (typeof object.value === 'string')
          $util.base64.decode(
            object.value,
            (message.value = $util.newBuffer(
              $util.base64.length(object.value)
            )),
            0
          );
        else if (object.value.length >= 0) message.value = object.value;
      return message;
    };

    /**
     * Creates a plain object from an Option message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.Option
     * @static
     * @param {polycentric_ffi.Option} message Option
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Option.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (message.value != null && message.hasOwnProperty('value')) {
        object.value =
          options.bytes === String
            ? $util.base64.encode(message.value, 0, message.value.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.value)
              : message.value;
        if (options.oneofs) object._value = 'value';
      }
      return object;
    };

    /**
     * Converts this Option to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.Option
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Option.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Option
     * @function getTypeUrl
     * @memberof polycentric_ffi.Option
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Option.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.Option';
    };

    return Option;
  })();

  polycentric_ffi.SyncResult = (function () {
    /**
     * Properties of a SyncResult.
     * @memberof polycentric_ffi
     * @interface ISyncResult
     * @property {Uint8Array|null} [eventsBytes] SyncResult eventsBytes
     * @property {Array.<polycentric_ffi.IServerError>|null} [errors] SyncResult errors
     */

    /**
     * Constructs a new SyncResult.
     * @memberof polycentric_ffi
     * @classdesc Represents a SyncResult.
     * @implements ISyncResult
     * @constructor
     * @param {polycentric_ffi.ISyncResult=} [properties] Properties to set
     */
    function SyncResult(properties) {
      this.errors = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * SyncResult eventsBytes.
     * @member {Uint8Array} eventsBytes
     * @memberof polycentric_ffi.SyncResult
     * @instance
     */
    SyncResult.prototype.eventsBytes = $util.newBuffer([]);

    /**
     * SyncResult errors.
     * @member {Array.<polycentric_ffi.IServerError>} errors
     * @memberof polycentric_ffi.SyncResult
     * @instance
     */
    SyncResult.prototype.errors = $util.emptyArray;

    /**
     * Creates a new SyncResult instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {polycentric_ffi.ISyncResult=} [properties] Properties to set
     * @returns {polycentric_ffi.SyncResult} SyncResult instance
     */
    SyncResult.create = function create(properties) {
      return new SyncResult(properties);
    };

    /**
     * Encodes the specified SyncResult message. Does not implicitly {@link polycentric_ffi.SyncResult.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {polycentric_ffi.ISyncResult} message SyncResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SyncResult.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.eventsBytes != null &&
        Object.hasOwnProperty.call(message, 'eventsBytes')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.eventsBytes);
      if (message.errors != null && message.errors.length)
        for (let i = 0; i < message.errors.length; ++i)
          $root.polycentric_ffi.ServerError.encode(
            message.errors[i],
            writer.uint32(/* id 3, wireType 2 =*/ 26).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified SyncResult message, length delimited. Does not implicitly {@link polycentric_ffi.SyncResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {polycentric_ffi.ISyncResult} message SyncResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SyncResult.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SyncResult message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.SyncResult} SyncResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SyncResult.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.SyncResult();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.eventsBytes = reader.bytes();
            break;
          }
          case 3: {
            if (!(message.errors && message.errors.length)) message.errors = [];
            message.errors.push(
              $root.polycentric_ffi.ServerError.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a SyncResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.SyncResult} SyncResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SyncResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SyncResult message.
     * @function verify
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SyncResult.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.eventsBytes != null && message.hasOwnProperty('eventsBytes'))
        if (
          !(
            (message.eventsBytes &&
              typeof message.eventsBytes.length === 'number') ||
            $util.isString(message.eventsBytes)
          )
        )
          return 'eventsBytes: buffer expected';
      if (message.errors != null && message.hasOwnProperty('errors')) {
        if (!Array.isArray(message.errors)) return 'errors: array expected';
        for (let i = 0; i < message.errors.length; ++i) {
          let error = $root.polycentric_ffi.ServerError.verify(
            message.errors[i]
          );
          if (error) return 'errors.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a SyncResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.SyncResult} SyncResult
     */
    SyncResult.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.SyncResult) return object;
      let message = new $root.polycentric_ffi.SyncResult();
      if (object.eventsBytes != null)
        if (typeof object.eventsBytes === 'string')
          $util.base64.decode(
            object.eventsBytes,
            (message.eventsBytes = $util.newBuffer(
              $util.base64.length(object.eventsBytes)
            )),
            0
          );
        else if (object.eventsBytes.length >= 0)
          message.eventsBytes = object.eventsBytes;
      if (object.errors) {
        if (!Array.isArray(object.errors))
          throw TypeError('.polycentric_ffi.SyncResult.errors: array expected');
        message.errors = [];
        for (let i = 0; i < object.errors.length; ++i) {
          if (typeof object.errors[i] !== 'object')
            throw TypeError(
              '.polycentric_ffi.SyncResult.errors: object expected'
            );
          message.errors[i] = $root.polycentric_ffi.ServerError.fromObject(
            object.errors[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a SyncResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {polycentric_ffi.SyncResult} message SyncResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SyncResult.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.errors = [];
      if (options.defaults)
        if (options.bytes === String) object.eventsBytes = '';
        else {
          object.eventsBytes = [];
          if (options.bytes !== Array)
            object.eventsBytes = $util.newBuffer(object.eventsBytes);
        }
      if (message.eventsBytes != null && message.hasOwnProperty('eventsBytes'))
        object.eventsBytes =
          options.bytes === String
            ? $util.base64.encode(
                message.eventsBytes,
                0,
                message.eventsBytes.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.eventsBytes)
              : message.eventsBytes;
      if (message.errors && message.errors.length) {
        object.errors = [];
        for (let j = 0; j < message.errors.length; ++j)
          object.errors[j] = $root.polycentric_ffi.ServerError.toObject(
            message.errors[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this SyncResult to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.SyncResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SyncResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SyncResult
     * @function getTypeUrl
     * @memberof polycentric_ffi.SyncResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SyncResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.SyncResult';
    };

    return SyncResult;
  })();

  polycentric_ffi.ServerError = (function () {
    /**
     * Properties of a ServerError.
     * @memberof polycentric_ffi
     * @interface IServerError
     * @property {string|null} [server] ServerError server
     * @property {string|null} [error] ServerError error
     */

    /**
     * Constructs a new ServerError.
     * @memberof polycentric_ffi
     * @classdesc Represents a ServerError.
     * @implements IServerError
     * @constructor
     * @param {polycentric_ffi.IServerError=} [properties] Properties to set
     */
    function ServerError(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ServerError server.
     * @member {string} server
     * @memberof polycentric_ffi.ServerError
     * @instance
     */
    ServerError.prototype.server = '';

    /**
     * ServerError error.
     * @member {string} error
     * @memberof polycentric_ffi.ServerError
     * @instance
     */
    ServerError.prototype.error = '';

    /**
     * Creates a new ServerError instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {polycentric_ffi.IServerError=} [properties] Properties to set
     * @returns {polycentric_ffi.ServerError} ServerError instance
     */
    ServerError.create = function create(properties) {
      return new ServerError(properties);
    };

    /**
     * Encodes the specified ServerError message. Does not implicitly {@link polycentric_ffi.ServerError.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {polycentric_ffi.IServerError} message ServerError message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ServerError.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.server != null &&
        Object.hasOwnProperty.call(message, 'server')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.server);
      if (message.error != null && Object.hasOwnProperty.call(message, 'error'))
        writer.uint32(/* id 2, wireType 2 =*/ 18).string(message.error);
      return writer;
    };

    /**
     * Encodes the specified ServerError message, length delimited. Does not implicitly {@link polycentric_ffi.ServerError.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {polycentric_ffi.IServerError} message ServerError message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ServerError.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ServerError message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.ServerError} ServerError
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ServerError.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.ServerError();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.server = reader.string();
            break;
          }
          case 2: {
            message.error = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ServerError message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.ServerError} ServerError
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ServerError.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ServerError message.
     * @function verify
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ServerError.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.server != null && message.hasOwnProperty('server'))
        if (!$util.isString(message.server)) return 'server: string expected';
      if (message.error != null && message.hasOwnProperty('error'))
        if (!$util.isString(message.error)) return 'error: string expected';
      return null;
    };

    /**
     * Creates a ServerError message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.ServerError} ServerError
     */
    ServerError.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.ServerError) return object;
      let message = new $root.polycentric_ffi.ServerError();
      if (object.server != null) message.server = String(object.server);
      if (object.error != null) message.error = String(object.error);
      return message;
    };

    /**
     * Creates a plain object from a ServerError message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {polycentric_ffi.ServerError} message ServerError
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ServerError.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.server = '';
        object.error = '';
      }
      if (message.server != null && message.hasOwnProperty('server'))
        object.server = message.server;
      if (message.error != null && message.hasOwnProperty('error'))
        object.error = message.error;
      return object;
    };

    /**
     * Converts this ServerError to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.ServerError
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ServerError.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ServerError
     * @function getTypeUrl
     * @memberof polycentric_ffi.ServerError
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ServerError.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.ServerError';
    };

    return ServerError;
  })();

  polycentric_ffi.ResultAndServerErrors = (function () {
    /**
     * Properties of a ResultAndServerErrors.
     * @memberof polycentric_ffi
     * @interface IResultAndServerErrors
     * @property {Uint8Array|null} [result] ResultAndServerErrors result
     * @property {Array.<polycentric_ffi.IServerError>|null} [errors] ResultAndServerErrors errors
     */

    /**
     * Constructs a new ResultAndServerErrors.
     * @memberof polycentric_ffi
     * @classdesc Represents a ResultAndServerErrors.
     * @implements IResultAndServerErrors
     * @constructor
     * @param {polycentric_ffi.IResultAndServerErrors=} [properties] Properties to set
     */
    function ResultAndServerErrors(properties) {
      this.errors = [];
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ResultAndServerErrors result.
     * @member {Uint8Array} result
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @instance
     */
    ResultAndServerErrors.prototype.result = $util.newBuffer([]);

    /**
     * ResultAndServerErrors errors.
     * @member {Array.<polycentric_ffi.IServerError>} errors
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @instance
     */
    ResultAndServerErrors.prototype.errors = $util.emptyArray;

    /**
     * Creates a new ResultAndServerErrors instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {polycentric_ffi.IResultAndServerErrors=} [properties] Properties to set
     * @returns {polycentric_ffi.ResultAndServerErrors} ResultAndServerErrors instance
     */
    ResultAndServerErrors.create = function create(properties) {
      return new ResultAndServerErrors(properties);
    };

    /**
     * Encodes the specified ResultAndServerErrors message. Does not implicitly {@link polycentric_ffi.ResultAndServerErrors.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {polycentric_ffi.IResultAndServerErrors} message ResultAndServerErrors message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResultAndServerErrors.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.result != null &&
        Object.hasOwnProperty.call(message, 'result')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.result);
      if (message.errors != null && message.errors.length)
        for (let i = 0; i < message.errors.length; ++i)
          $root.polycentric_ffi.ServerError.encode(
            message.errors[i],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified ResultAndServerErrors message, length delimited. Does not implicitly {@link polycentric_ffi.ResultAndServerErrors.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {polycentric_ffi.IResultAndServerErrors} message ResultAndServerErrors message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ResultAndServerErrors.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ResultAndServerErrors message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.ResultAndServerErrors} ResultAndServerErrors
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResultAndServerErrors.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.ResultAndServerErrors();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.result = reader.bytes();
            break;
          }
          case 2: {
            if (!(message.errors && message.errors.length)) message.errors = [];
            message.errors.push(
              $root.polycentric_ffi.ServerError.decode(reader, reader.uint32())
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ResultAndServerErrors message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.ResultAndServerErrors} ResultAndServerErrors
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ResultAndServerErrors.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ResultAndServerErrors message.
     * @function verify
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ResultAndServerErrors.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.result != null && message.hasOwnProperty('result'))
        if (
          !(
            (message.result && typeof message.result.length === 'number') ||
            $util.isString(message.result)
          )
        )
          return 'result: buffer expected';
      if (message.errors != null && message.hasOwnProperty('errors')) {
        if (!Array.isArray(message.errors)) return 'errors: array expected';
        for (let i = 0; i < message.errors.length; ++i) {
          let error = $root.polycentric_ffi.ServerError.verify(
            message.errors[i]
          );
          if (error) return 'errors.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a ResultAndServerErrors message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.ResultAndServerErrors} ResultAndServerErrors
     */
    ResultAndServerErrors.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.ResultAndServerErrors)
        return object;
      let message = new $root.polycentric_ffi.ResultAndServerErrors();
      if (object.result != null)
        if (typeof object.result === 'string')
          $util.base64.decode(
            object.result,
            (message.result = $util.newBuffer(
              $util.base64.length(object.result)
            )),
            0
          );
        else if (object.result.length >= 0) message.result = object.result;
      if (object.errors) {
        if (!Array.isArray(object.errors))
          throw TypeError(
            '.polycentric_ffi.ResultAndServerErrors.errors: array expected'
          );
        message.errors = [];
        for (let i = 0; i < object.errors.length; ++i) {
          if (typeof object.errors[i] !== 'object')
            throw TypeError(
              '.polycentric_ffi.ResultAndServerErrors.errors: object expected'
            );
          message.errors[i] = $root.polycentric_ffi.ServerError.fromObject(
            object.errors[i]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a ResultAndServerErrors message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {polycentric_ffi.ResultAndServerErrors} message ResultAndServerErrors
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ResultAndServerErrors.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.arrays || options.defaults) object.errors = [];
      if (options.defaults)
        if (options.bytes === String) object.result = '';
        else {
          object.result = [];
          if (options.bytes !== Array)
            object.result = $util.newBuffer(object.result);
        }
      if (message.result != null && message.hasOwnProperty('result'))
        object.result =
          options.bytes === String
            ? $util.base64.encode(message.result, 0, message.result.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.result)
              : message.result;
      if (message.errors && message.errors.length) {
        object.errors = [];
        for (let j = 0; j < message.errors.length; ++j)
          object.errors[j] = $root.polycentric_ffi.ServerError.toObject(
            message.errors[j],
            options
          );
      }
      return object;
    };

    /**
     * Converts this ResultAndServerErrors to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ResultAndServerErrors.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ResultAndServerErrors
     * @function getTypeUrl
     * @memberof polycentric_ffi.ResultAndServerErrors
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ResultAndServerErrors.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.ResultAndServerErrors';
    };

    return ResultAndServerErrors;
  })();

  polycentric_ffi.Cursor = (function () {
    /**
     * Properties of a Cursor.
     * @memberof polycentric_ffi
     * @interface ICursor
     * @property {Uint8Array|null} [cursor] Cursor cursor
     */

    /**
     * Constructs a new Cursor.
     * @memberof polycentric_ffi
     * @classdesc Represents a Cursor.
     * @implements ICursor
     * @constructor
     * @param {polycentric_ffi.ICursor=} [properties] Properties to set
     */
    function Cursor(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * Cursor cursor.
     * @member {Uint8Array|null|undefined} cursor
     * @memberof polycentric_ffi.Cursor
     * @instance
     */
    Cursor.prototype.cursor = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(Cursor.prototype, '_cursor', {
      get: $util.oneOfGetter(($oneOfFields = ['cursor'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new Cursor instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {polycentric_ffi.ICursor=} [properties] Properties to set
     * @returns {polycentric_ffi.Cursor} Cursor instance
     */
    Cursor.create = function create(properties) {
      return new Cursor(properties);
    };

    /**
     * Encodes the specified Cursor message. Does not implicitly {@link polycentric_ffi.Cursor.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {polycentric_ffi.ICursor} message Cursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Cursor.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.cursor);
      return writer;
    };

    /**
     * Encodes the specified Cursor message, length delimited. Does not implicitly {@link polycentric_ffi.Cursor.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {polycentric_ffi.ICursor} message Cursor message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    Cursor.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a Cursor message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.Cursor} Cursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Cursor.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.Cursor();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.cursor = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a Cursor message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.Cursor} Cursor
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    Cursor.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a Cursor message.
     * @function verify
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    Cursor.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        properties._cursor = 1;
        if (
          !(
            (message.cursor && typeof message.cursor.length === 'number') ||
            $util.isString(message.cursor)
          )
        )
          return 'cursor: buffer expected';
      }
      return null;
    };

    /**
     * Creates a Cursor message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.Cursor} Cursor
     */
    Cursor.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.Cursor) return object;
      let message = new $root.polycentric_ffi.Cursor();
      if (object.cursor != null)
        if (typeof object.cursor === 'string')
          $util.base64.decode(
            object.cursor,
            (message.cursor = $util.newBuffer(
              $util.base64.length(object.cursor)
            )),
            0
          );
        else if (object.cursor.length >= 0) message.cursor = object.cursor;
      return message;
    };

    /**
     * Creates a plain object from a Cursor message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {polycentric_ffi.Cursor} message Cursor
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    Cursor.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        object.cursor =
          options.bytes === String
            ? $util.base64.encode(message.cursor, 0, message.cursor.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.cursor)
              : message.cursor;
        if (options.oneofs) object._cursor = 'cursor';
      }
      return object;
    };

    /**
     * Converts this Cursor to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.Cursor
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    Cursor.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for Cursor
     * @function getTypeUrl
     * @memberof polycentric_ffi.Cursor
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    Cursor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.Cursor';
    };

    return Cursor;
  })();

  polycentric_ffi.ServerCursors = (function () {
    /**
     * Properties of a ServerCursors.
     * @memberof polycentric_ffi
     * @interface IServerCursors
     * @property {Object.<string,polycentric_ffi.IOption>|null} [cursors] ServerCursors cursors
     */

    /**
     * Constructs a new ServerCursors.
     * @memberof polycentric_ffi
     * @classdesc Represents a ServerCursors.
     * @implements IServerCursors
     * @constructor
     * @param {polycentric_ffi.IServerCursors=} [properties] Properties to set
     */
    function ServerCursors(properties) {
      this.cursors = {};
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ServerCursors cursors.
     * @member {Object.<string,polycentric_ffi.IOption>} cursors
     * @memberof polycentric_ffi.ServerCursors
     * @instance
     */
    ServerCursors.prototype.cursors = $util.emptyObject;

    /**
     * Creates a new ServerCursors instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {polycentric_ffi.IServerCursors=} [properties] Properties to set
     * @returns {polycentric_ffi.ServerCursors} ServerCursors instance
     */
    ServerCursors.create = function create(properties) {
      return new ServerCursors(properties);
    };

    /**
     * Encodes the specified ServerCursors message. Does not implicitly {@link polycentric_ffi.ServerCursors.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {polycentric_ffi.IServerCursors} message ServerCursors message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ServerCursors.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.cursors != null &&
        Object.hasOwnProperty.call(message, 'cursors')
      )
        for (
          let keys = Object.keys(message.cursors), i = 0;
          i < keys.length;
          ++i
        ) {
          writer
            .uint32(/* id 1, wireType 2 =*/ 10)
            .fork()
            .uint32(/* id 1, wireType 2 =*/ 10)
            .string(keys[i]);
          $root.polycentric_ffi.Option.encode(
            message.cursors[keys[i]],
            writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
          )
            .ldelim()
            .ldelim();
        }
      return writer;
    };

    /**
     * Encodes the specified ServerCursors message, length delimited. Does not implicitly {@link polycentric_ffi.ServerCursors.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {polycentric_ffi.IServerCursors} message ServerCursors message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ServerCursors.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ServerCursors message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.ServerCursors} ServerCursors
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ServerCursors.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.ServerCursors(),
        key,
        value;
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            if (message.cursors === $util.emptyObject) message.cursors = {};
            let end2 = reader.uint32() + reader.pos;
            key = '';
            value = null;
            while (reader.pos < end2) {
              let tag2 = reader.uint32();
              switch (tag2 >>> 3) {
                case 1:
                  key = reader.string();
                  break;
                case 2:
                  value = $root.polycentric_ffi.Option.decode(
                    reader,
                    reader.uint32()
                  );
                  break;
                default:
                  reader.skipType(tag2 & 7);
                  break;
              }
            }
            message.cursors[key] = value;
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ServerCursors message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.ServerCursors} ServerCursors
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ServerCursors.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ServerCursors message.
     * @function verify
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ServerCursors.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.cursors != null && message.hasOwnProperty('cursors')) {
        if (!$util.isObject(message.cursors)) return 'cursors: object expected';
        let key = Object.keys(message.cursors);
        for (let i = 0; i < key.length; ++i) {
          let error = $root.polycentric_ffi.Option.verify(
            message.cursors[key[i]]
          );
          if (error) return 'cursors.' + error;
        }
      }
      return null;
    };

    /**
     * Creates a ServerCursors message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.ServerCursors} ServerCursors
     */
    ServerCursors.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.ServerCursors) return object;
      let message = new $root.polycentric_ffi.ServerCursors();
      if (object.cursors) {
        if (typeof object.cursors !== 'object')
          throw TypeError(
            '.polycentric_ffi.ServerCursors.cursors: object expected'
          );
        message.cursors = {};
        for (
          let keys = Object.keys(object.cursors), i = 0;
          i < keys.length;
          ++i
        ) {
          if (typeof object.cursors[keys[i]] !== 'object')
            throw TypeError(
              '.polycentric_ffi.ServerCursors.cursors: object expected'
            );
          message.cursors[keys[i]] = $root.polycentric_ffi.Option.fromObject(
            object.cursors[keys[i]]
          );
        }
      }
      return message;
    };

    /**
     * Creates a plain object from a ServerCursors message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {polycentric_ffi.ServerCursors} message ServerCursors
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ServerCursors.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.objects || options.defaults) object.cursors = {};
      let keys2;
      if (message.cursors && (keys2 = Object.keys(message.cursors)).length) {
        object.cursors = {};
        for (let j = 0; j < keys2.length; ++j)
          object.cursors[keys2[j]] = $root.polycentric_ffi.Option.toObject(
            message.cursors[keys2[j]],
            options
          );
      }
      return object;
    };

    /**
     * Converts this ServerCursors to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.ServerCursors
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ServerCursors.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ServerCursors
     * @function getTypeUrl
     * @memberof polycentric_ffi.ServerCursors
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ServerCursors.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.ServerCursors';
    };

    return ServerCursors;
  })();

  polycentric_ffi.ServerFeedQuery = (function () {
    /**
     * Properties of a ServerFeedQuery.
     * @memberof polycentric_ffi
     * @interface IServerFeedQuery
     * @property {number|Long|null} [perServerLimit] ServerFeedQuery perServerLimit
     * @property {string|null} [moderationFilters] ServerFeedQuery moderationFilters
     */

    /**
     * Constructs a new ServerFeedQuery.
     * @memberof polycentric_ffi
     * @classdesc Represents a ServerFeedQuery.
     * @implements IServerFeedQuery
     * @constructor
     * @param {polycentric_ffi.IServerFeedQuery=} [properties] Properties to set
     */
    function ServerFeedQuery(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * ServerFeedQuery perServerLimit.
     * @member {number|Long|null|undefined} perServerLimit
     * @memberof polycentric_ffi.ServerFeedQuery
     * @instance
     */
    ServerFeedQuery.prototype.perServerLimit = null;

    /**
     * ServerFeedQuery moderationFilters.
     * @member {string|null|undefined} moderationFilters
     * @memberof polycentric_ffi.ServerFeedQuery
     * @instance
     */
    ServerFeedQuery.prototype.moderationFilters = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(ServerFeedQuery.prototype, '_perServerLimit', {
      get: $util.oneOfGetter(($oneOfFields = ['perServerLimit'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(ServerFeedQuery.prototype, '_moderationFilters', {
      get: $util.oneOfGetter(($oneOfFields = ['moderationFilters'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new ServerFeedQuery instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {polycentric_ffi.IServerFeedQuery=} [properties] Properties to set
     * @returns {polycentric_ffi.ServerFeedQuery} ServerFeedQuery instance
     */
    ServerFeedQuery.create = function create(properties) {
      return new ServerFeedQuery(properties);
    };

    /**
     * Encodes the specified ServerFeedQuery message. Does not implicitly {@link polycentric_ffi.ServerFeedQuery.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {polycentric_ffi.IServerFeedQuery} message ServerFeedQuery message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ServerFeedQuery.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.perServerLimit != null &&
        Object.hasOwnProperty.call(message, 'perServerLimit')
      )
        writer
          .uint32(/* id 2, wireType 0 =*/ 16)
          .uint64(message.perServerLimit);
      if (
        message.moderationFilters != null &&
        Object.hasOwnProperty.call(message, 'moderationFilters')
      )
        writer
          .uint32(/* id 3, wireType 2 =*/ 26)
          .string(message.moderationFilters);
      return writer;
    };

    /**
     * Encodes the specified ServerFeedQuery message, length delimited. Does not implicitly {@link polycentric_ffi.ServerFeedQuery.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {polycentric_ffi.IServerFeedQuery} message ServerFeedQuery message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    ServerFeedQuery.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a ServerFeedQuery message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.ServerFeedQuery} ServerFeedQuery
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ServerFeedQuery.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.ServerFeedQuery();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 2: {
            message.perServerLimit = reader.uint64();
            break;
          }
          case 3: {
            message.moderationFilters = reader.string();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a ServerFeedQuery message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.ServerFeedQuery} ServerFeedQuery
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    ServerFeedQuery.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a ServerFeedQuery message.
     * @function verify
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    ServerFeedQuery.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (
        message.perServerLimit != null &&
        message.hasOwnProperty('perServerLimit')
      ) {
        properties._perServerLimit = 1;
        if (
          !$util.isInteger(message.perServerLimit) &&
          !(
            message.perServerLimit &&
            $util.isInteger(message.perServerLimit.low) &&
            $util.isInteger(message.perServerLimit.high)
          )
        )
          return 'perServerLimit: integer|Long expected';
      }
      if (
        message.moderationFilters != null &&
        message.hasOwnProperty('moderationFilters')
      ) {
        properties._moderationFilters = 1;
        if (!$util.isString(message.moderationFilters))
          return 'moderationFilters: string expected';
      }
      return null;
    };

    /**
     * Creates a ServerFeedQuery message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.ServerFeedQuery} ServerFeedQuery
     */
    ServerFeedQuery.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.ServerFeedQuery)
        return object;
      let message = new $root.polycentric_ffi.ServerFeedQuery();
      if (object.perServerLimit != null)
        if ($util.Long)
          (message.perServerLimit = $util.Long.fromValue(
            object.perServerLimit
          )).unsigned = true;
        else if (typeof object.perServerLimit === 'string')
          message.perServerLimit = parseInt(object.perServerLimit, 10);
        else if (typeof object.perServerLimit === 'number')
          message.perServerLimit = object.perServerLimit;
        else if (typeof object.perServerLimit === 'object')
          message.perServerLimit = new $util.LongBits(
            object.perServerLimit.low >>> 0,
            object.perServerLimit.high >>> 0
          ).toNumber(true);
      if (object.moderationFilters != null)
        message.moderationFilters = String(object.moderationFilters);
      return message;
    };

    /**
     * Creates a plain object from a ServerFeedQuery message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {polycentric_ffi.ServerFeedQuery} message ServerFeedQuery
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    ServerFeedQuery.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (
        message.perServerLimit != null &&
        message.hasOwnProperty('perServerLimit')
      ) {
        if (typeof message.perServerLimit === 'number')
          object.perServerLimit =
            options.longs === String
              ? String(message.perServerLimit)
              : message.perServerLimit;
        else
          object.perServerLimit =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.perServerLimit)
              : options.longs === Number
                ? new $util.LongBits(
                    message.perServerLimit.low >>> 0,
                    message.perServerLimit.high >>> 0
                  ).toNumber(true)
                : message.perServerLimit;
        if (options.oneofs) object._perServerLimit = 'perServerLimit';
      }
      if (
        message.moderationFilters != null &&
        message.hasOwnProperty('moderationFilters')
      ) {
        object.moderationFilters = message.moderationFilters;
        if (options.oneofs) object._moderationFilters = 'moderationFilters';
      }
      return object;
    };

    /**
     * Converts this ServerFeedQuery to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.ServerFeedQuery
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    ServerFeedQuery.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for ServerFeedQuery
     * @function getTypeUrl
     * @memberof polycentric_ffi.ServerFeedQuery
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    ServerFeedQuery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.ServerFeedQuery';
    };

    return ServerFeedQuery;
  })();

  polycentric_ffi.InternalFeedResult = (function () {
    /**
     * Properties of an InternalFeedResult.
     * @memberof polycentric_ffi
     * @interface IInternalFeedResult
     * @property {polycentric_ffi.IResultAndServerErrors|null} [result] InternalFeedResult result
     * @property {polycentric_ffi.ICursor|null} [cursor] InternalFeedResult cursor
     */

    /**
     * Constructs a new InternalFeedResult.
     * @memberof polycentric_ffi
     * @classdesc Represents an InternalFeedResult.
     * @implements IInternalFeedResult
     * @constructor
     * @param {polycentric_ffi.IInternalFeedResult=} [properties] Properties to set
     */
    function InternalFeedResult(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * InternalFeedResult result.
     * @member {polycentric_ffi.IResultAndServerErrors|null|undefined} result
     * @memberof polycentric_ffi.InternalFeedResult
     * @instance
     */
    InternalFeedResult.prototype.result = null;

    /**
     * InternalFeedResult cursor.
     * @member {polycentric_ffi.ICursor|null|undefined} cursor
     * @memberof polycentric_ffi.InternalFeedResult
     * @instance
     */
    InternalFeedResult.prototype.cursor = null;

    /**
     * Creates a new InternalFeedResult instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {polycentric_ffi.IInternalFeedResult=} [properties] Properties to set
     * @returns {polycentric_ffi.InternalFeedResult} InternalFeedResult instance
     */
    InternalFeedResult.create = function create(properties) {
      return new InternalFeedResult(properties);
    };

    /**
     * Encodes the specified InternalFeedResult message. Does not implicitly {@link polycentric_ffi.InternalFeedResult.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {polycentric_ffi.IInternalFeedResult} message InternalFeedResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    InternalFeedResult.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.result != null &&
        Object.hasOwnProperty.call(message, 'result')
      )
        $root.polycentric_ffi.ResultAndServerErrors.encode(
          message.result,
          writer.uint32(/* id 1, wireType 2 =*/ 10).fork()
        ).ldelim();
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        $root.polycentric_ffi.Cursor.encode(
          message.cursor,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified InternalFeedResult message, length delimited. Does not implicitly {@link polycentric_ffi.InternalFeedResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {polycentric_ffi.IInternalFeedResult} message InternalFeedResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    InternalFeedResult.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes an InternalFeedResult message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.InternalFeedResult} InternalFeedResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    InternalFeedResult.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.InternalFeedResult();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.result = $root.polycentric_ffi.ResultAndServerErrors.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          case 2: {
            message.cursor = $root.polycentric_ffi.Cursor.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes an InternalFeedResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.InternalFeedResult} InternalFeedResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    InternalFeedResult.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies an InternalFeedResult message.
     * @function verify
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    InternalFeedResult.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.result != null && message.hasOwnProperty('result')) {
        let error = $root.polycentric_ffi.ResultAndServerErrors.verify(
          message.result
        );
        if (error) return 'result.' + error;
      }
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        let error = $root.polycentric_ffi.Cursor.verify(message.cursor);
        if (error) return 'cursor.' + error;
      }
      return null;
    };

    /**
     * Creates an InternalFeedResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.InternalFeedResult} InternalFeedResult
     */
    InternalFeedResult.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.InternalFeedResult)
        return object;
      let message = new $root.polycentric_ffi.InternalFeedResult();
      if (object.result != null) {
        if (typeof object.result !== 'object')
          throw TypeError(
            '.polycentric_ffi.InternalFeedResult.result: object expected'
          );
        message.result = $root.polycentric_ffi.ResultAndServerErrors.fromObject(
          object.result
        );
      }
      if (object.cursor != null) {
        if (typeof object.cursor !== 'object')
          throw TypeError(
            '.polycentric_ffi.InternalFeedResult.cursor: object expected'
          );
        message.cursor = $root.polycentric_ffi.Cursor.fromObject(object.cursor);
      }
      return message;
    };

    /**
     * Creates a plain object from an InternalFeedResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {polycentric_ffi.InternalFeedResult} message InternalFeedResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    InternalFeedResult.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.result = null;
        object.cursor = null;
      }
      if (message.result != null && message.hasOwnProperty('result'))
        object.result = $root.polycentric_ffi.ResultAndServerErrors.toObject(
          message.result,
          options
        );
      if (message.cursor != null && message.hasOwnProperty('cursor'))
        object.cursor = $root.polycentric_ffi.Cursor.toObject(
          message.cursor,
          options
        );
      return object;
    };

    /**
     * Converts this InternalFeedResult to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.InternalFeedResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    InternalFeedResult.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for InternalFeedResult
     * @function getTypeUrl
     * @memberof polycentric_ffi.InternalFeedResult
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    InternalFeedResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.InternalFeedResult';
    };

    return InternalFeedResult;
  })();

  polycentric_ffi.SearchQuery = (function () {
    /**
     * Properties of a SearchQuery.
     * @memberof polycentric_ffi
     * @interface ISearchQuery
     * @property {string|null} [query] SearchQuery query
     * @property {polycentric_ffi.SearchType|null} [type] SearchQuery type
     */

    /**
     * Constructs a new SearchQuery.
     * @memberof polycentric_ffi
     * @classdesc Represents a SearchQuery.
     * @implements ISearchQuery
     * @constructor
     * @param {polycentric_ffi.ISearchQuery=} [properties] Properties to set
     */
    function SearchQuery(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * SearchQuery query.
     * @member {string} query
     * @memberof polycentric_ffi.SearchQuery
     * @instance
     */
    SearchQuery.prototype.query = '';

    /**
     * SearchQuery type.
     * @member {polycentric_ffi.SearchType} type
     * @memberof polycentric_ffi.SearchQuery
     * @instance
     */
    SearchQuery.prototype.type = 0;

    /**
     * Creates a new SearchQuery instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {polycentric_ffi.ISearchQuery=} [properties] Properties to set
     * @returns {polycentric_ffi.SearchQuery} SearchQuery instance
     */
    SearchQuery.create = function create(properties) {
      return new SearchQuery(properties);
    };

    /**
     * Encodes the specified SearchQuery message. Does not implicitly {@link polycentric_ffi.SearchQuery.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {polycentric_ffi.ISearchQuery} message SearchQuery message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SearchQuery.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.query != null && Object.hasOwnProperty.call(message, 'query'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).string(message.query);
      if (message.type != null && Object.hasOwnProperty.call(message, 'type'))
        writer.uint32(/* id 2, wireType 0 =*/ 16).int32(message.type);
      return writer;
    };

    /**
     * Encodes the specified SearchQuery message, length delimited. Does not implicitly {@link polycentric_ffi.SearchQuery.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {polycentric_ffi.ISearchQuery} message SearchQuery message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    SearchQuery.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a SearchQuery message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.SearchQuery} SearchQuery
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SearchQuery.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.SearchQuery();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.query = reader.string();
            break;
          }
          case 2: {
            message.type = reader.int32();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a SearchQuery message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.SearchQuery} SearchQuery
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    SearchQuery.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a SearchQuery message.
     * @function verify
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    SearchQuery.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.query != null && message.hasOwnProperty('query'))
        if (!$util.isString(message.query)) return 'query: string expected';
      if (message.type != null && message.hasOwnProperty('type'))
        switch (message.type) {
          default:
            return 'type: enum value expected';
          case 0:
          case 1:
            break;
        }
      return null;
    };

    /**
     * Creates a SearchQuery message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.SearchQuery} SearchQuery
     */
    SearchQuery.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.SearchQuery) return object;
      let message = new $root.polycentric_ffi.SearchQuery();
      if (object.query != null) message.query = String(object.query);
      switch (object.type) {
        default:
          if (typeof object.type === 'number') {
            message.type = object.type;
            break;
          }
          break;
        case 'messages':
        case 0:
          message.type = 0;
          break;
        case 'profiles':
        case 1:
          message.type = 1;
          break;
      }
      return message;
    };

    /**
     * Creates a plain object from a SearchQuery message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {polycentric_ffi.SearchQuery} message SearchQuery
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    SearchQuery.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        object.query = '';
        object.type = options.enums === String ? 'messages' : 0;
      }
      if (message.query != null && message.hasOwnProperty('query'))
        object.query = message.query;
      if (message.type != null && message.hasOwnProperty('type'))
        object.type =
          options.enums === String
            ? $root.polycentric_ffi.SearchType[message.type] === undefined
              ? message.type
              : $root.polycentric_ffi.SearchType[message.type]
            : message.type;
      return object;
    };

    /**
     * Converts this SearchQuery to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.SearchQuery
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    SearchQuery.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for SearchQuery
     * @function getTypeUrl
     * @memberof polycentric_ffi.SearchQuery
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    SearchQuery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.SearchQuery';
    };

    return SearchQuery;
  })();

  /**
   * SearchType enum.
   * @name polycentric_ffi.SearchType
   * @enum {number}
   * @property {number} messages=0 messages value
   * @property {number} profiles=1 profiles value
   */
  polycentric_ffi.SearchType = (function () {
    const valuesById = {},
      values = Object.create(valuesById);
    values[(valuesById[0] = 'messages')] = 0;
    values[(valuesById[1] = 'profiles')] = 1;
    return values;
  })();

  polycentric_ffi.CommentsFeedState = (function () {
    /**
     * Properties of a CommentsFeedState.
     * @memberof polycentric_ffi
     * @interface ICommentsFeedState
     * @property {Uint8Array|null} [event] CommentsFeedState event
     * @property {polycentric_ffi.IServerCursors|null} [cursors] CommentsFeedState cursors
     */

    /**
     * Constructs a new CommentsFeedState.
     * @memberof polycentric_ffi
     * @classdesc Represents a CommentsFeedState.
     * @implements ICommentsFeedState
     * @constructor
     * @param {polycentric_ffi.ICommentsFeedState=} [properties] Properties to set
     */
    function CommentsFeedState(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * CommentsFeedState event.
     * @member {Uint8Array} event
     * @memberof polycentric_ffi.CommentsFeedState
     * @instance
     */
    CommentsFeedState.prototype.event = $util.newBuffer([]);

    /**
     * CommentsFeedState cursors.
     * @member {polycentric_ffi.IServerCursors|null|undefined} cursors
     * @memberof polycentric_ffi.CommentsFeedState
     * @instance
     */
    CommentsFeedState.prototype.cursors = null;

    /**
     * Creates a new CommentsFeedState instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {polycentric_ffi.ICommentsFeedState=} [properties] Properties to set
     * @returns {polycentric_ffi.CommentsFeedState} CommentsFeedState instance
     */
    CommentsFeedState.create = function create(properties) {
      return new CommentsFeedState(properties);
    };

    /**
     * Encodes the specified CommentsFeedState message. Does not implicitly {@link polycentric_ffi.CommentsFeedState.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {polycentric_ffi.ICommentsFeedState} message CommentsFeedState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommentsFeedState.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.event != null && Object.hasOwnProperty.call(message, 'event'))
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.event);
      if (
        message.cursors != null &&
        Object.hasOwnProperty.call(message, 'cursors')
      )
        $root.polycentric_ffi.ServerCursors.encode(
          message.cursors,
          writer.uint32(/* id 2, wireType 2 =*/ 18).fork()
        ).ldelim();
      return writer;
    };

    /**
     * Encodes the specified CommentsFeedState message, length delimited. Does not implicitly {@link polycentric_ffi.CommentsFeedState.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {polycentric_ffi.ICommentsFeedState} message CommentsFeedState message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    CommentsFeedState.encodeDelimited = function encodeDelimited(
      message,
      writer
    ) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a CommentsFeedState message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.CommentsFeedState} CommentsFeedState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommentsFeedState.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.CommentsFeedState();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.event = reader.bytes();
            break;
          }
          case 2: {
            message.cursors = $root.polycentric_ffi.ServerCursors.decode(
              reader,
              reader.uint32()
            );
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a CommentsFeedState message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.CommentsFeedState} CommentsFeedState
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    CommentsFeedState.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a CommentsFeedState message.
     * @function verify
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    CommentsFeedState.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.event != null && message.hasOwnProperty('event'))
        if (
          !(
            (message.event && typeof message.event.length === 'number') ||
            $util.isString(message.event)
          )
        )
          return 'event: buffer expected';
      if (message.cursors != null && message.hasOwnProperty('cursors')) {
        let error = $root.polycentric_ffi.ServerCursors.verify(message.cursors);
        if (error) return 'cursors.' + error;
      }
      return null;
    };

    /**
     * Creates a CommentsFeedState message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.CommentsFeedState} CommentsFeedState
     */
    CommentsFeedState.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.CommentsFeedState)
        return object;
      let message = new $root.polycentric_ffi.CommentsFeedState();
      if (object.event != null)
        if (typeof object.event === 'string')
          $util.base64.decode(
            object.event,
            (message.event = $util.newBuffer(
              $util.base64.length(object.event)
            )),
            0
          );
        else if (object.event.length >= 0) message.event = object.event;
      if (object.cursors != null) {
        if (typeof object.cursors !== 'object')
          throw TypeError(
            '.polycentric_ffi.CommentsFeedState.cursors: object expected'
          );
        message.cursors = $root.polycentric_ffi.ServerCursors.fromObject(
          object.cursors
        );
      }
      return message;
    };

    /**
     * Creates a plain object from a CommentsFeedState message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {polycentric_ffi.CommentsFeedState} message CommentsFeedState
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    CommentsFeedState.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults) {
        if (options.bytes === String) object.event = '';
        else {
          object.event = [];
          if (options.bytes !== Array)
            object.event = $util.newBuffer(object.event);
        }
        object.cursors = null;
      }
      if (message.event != null && message.hasOwnProperty('event'))
        object.event =
          options.bytes === String
            ? $util.base64.encode(message.event, 0, message.event.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.event)
              : message.event;
      if (message.cursors != null && message.hasOwnProperty('cursors'))
        object.cursors = $root.polycentric_ffi.ServerCursors.toObject(
          message.cursors,
          options
        );
      return object;
    };

    /**
     * Converts this CommentsFeedState to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.CommentsFeedState
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    CommentsFeedState.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for CommentsFeedState
     * @function getTypeUrl
     * @memberof polycentric_ffi.CommentsFeedState
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    CommentsFeedState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.CommentsFeedState';
    };

    return CommentsFeedState;
  })();

  polycentric_ffi.FeedQuery = (function () {
    /**
     * Properties of a FeedQuery.
     * @memberof polycentric_ffi
     * @interface IFeedQuery
     * @property {Uint8Array|null} [systemBytes] FeedQuery systemBytes
     * @property {number|Long|null} [startTime] FeedQuery startTime
     * @property {number|Long|null} [endTime] FeedQuery endTime
     * @property {number|Long|null} [limit] FeedQuery limit
     * @property {Uint8Array|null} [cursor] FeedQuery cursor
     */

    /**
     * Constructs a new FeedQuery.
     * @memberof polycentric_ffi
     * @classdesc Represents a FeedQuery.
     * @implements IFeedQuery
     * @constructor
     * @param {polycentric_ffi.IFeedQuery=} [properties] Properties to set
     */
    function FeedQuery(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * FeedQuery systemBytes.
     * @member {Uint8Array} systemBytes
     * @memberof polycentric_ffi.FeedQuery
     * @instance
     */
    FeedQuery.prototype.systemBytes = $util.newBuffer([]);

    /**
     * FeedQuery startTime.
     * @member {number|Long|null|undefined} startTime
     * @memberof polycentric_ffi.FeedQuery
     * @instance
     */
    FeedQuery.prototype.startTime = null;

    /**
     * FeedQuery endTime.
     * @member {number|Long|null|undefined} endTime
     * @memberof polycentric_ffi.FeedQuery
     * @instance
     */
    FeedQuery.prototype.endTime = null;

    /**
     * FeedQuery limit.
     * @member {number|Long|null|undefined} limit
     * @memberof polycentric_ffi.FeedQuery
     * @instance
     */
    FeedQuery.prototype.limit = null;

    /**
     * FeedQuery cursor.
     * @member {Uint8Array|null|undefined} cursor
     * @memberof polycentric_ffi.FeedQuery
     * @instance
     */
    FeedQuery.prototype.cursor = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(FeedQuery.prototype, '_startTime', {
      get: $util.oneOfGetter(($oneOfFields = ['startTime'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(FeedQuery.prototype, '_endTime', {
      get: $util.oneOfGetter(($oneOfFields = ['endTime'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(FeedQuery.prototype, '_limit', {
      get: $util.oneOfGetter(($oneOfFields = ['limit'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    // Virtual OneOf for proto3 optional field
    Object.defineProperty(FeedQuery.prototype, '_cursor', {
      get: $util.oneOfGetter(($oneOfFields = ['cursor'])),
      set: $util.oneOfSetter($oneOfFields),
    });

    /**
     * Creates a new FeedQuery instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {polycentric_ffi.IFeedQuery=} [properties] Properties to set
     * @returns {polycentric_ffi.FeedQuery} FeedQuery instance
     */
    FeedQuery.create = function create(properties) {
      return new FeedQuery(properties);
    };

    /**
     * Encodes the specified FeedQuery message. Does not implicitly {@link polycentric_ffi.FeedQuery.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {polycentric_ffi.IFeedQuery} message FeedQuery message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedQuery.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (
        message.systemBytes != null &&
        Object.hasOwnProperty.call(message, 'systemBytes')
      )
        writer.uint32(/* id 1, wireType 2 =*/ 10).bytes(message.systemBytes);
      if (
        message.startTime != null &&
        Object.hasOwnProperty.call(message, 'startTime')
      )
        writer.uint32(/* id 2, wireType 0 =*/ 16).uint64(message.startTime);
      if (
        message.endTime != null &&
        Object.hasOwnProperty.call(message, 'endTime')
      )
        writer.uint32(/* id 3, wireType 0 =*/ 24).uint64(message.endTime);
      if (message.limit != null && Object.hasOwnProperty.call(message, 'limit'))
        writer.uint32(/* id 4, wireType 0 =*/ 32).uint64(message.limit);
      if (
        message.cursor != null &&
        Object.hasOwnProperty.call(message, 'cursor')
      )
        writer.uint32(/* id 5, wireType 2 =*/ 42).bytes(message.cursor);
      return writer;
    };

    /**
     * Encodes the specified FeedQuery message, length delimited. Does not implicitly {@link polycentric_ffi.FeedQuery.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {polycentric_ffi.IFeedQuery} message FeedQuery message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    FeedQuery.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a FeedQuery message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.FeedQuery} FeedQuery
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedQuery.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.FeedQuery();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.systemBytes = reader.bytes();
            break;
          }
          case 2: {
            message.startTime = reader.uint64();
            break;
          }
          case 3: {
            message.endTime = reader.uint64();
            break;
          }
          case 4: {
            message.limit = reader.uint64();
            break;
          }
          case 5: {
            message.cursor = reader.bytes();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a FeedQuery message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.FeedQuery} FeedQuery
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    FeedQuery.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a FeedQuery message.
     * @function verify
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    FeedQuery.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      let properties = {};
      if (message.systemBytes != null && message.hasOwnProperty('systemBytes'))
        if (
          !(
            (message.systemBytes &&
              typeof message.systemBytes.length === 'number') ||
            $util.isString(message.systemBytes)
          )
        )
          return 'systemBytes: buffer expected';
      if (message.startTime != null && message.hasOwnProperty('startTime')) {
        properties._startTime = 1;
        if (
          !$util.isInteger(message.startTime) &&
          !(
            message.startTime &&
            $util.isInteger(message.startTime.low) &&
            $util.isInteger(message.startTime.high)
          )
        )
          return 'startTime: integer|Long expected';
      }
      if (message.endTime != null && message.hasOwnProperty('endTime')) {
        properties._endTime = 1;
        if (
          !$util.isInteger(message.endTime) &&
          !(
            message.endTime &&
            $util.isInteger(message.endTime.low) &&
            $util.isInteger(message.endTime.high)
          )
        )
          return 'endTime: integer|Long expected';
      }
      if (message.limit != null && message.hasOwnProperty('limit')) {
        properties._limit = 1;
        if (
          !$util.isInteger(message.limit) &&
          !(
            message.limit &&
            $util.isInteger(message.limit.low) &&
            $util.isInteger(message.limit.high)
          )
        )
          return 'limit: integer|Long expected';
      }
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        properties._cursor = 1;
        if (
          !(
            (message.cursor && typeof message.cursor.length === 'number') ||
            $util.isString(message.cursor)
          )
        )
          return 'cursor: buffer expected';
      }
      return null;
    };

    /**
     * Creates a FeedQuery message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.FeedQuery} FeedQuery
     */
    FeedQuery.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.FeedQuery) return object;
      let message = new $root.polycentric_ffi.FeedQuery();
      if (object.systemBytes != null)
        if (typeof object.systemBytes === 'string')
          $util.base64.decode(
            object.systemBytes,
            (message.systemBytes = $util.newBuffer(
              $util.base64.length(object.systemBytes)
            )),
            0
          );
        else if (object.systemBytes.length >= 0)
          message.systemBytes = object.systemBytes;
      if (object.startTime != null)
        if ($util.Long)
          (message.startTime = $util.Long.fromValue(
            object.startTime
          )).unsigned = true;
        else if (typeof object.startTime === 'string')
          message.startTime = parseInt(object.startTime, 10);
        else if (typeof object.startTime === 'number')
          message.startTime = object.startTime;
        else if (typeof object.startTime === 'object')
          message.startTime = new $util.LongBits(
            object.startTime.low >>> 0,
            object.startTime.high >>> 0
          ).toNumber(true);
      if (object.endTime != null)
        if ($util.Long)
          (message.endTime = $util.Long.fromValue(object.endTime)).unsigned =
            true;
        else if (typeof object.endTime === 'string')
          message.endTime = parseInt(object.endTime, 10);
        else if (typeof object.endTime === 'number')
          message.endTime = object.endTime;
        else if (typeof object.endTime === 'object')
          message.endTime = new $util.LongBits(
            object.endTime.low >>> 0,
            object.endTime.high >>> 0
          ).toNumber(true);
      if (object.limit != null)
        if ($util.Long)
          (message.limit = $util.Long.fromValue(object.limit)).unsigned = true;
        else if (typeof object.limit === 'string')
          message.limit = parseInt(object.limit, 10);
        else if (typeof object.limit === 'number') message.limit = object.limit;
        else if (typeof object.limit === 'object')
          message.limit = new $util.LongBits(
            object.limit.low >>> 0,
            object.limit.high >>> 0
          ).toNumber(true);
      if (object.cursor != null)
        if (typeof object.cursor === 'string')
          $util.base64.decode(
            object.cursor,
            (message.cursor = $util.newBuffer(
              $util.base64.length(object.cursor)
            )),
            0
          );
        else if (object.cursor.length >= 0) message.cursor = object.cursor;
      return message;
    };

    /**
     * Creates a plain object from a FeedQuery message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {polycentric_ffi.FeedQuery} message FeedQuery
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    FeedQuery.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults)
        if (options.bytes === String) object.systemBytes = '';
        else {
          object.systemBytes = [];
          if (options.bytes !== Array)
            object.systemBytes = $util.newBuffer(object.systemBytes);
        }
      if (message.systemBytes != null && message.hasOwnProperty('systemBytes'))
        object.systemBytes =
          options.bytes === String
            ? $util.base64.encode(
                message.systemBytes,
                0,
                message.systemBytes.length
              )
            : options.bytes === Array
              ? Array.prototype.slice.call(message.systemBytes)
              : message.systemBytes;
      if (message.startTime != null && message.hasOwnProperty('startTime')) {
        if (typeof message.startTime === 'number')
          object.startTime =
            options.longs === String
              ? String(message.startTime)
              : message.startTime;
        else
          object.startTime =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.startTime)
              : options.longs === Number
                ? new $util.LongBits(
                    message.startTime.low >>> 0,
                    message.startTime.high >>> 0
                  ).toNumber(true)
                : message.startTime;
        if (options.oneofs) object._startTime = 'startTime';
      }
      if (message.endTime != null && message.hasOwnProperty('endTime')) {
        if (typeof message.endTime === 'number')
          object.endTime =
            options.longs === String
              ? String(message.endTime)
              : message.endTime;
        else
          object.endTime =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.endTime)
              : options.longs === Number
                ? new $util.LongBits(
                    message.endTime.low >>> 0,
                    message.endTime.high >>> 0
                  ).toNumber(true)
                : message.endTime;
        if (options.oneofs) object._endTime = 'endTime';
      }
      if (message.limit != null && message.hasOwnProperty('limit')) {
        if (typeof message.limit === 'number')
          object.limit =
            options.longs === String ? String(message.limit) : message.limit;
        else
          object.limit =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.limit)
              : options.longs === Number
                ? new $util.LongBits(
                    message.limit.low >>> 0,
                    message.limit.high >>> 0
                  ).toNumber(true)
                : message.limit;
        if (options.oneofs) object._limit = 'limit';
      }
      if (message.cursor != null && message.hasOwnProperty('cursor')) {
        object.cursor =
          options.bytes === String
            ? $util.base64.encode(message.cursor, 0, message.cursor.length)
            : options.bytes === Array
              ? Array.prototype.slice.call(message.cursor)
              : message.cursor;
        if (options.oneofs) object._cursor = 'cursor';
      }
      return object;
    };

    /**
     * Converts this FeedQuery to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.FeedQuery
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    FeedQuery.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for FeedQuery
     * @function getTypeUrl
     * @memberof polycentric_ffi.FeedQuery
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    FeedQuery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.FeedQuery';
    };

    return FeedQuery;
  })();

  polycentric_ffi.LogicalClock = (function () {
    /**
     * Properties of a LogicalClock.
     * @memberof polycentric_ffi
     * @interface ILogicalClock
     * @property {number|Long|null} [clock] LogicalClock clock
     */

    /**
     * Constructs a new LogicalClock.
     * @memberof polycentric_ffi
     * @classdesc Represents a LogicalClock.
     * @implements ILogicalClock
     * @constructor
     * @param {polycentric_ffi.ILogicalClock=} [properties] Properties to set
     */
    function LogicalClock(properties) {
      if (properties)
        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null) this[keys[i]] = properties[keys[i]];
    }

    /**
     * LogicalClock clock.
     * @member {number|Long} clock
     * @memberof polycentric_ffi.LogicalClock
     * @instance
     */
    LogicalClock.prototype.clock = $util.Long
      ? $util.Long.fromBits(0, 0, true)
      : 0;

    /**
     * Creates a new LogicalClock instance using the specified properties.
     * @function create
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {polycentric_ffi.ILogicalClock=} [properties] Properties to set
     * @returns {polycentric_ffi.LogicalClock} LogicalClock instance
     */
    LogicalClock.create = function create(properties) {
      return new LogicalClock(properties);
    };

    /**
     * Encodes the specified LogicalClock message. Does not implicitly {@link polycentric_ffi.LogicalClock.verify|verify} messages.
     * @function encode
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {polycentric_ffi.ILogicalClock} message LogicalClock message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LogicalClock.encode = function encode(message, writer) {
      if (!writer) writer = $Writer.create();
      if (message.clock != null && Object.hasOwnProperty.call(message, 'clock'))
        writer.uint32(/* id 1, wireType 0 =*/ 8).uint64(message.clock);
      return writer;
    };

    /**
     * Encodes the specified LogicalClock message, length delimited. Does not implicitly {@link polycentric_ffi.LogicalClock.verify|verify} messages.
     * @function encodeDelimited
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {polycentric_ffi.ILogicalClock} message LogicalClock message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    LogicalClock.encodeDelimited = function encodeDelimited(message, writer) {
      return this.encode(message, writer).ldelim();
    };

    /**
     * Decodes a LogicalClock message from the specified reader or buffer.
     * @function decode
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {polycentric_ffi.LogicalClock} LogicalClock
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LogicalClock.decode = function decode(reader, length, error) {
      if (!(reader instanceof $Reader)) reader = $Reader.create(reader);
      let end = length === undefined ? reader.len : reader.pos + length,
        message = new $root.polycentric_ffi.LogicalClock();
      while (reader.pos < end) {
        let tag = reader.uint32();
        if (tag === error) break;
        switch (tag >>> 3) {
          case 1: {
            message.clock = reader.uint64();
            break;
          }
          default:
            reader.skipType(tag & 7);
            break;
        }
      }
      return message;
    };

    /**
     * Decodes a LogicalClock message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {polycentric_ffi.LogicalClock} LogicalClock
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    LogicalClock.decodeDelimited = function decodeDelimited(reader) {
      if (!(reader instanceof $Reader)) reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a LogicalClock message.
     * @function verify
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    LogicalClock.verify = function verify(message) {
      if (typeof message !== 'object' || message === null)
        return 'object expected';
      if (message.clock != null && message.hasOwnProperty('clock'))
        if (
          !$util.isInteger(message.clock) &&
          !(
            message.clock &&
            $util.isInteger(message.clock.low) &&
            $util.isInteger(message.clock.high)
          )
        )
          return 'clock: integer|Long expected';
      return null;
    };

    /**
     * Creates a LogicalClock message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {polycentric_ffi.LogicalClock} LogicalClock
     */
    LogicalClock.fromObject = function fromObject(object) {
      if (object instanceof $root.polycentric_ffi.LogicalClock) return object;
      let message = new $root.polycentric_ffi.LogicalClock();
      if (object.clock != null)
        if ($util.Long)
          (message.clock = $util.Long.fromValue(object.clock)).unsigned = true;
        else if (typeof object.clock === 'string')
          message.clock = parseInt(object.clock, 10);
        else if (typeof object.clock === 'number') message.clock = object.clock;
        else if (typeof object.clock === 'object')
          message.clock = new $util.LongBits(
            object.clock.low >>> 0,
            object.clock.high >>> 0
          ).toNumber(true);
      return message;
    };

    /**
     * Creates a plain object from a LogicalClock message. Also converts values to other types if specified.
     * @function toObject
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {polycentric_ffi.LogicalClock} message LogicalClock
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    LogicalClock.toObject = function toObject(message, options) {
      if (!options) options = {};
      let object = {};
      if (options.defaults)
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.clock =
            options.longs === String
              ? long.toString()
              : options.longs === Number
                ? long.toNumber()
                : long;
        } else object.clock = options.longs === String ? '0' : 0;
      if (message.clock != null && message.hasOwnProperty('clock'))
        if (typeof message.clock === 'number')
          object.clock =
            options.longs === String ? String(message.clock) : message.clock;
        else
          object.clock =
            options.longs === String
              ? $util.Long.prototype.toString.call(message.clock)
              : options.longs === Number
                ? new $util.LongBits(
                    message.clock.low >>> 0,
                    message.clock.high >>> 0
                  ).toNumber(true)
                : message.clock;
      return object;
    };

    /**
     * Converts this LogicalClock to JSON.
     * @function toJSON
     * @memberof polycentric_ffi.LogicalClock
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    LogicalClock.prototype.toJSON = function toJSON() {
      return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the default type url for LogicalClock
     * @function getTypeUrl
     * @memberof polycentric_ffi.LogicalClock
     * @static
     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
     * @returns {string} The default type url
     */
    LogicalClock.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
      if (typeUrlPrefix === undefined) {
        typeUrlPrefix = 'type.googleapis.com';
      }
      return typeUrlPrefix + '/polycentric_ffi.LogicalClock';
    };

    return LogicalClock;
  })();

  return polycentric_ffi;
})());

export { $root as default };
